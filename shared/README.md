# shared

`shared/brand/mainBrandGeometry.js` is the sole cross-entry JavaScript asset.
It provides the current shared brand geometry used by supported entry points.

New shared TypeScript belongs under `src/`; do not add application runtime or
storage compatibility modules under `shared/`.
