---
phase: 10.6.1-legacy-editor-removal-and-detail-modal-capability-absorption
verified: 2026-06-26T02:00:28Z
status: passed
score: 9/9 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 10.6.1: Legacy Editor Removal and Detail Modal Capability Absorption Verification Report

**Phase Goal:** Remove the auxiliary legacy financial editor path and absorb any still-useful editing capabilities into the Financial Detail Modal as the only primary Step 1 editor.  
**Verified:** 2026-06-26T02:00:28Z  
**Status:** passed  
**Re-verification:** No - initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | UXR-09 / D-01: Ordinary Step 1 income, expense, investment, savings, and account edits complete through Financial Detail Modal without a second primary editor path. | VERIFIED | `apps/main/modules/bootstrap-controller.js` creates render, persistence, and visualization controllers only; `apps/main/modules/event-bindings.js:87-91` binds `createFinancialModalController(...).bind()`. Phase 10.6.1 modal-only test passed. |
| 2 | D-02: Step 1 summary surface remains scan-and-open only, not an ordinary item editing host. | VERIFIED | `apps/main/index.html` exposes `#financialSettingsDetail` / `data-financial-settings-detail`; removed `#editIncomeItems`, `#editExpenseItems`, `#editSavingsItems`, `#editInvestItems`, and `#editAccountItems` are asserted absent in `tests/step1.spec.ts:357-360`. |
| 3 | D-03: Hidden secondary editor DOM, item-editor controller wiring, global pending bar, and mobile editor FAB are removed from normal paths. | VERIFIED | `apps/main/modules/item-editor-controller.js` is absent. Source marker audit found no `createItemEditorController`, `item-editor-controller`, `data-legacy-flow-editor`, `mobileEditorFab`, or `#pendingBar` in app source. `apps/main/index.html` contains `#financialModalPendingBar` only. |
| 4 | D-04: Useful legacy-only capabilities were absorbed before removal. | VERIFIED | Modal capability tests at `tests/step1.spec.ts:2580-2770` pass before final removal tests; implementation exists in `apps/main/modules/financial-modal-controller.js:916-1000` and event handlers at `2079-2093`. |
| 5 | UXR-10 / D-05: Income rows edit multiple destination-account allocations inside modal row edit state, with compact rows showing only representative deposit info. | VERIFIED | `renderIncomeAllocationEditor()` builds modal allocation rows, account selects, add/remove controls, and amount fields at `financial-modal-controller.js:916-960`; validation rejects over-allocation at `570-585`. Test `edits income destination allocations inside the modal and blocks over-allocation` passed. |
| 6 | UXR-11 / D-06: Savings maturity month can be edited in the modal, and matured savings stop future monthly contributions while retained balances remain in projections. | VERIFIED | Modal savings additional settings include `type="month"` `maturityMonth` at `financial-modal-controller.js:992-996`. `calculator.js:291-295` closes matured savings buckets and test projection probe verifies savings contribution stops while net asset remains positive. |
| 7 | UXR-12 / D-07: Item-level savings yield can be edited in collapsed savings-row settings without replacing the global default for other savings items. | VERIFIED | Modal `annualRate` input at `financial-modal-controller.js:983-990`; `calculator.js:151-153` uses item annualRate only when present and otherwise global fallback. Tests assert edited item has `annualRate` and sibling uses fallback rate `3`. |
| 8 | D-08: Conflicting secondary-editor user-facing language and stale removal markers are absent from the source-first surface. | VERIFIED | Final source audit test at `tests/step1.spec.ts:2915-2963` fetches source files and asserts forbidden English copy and stale markers are absent. Additional grep found Korean `수동 이체` only in dead transfer-rule rendering code with no corresponding DOM in `apps/main/index.html`; not a current user-facing Step 1 path. |
| 9 | D-09: Sanitizer, persistence, Sankey rendering, share/import, and Phase 10.6 modal row-editing/pending-bar contracts continue to pass. | VERIFIED | Final regression test at `tests/step1.spec.ts:2847-2913` verifies apply, localStorage, import envelope, Sankey labels, maturity, yield, and fallback. Phase 10.6 targeted suite passed 20/20; full `tests/step1.spec.ts` passed 65/65. |

**Score:** 9/9 truths verified (0 present-but-behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `apps/main/modules/financial-modal-controller.js` | Modal row edit controls for income allocation and savings additional settings | VERIFIED | Substantive 2,287-line controller; allocation, yield, maturity, validation, pending-bar, and save path are wired. |
| `apps/main/modules/calculator.js` | Savings maturity/yield projection behavior | VERIFIED | `buildSavingsBuckets()` preserves global fallback and maturity index; `simulateProjection()` closes matured savings buckets. |
| `apps/main/modules/input-sanitizer.js` | Durable data normalization for annualRate, maturityMonth, and allocations | VERIFIED | `sanitizeSavingsItems()` preserves valid item yield; `sanitizeAllocationItems(... allowMaturity)` normalizes maturity. |
| `apps/main/modules/sankey-builder.js` | Sankey account allocation rendering | VERIFIED | Reads income `allocations`; final test checks edited allocation accounts appear in Sankey labels. |
| `apps/main/modules/bootstrap-controller.js` | Bootstrap without item-editor construction | VERIFIED | No item-editor import or construction; `bindStep1Events(controllers)` receives no `itemEditor`. |
| `apps/main/modules/event-bindings.js` | Modal-only Step 1 event binding | VERIFIED | Imports `createFinancialModalController`; `openFinancialModalCategory()` dispatches `open-financial-modal`. |
| `apps/main/modules/dom.js` / `ui-controller.js` | No old global item-editor handles/helpers | VERIFIED | Legacy pending/FAB handles and helpers absent; modal-local handles remain. |
| `apps/main/index.html` | Source DOM without hidden legacy editor panels/global pending controls | VERIFIED | Contains `financialModalPendingBar`; no `#pendingBar`, `#mobileEditorFab`, or `data-legacy-flow-editor`. |
| `apps/main/modules/item-editor-controller.js` | Deleted auxiliary editor module | VERIFIED | File is missing by design. gsd artifact helper reported missing, manually reclassified as pass because deletion is the explicit artifact. |
| `tests/step1.spec.ts` | Phase 10.6.1 absorption/removal/final regression tests | VERIFIED | Contains 5 Phase 10.6.1 tests; all passed. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `bootstrap-controller.js` | `financial-modal-controller.js` | `bindStep1Events()` then `createFinancialModalController(...).bind()` | WIRED | Modal is the ordinary editor binding; no itemEditor controller is constructed. |
| `index.html` | `tests/step1.spec.ts` | Absence assertions plus modal save path | WIRED | Tests assert removed DOM is absent and still save via Financial Detail Modal. |
| `financial-modal-controller.js` | `input-sanitizer.js` / `calculator.js` | `parseSavingsAnnualRateInput`, `maturityMonth`, `annualRate` fields | WIRED | Modal writes sanitizer-compatible shapes consumed by calculator. |
| `tests/step1.spec.ts` | `persistence-controller.js` / import guard / share envelope | Import envelope round trip | WIRED | Final regression normalizes imported data and checks fields survive. |
| `styles.css` | `index.html` | Removed selectors no longer target absent DOM | WIRED | gsd key-link helper reported pattern-not-found; manually reclassified as pass because absence of `mobile-editor-fab`/`editor-actions` is the intended removal evidence. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|---|---|---|---|---|
| `financial-modal-controller.js` | `incomes[].allocations` | Modal allocation controls mutate draft items, then `persistence.commitImmediateInputs(nextInputs)` | Yes | FLOWING |
| `financial-modal-controller.js` | `savingsItems[].annualRate` | Savings additional settings parse through `parseSavingsAnnualRateInput`; missing value deletes field | Yes | FLOWING |
| `financial-modal-controller.js` | `savingsItems[].maturityMonth` | Savings additional settings write month string; sanitizer normalizes | Yes | FLOWING |
| `calculator.js` | `annualRate`, `maturityMonthIndex` | `buildSavingsBuckets(inputs)` consumes sanitized savings items | Yes | FLOWING |
| `sankey-builder.js` | income allocation links | `buildSankeyData(buildMonthlySnapshot(inputs))` consumes allocations | Yes | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Static/project checks | `npm run check` | `tsc --noEmit` passed | PASS |
| Phase 10.6.1 behavior | `npx playwright test tests/step1.spec.ts -g "Phase 10.6.1" --reporter=list` | 5/5 passed | PASS |
| Phase 10.6 regression boundary | `npx playwright test tests/step1.spec.ts -g "Phase 10.6" --reporter=list` | 20/20 passed | PASS |
| Full Step 1 regression | `npx playwright test tests/step1.spec.ts --reporter=list` | 65/65 passed | PASS |
| Source-first build reproducibility | `npm run build` | Build passed; generated version changes were reverted after verification | PASS |
| `dist/` policy | `git status --short dist` | no tracked `dist/` changes | PASS |

### Probe Execution

No phase-declared `scripts/*/tests/probe-*.sh` probes were present or required. Step 7c: SKIPPED.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| UXR-09 | 10.6.1-02, 10.6.1-03 | User completes ordinary Step 1 edits in Financial Detail Modal without a second primary financial editor path. | SATISFIED | Deleted module, removed DOM/wiring, modal-only boot/save test passed. |
| UXR-10 | 10.6.1-01, 10.6.1-03 | User can edit income account allocation inside modal row edit state without separate transfer-rule or legacy item-editor UI. | SATISFIED | Allocation add/edit/remove and over-allocation test passed; saved shape verified. |
| UXR-11 | 10.6.1-01, 10.6.1-03 | User can edit savings maturity month in modal, and matured savings stop new contributions while retained balances remain. | SATISFIED | Modal maturity input exists; projection probe passed. |
| UXR-12 | 10.6.1-01, 10.6.1-03 | User can edit item-level savings yield without replacing global default behavior for other items. | SATISFIED | Modal annualRate input exists; fallback bucket rate verified. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---:|---|---|---|
| `apps/main/modules/financial-modal-controller.js` | 988 | `placeholder` | INFO | Real fallback hint for global savings yield, not a stub. |
| `apps/main/index.html` | 149, 366 | `placeholder` | INFO | Real input hints, not placeholders for missing implementation. |
| `apps/main/modules/list-renderer.js` | 264, 349 | Korean manual-transfer wording in dead transfer-rule helper/comment | INFO | No corresponding `transferRuleList` DOM in `apps/main/index.html`; not a current user-facing path for this phase. |

No unreferenced `TBD`, `FIXME`, or `XXX` blockers were found in modified phase files.

### Human Verification Required

None. The behavior-dependent state transitions for allocation validation, modal apply/cancel, maturity closure, item yield fallback, import round trip, Sankey refresh, and pending bar behavior are exercised by passing Playwright tests.

### Gaps Summary

No blocking gaps found. The phase goal is achieved in source and tests: the auxiliary editor path is removed, useful legacy-only editing capabilities are absorbed into Financial Detail Modal, old normal-path DOM/wiring is absent, and sanitizer/persistence/Sankey/share/import/Phase 10.6 regressions pass.

---

_Verified: 2026-06-26T02:00:28Z_  
_Verifier: the agent (gsd-verifier)_
