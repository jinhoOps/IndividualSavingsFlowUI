---
phase: 10.6.1-legacy-editor-removal-and-detail-modal-capability-absorption
plan: 02
subsystem: ui
tags: [step1, financial-modal, legacy-removal, playwright]
requires:
  - phase: 10.6.1-01
    provides: Financial Detail Modal income allocation, savings maturity, and item-level savings yield editing
provides:
  - Deleted auxiliary item editor module
  - Removed normal-path legacy editor DOM, bootstrap wiring, event dependencies, global pending controls, and mobile editor FAB
  - Playwright coverage proving modal-only ordinary Step 1 editing after removal
affects: [10.6.1-03, step1-financial-modal, legacy-editor-cleanup]
tech-stack:
  added: []
  patterns:
    - Modal-only Step 1 ordinary editing through Financial Detail Modal
    - Source marker audit for removed legacy editor entry points
key-files:
  created:
    - .planning/phases/10.6.1-legacy-editor-removal-and-detail-modal-capability-absorption/10.6.1-02-SUMMARY.md
  modified:
    - tests/step1.spec.ts
    - apps/main/index.html
    - apps/main/modules/bootstrap-controller.js
    - apps/main/modules/event-bindings.js
    - apps/main/modules/dom.js
    - apps/main/modules/ui-controller.js
    - apps/main/modules/persistence-controller.js
    - apps/main/styles.css
  deleted:
    - apps/main/modules/item-editor-controller.js
key-decisions:
  - "The auxiliary item editor module was deleted rather than retained as an adapter because Plan 01 moved useful ordinary edit capabilities into the Financial Detail Modal."
  - "Normal Step 1 edit entry points now route through Financial Detail Modal; old global pending and mobile editor controls are absent from source DOM."
patterns-established:
  - "Legacy removal tests fetch source modules and assert absence markers while separately proving modal save behavior."
requirements-completed: [UXR-09]
duration: 13 min
completed: 2026-06-26
status: complete
---

# Phase 10.6.1 Plan 02: Legacy Editor Removal Summary

**Auxiliary Step 1 item editor wiring was removed so ordinary financial edits complete through the Financial Detail Modal only.**

## Performance

- **Duration:** 13 min
- **Started:** 2026-06-26T00:59:59Z
- **Completed:** 2026-06-26T01:13:00Z
- **Tasks:** 2
- **Files modified:** 9

## Accomplishments

- Replaced the old controller export contract with a Phase 10.6.1 modal-only absence test.
- Deleted `apps/main/modules/item-editor-controller.js`.
- Removed hidden legacy editor panels, global `#pendingBar`, mobile editor FAB, bootstrap construction, event binding dependencies, stale DOM handles, and UI helper exports tied to the old editor.
- Preserved Financial Detail Modal open/save behavior and Phase 10.6 modal regression contracts.

## Task Commits

1. **Task 1: Add legacy-editor absence and modal-only boot tests** - `d502701` (test)
2. **Task 2: Delete the auxiliary item editor module and remove normal-path wiring** - `1b7ba95` (feat)

**Plan metadata:** pending final docs commit

## Files Created/Modified

- `tests/step1.spec.ts` - Replaced the old item editor module contract with modal-only absence and save-path coverage.
- `apps/main/index.html` - Removed hidden legacy editor panels, old global pending controls, and mobile editor FAB.
- `apps/main/modules/bootstrap-controller.js` - Removed item editor import, construction, and controller registration.
- `apps/main/modules/event-bindings.js` - Removed `controllers.itemEditor` dependencies and routed remaining readonly category navigation through Financial Detail Modal events.
- `apps/main/modules/dom.js` - Removed legacy editor-only DOM handles.
- `apps/main/modules/ui-controller.js` - Removed global item-editor pending and mobile FAB synchronization helpers.
- `apps/main/modules/persistence-controller.js` - Removed stale global pending bar DOM mutation.
- `apps/main/styles.css` - Removed stale legacy-flow selector after source DOM deletion.
- `apps/main/modules/item-editor-controller.js` - Deleted auxiliary editor module.
- `.planning/phases/10.6.1-legacy-editor-removal-and-detail-modal-capability-absorption/10.6.1-02-SUMMARY.md` - Plan execution summary.

## Decisions Made

- Deleted the auxiliary editor module outright, matching the ADR and Plan 02 decision instead of retaining a compatibility adapter.
- Kept `financialModalPendingBar`, `financialModalCancel`, and `financialModalSave` intact as the single modal-local pending boundary.
- Treated stale CSS and persistence references as part of the same removal because the plan's marker audit requires old source markers to be absent.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Removed stale legacy marker sources outside the initial file list**
- **Found during:** Task 2
- **Issue:** The required marker audit still found a stale `[data-legacy-flow-editor]` CSS selector, and `persistence-controller.js` still referenced the old global pending bar through `dom.pendingBar`.
- **Fix:** Removed the stale CSS rule and reduced global pending bar persistence mutation to a no-op because the Financial Detail Modal owns the remaining pending boundary.
- **Files modified:** `apps/main/styles.css`, `apps/main/modules/persistence-controller.js`
- **Verification:** Source marker audit passed with `PASS: legacy editor source markers absent`; `npm run check`; targeted Playwright tests.
- **Committed in:** `1b7ba95`

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** The deviation completes the planned removal and marker audit without expanding user-facing scope.

## Issues Encountered

- Vite can return an HTML fallback for missing source-module fetches, so the absence test checks for the deleted module's export marker instead of relying only on HTTP status.

## Verification

| Command | Result |
|---|---|
| `npx playwright test tests/step1.spec.ts -g "Phase 10.6.1 legacy editor removal" --reporter=list` | PASS - 1/1 test passed |
| `npm run check` | PASS - `tsc --noEmit` completed |
| `npx playwright test tests/step1.spec.ts -g "Phase 10.6" --reporter=list` | PASS - 18/18 matching tests passed |
| `if (Test-Path 'apps/main/modules/item-editor-controller.js') ... rg ...` | PASS - deleted module absent and legacy source markers absent |

## Known Stubs

None. Placeholder scan found real input hint text in existing UI, not a stub.

## Threat Flags

None. No new network endpoints, auth paths, file access patterns, or trust-boundary schemas were introduced.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Ready for 10.6.1-03. Source-level normal user paths into the auxiliary editor are gone, and Plan 03 can focus on stale renderer/state/style trace cleanup and final source-first build verification.

## Self-Check: PASSED

- `tests/step1.spec.ts` exists and contains the Phase 10.6.1 legacy editor removal coverage.
- `apps/main/modules/item-editor-controller.js` is deleted.
- `apps/main/index.html` no longer contains hidden legacy editor panels, `#pendingBar`, or mobile editor FAB.
- Commits found: `d502701`, `1b7ba95`.

---
*Phase: 10.6.1-legacy-editor-removal-and-detail-modal-capability-absorption*
*Completed: 2026-06-26*
