---
phase: 10-step-1-2-household-budget-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/main/modules/constants.js
  - apps/main/modules/input-sanitizer.js
  - apps/main/modules/household-budget.js
  - tests/step1.spec.ts
autonomous: true
requirements:
  - HH-01
  - HH-02
  - BUD-01
  - BUD-02
  - BUD-03
  - BUD-04
must_haves:
  truths:
    - "D-01 D-02 D-03 D-04 D-05 D-06 D-07 D-08 D-09 D-10 D-11 D-12 D-13 D-14 D-15 D-16 are locked Phase 10 truths and must remain traceable across the plan set."
    - "HH-02: Sanitized Step 1 data has a newlywed household context that supports one-income and dual-income cases without requiring spouse income."
    - "BUD-01 BUD-02: Variable expense rows keep amount as the monthly target and add actualSpent as the current-month actual value."
    - "BUD-03 BUD-04: Budget status, remaining amount, progress, and month-progress projection are derived from sanitized data, not persisted as stale fields."
  artifacts:
    - path: "apps/main/modules/household-budget.js"
      provides: "Pure household budget summary, row, status, and projection helpers"
      exports: ["HOUSEHOLD_INCOME_MODES", "BUDGET_STATUS_LABELS", "isVariableExpenseItem", "projectMonthEndSpending", "resolveBudgetStatus", "buildVariableExpenseBudgetRows", "buildHouseholdBudgetSummary"]
    - path: "apps/main/modules/input-sanitizer.js"
      provides: "Backward-compatible householdContext and variable actualSpent sanitization"
      exports: ["sanitizeHouseholdContext", "isVariableExpenseItem"]
    - path: "apps/main/modules/constants.js"
      provides: "DEFAULT_INPUTS.householdContext default"
      contains: "householdContext"
    - path: "tests/step1.spec.ts"
      provides: "Phase 10 data-model and helper regression tests"
      contains: "Phase 10 household budget data model"
  key_links:
    - from: "apps/main/modules/input-sanitizer.js"
      to: "apps/main/modules/household-budget.js"
      via: "shared variable-expense predicate"
      pattern: "isVariableExpenseItem"
    - from: "apps/main/modules/household-budget.js"
      to: "tests/step1.spec.ts"
      via: "browser-module imports in Playwright"
      pattern: "projectMonthEndSpending"
---

<objective>
Create the Phase 10 data contract for Step 1.2 household budgeting.

Purpose: Make D-04, D-05, D-06, D-10, D-11, D-12, D-13, D-14, D-15, and D-16 true before UI work starts, so every later panel/modal/save path consumes one sanitized model.
Output: Sanitized `householdContext`, variable-only `actualSpent`, and pure derived helpers for `여유` / `주의` / `초과`, remaining budget, progress, and `월말 예상`.
</objective>

<execution_context>
@C:/Users/LENOVO/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/LENOVO/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-step-1-2-household-budget-foundation/10-CONTEXT.md
@.planning/phases/10-step-1-2-household-budget-foundation/10-RESEARCH.md
@.planning/phases/10-step-1-2-household-budget-foundation/10-UI-SPEC.md
@.planning/milestones/v1.8-phases/09-step-1-financial-settings-input-uiux-rebuild/09-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@apps/main/modules/constants.js
@apps/main/modules/input-sanitizer.js
@apps/main/modules/external-input-guard.js
@apps/main/modules/state.js
@tests/step1.spec.ts
</context>

## Artifacts This Phase Produces

- New file: `apps/main/modules/household-budget.js`.
- New persisted fields: `inputs.householdContext`, `householdContext.profile`, `householdContext.incomeMode`, `householdContext.spouseMonthlyIncome`, `expenseItems[].actualSpent`.
- New exported functions/constants: `sanitizeHouseholdContext`, `HOUSEHOLD_INCOME_MODES`, `BUDGET_STATUS_LABELS`, `isVariableExpenseItem`, `projectMonthEndSpending`, `resolveBudgetStatus`, `buildVariableExpenseBudgetRows`, `buildHouseholdBudgetSummary`, `renderHouseholdBudgetPanel`, `createHouseholdBudgetController`.
- New DOM selectors: `#householdBudgetPanel`, `#openHouseholdBudgetModal`, `#householdBudgetModal`, `#householdBudgetModalTitle`, `#householdIncomeModeSingle`, `#householdIncomeModeDual`, `#spouseMonthlyIncome`, `#householdBudgetRows`, `#householdBudgetSave`, `#householdBudgetCancel`, `[data-household-budget-status]`, `[data-household-budget-row]`, `[data-household-budget-actual]`, `[data-household-budget-target]`.
- Required UI labels: `신혼부부 예산`, `예산 상세 편집`, `예산 저장`, `편집 취소`, `여유`, `주의`, `초과`.
- New tests/selectors: `Phase 10 household budget data model`, `Phase 10 household budget summary panel`, `Phase 10 household budget modal workflow`.

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | Phase 10 | Step 1.2 household planning surface and variable budget fields without breaking Step 1 data flow | 10-01, 10-02, 10-03 | COVERED | Data first, summary second, modal/save third
REQ | HH-01 | Open Step 1.2 household planning surface without losing Step 1 context | 10-02, 10-03 | COVERED | Summary CTA and modal use existing Step 1 state
REQ | HH-02 | One-income and dual-income household context | 10-01, 10-03 | COVERED | Sanitizer plus modal segmented control
REQ | BUD-01 | Monthly target on variable expense items | 10-01, 10-03 | COVERED | Existing amount remains target
REQ | BUD-02 | Actual spending separate from planned amount | 10-01, 10-03 | COVERED | actualSpent only on variable rows
REQ | BUD-03 | Progress, remaining, overspend state | 10-01, 10-02, 10-03 | COVERED | Derived helpers render in panel/modal
REQ | BUD-04 | End-of-month projection | 10-01, 10-03 | COVERED | Month-progress helper and modal row display
RESEARCH | no packages | Phase 10 installs no external packages | 10-01, 10-02, 10-03 | COVERED | All plans avoid package installs
RESEARCH | sanitizer-first | Persisted fields must be normalized in sanitizeInputs() | 10-01 | COVERED | Data contract starts here
RESEARCH | amount-as-target | Keep expenseItems[].amount as canonical target | 10-01, 10-03 | COVERED | No duplicate target field
RESEARCH | explicit-save | Modal edits commit only through persistence.commitImmediateInputs() | 10-03 | COVERED | Controller save contract
RESEARCH | DOM safety | Render user-controlled names via DOM/textContent | 10-02, 10-03 | COVERED | Renderer/controller instructions
CONTEXT | D-01 | Hybrid compact panel plus modal | 10-02, 10-03 | COVERED | Panel and modal planned separately
CONTEXT | D-02 | Summary-first default and explicit save/cancel | 10-02, 10-03 | COVERED | UI ordering and commit boundary
CONTEXT | D-03 | Default panel is not full editor | 10-02 | COVERED | No default-screen rows
CONTEXT | D-04 | Newlywed household model | 10-01, 10-02, 10-03 | COVERED | Profile default and labels
CONTEXT | D-05 | Spouse income optional | 10-01, 10-03 | COVERED | Sanitized to 0 when absent
CONTEXT | D-06 | One-income and dual-income supported | 10-01, 10-03 | COVERED | incomeMode contract
CONTEXT | D-07 | Three compact summary metrics | 10-02 | COVERED | Renderer acceptance
CONTEXT | D-08 | Status badge 여유/주의/초과 | 10-01, 10-02 | COVERED | Helper and panel
CONTEXT | D-09 | Category rows inside modal only | 10-02, 10-03 | COVERED | Default-screen row ban
CONTEXT | D-10 | Only variable expense tracking | 10-01, 10-03 | COVERED | Predicate and modal filtering
CONTEXT | D-11 | Fixed expenses keep planned values only | 10-01, 10-03 | COVERED | actualSpent omitted for fixed rows
CONTEXT | D-12 | Target, actual, remaining, overspend | 10-01, 10-03 | COVERED | Derived rows
CONTEXT | D-13 | Phase 11 can feed same actual field | 10-01 | COVERED | actualSpent is stable persisted field
CONTEXT | D-14 | Simple month-progress projection | 10-01 | COVERED | projectMonthEndSpending
CONTEXT | D-15 | Projection is lightweight estimate | 10-01, 10-03 | COVERED | Helper copy and UI acceptance
CONTEXT | D-16 | Advanced forecasting deferred | 10-01, 10-03 | COVERED | No transaction-history logic
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add sanitizer-backed household budget fields</name>
  <files>apps/main/modules/constants.js, apps/main/modules/input-sanitizer.js, tests/step1.spec.ts</files>
  <read_first>
    @apps/main/modules/constants.js
    @apps/main/modules/input-sanitizer.js
    @apps/main/modules/external-input-guard.js
    @apps/main/modules/state.js
    @.planning/phases/10-step-1-2-household-budget-foundation/10-CONTEXT.md
    @.planning/phases/10-step-1-2-household-budget-foundation/10-RESEARCH.md
  </read_first>
  <behavior>
    - Legacy inputs without `householdContext` sanitize to `{ profile: "newlywed", incomeMode: "single-income", spouseMonthlyIncome: 0 }`.
    - `incomeMode` accepts only `"single-income"` and `"dual-income"`; unknown values fall back to `"single-income"`.
    - Variable expense rows preserve non-negative `actualSpent`; non-variable expense rows do not keep `actualSpent`.
    - `expenseItems[].amount` remains the monthly target and is not renamed or duplicated.
  </behavior>
  <action>Add `DEFAULT_INPUTS.householdContext` in `constants.js` with profile `newlywed`, incomeMode `single-income`, and spouseMonthlyIncome `0` per D-04, D-05, and D-06. In `input-sanitizer.js`, export `sanitizeHouseholdContext(rawContext)` and `isVariableExpenseItem(item)`. `sanitizeInputs()` must set `householdContext: sanitizeHouseholdContext(raw.householdContext)` before repair. Replace `sanitizeExpenseItems()` with an expense-specific wrapper that calls `sanitizeAllocationItems()` and then preserves `actualSpent` only when `isVariableExpenseItem(item)` is true per D-10 and D-11. Use `window.IsfUtils.sanitizeMoney(value, 0, 0)` for spouse income and actual spending; do not add transaction capture, history, merge, affordability, or comparison behavior.</action>
  <acceptance_criteria>
    - Source contains `export function sanitizeHouseholdContext` and `export function isVariableExpenseItem`.
    - `sanitizeInputs({})` returns `householdContext.incomeMode === "single-income"` and `spouseMonthlyIncome === 0`.
    - A variable expense with `actualSpent: 123456` keeps that exact Won value after sanitization.
    - A fixed expense with `actualSpent` returns without an `actualSpent` property.
    - `monthlyExpense` still equals the sum of sanitized `expenseItems[].amount`.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/step1.spec.ts -g "Phase 10 household budget data model"</automated>
  </verify>
  <done>HH-02, BUD-01, and BUD-02 have a backward-compatible sanitized data contract, and legacy saved/imported/shared Step 1 records still normalize through the existing boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create derived household budget helpers</name>
  <files>apps/main/modules/household-budget.js, tests/step1.spec.ts</files>
  <read_first>
    @apps/main/modules/input-sanitizer.js
    @apps/main/modules/financial-summary.js
    @shared/core/utils.js
    @.planning/phases/10-step-1-2-household-budget-foundation/10-UI-SPEC.md
  </read_first>
  <behavior>
    - Household monthly income is base Step 1 income plus spouseMonthlyIncome only when incomeMode is `"dual-income"`.
    - `projectMonthEndSpending(actualSpent, now)` computes current actual scaled by elapsed days in the month and never returns NaN or Infinity.
    - Status is `초과` when actual > target, `주의` when actual <= target and projected > target or actual/target >= 0.8, otherwise `여유`.
    - Variable rows expose target, actual, remaining, progressRate, projectedMonthEnd, and status labels.
  </behavior>
  <action>Create `apps/main/modules/household-budget.js`. Import `IsfUtils`, `getMonthlyIncomeTotalWon`, and `isVariableExpenseItem`. Export `HOUSEHOLD_INCOME_MODES`, `BUDGET_STATUS_LABELS`, `projectMonthEndSpending(actualSpent, now = new Date())`, `resolveBudgetStatus({ target, actual, projected })`, `buildVariableExpenseBudgetRows(inputs, now = new Date())`, and `buildHouseholdBudgetSummary(inputs, now = new Date())`. Use Won values internally and `IsfUtils.formatMoney()` only for display labels if helper output includes formatted text. Include the UI-SPEC projection helper copy `현재 사용 속도를 단순 환산한 참고값입니다.` in the summary output per D-15. Do not persist status, remaining, or projection.</action>
  <acceptance_criteria>
    - Source exports `BUDGET_STATUS_LABELS` containing exact labels `여유`, `주의`, and `초과`.
    - With target 100000 and actual 120000, row status is `초과` and remaining is `-20000`.
    - With target 100000, actual 80000, and a fixed date where projected stays under target, status is `주의`.
    - With actual 0, projection is 0 and finite.
    - `buildHouseholdBudgetSummary()` returns exactly three summary metric definitions: `가구 월수입`, `변동비 실제/목표`, and `남은 변동비`.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/step1.spec.ts -g "Phase 10 household budget data model"</automated>
  </verify>
  <done>BUD-03 and BUD-04 are represented as deterministic derived calculations that downstream UI can render without duplicating financial logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| imported/share/local Step 1 data -> sanitizer | User-controlled JSON/hash/localStorage values become durable app state. |
| sanitized inputs -> budget helpers | Derived calculations consume trusted normalized Won values and must not reintroduce unit drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | `sanitizeHouseholdContext`, `sanitizeExpenseItems` | mitigate | Clamp spouse income and actualSpent through `IsfUtils.sanitizeMoney`; reject unknown incomeMode values. |
| T-10-02 | Tampering | `projectMonthEndSpending` | mitigate | Clamp elapsed days to 1..daysInMonth and return finite integer Won values only. |
| T-10-03 | Information Disclosure | household budget helpers | accept | No network, auth, or cross-user boundary is introduced; data remains local-first. |
| T-10-SC | Tampering | npm installs | mitigate | Phase 10 installs no packages; if package-manager install appears, stop and require Package Legitimacy Audit recheck. |
</threat_model>

<verification>
Run the targeted Phase 10 data/helper tests, then run the full Step 1 suite after later UI plans complete:
<automated>npx playwright test tests/step1.spec.ts -g "Phase 10 household budget data model"</automated>
</verification>

<success_criteria>
- `sanitizeInputs()` produces household context defaults for legacy records.
- Variable expense actual spending survives sanitizer/persistence; fixed expense rows do not gain actual-spending data.
- Projection, remaining, progress, and `여유` / `주의` / `초과` are derived in `household-budget.js`.
- No deferred Phase 11-14 feature is introduced.
</success_criteria>

<output>
Create `.planning/phases/10-step-1-2-household-budget-foundation/10-01-SUMMARY.md` when done.
</output>
