---
phase: 10-step-1-2-household-budget-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - "10-01"
  - "10-02"
files_modified:
  - apps/main/index.html
  - apps/main/modules/dom.js
  - apps/main/modules/event-bindings.js
  - apps/main/modules/household-budget-controller.js
  - apps/main/styles.css
  - tests/step1.spec.ts
autonomous: true
requirements:
  - HH-01
  - HH-02
  - BUD-01
  - BUD-02
  - BUD-03
  - BUD-04
must_haves:
  truths:
    - "D-01 D-02: `예산 상세 편집` opens a detailed modal and `예산 저장` is the only durable mutation path."
    - "D-04 D-05 D-06: The modal supports newlywed one-income and dual-income context with optional spouse income."
    - "D-10 D-11 D-12 D-13: Only variable expense rows expose target/actual editing; fixed expenses retain planned values only and Phase 11 can later write the same actualSpent field."
    - "D-14 D-15 D-16: Modal projection uses simple month-progress estimate copy and excludes transaction-history forecasting."
    - "HH-01 HH-02 BUD-01 BUD-02 BUD-03 BUD-04 are all covered by the complete modal/save/regression workflow."
  artifacts:
    - path: "apps/main/index.html"
      provides: "Detailed household budget modal shell"
      contains: "householdBudgetModal"
    - path: "apps/main/modules/household-budget-controller.js"
      provides: "Draft modal controller with explicit save/cancel behavior"
      exports: ["createHouseholdBudgetController"]
    - path: "apps/main/modules/event-bindings.js"
      provides: "Controller binding from Step 1 startup flow"
      contains: "createHouseholdBudgetController"
    - path: "apps/main/styles.css"
      provides: "Responsive household modal and variable-row styles"
      contains: "household-budget-modal"
    - path: "tests/step1.spec.ts"
      provides: "Phase 10 modal workflow and full Step 1 regression checks"
      contains: "Phase 10 household budget modal workflow"
  key_links:
    - from: "apps/main/modules/household-budget-controller.js"
      to: "apps/main/modules/persistence-controller.js"
      via: "persistence.commitImmediateInputs(nextInputs)"
      pattern: "commitImmediateInputs"
    - from: "apps/main/modules/event-bindings.js"
      to: "apps/main/modules/household-budget-controller.js"
      via: "createHouseholdBudgetController(...).bind()"
      pattern: "createHouseholdBudgetController"
    - from: "apps/main/modules/household-budget-controller.js"
      to: "apps/main/modules/household-budget.js"
      via: "buildVariableExpenseBudgetRows for modal rows"
      pattern: "buildVariableExpenseBudgetRows"
---

<objective>
Implement the Step 1.2 detailed household budget modal and protect Step 1 regressions.

Purpose: Complete D-01 through D-16 by making `예산 상세 편집` open a draft modal, supporting one-income/dual-income context, saving `actualSpent` only for variable expense rows, showing target/actual/remaining/projection, and preserving existing Step 1 summary/modal/persistence/Sankey behavior.
Output: Modal shell, DOM registry, controller binding, responsive modal styling, and Playwright coverage for save/cancel, optional spouse income, variable-only tracking, projection, persistence, and regressions.
</objective>

<execution_context>
@C:/Users/LENOVO/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/LENOVO/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-step-1-2-household-budget-foundation/10-CONTEXT.md
@.planning/phases/10-step-1-2-household-budget-foundation/10-RESEARCH.md
@.planning/phases/10-step-1-2-household-budget-foundation/10-UI-SPEC.md
@.planning/milestones/v1.8-phases/09-step-1-financial-settings-input-uiux-rebuild/09-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@apps/main/index.html
@apps/main/modules/dom.js
@apps/main/modules/event-bindings.js
@apps/main/modules/financial-modal-controller.js
@apps/main/modules/persistence-controller.js
@apps/main/modules/household-budget.js
@apps/main/styles.css
@tests/step1.spec.ts
</context>

## Artifacts This Phase Produces

- New file: `apps/main/modules/household-budget.js`.
- New file: `apps/main/modules/household-budget-renderer.js`.
- New file: `apps/main/modules/household-budget-controller.js`.
- New persisted fields: `inputs.householdContext`, `householdContext.profile`, `householdContext.incomeMode`, `householdContext.spouseMonthlyIncome`, `expenseItems[].actualSpent`.
- New exported functions/constants: `sanitizeHouseholdContext`, `HOUSEHOLD_INCOME_MODES`, `BUDGET_STATUS_LABELS`, `isVariableExpenseItem`, `projectMonthEndSpending`, `resolveBudgetStatus`, `buildVariableExpenseBudgetRows`, `buildHouseholdBudgetSummary`, `renderHouseholdBudgetPanel`, `createHouseholdBudgetController`.
- New DOM selectors: `#householdBudgetPanel`, `#openHouseholdBudgetModal`, `#householdBudgetModal`, `#householdBudgetModalTitle`, `#householdIncomeModeSingle`, `#householdIncomeModeDual`, `#spouseMonthlyIncome`, `#householdBudgetRows`, `#householdBudgetSave`, `#householdBudgetCancel`, `[data-household-budget-status]`, `[data-household-budget-row]`, `[data-household-budget-actual]`, `[data-household-budget-target]`.
- Required UI labels: `신혼부부 예산`, `예산 상세 편집`, `예산 저장`, `편집 취소`, `여유`, `주의`, `초과`.
- New tests/selectors: `Phase 10 household budget data model`, `Phase 10 household budget summary panel`, `Phase 10 household budget modal workflow`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the detailed household budget modal shell</name>
  <files>apps/main/index.html, apps/main/modules/dom.js, apps/main/styles.css, tests/step1.spec.ts</files>
  <read_first>
    @apps/main/index.html
    @apps/main/modules/dom.js
    @apps/main/styles.css
    @.planning/phases/10-step-1-2-household-budget-foundation/10-UI-SPEC.md
  </read_first>
  <behavior>
    - Modal title is exactly `신혼부부 예산 상세`.
    - Modal description is exactly `생활 변동비의 목표와 이번 달 실제 사용액을 저장할 때만 반영합니다.`
    - Modal actions are visible text buttons `편집 취소` and `예산 저장`.
    - Household context section contains segmented controls for `1인 소득` and `맞벌이`.
  </behavior>
  <action>In `apps/main/index.html`, add `#householdBudgetModal` as a sibling modal overlay near `#financialModal`, not inside the existing financial modal. Include `#householdBudgetModalTitle`, a close button with accessible label, a description paragraph, household context segmented buttons `#householdIncomeModeSingle` and `#householdIncomeModeDual`, spouse income field `#spouseMonthlyIncome` using `data-money-input="won"`, budget summary host, `#householdBudgetRows`, empty state copy from UI-SPEC, and footer buttons `#householdBudgetCancel` / `#householdBudgetSave`. In `dom.js`, register every modal element used by the controller. In `styles.css`, add responsive modal and row styles using existing modal tokens and 44px mobile touch targets.</action>
  <acceptance_criteria>
    - `#householdBudgetModal` exists and is hidden by default.
    - The modal DOM contains exact labels `신혼부부 예산 상세`, `생활 변동비의 목표와 이번 달 실제 사용액을 저장할 때만 반영합니다.`, `편집 취소`, and `예산 저장`.
    - `#spouseMonthlyIncome` is present and uses `data-money-input="won"`.
    - At mobile width, modal controls have no overlap and no page overflow greater than 4px.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/step1.spec.ts -g "Phase 10 household budget modal workflow"</automated>
  </verify>
  <done>The detailed Step 1.2 editing surface exists with exact UI-SPEC labels and responsive structure, without changing the existing financial modal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind draft editing, save, and cancel behavior</name>
  <files>apps/main/modules/household-budget-controller.js, apps/main/modules/event-bindings.js, tests/step1.spec.ts</files>
  <read_first>
    @apps/main/modules/event-bindings.js
    @apps/main/modules/financial-modal-controller.js
    @apps/main/modules/persistence-controller.js
    @apps/main/modules/household-budget.js
    @apps/main/modules/input-sanitizer.js
    @.planning/phases/10-step-1-2-household-budget-foundation/10-RESEARCH.md
  </read_first>
  <behavior>
    - Clicking `#openHouseholdBudgetModal` opens `#householdBudgetModal` without navigation and without changing Sankey.
    - Draft changes update modal metrics immediately but do not write localStorage until `예산 저장`.
    - `편집 취소` and the close button discard householdContext and actualSpent edits.
    - `예산 저장` calls `persistence.commitImmediateInputs(nextInputs)` with updated householdContext and expenseItems.
  </behavior>
  <action>Create `apps/main/modules/household-budget-controller.js` exporting `createHouseholdBudgetController({ persistence, getVisibleInputs, renderAll } = {})`. Follow the existing `createFinancialModalController` draft pattern: clone baseline inputs on open, keep modal-only draft state, render only variable expense rows from `buildVariableExpenseBudgetRows()`, update row target via `expenseItems[index].amount`, update actual via `expenseItems[index].actualSpent`, and update householdContext from income mode and spouse income controls. `예산 저장` must call `persistence.commitImmediateInputs({ ...baselineInputs, householdContext: draftHouseholdContext, expenseItems: draftExpenseItems })` and then close. `편집 취소` must close without calling persistence. In `event-bindings.js`, import `createHouseholdBudgetController` and call `.bind()` in `bindStep1Events()` after the financial modal controller binding. Use DOM/textContent for row names and metric labels; no row rendering with user-controlled `innerHTML`.</action>
  <acceptance_criteria>
    - Source contains `export function createHouseholdBudgetController`.
    - `event-bindings.js` imports and binds the household controller.
    - Saved localStorage includes `householdContext.incomeMode === "dual-income"` and a numeric `spouseMonthlyIncome` only after `예산 저장`.
    - Canceling an edited modal leaves localStorage and default summary panel unchanged.
    - Fixed expense rows do not render `[data-household-budget-actual]` controls.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/step1.spec.ts -g "Phase 10 household budget modal workflow"</automated>
  </verify>
  <done>HH-01, HH-02, BUD-01, BUD-02, BUD-03, and BUD-04 work through an explicit modal save/cancel lifecycle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Cover persistence, projection, and Step 1 regressions</name>
  <files>tests/step1.spec.ts</files>
  <read_first>
    @tests/step1.spec.ts
    @apps/main/modules/persistence-controller.js
    @apps/main/modules/financial-summary.js
    @apps/main/modules/financial-modal-controller.js
    @apps/main/modules/sankey-builder.js
    @.planning/phases/10-step-1-2-household-budget-foundation/10-CONTEXT.md
  </read_first>
  <behavior>
    - A saved variable budget actual survives page reload through localStorage.
    - Imported/share-shaped legacy data without household fields still opens the panel and modal.
    - Month projection text includes `월말 예상` and `현재 사용 속도를 단순 환산한 참고값입니다.`
    - Existing Phase 09 summary cards, category modal editing, source-account flow, and Sankey tooltip regressions stay green.
  </behavior>
  <action>Extend `tests/step1.spec.ts` under `test.describe('Phase 10 household budget modal workflow', ...)`. Add tests that create sanitized inputs with at least one `변동비` row, open `예산 상세 편집`, toggle `맞벌이`, fill `#spouseMonthlyIncome`, edit target and actual via `[data-household-budget-target]` and `[data-household-budget-actual]`, verify draft-only updates before save, save, reload, and assert localStorage plus summary panel text. Add a cancel test that edits fields and clicks `편집 취소`, then asserts durable state is unchanged. Add a fixed-row exclusion assertion by seeding a `고정비` row with `actualSpent` and verifying no modal actual input renders for it. Keep all existing Phase 09 describe blocks intact.</action>
  <acceptance_criteria>
    - Tests assert exact labels `신혼부부 예산`, `예산 상세 편집`, `예산 저장`, `편집 취소`, `여유`, `주의`, and `초과` where applicable.
    - Tests assert `actualSpent` persists for variable rows and is absent from fixed rows.
    - Tests assert the modal projection copy includes `월말 예상` and `현재 사용 속도를 단순 환산한 참고값입니다.`
    - Full `tests/step1.spec.ts` passes after Phase 10 implementation.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/step1.spec.ts</automated>
  </verify>
  <done>Backward-compatible sanitizer/persistence/share/import behavior and existing Step 1 summary, modal, persistence, and Sankey regressions are protected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| modal form controls -> draft state | User-entered amounts and mode selections become candidate app state. |
| draft state -> persistence.commitImmediateInputs | Draft household budget data crosses into durable local/share/import-compatible Step 1 state. |
| sanitized data -> Sankey/summary renderers | New budget fields must not corrupt existing Sankey or summary consumers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | Tampering | `createHouseholdBudgetController` | mitigate | Keep edits in draft state and commit only through `persistence.commitImmediateInputs()` after `예산 저장`. |
| T-10-08 | Tampering | modal amount fields | mitigate | Parse target/actual/spouse income through existing Won helpers and rely on sanitizer for final durable values. |
| T-10-09 | Information Disclosure | modal row rendering | mitigate | Render expense names with `textContent`; do not use user-controlled `innerHTML`. |
| T-10-10 | Denial of Service | modal responsive layout | mitigate | Playwright checks 390px overflow and control overlap; CSS uses stacked compact cards on mobile. |
| T-10-SC | Tampering | npm installs | mitigate | Phase 10 installs no packages; if package-manager install appears, stop and require Package Legitimacy Audit recheck. |
</threat_model>

<verification>
Run targeted modal workflow tests, then the full Step 1 suite:
<automated>npx playwright test tests/step1.spec.ts -g "Phase 10 household budget modal workflow"</automated>
<automated>npx playwright test tests/step1.spec.ts</automated>
</verification>

<success_criteria>
- `예산 상세 편집` opens `신혼부부 예산 상세` without losing existing Step 1 context.
- `1인 소득` and `맞벌이` both work, and spouse income remains optional.
- Variable rows support target, actual, remaining, progress, status, and month-progress projection.
- `편집 취소` discards draft changes; `예산 저장` persists through the existing sanitizer/persistence path.
- Existing Step 1 summary cards, financial modal editing, local persistence, and Sankey tests still pass.
</success_criteria>

<output>
Create `.planning/phases/10-step-1-2-household-budget-foundation/10-03-SUMMARY.md` when done.
</output>
