---
phase: 10-step-1-2-household-budget-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - apps/main/index.html
  - apps/main/modules/dom.js
  - apps/main/modules/render-orchestrator.js
  - apps/main/modules/household-budget-renderer.js
  - apps/main/styles.css
  - tests/step1.spec.ts
autonomous: true
requirements:
  - HH-01
  - HH-02
  - BUD-01
  - BUD-02
  - BUD-03
  - BUD-04
must_haves:
  truths:
    - "D-01 D-02 D-03: The Step 1 default screen shows a compact `신혼부부 예산` status and entry panel, not a full editor."
    - "D-07 D-08 D-09: The summary panel renders exactly three metrics plus one `여유` / `주의` / `초과` badge; category-level rows stay out of the default screen."
    - "HH-01: The user can see and open the Step 1.2 household planning entry point while the existing Step 1 summary cards and Sankey remain in order."
    - "BUD-03 BUD-04: Summary metrics render from the same derived household budget model created in Plan 10-01."
  artifacts:
    - path: "apps/main/index.html"
      provides: "Summary-panel host for the Step 1.2 household budget entry"
      contains: "householdBudgetPanel"
    - path: "apps/main/modules/household-budget-renderer.js"
      provides: "DOM-safe compact summary panel renderer"
      exports: ["renderHouseholdBudgetPanel"]
    - path: "apps/main/modules/render-orchestrator.js"
      provides: "renderAll connection from sanitized inputs to household summary panel"
      contains: "renderHouseholdBudgetPanel"
    - path: "apps/main/styles.css"
      provides: "Responsive flat-panel styling for summary panel and status badges"
      contains: "household-budget-panel"
    - path: "tests/step1.spec.ts"
      provides: "Phase 10 summary-panel regression tests"
      contains: "Phase 10 household budget summary panel"
  key_links:
    - from: "apps/main/modules/render-orchestrator.js"
      to: "apps/main/modules/household-budget-renderer.js"
      via: "renderAll calls renderHouseholdBudgetPanel"
      pattern: "renderHouseholdBudgetPanel"
    - from: "apps/main/modules/household-budget-renderer.js"
      to: "apps/main/modules/household-budget.js"
      via: "buildHouseholdBudgetSummary output"
      pattern: "summary.metrics"
---

<objective>
Add the Step 1 default-screen `신혼부부 예산` compact summary panel.

Purpose: Implement D-01, D-02, D-03, D-07, D-08, and D-09 as a compact operational entry point that preserves the Phase 09 summary-first layout and keeps Sankey directly below the summary area.
Output: Summary host markup, DOM registry entries, renderer, render pipeline wiring, responsive styles, and Playwright coverage for the three locked metrics and badge labels.
</objective>

<execution_context>
@C:/Users/LENOVO/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/LENOVO/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-step-1-2-household-budget-foundation/10-CONTEXT.md
@.planning/phases/10-step-1-2-household-budget-foundation/10-RESEARCH.md
@.planning/phases/10-step-1-2-household-budget-foundation/10-UI-SPEC.md
@.planning/milestones/v1.8-phases/09-step-1-financial-settings-input-uiux-rebuild/09-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@apps/main/index.html
@apps/main/modules/dom.js
@apps/main/modules/render-orchestrator.js
@apps/main/modules/financial-summary-renderer.js
@apps/main/modules/household-budget.js
@apps/main/styles.css
@tests/step1.spec.ts
</context>

## Artifacts This Phase Produces

- New file: `apps/main/modules/household-budget.js`.
- New file: `apps/main/modules/household-budget-renderer.js`.
- New persisted fields: `inputs.householdContext`, `householdContext.profile`, `householdContext.incomeMode`, `householdContext.spouseMonthlyIncome`, `expenseItems[].actualSpent`.
- New exported functions/constants: `sanitizeHouseholdContext`, `HOUSEHOLD_INCOME_MODES`, `BUDGET_STATUS_LABELS`, `isVariableExpenseItem`, `projectMonthEndSpending`, `resolveBudgetStatus`, `buildVariableExpenseBudgetRows`, `buildHouseholdBudgetSummary`, `renderHouseholdBudgetPanel`, `createHouseholdBudgetController`.
- New DOM selectors: `#householdBudgetPanel`, `#openHouseholdBudgetModal`, `#householdBudgetModal`, `#householdBudgetModalTitle`, `#householdIncomeModeSingle`, `#householdIncomeModeDual`, `#spouseMonthlyIncome`, `#householdBudgetRows`, `#householdBudgetSave`, `#householdBudgetCancel`, `[data-household-budget-status]`, `[data-household-budget-row]`, `[data-household-budget-actual]`, `[data-household-budget-target]`.
- Required UI labels: `신혼부부 예산`, `예산 상세 편집`, `예산 저장`, `편집 취소`, `여유`, `주의`, `초과`.
- New tests/selectors: `Phase 10 household budget data model`, `Phase 10 household budget summary panel`, `Phase 10 household budget modal workflow`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the compact summary panel host and DOM registry</name>
  <files>apps/main/index.html, apps/main/modules/dom.js, tests/step1.spec.ts</files>
  <read_first>
    @apps/main/index.html
    @apps/main/modules/dom.js
    @.planning/phases/10-step-1-2-household-budget-foundation/10-UI-SPEC.md
    @.planning/milestones/v1.8-phases/09-step-1-financial-settings-input-uiux-rebuild/09-CONTEXT.md
  </read_first>
  <behavior>
    - The summary area contains a dedicated `#householdBudgetPanel` before `#summaryCards`.
    - The Sankey panel remains after the entire summary area.
    - The host does not contain category rows, item inputs, pasted capture, merge controls, comparison chart, or affordability controls.
  </behavior>
  <action>In `apps/main/index.html`, add a dedicated empty host inside `<section class="panel summary-panel">` after the section heading and before `#summaryCards`: `div#householdBudgetPanel` with an appropriate class and `aria-live="polite"`. In `apps/main/modules/dom.js`, register `householdBudgetPanel: document.getElementById("householdBudgetPanel")`. Keep existing `summaryCards` and Sankey markup untouched, and do not move the controls panel.</action>
  <acceptance_criteria>
    - `#householdBudgetPanel` exists before `#summaryCards` in DOM order.
    - `.summary-panel` still precedes `.sankey-panel`.
    - No `[data-household-budget-row]` or actual-spending input exists on the default screen.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/step1.spec.ts -g "Phase 10 household budget summary panel"</automated>
  </verify>
  <done>HH-01 has a stable default-screen Step 1.2 entry host that preserves Phase 09 summary-first ordering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render the three-metric household budget panel</name>
  <files>apps/main/modules/household-budget-renderer.js, apps/main/modules/render-orchestrator.js, tests/step1.spec.ts</files>
  <read_first>
    @apps/main/modules/household-budget.js
    @apps/main/modules/financial-summary-renderer.js
    @apps/main/modules/render-orchestrator.js
    @shared/core/utils.js
    @.planning/phases/10-step-1-2-household-budget-foundation/10-UI-SPEC.md
  </read_first>
  <behavior>
    - The panel title is exactly `신혼부부 예산`.
    - The CTA text is exactly `예산 상세 편집`.
    - The panel renders exactly three metrics: `가구 월수입`, `변동비 실제/목표`, `남은 변동비`.
    - The panel renders one visible badge whose text is one of `여유`, `주의`, or `초과`.
  </behavior>
  <action>Create `apps/main/modules/household-budget-renderer.js` exporting `renderHouseholdBudgetPanel(host, summary)`. Build all nodes with `document.createElement()` and assign `textContent`; do not render user-controlled names with `innerHTML`. The CTA button must have `id="openHouseholdBudgetModal"`, `type="button"`, and `data-household-budget-action="open"`. In `render-orchestrator.js`, import `buildHouseholdBudgetSummary` and `renderHouseholdBudgetPanel`, then call `renderHouseholdBudgetPanel(dom.householdBudgetPanel, buildHouseholdBudgetSummary(inputs))` inside `renderAll()` before rendering existing financial summary groups. Preserve the existing `renderFinancialSummaryGroups(dom.summaryCards, buildFinancialSummaryGroups(...))` call.</action>
  <acceptance_criteria>
    - `renderHouseholdBudgetPanel` is exported and uses `replaceChildren`.
    - `#householdBudgetPanel` text includes `신혼부부 예산`, `예산 상세 편집`, `가구 월수입`, `변동비 실제/목표`, and `남은 변동비`.
    - The panel has exactly three `.household-budget-metric` elements.
    - Existing `[data-financial-summary-group="core-metrics"]` and `[data-financial-summary-group="outflow"]` still render.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/step1.spec.ts -g "Phase 10 household budget summary panel|Phase 09 financial summary card surface"</automated>
  </verify>
  <done>D-07 and D-08 are visible on the default screen from the Plan 10-01 derived model, without changing existing summary cards.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Style and regression-test the summary panel density</name>
  <files>apps/main/styles.css, tests/step1.spec.ts</files>
  <read_first>
    @apps/main/styles.css
    @.planning/phases/10-step-1-2-household-budget-foundation/10-UI-SPEC.md
    @tests/step1.spec.ts
  </read_first>
  <behavior>
    - Desktop layout is a flat panel with title/badge/CTA row and a 3-column metric grid.
    - Mobile layout at 390px has no horizontal overflow and safely wraps title, badge, CTA, and metrics.
    - New Phase 10 selectors use flat borders, existing tokens, 8px or smaller radius, and no decorative gradients/orbs.
  </behavior>
  <action>Add `household-budget-panel` CSS near existing financial summary styles and responsive rules near the existing `@media` blocks. Use UI-SPEC typography and status colors: `여유` with `var(--tone-accent)` or `#1e8b7c`, `주의` with amber, and `초과` with red. The CTA must meet the UI-SPEC minimum touch target at mobile widths. Add Playwright assertions under `test.describe('Phase 10 household budget summary panel', ...)` for 1280, 768, and 390 widths: exactly three metrics, one allowed badge label, no default-screen row table, no overflow greater than 4px, and existing Sankey labels still render.</action>
  <acceptance_criteria>
    - CSS includes `.household-budget-panel`, `.household-budget-panel__metrics`, `.household-budget-metric`, and `[data-household-budget-status]` selectors.
    - At 390px viewport, `document.documentElement.scrollWidth - document.documentElement.clientWidth <= 4`.
    - The default screen does not show `[data-household-budget-row]` or `[data-household-budget-actual]`.
    - Existing Phase 09 summary-first Sankey workflow test remains green.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/step1.spec.ts -g "Phase 10 household budget summary panel|Phase 09 final responsive user flow coverage"</automated>
  </verify>
  <done>D-01, D-02, D-03, D-07, D-08, and D-09 are implemented on the default screen with responsive density and regression coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sanitized inputs -> DOM summary panel | Local user/imported values become visible text in the browser. |
| summary CTA -> modal controller | User interaction crosses from status surface into editing surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Tampering | `renderHouseholdBudgetPanel` | mitigate | Render via DOM creation and `textContent`; keep CTA as a button with explicit dataset action. |
| T-10-05 | Denial of Service | responsive summary panel | mitigate | CSS must wrap metric values and avoid horizontal page overflow at 390px. |
| T-10-06 | Repudiation | summary panel data | accept | Panel is derived status only; durable mutation is in Plan 10-03 modal save path. |
| T-10-SC | Tampering | npm installs | mitigate | Phase 10 installs no packages; if package-manager install appears, stop and require Package Legitimacy Audit recheck. |
</threat_model>

<verification>
Run the targeted summary and Phase 09 regression checks:
<automated>npx playwright test tests/step1.spec.ts -g "Phase 10 household budget summary panel|Phase 09 financial summary card surface|Phase 09 final responsive user flow coverage"</automated>
</verification>

<success_criteria>
- `신혼부부 예산` appears in the Step 1 summary area before Sankey.
- The default panel shows exactly three metrics and one status badge.
- Detailed budget rows are absent from the default screen.
- Existing Step 1 summary cards and Sankey order remain protected.
</success_criteria>

<output>
Create `.planning/phases/10-step-1-2-household-budget-foundation/10-02-SUMMARY.md` when done.
</output>
