---
phase: 10.6-financial-detail-modal-editing-ux-repair
plan: 02
type: execute
wave: 2
depends_on:
  - 10.6-01
files_modified:
  - tests/step1.spec.ts
  - apps/main/index.html
  - apps/main/modules/financial-modal-controller.js
autonomous: true
requirements:
  - UXR-06
  - UXR-07
  - UXR-08
must_haves:
  truths:
    - "D-35 D-36 D-37 D-38 D-39 D-40: The in-modal pending bar is the only apply/cancel path, uses 취소 and 적용, keeps the modal open, animates out, and is mobile-safe."
    - "D-41 D-42 D-43 D-44 D-45 D-46 D-47: Each tab owns its add action; inline temporary rows open in edit mode, stay non-pending while empty, become pending after meaningful values, and apply/cancel correctly."
    - "D-48 D-49 D-50 D-51: Delete is draft-only for existing rows and immediately removes empty temporary rows without pending state."
    - "D-52 D-53 D-54 D-55: X, outside click, and ESC prompt only when pending changes exist; confirm discards and closes; cancel keeps modal and pending changes."
    - "D-61 D-62 D-63 D-64: Apply-time validation remains row-local, keeps the pending bar open, expands the first invalid row, and uses the locked short Korean messages."
  artifacts:
    - path: "tests/step1.spec.ts"
      provides: "Pending bar, inline add/delete, close prompt, and row-local validation regressions"
    - path: "apps/main/index.html"
      provides: "Stable pending bar DOM with D-36 copy and no implementation-language comment"
    - path: "apps/main/modules/financial-modal-controller.js"
      provides: "In-place apply/cancel, inline new-row lifecycle, draft-only deletion, close guard, and row-local validation"
  key_links:
    - from: "apps/main/modules/financial-modal-controller.js"
      to: "apps/main/modules/persistence-controller.js"
      via: "persistence.commitImmediateInputs(nextInputs) only when pending 적용 passes validation"
      pattern: "commitImmediateInputs"
    - from: "apps/main/index.html"
      to: "apps/main/modules/dom.js"
      via: "existing financialModalCancel, financialModalSave, and financialModalPendingBar IDs"
      pattern: "financialModalSave"
---

<objective>
Convert the in-modal pending bar into the sole explicit pending-change boundary, and repair add, delete, close, and validation behavior around it.

Purpose: Make draft changes predictable and non-destructive while preserving the existing sanitizer/persistence boundary from Phase 10.5.
Output: Tests and implementation for UXR-06, UXR-07, and UXR-08 covering D-35, D-36, D-37, D-38, D-39, D-40, D-41, D-42, D-43, D-44, D-45, D-46, D-47, D-48, D-49, D-50, D-51, D-52, D-53, D-54, D-55, D-61, D-62, D-63, D-64.
</objective>

<execution_context>
@C:/Users/LENOVO/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/LENOVO/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10.6-financial-detail-modal-editing-ux-repair/10.6-CONTEXT.md
@.planning/phases/10.6-financial-detail-modal-editing-ux-repair/10.6-RESEARCH.md
@.planning/phases/10.6-financial-detail-modal-editing-ux-repair/10.6-PATTERNS.md
@.planning/phases/10.5-financial-settings-ux-integration-repair/10.5-VERIFICATION.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@apps/main/index.html
@apps/main/modules/dom.js
@apps/main/modules/financial-modal-controller.js
@apps/main/modules/persistence-controller.js
@tests/step1.spec.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add pending bar, inline add/delete, close guard, and validation tests</name>
  <files>tests/step1.spec.ts</files>
  <behavior>
    - Pending bar buttons read 취소 and 적용, appear only after real draft changes, and both actions keep the modal open.
    - 취소 restores baseline values, removes draft-only rows, folds rows, hides the pending bar, and does not mutate localStorage.
    - 적용 validates rows, persists through existing storage, keeps the active tab and modal open, folds rows, refreshes summary/Sankey, and hides the pending bar after feedback.
    - Tab-header add actions create inline temporary rows in the visible section; empty temp rows disappear without pending state, non-empty temp rows are pending and persist only on apply.
    - Existing-row delete is reversible until apply; empty temp-row delete needs no pending state.
    - Pending close attempts trigger the locked confirm message for X, overlay, and ESC; cancel preserves the modal and pending bar, confirm discards and closes.
    - Apply-time invalid rows show the locked row-local messages and expand the first invalid row.
  </behavior>
  <action>Add Phase 10.6 tests for D-35 through D-55 and D-61 through D-64. Use Playwright dialog handlers for close confirmation, localStorage assertions for apply/cancel/delete boundaries, and row-local locator assertions instead of alert expectations. Keep Phase 10.5 no-couple-UI helper calls in these flows so pending work cannot reintroduce prohibited labels or panels.</action>
  <verify>
    <automated>npx playwright test tests/step1.spec.ts -g "Phase 10.6 pending|Phase 10.6 add|Phase 10.6 delete|Phase 10.6 close|Phase 10.6 validation" --reporter=list</automated>
  </verify>
  <done>Tests cover every pending/add/delete/close/validation decision in D-35 through D-55 and D-61 through D-64, failing before Task 2 and passing after Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement in-place pending apply/cancel, inline item lifecycle, draft delete, close guard, and row-local validation</name>
  <files>apps/main/index.html, apps/main/modules/financial-modal-controller.js, tests/step1.spec.ts</files>
  <behavior>
    - Pending cancel and apply are separate from modal close and never close the modal by themselves.
    - The add action is rendered inside the active tab item header with tab-specific copy and creates an inline draft row at the bottom of the visible section.
    - Delete marks existing rows removed only in draft state and restores them on cancel.
    - Close requests branch on hasRealDraftChanges and use the locked confirmation message.
    - Validation errors live in the editing row and block apply without hiding the pending bar.
  </behavior>
  <action>Update apps/main/index.html pending bar copy to 취소 and 적용 while preserving the stable IDs registered by dom.js. Remove the implementation-language modal comment without changing unrelated DOM. In financial-modal-controller.js, split modal close from pending cancel/apply; cancel must restore from baselineInputs and keep the modal open, while apply must validate row-local state, build next inputs from all integrated draft categories, call persistence.commitImmediateInputs(nextInputs), refresh baselineInputs from the applied visible data, keep the active tab, fold rows, and hide the pending bar after short feedback. Replace detached create flow behavior with tab-header add controls and inline temporary-row lifecycle. Change remove behavior so existing rows are draft-removed and empty temporary rows are simply discarded. Add close guards for close button, overlay click, and ESC only when real pending changes exist.</action>
  <verify>
    <automated>npm run check</automated>
    <automated>npx playwright test tests/step1.spec.ts -g "Phase 10.6 pending|Phase 10.6 add|Phase 10.6 delete|Phase 10.6 close|Phase 10.6 validation" --reporter=list</automated>
  </verify>
  <done>UXR-07 is fully satisfied: pending changes apply/discard only through the modal pending bar, the modal stays open unless the user confirms a pending close, and validation remains actionable inside rows.</done>
</task>
</tasks>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | phase-10.6 | Pending changes are applied only through an explicit in-modal pending bar | 10.6-02 | COVERED | Pending apply/cancel, close guard, and validation behavior.
REQ | UXR-06 | Predictable row-level editing lifecycle | 10.6-02 | COVERED | Inline add/delete and validation complete the row lifecycle.
REQ | UXR-07 | Pending bar applies/discards draft changes without closing modal | 10.6-02 | COVERED | Directly implements D-35 through D-40.
REQ | UXR-08 | Preserve sanitizer/persistence/Sankey/no-couple contracts | 10.6-02 | PARTIAL | Persistence boundary and no-couple checks covered; full regression in 10.6-03.
RESEARCH | apply/cancel in place through commitImmediateInputs | 10.6-02 | COVERED | Existing save boundary is preserved.
RESEARCH | row-local validation instead of alert validation | 10.6-02 | COVERED | D-61 through D-64 implemented.
CONTEXT | D-35 D-36 D-37 D-38 D-39 D-40 D-41 D-42 D-43 D-44 D-45 D-46 D-47 D-48 D-49 D-50 D-51 D-52 D-53 D-54 D-55 D-61 D-62 D-63 D-64 | Locked Phase 10.6 decisions | 10.6-02 | COVERED | Explicitly cited in objective, must-haves, and task actions.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Draft UI to local persistence | User-edited values become durable only through apply. |
| Browser close interactions to draft state | X, outside click, and ESC can discard draft data only after confirmation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.6-03 | Tampering | pending apply path | mitigate | Use row-local validation before commitImmediateInputs and keep sanitizeInputs after apply. |
| T-10.6-04 | Repudiation | pending close/discard | mitigate | Use explicit confirmation copy when pending changes exist. |
| T-10.6-05 | Denial of Service | stuck validation state | mitigate | Expand first invalid row and keep pending bar visible so the user can resolve or cancel. |
| T-10.6-SC | Tampering | npm installs | accept | No package installation is planned for this phase. |
</threat_model>

<verification>
Run npm run check plus targeted Phase 10.6 pending/add/delete/close/validation Playwright tests. Do not consider this plan complete if any pending action closes the modal by itself.
</verification>

<success_criteria>
UXR-07 is passing with tests; UXR-06 row lifecycle is complete; UXR-08 persistence and no-couple contracts are preserved in the touched flows.
</success_criteria>

<output>
Create `.planning/phases/10.6-financial-detail-modal-editing-ux-repair/10.6-02-SUMMARY.md` when done.
</output>
