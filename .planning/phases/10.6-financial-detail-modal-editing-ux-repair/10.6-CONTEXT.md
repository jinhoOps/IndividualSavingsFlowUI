# Phase 10.6: Financial Detail Modal Editing UX Repair - Context

**Gathered:** 2026-06-25
**Status:** Ready for planning

<domain>
## Phase Boundary

Phase 10.6 repairs the existing `재무설정 상세` modal editing experience. It does not add a new budgeting product surface. It makes the modal open in compact read mode, supports predictable row-level editing, fixes pending-change behavior, simplifies variable-expense treatment, and keeps all durable mutation behind an explicit in-modal pending bar.

This phase must preserve the Phase 10.5 decision to remove couple/self/spouse UI from the visible flow. It must not restore `#householdBudgetPanel`, `부부합산`, `본인 설정`, `배우자 설정`, spouse income editing, or household mode controls.

</domain>

<decisions>
## Implementation Decisions

### Scope And Carry-Forward
- **D-01:** Use Phase 10.6 as a new follow-up phase rather than reopening Phase 10.5.
- **D-02:** Keep the Phase 10.5 integrated `재무설정 상세` direction. This phase polishes the modal editing UX; it does not reintroduce separate household/couple UI.
- **D-03:** Keep the default Step 1 screen light. Dense editing remains inside the modal.

### Compact Read Mode
- **D-04:** The modal opens in compact read mode. Opening the modal must not show the pending bar.
- **D-05:** Read-mode rows show only `항목명 + 금액` by default.
- **D-06:** Hide repeated category/group labels when they duplicate the row meaning. Avoid outputs like `수입, 수입` or `변동비, 변동비`.
- **D-07:** Account/group/category details are available in edit mode or supporting detail, not required in compact row read mode.
- **D-08:** Remove repeated panel titles such as `financial-detail-panel__title` when the active tab name already communicates the section.
- **D-09:** Remove or hide user-facing traces of implementation language such as "신설 모달".
- **D-10:** Desktop/wide screens may keep a right-side edit icon if it does not cause wrapping. Rows themselves should also be clickable for edit entry.
- **D-11:** Mobile/narrow screens use whole-row click for edit entry. Edit icons may be hidden or made non-layout-affecting to protect `이름 + 금액` density.

### Row-Level Editing
- **D-12:** Editing is row-level. One selected row expands; other rows remain in compact read mode.
- **D-13:** Expanded edit field order is: 이름 -> 금액 -> 계좌/그룹.
- **D-14:** Existing row values must be prefilled when editing begins. Editing must feel like modifying an existing row, not recreating it.
- **D-15:** Clicking outside the edited row folds it back to read mode.
- **D-16:** If no value changed, folding the row leaves no pending state.
- **D-17:** If values changed, folding the row preserves draft changes and shows the pending bar.
- **D-18:** Clicking another row while changes are pending is allowed: the current row folds, the new row opens, and accumulated draft changes remain pending.
- **D-19:** Tab navigation while changes are pending is allowed. The current row folds, draft changes persist, and the pending bar remains visible.
- **D-20:** `적용` persists all accumulated draft changes across tabs at once. `취소` reverts all accumulated draft changes across tabs.

### Money Editing
- **D-21:** Money fields support direct input, `+` / `-` steppers, and quick increase buttons.
- **D-22:** `+` / `-` steppers change by 10,000 KRW.
- **D-23:** Quick buttons are increase-only: `+5만`, `+10만`, `+100만`.
- **D-24:** Direct input may use 1,000 KRW increments and must reject invalid non-1,000-unit amounts.
- **D-25:** Money values must not go below 0.
- **D-26:** Direct-input validation copy should be short and local to the row, for example `금액은 1,000원 단위로 입력해 주세요.`

### Variable Expense Simplification
- **D-27:** Variable expenses should be simplified. Users are expected to enter an average expected amount, not manage heavy actual-spending/projection detail in the primary editing flow.
- **D-28:** Treat variable expenses similarly to fixed expenses in row behavior, with the name/type carrying the meaning.
- **D-29:** Add a separate variance/range field for variable expenses.
- **D-30:** Variance input supports direct entry plus quick buttons `±1만`, `±5만`, `±10만`.
- **D-31:** Individual read-mode rows still show only the average amount. Do not add per-row `±범위` text to compact rows.
- **D-32:** Individual edit mode exposes average amount and `±범위`.
- **D-33:** Show range effects only in the `재무설정 상세` summary area, for example as a living-expense expected range. Do not force range display into every row.

### Pending Bar
- **D-34:** The pending bar appears only after real draft changes. Edit entry, modal open, tab switch, row selection, and opening an empty new-item row do not count as changes by themselves.
- **D-35:** The pending bar is the only visible path for applying or canceling draft changes. Do not show a separate active `재무설정 저장` button at the same time.
- **D-36:** Pending bar button copy is `취소` / `적용`.
- **D-37:** `취소` reverts draft changes, removes draft-only new rows, folds edit rows, keeps the modal open, and hides the pending bar with a short fade/slide down animation.
- **D-38:** `적용` persists draft changes through the existing save boundary, keeps the modal open, leaves the user on the current tab, folds all rows to read mode, gives a short point-color feedback, then hides the pending bar with fade/slide down.
- **D-39:** On mobile, the pending bar is sticky at the modal bottom. Add enough modal-body bottom padding so it never covers focused fields or the last row.
- **D-40:** Pending bar touch targets must be at least 44px on mobile.

### New Item Flow
- **D-41:** Each tab's item header owns the add action on the right side, using tab-specific copy such as `수입 추가`, `생활비 추가`, `투자 추가`, or `저축 추가`.
- **D-42:** On mobile, the add action may wrap below the header when needed.
- **D-43:** Clicking add creates a draft-only temporary row at the bottom of that tab's list and opens it in edit mode.
- **D-44:** Creating an empty temporary row does not show the pending bar.
- **D-45:** If the temporary row is still empty and the user clicks away, remove it without showing the pending bar.
- **D-46:** Once meaningful values are entered, the temporary row becomes part of draft changes and the pending bar appears.
- **D-47:** `취소` removes value-filled temporary rows along with other draft changes. `적용` persists them as real rows.

### Deletion
- **D-48:** Delete is draft-only. Deleting an existing row marks it removed in draft state and shows the pending bar.
- **D-49:** Deleted rows are permanently removed only after `적용`.
- **D-50:** `취소` restores deleted rows.
- **D-51:** Deleting an empty temporary row simply removes it without requiring pending state.

### Modal Close
- **D-52:** If there are pending changes and the user attempts to close via X, outside click, or ESC, ask: `변경된 내용을 저장하지 않고 닫으시겠습니까?`
- **D-53:** Confirming the close prompt discards draft changes and closes the modal.
- **D-54:** Canceling the close prompt keeps the modal open and preserves the pending bar.
- **D-55:** If there are no pending changes, modal close behaves normally.

### Mobile Layout
- **D-56:** Mobile summary rail is reduced to a compact 2-line summary.
- **D-57:** Mobile rail fields are `수입`, `생활비`, `투자`, `저축`.
- **D-58:** Mobile `저축` means explicit savings plus automatic savings. Do not split `자동 저축` into a separate mobile rail cell.
- **D-59:** Mobile rows prioritize amount visibility. Use `이름 + 금액`; names truncate to one line if needed; amounts must not wrap, clip, or ellipsize.
- **D-60:** Row height should stay compact and consistent on mobile.

### Validation And Error Display
- **D-61:** Validation errors appear inside the relevant editing row, not in the pending bar.
- **D-62:** If `적용` finds an error, keep the modal and pending bar open, expand the first invalid row, and show the row-local error.
- **D-63:** Multiple invalid rows can be revealed progressively, but the first error should be actionable immediately.
- **D-64:** Use short row-local copy: `이름을 입력해 주세요.`, `금액은 1,000원 단위로 입력해 주세요.`, `금액은 0원 이상이어야 합니다.`

### the agent's Discretion
- The planner may choose exact class names and DOM data attributes, provided they fit existing Step 1 `financial-modal-*` and `financial-detail-*` patterns.
- The planner may split helper functions out of `financial-modal-controller.js` if it reduces complexity, but must preserve the existing sanitizer, persistence, and render boundaries.
- The planner may choose the exact point-color feedback animation details for `적용`, as long as it is short and non-blocking.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Phase Context
- `.planning/ROADMAP.md` — Phase 10.6 scope, requirements, and success criteria.
- `.planning/phases/10.6-financial-detail-modal-editing-ux-repair/10.6-CONTEXT.md` — This locked decision context.
- `.planning/phases/10.5-financial-settings-ux-integration-repair/10.5-CONTEXT.md` — Prior integrated modal decisions and no-couple-UI constraints.
- `.planning/phases/10.5-financial-settings-ux-integration-repair/10.5-SPEC.md` — Phase 10.5 requirements and boundaries that must not be contradicted unless Phase 10.6 explicitly supersedes UX presentation details.
- `.planning/phases/10.5-financial-settings-ux-integration-repair/10.5-VERIFICATION.md` — Existing verified behavior to preserve.
- `.planning/phases/10-step-1-2-household-budget-foundation/10-CONTEXT.md` — Variable expense data model and budget foundation history.
- `.planning/phases/10-step-1-2-household-budget-foundation/10-UI-REVIEW.md` — Earlier UI blockers that motivated the integrated repair.

### Project Context
- `.planning/PROJECT.md` — Core value: simple, intuitive planning with low cognitive load.
- `.planning/REQUIREMENTS.md` — v1.9 requirements and deferred scope.
- `.planning/STATE.md` — Current GSD state and prior decisions.

### Codebase Maps
- `.planning/codebase/CONVENTIONS.md` — DOM safety, controller style, validation, and testing conventions.
- `.planning/codebase/STRUCTURE.md` — Step 1 file locations and where to add code.
- `.planning/codebase/STACK.md` — Vite/browser runtime and test stack.

### Step 1 Source
- `apps/main/index.html` — Financial modal shell and pending-bar DOM.
- `apps/main/styles.css` — Modal, row, pending bar, and mobile layout styles.
- `apps/main/modules/dom.js` — DOM handles for modal and pending bar.
- `apps/main/modules/financial-modal-controller.js` — Primary implementation surface for row rendering, draft state, pending bar, add/delete, and save/cancel behavior.
- `apps/main/modules/household-budget.js` — Existing variable expense helpers and projection copy; Phase 10.6 may simplify visible UI while preserving data compatibility.
- `apps/main/modules/input-sanitizer.js` — Sanitizer boundary for amounts, variable expense fields, and persisted data.
- `apps/main/modules/persistence-controller.js` — `commitImmediateInputs()` save boundary.
- `apps/main/modules/render-orchestrator.js` — Summary and Sankey refresh path after applying changes.
- `tests/step1.spec.ts` — Existing Step 1 and Phase 10.5 Playwright regression suite; add Phase 10.6 coverage here.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `apps/main/modules/financial-modal-controller.js` already owns modal open/close, draft arrays, selected-row editing, create/delete controls, and persistence commit. It is the main Phase 10.6 target.
- `renderCompactRow()` and `renderEditingRow()` already separate read and edit row render paths; Phase 10.6 can repair density and field layout from these seams.
- `syncFinancialModalPendingBar()` already owns pending-bar visibility, but currently treats integrated outflow mode as changed too eagerly. The change detector needs to compare baseline inputs to draft inputs.
- `apps/main/modules/input-sanitizer.js` already enforces sanitized persisted data and should remain the durable boundary.
- `tests/step1.spec.ts` already contains Phase 10.5 tests for integrated modal shell, no couple UI, variable rows, automatic savings adjustment, and repeated-save regressions.

### Established Patterns
- Step 1 uses vanilla ES modules and DOM APIs. Use `document.createElement`, `textContent`, `dataset`, `hidden`, ARIA attributes, and event delegation.
- Existing Step 1 edits are draft-only until a deliberate persistence call.
- Summary-first default screen and modal/detail editing are established Step 1 patterns from Phase 09 and Phase 10.5.
- Mobile layouts should avoid horizontal overflow and protect amount readability.
- Tests should verify user-visible behavior with Playwright rather than relying only on helper-level assertions.

### Integration Points
- Financial summary cards and the `재무설정 상세` CTA should continue opening the same modal path.
- Pending bar actions must apply through the existing `persistence.commitImmediateInputs()` boundary.
- Applying changes should refresh summaries/Sankey through existing render pathways without creating duplicate rows or branches.
- New variable-expense range fields should be sanitized and persisted compatibly, without breaking existing `actualSpent` data.

</code_context>

<specifics>
## Specific Ideas

- The modal should feel like a compact editable list, not a multi-layer form.
- The user specifically called out `<!-- 신설 모달 전용 플로팅 펜딩 바 -->` as a symptom of implementation language leaking into the design model.
- The user prefers direct row manipulation: click the row, edit existing values, use small money controls, then apply all changes.
- The user wants pending-bar cancel/apply to affect only draft edits, never close the entire modal.
- Variable expenses should shift toward average expected amount plus optional variance range, with range surfaced in summary rather than every row.

</specifics>

<deferred>
## Deferred Ideas

- Couple/self/spouse UI remains deferred to a future dedicated spec.
- Separate household budget panel restoration remains out of scope.
- Heavy actual-spending tracking, remaining amount, and month-end projection UI should not be foregrounded in Phase 10.6. Preserve data compatibility, but do not make that the main visible interaction.
- Phase 11 spending capture, Phase 12 household merge, Phase 13 comparison, and Phase 14 real-estate affordability remain separate roadmap phases.

</deferred>

---

*Phase: 10.6-financial-detail-modal-editing-ux-repair*
*Context gathered: 2026-06-25*
