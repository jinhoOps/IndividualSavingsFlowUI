---
phase: 08-step-2-redesign-re-planning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/simulation/modules/state.js
  - apps/simulation/modules/step1-connector.js
  - apps/simulation/modules/feature-controllers.js
  - apps/simulation/modules/storage-fallback.js
  - src/core/storage/CompatibilityBridge.ts
  - src/core/storage/IsfStore.ts
  - src/core/types/models.ts
  - tests/step2.spec.ts
autonomous: true
requirements: [UI-03]
must_haves:
  truths:
    - "D-08: Step 2 진입 시 원본 Step 1 월 투자 가능 금액이 자동 반영된다"
    - "D-08: Step 2에서 월 투자 가능 금액을 수정해도 Step 1 원본 데이터는 변경되지 않는다"
    - "D-09: Step 2 초기화는 Step 2 편집값을 버리고 원본 Step 1 값을 다시 가져온다"
    - "D-16/D-17: IndexedDB 실패 환경에서도 Step 2 시뮬레이션 저장, 목록, 불러오기, 삭제가 LocalStorage 폴백으로 동작한다"
  artifacts:
    - path: "apps/simulation/modules/storage-fallback.js"
      provides: "Step 2 simulation list LocalStorage CRUD fallback"
    - path: "apps/simulation/modules/step1-connector.js"
      provides: "원본 Step 1 import cache and reset re-import boundary"
    - path: "src/core/storage/CompatibilityBridge.ts"
      provides: "saveStep2Entry returns saved entry contract"
  key_links:
    - from: "apps/simulation/modules/feature-controllers.js"
      to: "apps/simulation/modules/storage-fallback.js"
      via: "save/list/load/delete facade"
      pattern: "storageFallback|step2Storage"
    - from: "apps/simulation/modules/step1-connector.js"
      to: "window.IsfStorageHub.getLatestStep1Snapshot"
      via: "resolveLatestStep1Snapshot"
      pattern: "getLatestStep1Snapshot"
---

<objective>
Step 2의 저장 계약, LocalStorage 폴백, Step 1 월 투자금 자동 import/reset 경계를 먼저 고칩니다.

Purpose: Phase 08의 화면 재설계가 신뢰 가능한 데이터 흐름 위에서 동작하도록 D-08, D-09, D-16, D-17을 선행 구현합니다.
Output: Step 2 전용 저장 facade, 원본 Step 1 import 메타 상태, IndexedDB 실패 폴백 회귀 테스트.
</objective>

<execution_context>
@C:/Users/LENOVO/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/LENOVO/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-step-2-redesign-re-planning/08-CONTEXT.md
@.planning/phases/08-step-2-redesign-re-planning/08-RESEARCH.md
@.planning/phases/07-step-1-ui-ux-refactoring-modularization/07-VERIFICATION-GAP-CLOSURE-SUMMARY.md
@apps/simulation/app.js
@apps/simulation/modules/state.js
@apps/simulation/modules/step1-connector.js
@apps/simulation/modules/feature-controllers.js
@src/core/storage/CompatibilityBridge.ts
@src/core/storage/IsfStore.ts
@src/core/types/models.ts
@shared/storage/hub-storage.js
@tests/step1.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Step 2 storage/import regression coverage</name>
  <files>tests/step2.spec.ts</files>
  <action>Create Step 2 Playwright coverage for D-08, D-09, D-16, and D-17 before production edits in this plan. The test file should open `apps/simulation/index.html`, block service workers through the existing Playwright config, seed the Step 1 source with `isf-step1-active`, assert original monthly investment import, assert Step 2-only editing does not mutate the Step 1 source, assert reset re-imports the original Step 1 value, and assert LocalStorage fallback save/list/load/delete works when IndexedDB or the bridge path fails. Use direct module calls through `page.evaluate` only where the visible UI does not yet expose a save control; keep user-facing CRUD coverage for the DataHub list once Plan 03 adds the final controls.</action>
  <verify>
    <automated>npx playwright test tests/step2.spec.ts -g "Phase 08 storage|Phase 08 Step 1 import" --reporter=list --timeout=30000</automated>
  </verify>
  <done>Tests fail before this plan's implementation for the known broken import/reset/storage contract, then pass after Task 2 and Task 3.</done>
</task>

<task type="auto">
  <name>Task 2: Original Step 1 import and reset boundary</name>
  <files>apps/simulation/modules/state.js, apps/simulation/modules/step1-connector.js, apps/simulation/modules/feature-controllers.js</files>
  <action>Implement D-08 and D-09 by storing Step 1 source import metadata separately from editable Step 2 draft fields. Add state for the original imported monthly investment and initial investment source values, plus enough timestamp/id metadata to render the sync banner. `checkStep1SyncData()` should auto-import the original Step 1 monthly investment on entry when no loaded Step 2 simulation intentionally overrides it. Editing the Step 2 monthly investment must only mark Step 2 dirty and must not call any Step 1 save path. Update `featureController.reset()` so reset discards Step 2-only edits, clears current simulation id, then re-imports the original Step 1 monthly investment and initial investment if source data exists; otherwise it falls back to `createEmptyDraft()`. Keep all money values in Won internally.</action>
  <verify>
    <automated>node --check apps/simulation/modules/state.js</automated>
    <automated>node --check apps/simulation/modules/step1-connector.js</automated>
    <automated>node --check apps/simulation/modules/feature-controllers.js</automated>
  </verify>
  <done>On Step 2 load, Step 1 monthly investment is visible; editing Step 2 leaves `isf-step1-active` unchanged; reset restores the Step 1 source value rather than the last Step 2 edit.</done>
</task>

<task type="auto">
  <name>Task 3: Step 2 storage facade and bridge return contract</name>
  <files>apps/simulation/modules/storage-fallback.js, apps/simulation/modules/feature-controllers.js, src/core/storage/CompatibilityBridge.ts, src/core/storage/IsfStore.ts, src/core/types/models.ts</files>
  <action>Implement D-16 and D-17 through a thin Step 2 storage facade. Create `storage-fallback.js` with normalized save/list/load/delete functions that first attempt `window.IsfStorageHub` and switch to a LocalStorage key for Step 2 simulations after an IndexedDB/bridge failure. The fallback must show only a small app-header/status feedback such as temporary save mode and must not open a blocking modal. Fix `CompatibilityBridge.saveStep2Entry()` and `IsfStore.saveStep2Simulation()` so the modern IndexedDB path returns the saved entry object expected by `featureController.saveCurrent()`. Extend the Step 2 model/portable format so `totalInitialAsset`, `name`, `updatedAt`, `id`, `modelVersion`, and the Step 2 strategy fields survive save/load. Do not implement full backup/restore parity in the fallback path; keep backup calls on the IndexedDB-capable path and make fallback CRUD independent.</action>
  <verify>
    <automated>node --check apps/simulation/modules/storage-fallback.js</automated>
    <automated>npm run check</automated>
    <automated>npx playwright test tests/step2.spec.ts -g "Phase 08 storage|Phase 08 Step 1 import" --reporter=list --timeout=30000</automated>
  </verify>
  <done>IndexedDB save returns an entry with `id`; simulated IndexedDB failure still allows Step 2 save/list/load/delete through LocalStorage; status feedback indicates temporary save mode without interrupting the flow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Step 1 storage -> Step 2 draft | Previously persisted Step 1 data enters editable Step 2 state |
| Step 2 draft -> IndexedDB/LocalStorage | User/imported simulation data is persisted locally |
| LocalStorage fallback -> DataHub list | Fallback entries are later rendered and loaded by UI code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | `step1-connector.js` | mitigate | Normalize Step 1 values with existing Won utilities and store source metadata separately from editable draft state |
| T-08-02 | Denial of Service | `storage-fallback.js` | mitigate | Catch IndexedDB/bridge failures and route Step 2 save/list/load/delete to LocalStorage with status feedback |
| T-08-03 | Repudiation | `feature-controllers.js` | mitigate | Return the saved entry id from both storage paths and update header status after save/load/delete |
| T-08-SC | Tampering | npm installs | accept | No package install is planned for this phase |
</threat_model>

<verification>
Run the task-level commands plus `npm run check`. Confirm the new `tests/step2.spec.ts` storage/import cases pass and do not require network or external services.
</verification>

<success_criteria>
- D-08 and D-09 are implemented exactly: Step 2 imports original Step 1 monthly investment, Step 2 edits stay local, reset re-imports Step 1.
- D-16 and D-17 are implemented exactly: IndexedDB failure falls back silently to LocalStorage for Step 2 simulation save/list/load/delete with small status feedback.
- `CompatibilityBridge.saveStep2Entry()` no longer causes `featureController.saveCurrent()` to read `entry.id` from `undefined`.
</success_criteria>

<output>
Create `.planning/phases/08-step-2-redesign-re-planning/08-01-SUMMARY.md` when done.
</output>
