---
phase: 08-step-2-redesign-re-planning
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - apps/simulation/modules/state.js
  - apps/simulation/modules/constants.js
  - apps/simulation/modules/assumptions.js
  - apps/simulation/modules/comparison-calculator.js
  - apps/simulation/modules/calculator.js
  - scripts/generate_qqq_data.py
  - scripts/generate_market_data.py
  - public/data/indices/README.md
  - qqq_raw.csv
  - qqq_daily_raw.csv
  - qqq_daily_stooq.csv
  - tests/step2.spec.ts
autonomous: true
requirements: [UI-03]
must_haves:
  truths:
    - "D-06/D-07: strategy cards map to editable conservative assumptions grouped by index/growth, dividend growth, and covered-call/monthly-income"
    - "D-10: comparison set includes user-selectable Nasdaq or S&P 500 benchmark, SCHD, and JEPI/QQQI/DIVO covered-call examples"
    - "D-11: projection data contains final asset growth lines and monthly dividend/cash-flow support metrics"
    - "D-18/D-19/D-20: assumptions are conservative examples derived from project data context, shown as ranges, and editable through advanced settings"
    - "D-21: runtime code no longer depends on loose root QQQ CSV files"
  artifacts:
    - path: "apps/simulation/modules/assumptions.js"
      provides: "strategy group metadata, conservative defaults, display ranges, and example ETF labels"
    - path: "apps/simulation/modules/comparison-calculator.js"
      provides: "index/growth vs SCHD vs covered-call projection rows"
    - path: "public/data/indices/README.md"
      provides: "runtime market-data source note and root CSV cleanup rationale"
  key_links:
    - from: "apps/simulation/modules/calculator.js"
      to: "apps/simulation/modules/comparison-calculator.js"
      via: "calculateStrategyComparison export"
      pattern: "calculateStrategyComparison"
    - from: "apps/simulation/modules/assumptions.js"
      to: "public/data/indices"
      via: "documented evidence source metadata"
      pattern: "qqq|spy|schd"
---

<objective>
Step 2를 단일 배당 PR/TR 계산에서 지수/성장, 배당성장, 커버드콜/월 현금흐름 비교 모델로 확장합니다.

Purpose: 사용자가 총수익률과 월 현금흐름의 tradeoff를 숫자로 비교할 수 있도록 D-06, D-07, D-10, D-11, D-18, D-19, D-20, D-21의 계산·데이터 기반을 만듭니다.
Output: 보수 예시 가정 모듈, 다전략 비교 계산 모듈, 정리된 데이터 소스 문서, root CSV 정리.
</objective>

<scope_rationale>
Plan 08-02 intentionally keeps strategy assumptions, comparison calculation, data-source documentation, root CSV cleanup, and the Step 2 regression file together because they form one data/model boundary for D-06, D-07, D-10, D-11, D-18, D-19, D-20, and D-21. The 12 listed files are acceptable for this plan: five Step 2 model/calculator files, two data-generation scripts, one data README, three root CSV cleanup targets, and one existing Step 2 test file. This revision does not broaden the implementation scope.
</scope_rationale>

<execution_context>
@C:/Users/LENOVO/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/LENOVO/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-step-2-redesign-re-planning/08-CONTEXT.md
@.planning/phases/08-step-2-redesign-re-planning/08-RESEARCH.md
@.planning/phases/08-step-2-redesign-re-planning/08-01-SUMMARY.md
@apps/simulation/modules/state.js
@apps/simulation/modules/calculator.js
@apps/simulation/modules/constants.js
@public/data/indices/qqq.json
@public/data/indices/spy.json
@public/data/indices/schd.json
@scripts/generate_qqq_data.py
@scripts/generate_market_data.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strategy assumptions and comparison contract</name>
  <files>apps/simulation/modules/state.js, apps/simulation/modules/constants.js, apps/simulation/modules/assumptions.js, apps/simulation/modules/comparison-calculator.js, apps/simulation/modules/calculator.js</files>
  <action>Implement D-06, D-07, D-10, D-11, D-18, D-19, and D-20 at the data/calculation layer. Add `assumptions.js` with three strategy groups: index/growth, dividend growth, covered-call/monthly-income. Include benchmark options for Nasdaq and S&P 500, SCHD as the dividend-growth representative, and JEPI/QQQI/DIVO as covered-call/monthly-income examples. Store conservative internal defaults for dividend yield, dividend growth, capital growth, cash-flow yield, and DRIP behavior; expose separate display ranges so UI copy does not read as exact prediction. Use the resolved covered-call execution defaults: JEPI 7.0% cash-flow yield, 0.0% distribution growth, 1.5% capital growth, DRIP off, displayed as 6-9% / 0-1% / 0-3%; QQQI 9.0% cash-flow yield, 0.0% distribution growth, 2.0% capital growth, DRIP off, displayed as 7-11% / 0-1% / 0-4%; DIVO 4.5% cash-flow yield, 1.0% distribution growth, 3.0% capital growth, DRIP off, displayed as 3.5-6% / 0-2% / 1-5%. Add `comparison-calculator.js` that accepts the Step 2 draft and selected assumptions, returns yearly rows with principal, each strategy's final asset path, after-tax monthly cash flow, and benchmark delta. Keep all money values in Won and keep percent values numeric. Keep existing `calculateDividendProjection()` available only as a compatibility wrapper or delegate so other imports do not break.</action>
  <verify>
    <automated>node --check apps/simulation/modules/assumptions.js</automated>
    <automated>node --check apps/simulation/modules/comparison-calculator.js</automated>
    <automated>node --check apps/simulation/modules/calculator.js</automated>
  </verify>
  <done>Calculator output can distinguish selected index benchmark, SCHD, and covered-call strategy; each yearly row exposes final asset line values and monthly cash-flow support values.</done>
</task>

<task type="auto">
  <name>Task 2: Backdata cleanup and runtime data documentation</name>
  <files>scripts/generate_qqq_data.py, scripts/generate_market_data.py, public/data/indices/README.md, qqq_raw.csv, qqq_daily_raw.csv, qqq_daily_stooq.csv</files>
  <action>Implement D-19 and D-21 data hygiene. Ensure runtime code and documentation treat `public/data/indices/*.json` as the intentional market-data location. Add `public/data/indices/README.md` explaining that `qqq.json`, `spy.json`, and `schd.json` are the project evidence files for conservative examples, while JEPI/QQQI/DIVO use the resolved editable product assumptions and ranges from `assumptions.js`. Add short script comments/docstrings to `generate_qqq_data.py` and `generate_market_data.py` stating their output target. Delete the root CSV clutter files `qqq_raw.csv`, `qqq_daily_raw.csv`, and `qqq_daily_stooq.csv` after grep confirms no runtime/script dependency; if deletion is unsafe because a reference exists, move only the required file under a clearly documented non-runtime archive path such as `scripts/fixtures/qqq-backdata/` and update this plan summary with the reason.</action>
  <verify>
    <automated>node -e "const fs=require('fs'); for (const f of ['public/data/indices/qqq.json','public/data/indices/spy.json','public/data/indices/schd.json']) JSON.parse(fs.readFileSync(f,'utf8'));"</automated>
    <automated>node -e "const fs=require('fs'); const bad=['qqq_raw.csv','qqq_daily_raw.csv','qqq_daily_stooq.csv'].filter((f)=>fs.existsSync(f)); if (bad.length) { console.error(bad.join('\n')); process.exit(1); }"</automated>
  </verify>
  <done>Root QQQ CSV clutter is gone from the repository root or intentionally archived outside runtime paths; `public/data/indices/README.md` documents the runtime data source and conservative assumption boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Strategy comparison regression coverage</name>
  <files>tests/step2.spec.ts</files>
  <action>Extend the Step 2 Playwright suite for D-06, D-07, D-10, D-11, D-18, D-19, and D-20. Use page-level module imports to assert the assumptions contract exposes index/growth, dividend growth, and covered-call/monthly-income groups; assert benchmark options include Nasdaq and S&P 500; assert SCHD and JEPI/QQQI/DIVO examples are present; assert JEPI/QQQI/DIVO expose the resolved default values and display ranges; assert comparison rows contain index/SCHD/covered-call asset values and monthly cash-flow values; assert display ranges are present and internal defaults are editable numeric assumptions rather than hardcoded UI text. Keep tests deterministic and avoid external market requests.</action>
  <verify>
    <automated>npm run check</automated>
    <automated>npx playwright test tests/step2.spec.ts -g "Phase 08 strategy" --reporter=list --timeout=30000</automated>
  </verify>
  <done>Automated tests prove the comparison model and conservative range assumptions are available before the final UI plan consumes them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static market JSON -> strategy assumptions | Project data influences example ranges shown to users |
| User-edited assumptions -> calculator | Editable rates alter future asset and cash-flow projections |
| Repository root -> runtime assets | Loose files can be mistaken for runtime data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Tampering | `comparison-calculator.js` | mitigate | Sanitize money/rate inputs, clamp horizons to supported bounds, and keep Won-unit calculations |
| T-08-05 | Repudiation | `assumptions.js` | mitigate | Separate display ranges from internal defaults and label values as conservative examples, not guaranteed outcomes |
| T-08-06 | Information Disclosure | `public/data/indices/README.md` | accept | Files are public static market examples with no user data |
| T-08-SC | Tampering | npm installs | accept | No package install is planned for this phase |
</threat_model>

<verification>
Run the task-level commands, then confirm `rg -n "qqq_raw|qqq_daily_raw|qqq_daily_stooq" apps src shared scripts public tests` returns no runtime dependency hits.
</verification>

<success_criteria>
- D-06, D-07, D-10, D-11, D-18, D-19, D-20, and D-21 are covered by production modules and tests.
- The strategy model supports index/growth, SCHD, and covered-call/monthly-income comparison without live market integrations.
- Root QQQ CSV clutter no longer appears as a loose runtime data source.
</success_criteria>

<output>
Create `.planning/phases/08-step-2-redesign-re-planning/08-02-SUMMARY.md` when done.
</output>
