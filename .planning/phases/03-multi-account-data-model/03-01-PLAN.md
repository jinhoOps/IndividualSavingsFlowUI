---
phase: 03-multi-account-data-model
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/step1/index.html
  - apps/step1/modules/dom.js
  - apps/step1/modules/sankey-builder.js
  - apps/step1/modules/list-renderer.js
  - apps/step1/modules/calculator.js
  - apps/step1/app.js
  - apps/step1/styles.css
autonomous: true
requirements:
  - CORE-01
  - CORE-02
must_haves:
  truths:
    - "Sankey 차트에서 tone: 'transfer'인 계좌 간 내부 이체 링크가 완전히 배제된다 (D-01)."
    - "생키 차트 바로 아래에 '계좌 간 자동 이체 흐름' 보드가 신설되며, 모바일(<= 760px)에서 수직 스택, PC(>= 768px)에서 그리드로 반응형 배치된다 (D-02)."
    - "이체 카드의 패딩은 모바일에서 12px로 축소되고, Display 폰트(Gowun Batang)와 Body 폰트(Gowun Dodum)가 올바르게 적용된다."
    - "이체 카드는 호버(Hover) 시 테두리가 Sunset Orange 톤으로 바뀌며 위로 2px 부드럽게 들리는 수직 상승 트랜지션이 작동한다 (UX-04)."
    - "이체 방향 화살표에는 흐름의 방향성을 강조하는 미세한 가로 바운싱 애니메이션이 연출된다 (UX-04)."
    - "전체 연간 금융소득(이자/배당)이 1,900만 원 초과 시 warn, 3,400만 원 초과 시 crit 경고 뱃지와 테두리 링, 그리고 box-shadow 글로우 효과가 관련 계좌 카드에 적용된다 (D-03, UX-05)."
    - "경고 메시지는 '금융소득 종합과세 주의' 및 '금융소득 종합과세 대상 (한도 초과)'로 정확히 명시된다."
  artifacts:
    - path: "apps/step1/index.html"
      provides: "계좌 간 자동 이체 흐름 보드 컨테이너 추가"
    - path: "apps/step1/modules/dom.js"
      provides: "이체 보드 엘리먼트 DOM 참조 매핑"
    - path: "apps/step1/modules/sankey-builder.js"
      provides: "buildSankeyData에서 내부 이체를 links에서 제거하고 별도 transfers 배열로 반환하는 로직 구현"
    - path: "apps/step1/modules/list-renderer.js"
      provides: "이체 보드 카드 렌더러 구현 및 계좌 리스트 렌더링 시 금융소득 경고 상태(링/뱃지) 반영"
    - path: "apps/step1/modules/calculator.js"
      provides: "저축/투자 자산 기반 계좌별 연간 금융소득 계산 및 경고 등급 산출 함수 구현"
    - path: "apps/step1/app.js"
      provides: "renderAll에서 이체 보드 렌더링 호출 연동 및 계좌 목록 렌더링 시 warnings 옵션 전달"
    - path: "apps/step1/styles.css"
      provides: "이체 보드 레이아웃, 모바일 12px 콤팩트 패딩, 호버 부유/화살표 바운스 애니메이션, 경고 글로우 링 스타일 적용"
---

<objective>
다중 계좌 데이터 모델을 정교화하고, Sankey 차트의 내부 이체 선을 배제하여 단순화합니다. 이를 대체하기 위해 생키 하단에 '계좌 간 자동 이체 흐름' 보드를 신설하고, 연간 금융소득(이자/배당) 과세 경고 인디케이터와 호버/바운스 등 프리미엄 UX 인터랙션을 적용합니다.

Purpose: 생키 선 꼬임 오류 및 시각적 인지 과부하를 해소하고, 금융과세 임계 한도를 실시간으로 시각화하며, 세밀한 모션 인터랙션을 추가하여 프리미엄 자산 관리 도구의 완성도를 높입니다.
Output: 수정 완료된 index.html, dom.js, sankey-builder.js, list-renderer.js, calculator.js, app.js, styles.css
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-multi-account-data-model/03-CONTEXT.md
@.planning/phases/03-multi-account-data-model/03-UI-SPEC.md
@apps/step1/index.html
@apps/step1/modules/dom.js
@apps/step1/modules/sankey-builder.js
@apps/step1/modules/list-renderer.js
@apps/step1/modules/calculator.js
@apps/step1/app.js
@apps/step1/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: index.html 마크업 개편 및 dom.js DOM 매핑 추가</name>
  <files>
    - apps/step1/index.html
    - apps/step1/modules/dom.js
  </files>
  <read_first>
    - apps/step1/index.html
    - apps/step1/modules/dom.js
  </read_first>
  <action>
    - `index.html`에서 `sankeyLegend` 디렉티브 하단에 이체 보드 전용 영역 `#accountTransferBoard` 및 내부 리스트 `#transferCardList` 마크업을 신설합니다 (Section Title: "계좌 간 자동 이체 흐름").
    - `dom.js`에서 `#accountTransferBoard`와 `#transferCardList`에 대응되는 DOM 레지스트리 프로퍼티를 추가합니다.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - index.html의 구조가 무너지지 않고 정상적으로 마크업이 지정되어야 합니다.
    - dom.js에 새로 선언한 DOM 요소들이 누락 없이 반영되어야 합니다.
  </acceptance_criteria>
  <done>HTML 마크업 준비 및 DOM 매핑 추가가 완료됩니다.</done>
</task>

<task type="auto">
  <name>Task 2: sankey-builder.js 로직 수정 (D-01: 생키 다이어그램 단순화)</name>
  <files>apps/step1/modules/sankey-builder.js</files>
  <read_first>
    - apps/step1/modules/sankey-builder.js
  </read_first>
  <action>
    - `buildSankeyData` 함수를 수정하여, 내부 이체 계산(`tone === "transfer"`) 로직을 `links` 배열에 추가하지 않도록 변경합니다 (D-01).
    - 계산된 내부 이체 내역은 `transfers`라는 별도의 배열 프로퍼티(예: `[{ source, target, value, tone }]`)에 수집하여 반환 객체에 추가합니다.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - 생키 데이터의 `links` 배열에는 `tone: "transfer"`인 요소가 제외되어야 합니다.
    - 반환된 객체에 `transfers` 정보가 온전히 누적되어야 합니다.
  </acceptance_criteria>
  <done>생키 차트 단순화 및 내부 이체 데이터 분리 처리가 완료됩니다.</done>
</task>

<task type="auto">
  <name>Task 3: calculator.js에 금융소득 및 과세 경고 계산 기능 추가</name>
  <files>apps/step1/modules/calculator.js</files>
  <read_first>
    - apps/step1/modules/calculator.js
  </read_first>
  <action>
    - `calculateAccountFinancialIncomes(inputs)` 함수를 신설합니다.
    - 저축 버킷(시작 잔고 * annualRate / 100) 및 투자 버킷(시작 잔고 * 0.02)의 연간 이자/배당을 각 계좌(`accountId`)별로 할당하여 합산합니다 (D-03).
    - 전체 연간 금융소득 합계가 1,900만 원 초과 시 `warn`, 3,400만 원 초과 시 `crit` 경고 등급을 산출하여, 금융소득을 유발하는 모든 계좌 ID에 대한 경고 맵(`warnings[accountId] = { status, message }`)을 함께 반환합니다.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - 계좌별 연간 금융소득과 전체 금융소득이 정확히 연산되어야 합니다.
    - 1,900만 원 초과 시 warn, 3,400만 원 초과 시 crit 등급이 정상 매핑되어야 합니다.
  </acceptance_criteria>
  <done>계좌별 금융소득 계산 함수 구현이 완료됩니다.</done>
</task>

<task type="auto">
  <name>Task 4: list-renderer.js 컴포넌트 렌더러 구현 (이체 보드 & 금융소득 경고)</name>
  <files>apps/step1/modules/list-renderer.js</files>
  <read_first>
    - apps/step1/modules/list-renderer.js
  </read_first>
  <action>
    - `renderTransferBoard(transfers, accounts)` 함수를 구현합니다 (D-02).
      - 각 이체를 정갈한 카드 형태로 `#transferCardList`에 렌더링합니다.
      - 화살표 기호 `➔` 및 이체 금액 배지 `.badge-transfer`를 포함하고 Display/Body 타이포그래피 계층을 따릅니다.
    - `renderAccountItemHtml(item, opts)` 및 `renderItemList` 함수를 확장하여, `opts.warnings`로부터 계좌 ID별 경고 상태를 조회합니다.
      - 경고가 설정된 계좌에 `.account-row--warn` / `.account-row--crit` 아웃라인 링 클래스를 부여하고, 금융소득 종합과세 안내 배지(UI-SPEC 카피라이트 문구 준수)를 렌더링합니다.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - 이체 보드 카드가 깔끔한 HTML 스키마로 출력되어야 합니다.
    - 계좌 리스트 카드에 경고 클래스 및 배지 엘리먼트가 바르게 렌더링되어야 합니다.
  </acceptance_criteria>
  <done>이체 현황 보드 및 계좌 경고 컴포넌트 렌더러 작성이 완료됩니다.</done>
</task>

<task type="auto">
  <name>Task 5: app.js 컨트롤러 연동 및 UI 라이프사이클 통합</name>
  <files>apps/step1/app.js</files>
  <read_first>
    - apps/step1/app.js
  </read_first>
  <action>
    - `renderAll()` 함수 내부에서 `calculateAccountFinancialIncomes(state.inputs)`를 호출하여 계좌 경고 정보(`warnings`)를 취득합니다.
    - 취득한 `warnings` 객체를 `listRenderer.renderItemList("account", ...)` 호출 시 세 번째 인자 `{ editing: ..., warnings }`로 전달합니다.
    - `buildSankeyData`를 통해 추출된 `transfers` 정보를 `listRenderer.renderTransferBoard(transfers, accounts)`로 렌더링합니다.
    - 변경 적용(`applyChanges` 또는 `applyAccountItems` 등) 시에도 경고 상태가 동적으로 동기화되는지 검증합니다.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - 데이터 갱신 시(입력값 변경 등) 이체 보드 및 금융소득 경고 상태가 즉시 실시간으로 재랜더링되어야 합니다.
    - 기존 3계층 로직과 `markDirty` 등 헬퍼 함수가 소실되지 않고 외과적으로 보존되어야 합니다.
  </acceptance_criteria>
  <done>컨트롤러 UI 라이프사이클에 이체 보드 및 금융소득 경고 렌더링 연동이 완료됩니다.</done>
</task>

<task type="auto">
  <name>Task 6: styles.css 테마 및 반응형 레이아웃, 상세 인터랙션 모션 구현</name>
  <files>apps/step1/styles.css</files>
  <read_first>
    - apps/step1/styles.css
  </read_first>
  <action>
    - `apps/step1/styles.css`에 다음의 스타일을 정의합니다:
      - **이체 보드 (#accountTransferBoard, #transferCardList)**:
        - PC 화면(>= 768px)에서는 3열 그리드(`grid-template-columns: repeat(3, 1fr)`)로 넓게 정렬합니다.
        - 모바일 화면(<= 760px)에서는 수직 피드 스택(`display: flex; flex-direction: column; width: 100%;`) 구조로 배치하고, `-webkit-overflow-scrolling: touch` 속성으로 모바일 스크롤 관성을 보정합니다.
        - 이체 카드(`.transfer-card`)는 얇은 1px 실선 테두리(`border: 1px solid rgba(16, 34, 32, 0.12)`)와 Cream Canvas 테마에 맞는 화이트 배경을 가지며, 모바일 패딩은 12px로 축소합니다.
        - 마우스 호버(Hover) 시 카드 테두리가 `rgba(234, 91, 42, 0.3)`로 변화하고, `transform: translateY(-2px)` 및 `transition: all 0.2s ease-in-out` 속성으로 프리미엄 부유감을 줍니다 (UX-04).
        - 화살표 기호(`.transfer-card__arrow`)는 `@keyframes`를 정의하여 가로로 미세하게 통통 튀는 바운스 애니메이션(x축 이동)을 추가해 흐름의 생동감을 연출합니다 (UX-04).
        - 금액 배지(`.badge-transfer`)는 Soft Blue 톤(`color: #3175b6; background: rgba(49, 117, 182, 0.15)`)을 적용합니다.
      - **경고 아웃라인 링 & 뱃지**:
        - `.account-row--warn`: 테두리 컬러 `#f59e0b` (Yellow) 지정 및 `box-shadow: 0 0 0 2px rgba(245, 158, 11, 0.15)` 적용.
        - `.account-row--crit`: 테두리 컬러 `#dc2626` (Red) 지정 및 `box-shadow: 0 0 0 2px rgba(220, 38, 38, 0.15)` 적용.
        - `.status-badge--warn` / `.status-badge--crit` 경고 뱃지에 대응되는 정갈한 인라인 라벨 스타일을 구현하고, `transition: opacity 0.3s ease-in-out`를 사용해 부드럽게 점등되도록 합니다.
      - **물리적 무결성 수호**: styles.css 최하단의 기존 미디어 쿼리가 파손되거나 절삭되지 않도록 꼼꼼하게 교차 검증하며 추가합니다.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - PC와 모바일 미디어 쿼리가 올바르게 동작하여 이체 현황 보드의 구조가 의도대로 정렬되어야 합니다.
    - 마우스 호버 시 부드러운 수직 부유 및 화살표 바운스 애니메이션이 정상 실행되어야 합니다.
    - 경고 상태에 따른 테두리 색상, 글로우(box-shadow) 및 경고 뱃지 색상이 정확히 출력되어야 합니다.
    - 빌드 프로세스가 에러 없이 끝나야 합니다.
  </acceptance_criteria>
  <done>디자인 시스템 토큰과 상세 UX 모션 피드백을 수렴한 CSS 추가가 완료됩니다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client-Side Rendering | 브라우저 상의 DOM 데이터 바인딩 및 렌더링 무결성 신뢰 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Data Parsing | mitigate | 사용자 입력 및 IndexedDB 복원 데이터의 스키마 및 계좌 매핑 데이터가 유효한지 검증하는 `IsfUtils` 세니타이저 활용 |
| T-03-02 | Denial of Service | Sankey Rendering | mitigate | N:N 계좌 이체 순환(Loop)을 생키 렌더링에서 영구 제외하여 차트 꼬임 및 브라우저 프리징 현상 원천적 봉쇄 |
</threat_model>

<verification>
`npm run build` 명령을 실행하여 번들 및 컴파일 빌드가 통과하는지 확인하고, git diff 도구 및 라인수 검증을 거쳐 스타일과 반응형 코드의 물리적 무결성을 최종 체크합니다.
</verification>

<success_criteria>
1. Sankey Diagram에서 계좌 간의 내부 이체 링크(tone: 'transfer')가 제외되어 시각적으로 단순화됨.
2. 생키 하단에 '계좌 간 자동 이체 흐름' 보드가 신설되고, 모바일에서는 수직 피드 스택, PC에서는 3열 그리드로 반응형 렌더링이 이루어짐.
3. 이체 카드의 패딩이 모바일에서 12px로 축소 반영되고 Display/Body 폰트가 알맞게 적용됨.
4. 이체 카드 마우스 호버 시 2px 부유 트랜지션 및 화살표의 가로 바운스 애니메이션이 아름답게 작동함.
5. 연간 금융소득(이자/배당)이 1,900만 원 초과 시 warn, 3,400만 원 초과 시 crit 경고 뱃지와 테두리 글로우 링(box-shadow 포함)이 활성화됨.
6. 금융소득 경고 뱃지의 copywriting이 UI-SPEC 명세와 일치하게 출력됨.
</success_criteria>

<output>
작업 완료 시 `.planning/phases/03-multi-account-data-model/03-01-SUMMARY.md` 생성
</output>
