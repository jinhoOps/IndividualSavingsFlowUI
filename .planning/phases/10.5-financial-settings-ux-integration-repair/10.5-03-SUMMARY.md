---
phase: 10.5-financial-settings-ux-integration-repair
plan: 03
subsystem: ui
tags: [step1, financial-settings, living-expense, variable-expense, playwright, tdd]
requires:
  - plan: 10.5-02
    provides: integrated `재무설정 상세` modal tabs and persistent summary rail
provides:
  - Variable living-expense child rows under `월 생활비`
  - Selected-row target and actual draft editing for variable expenses
  - Remaining/status/month-end projection display with helper copy
  - Fixed-expense exclusion from actual-spending controls
affects: [phase-10.5, step1, financial-modal, household-budget]
tech-stack:
  added: []
  patterns:
    - Reuses `buildVariableExpenseBudgetRows()` for modal child-row derived values
    - Maps helper rows back to draft expense items through `sourceIndex`
    - Keeps variable actual edits inside the existing modal draft/save boundary
key-files:
  created:
    - .planning/phases/10.5-financial-settings-ux-integration-repair/10.5-03-SUMMARY.md
  modified:
    - apps/main/modules/financial-modal-controller.js
    - apps/main/modules/household-budget.js
    - apps/main/styles.css
    - tests/step1.spec.ts
key-decisions:
  - "`월 생활비` now uses a dedicated variable/fixed section instead of the generic group board so variable actual controls do not leak to fixed expenses."
  - "`buildVariableExpenseBudgetRows()` now returns `sourceIndex` to preserve stable draft writes back to `expenseItems[index]`."
  - "Variable target and actual edits remain draft-only until `재무설정 저장`; cancel/close still discard modal draft changes."
requirements-completed: [UXR-03, UXR-04]
duration: 29min
completed: 2026-06-24
status: complete
---

# Phase 10.5 Plan 03: Living Expense Variable Rows Summary

**Variable expense target/actual editing now lives inside the integrated `월 생활비` tab with fixed expenses excluded from actual-spending controls**

## Performance

- **Duration:** 29 min
- **Started:** 2026-06-24T02:09:03Z
- **Completed:** 2026-06-24T02:38:25Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- Added Playwright coverage for compact variable rows, selected-row expansion, target/actual draft editing, projection helper copy, fixed-row exclusion, empty state, and 390px overflow.
- Rendered `월 생활비` as a simple living-expense panel with one summary value, variable child rows, and fixed expense rows without actual-spending fields.
- Added `sourceIndex` to household budget helper rows so modal edits can update the correct draft `expenseItems[index]`.
- Added responsive CSS for compact and expanded variable rows.

## Task Commits

1. **Task 1: Add variable living-expense child row tests** - `56a88b7` (test)
2. **Task 2: Render and edit selected variable rows under monthly living expense** - `b7a06fe` (feat)

## Files Created/Modified

- `tests/step1.spec.ts` - Added `Phase 10.5 living expense variable rows` tests and corrected the test seed render call during GREEN.
- `apps/main/modules/financial-modal-controller.js` - Added variable row rendering, selected target/actual controls, draft `actualSpent` updates, fixed-row exclusion, and the living-expense panel.
- `apps/main/modules/household-budget.js` - Added `sourceIndex` to `buildVariableExpenseBudgetRows()` output.
- `apps/main/styles.css` - Added compact/expanded variable row, projection, empty-state, and mobile stacking styles.

## Decisions Made

- Used a dedicated `월 생활비` panel instead of the existing grouped drag board because Plan 03 needs one selected variable row to expose details while fixed rows stay compact and actual-free.
- Kept fixed expenses renderable through the existing row renderer, relying on the sanitizer to strip any legacy fixed `actualSpent` on save.
- Left the existing static SVG `innerHTML` edit icon untouched; user-controlled row names and values continue to render through DOM nodes and `textContent`.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed undefined row category reference in modal field updates**
- **Found during:** Task 2 verification
- **Issue:** `updateDraftFromField()` referenced `rowCategory`, which was not defined in that scope. The new `actualSpent` update path exposed the bug, and the existing account update path had the same typo.
- **Fix:** Replaced the undefined reference with the scoped `category` variable for both `actualSpent` and income account allocation updates.
- **Files modified:** `apps/main/modules/financial-modal-controller.js`
- **Verification:** Targeted Playwright assertions for variable actual draft save passed; `npm run check` passed.
- **Committed in:** `b7a06fe`

---

**Total deviations:** 1 auto-fixed (1 bug)
**Impact on plan:** The fix was required for correct draft editing and stayed within the touched modal controller.

## Issues Encountered

- `npx playwright test tests/step1.spec.ts -g "Phase 10.5 living expense variable rows|Phase 10.5 integrated modal shell"` executed all 4 targeted tests successfully, but the Playwright process did not exit before the shell timeout during webServer shutdown. The test output showed all 4 tests passing before timeout.

## Verification

- `npx playwright test tests/step1.spec.ts -g "Phase 10.5 living expense variable rows"` - RED confirmed; both new tests failed before implementation.
- `npm run check` - passed.
- `npx playwright test tests/step1.spec.ts -g "Phase 10.5 living expense variable rows|Phase 10.5 integrated modal shell"` - test assertions passed, 4/4; command timed out after success due Playwright process shutdown hang.

## Known Stubs

None for this plan. Stub-pattern scan only found the existing CSS `::placeholder` selector, not placeholder UI or unwired Plan 03 data.

## Threat Flags

None. No new network endpoint, auth path, file access pattern, or schema trust boundary was introduced. Variable row names and values are rendered with DOM creation and `textContent`; durable writes still pass through `commitImmediateInputs()` and `sanitizeInputs()`.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Plan 04 can build the automatic-savings adjustment gate on top of the integrated modal and summary rail. The `월 생활비` tab now exposes variable row details without restoring a default-screen household budget panel.

---
*Phase: 10.5-financial-settings-ux-integration-repair*
*Completed: 2026-06-24*

## Self-Check: PASSED

- Found modified files: `apps/main/modules/financial-modal-controller.js`, `apps/main/modules/household-budget.js`, `apps/main/styles.css`, `tests/step1.spec.ts`.
- Found summary file: `.planning/phases/10.5-financial-settings-ux-integration-repair/10.5-03-SUMMARY.md`.
- Found task commits: `56a88b7`, `b7a06fe`.
