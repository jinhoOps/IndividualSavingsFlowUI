---
phase: 10.5-financial-settings-ux-integration-repair
plan: 04
type: execute
wave: 4
depends_on:
  - "10.5-03"
files_modified:
  - apps/main/modules/financial-modal-controller.js
  - apps/main/styles.css
  - tests/step1.spec.ts
autonomous: true
requirements:
  - UXR-02
  - UXR-04
must_haves:
  truths:
    - "D-11 D-12: `자동 저축` appears in the summary rail and result tab as a derived result, not a manual peer input."
    - "D-13 D-14: Result-tab savings details navigate within the same modal to the `저축` tab and never open a nested modal."
    - "D-15 D-16 D-17: When `저축 + 투자` exceeds available income, save is blocked until the user chooses one of the three adjustment bases or resolves the excess, and adjustment applies inside the draft/save boundary."
    - "D-20 D-24: Cancel/close discard adjustment drafts and copy stays neutral without advice language."
  artifacts:
    - path: "apps/main/modules/financial-modal-controller.js"
      provides: "Derived automatic savings result, same-modal savings navigation, and excess adjustment save gate"
      exports: ["createFinancialModalController"]
    - path: "apps/main/styles.css"
      provides: "Neutral adjustment-choice and result-tab styling"
      contains: "financial-adjustment"
    - path: "tests/step1.spec.ts"
      provides: "Automatic savings and excess adjustment tests"
      contains: "Phase 10.5 automatic savings adjustment"
  key_links:
    - from: "apps/main/modules/financial-modal-controller.js"
      to: "apps/main/modules/persistence-controller.js"
      via: "selected adjustment is applied only when `재무설정 저장` calls `commitImmediateInputs(nextInputs)`"
      pattern: "commitImmediateInputs"
    - from: "apps/main/modules/financial-modal-controller.js"
      to: "apps/main/modules/input-sanitizer.js"
      via: "adjusted savings/invest arrays are sanitized after save"
      pattern: "sanitizeInputs"
---

<objective>
Implement derived automatic savings and the excess adjustment gate.

Purpose: Prevent silent durable mutations when planned savings/investments exceed available cash flow while keeping `자동 저축` clearly derived and navigable inside the same modal.
Output: Result tab, rail/result automatic-savings display, same-modal savings navigation, three adjustment choices, save blocking, idempotent adjustment application, and tests.
</objective>

<execution_context>
@C:/Users/LENOVO/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/LENOVO/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10.5-financial-settings-ux-integration-repair/10.5-CONTEXT.md
@.planning/phases/10.5-financial-settings-ux-integration-repair/10.5-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@apps/main/modules/financial-modal-controller.js
@apps/main/modules/input-sanitizer.js
@apps/main/modules/persistence-controller.js
@apps/main/styles.css
@tests/step1.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add automatic savings and excess adjustment tests</name>
  <files>tests/step1.spec.ts</files>
  <behavior>
    - `자동 저축` is visible in the summary rail and result tab and has no manual money input.
    - Result tab has a same-modal action that moves to the `저축` tab.
    - Over-budget data shows exactly three adjustment choices: `투자 먼저 줄이기`, `저축 먼저 줄이기`, `저축/투자 비율 유지해서 같이 줄이기`.
    - Save does not commit durable state until an adjustment basis is selected or excess is resolved.
    - Repeated save after adjustment does not duplicate items or Sankey branches.
  </behavior>
  <action>Add `test.describe('Phase 10.5 automatic savings adjustment', ...)` to `tests/step1.spec.ts`. Seed a balanced case and an excess case. Per D-11 through D-17, D-20, D-24, D-26, and D-27, assert derived `자동 저축` appears in both locations, no manual automatic-savings input exists, savings detail navigation stays inside `#financialModal`, excess choices are exact, save is blocked before choice, selected adjustment commits through localStorage only after `재무설정 저장`, cancel/close keeps saved state unchanged, and repeated save does not duplicate savings/invest items or Sankey labels.</action>
  <verify>
    <automated>npx playwright test tests/step1.spec.ts -g "Phase 10.5 automatic savings adjustment"</automated>
  </verify>
  <done>Tests fail until automatic savings is derived, adjustment is explicit, and save/cancel boundaries are protected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement result tab, same-modal savings navigation, and adjustment gate</name>
  <files>apps/main/modules/financial-modal-controller.js, apps/main/styles.css, tests/step1.spec.ts</files>
  <behavior>
    - Automatic savings is computed from draft income, living expense, investment, and savings commitments.
    - Result tab explains the derived value with neutral planning copy.
    - Savings details action sets active tab to `저축` without opening another modal.
    - Excess adjustment choices apply deterministic draft array replacement and are idempotent.
  </behavior>
  <action>In `financial-modal-controller.js`, compute the derived automatic-savings result from the current draft each render and display it in both the summary rail and `결과/자동 저축` tab per D-11 and D-12. Add a result-tab action that switches `activeDetailTab` to `저축` and focuses the savings area in the same modal per D-13 and D-14. Detect excess when savings plus investment commitments exceed available income after living expenses. Per D-15, do not mutate persisted state while detecting or previewing. Per D-16, render the three exact adjustment choices and store only the selected basis in draft modal state. Per D-17, block save with neutral inline feedback until the user chooses a basis or resolves the excess. On save, apply the selected basis to draft savings/invest arrays using replace-style updates: reduce investment first, reduce savings first, or reduce both proportionally while preserving item order and stable ids; then call the existing save path once. Add styles for the adjustment panel, choice buttons, and result tab using flat panels and existing tokens.</action>
  <verify>
    <automated>npx playwright test tests/step1.spec.ts -g "Phase 10.5 automatic savings adjustment|Phase 10.5 integrated modal shell"</automated>
  </verify>
  <done>`자동 저축` is derived and visible, excess adjustment is explicit and same-modal, and save applies selected adjustment inside the existing draft/save boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| draft totals -> adjustment choices | Derived excess state controls whether save can proceed. |
| adjustment basis -> durable financial arrays | User choice transforms savings/invest draft arrays before persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.5-10 | Tampering | excess adjustment application | mitigate | Apply adjustments once to draft arrays with stable ids and replace-style category updates. |
| T-10.5-11 | Repudiation | blocked save feedback | mitigate | Show visible neutral inline feedback when save is blocked and test it. |
| T-10.5-12 | Tampering | durable save path | mitigate | Call `persistence.commitImmediateInputs(nextInputs)` once after adjustment selection; cancel/close must not call it. |
| T-10.5-SC | Tampering | npm installs | mitigate | No package installs are planned; if an install appears, stop and re-run package legitimacy review. |
</threat_model>

<verification>
<automated>npm run check</automated>
<automated>npx playwright test tests/step1.spec.ts -g "Phase 10.5 automatic savings adjustment|Phase 10.5 living expense variable rows|Phase 10.5 integrated modal shell"</automated>
</verification>

<success_criteria>
- `자동 저축` appears in rail and result tab as a derived value.
- Result-tab savings details navigate to the `저축` tab within the same modal.
- Excess state shows the three locked adjustment choices and blocks save until selection or resolution.
- Cancel/close discards adjustment drafts; save commits exactly once through the existing persistence path.
</success_criteria>

<output>
Create `.planning/phases/10.5-financial-settings-ux-integration-repair/10.5-04-SUMMARY.md` when done.
</output>
