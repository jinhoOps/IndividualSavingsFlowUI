---
phase: 10.5-financial-settings-ux-integration-repair
plan: 02
type: execute
wave: 2
depends_on:
  - "10.5-01"
files_modified:
  - apps/main/modules/financial-modal-controller.js
  - apps/main/styles.css
  - tests/step1.spec.ts
autonomous: true
requirements:
  - UXR-02
  - UXR-04
must_haves:
  truths:
    - "D-04 D-05: The modal is a tab/step detail flow ordered `월 수입`, `월 생활비`, `투자`, `저축`, `결과/자동 저축`."
    - "D-06 D-07: The always-visible summary rail shows `월 수입`, `월 생활비`, `월 투자`, `자동 저축`, and a status badge, with warning/action only when `저축 + 투자` exceeds available income."
    - "D-18 D-21 D-22 D-24: Each tab starts simple, uses neutral copy, avoids crowded tables/nested modals, and remains usable without mobile horizontal scanning."
  artifacts:
    - path: "apps/main/modules/financial-modal-controller.js"
      provides: "Integrated tab state, summary rail view model, and same-modal navigation"
      exports: ["createFinancialModalController"]
    - path: "apps/main/styles.css"
      provides: "Responsive integrated financial modal and summary rail styles"
      contains: "financial-detail"
    - path: "tests/step1.spec.ts"
      provides: "Modal order, summary rail, neutral copy, and mobile no-overflow tests"
      contains: "Phase 10.5 integrated modal shell"
  key_links:
    - from: "apps/main/modules/financial-modal-controller.js"
      to: "apps/main/styles.css"
      via: "stable classes/data attributes for tab and rail layout"
      pattern: "financial-detail"
    - from: "apps/main/modules/financial-modal-controller.js"
      to: "tests/step1.spec.ts"
      via: "visible tab labels and summary rail selectors"
      pattern: "결과/자동 저축"
---

<objective>
Build the integrated modal shell and always-visible summary rail.

Purpose: Make the modal feel like one coherent monthly cash-flow setup instead of category-only fragments, while preserving Phase 09 selected-item editing as the detail pattern.
Output: Ordered tabs, a five-value summary rail, neutral copy, responsive modal styles, and Playwright coverage for order and overflow.
</objective>

<execution_context>
@C:/Users/LENOVO/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/LENOVO/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10.5-financial-settings-ux-integration-repair/10.5-CONTEXT.md
@.planning/phases/10.5-financial-settings-ux-integration-repair/10.5-RESEARCH.md
@.planning/phases/10-step-1-2-household-budget-foundation/10-UI-REVIEW.md
@.planning/phases/10-step-1-2-household-budget-foundation/10-UI-SPEC.md
@.planning/codebase/CONVENTIONS.md
@apps/main/modules/financial-modal-controller.js
@apps/main/styles.css
@tests/step1.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add integrated tab order and summary rail tests</name>
  <files>tests/step1.spec.ts</files>
  <behavior>
    - Modal opens with visible tabs in exact Phase 10.5 order.
    - Summary rail remains visible after navigating every tab.
    - Summary rail has five values: `월 수입`, `월 생활비`, `월 투자`, `자동 저축`, and a status badge.
    - At 390px, the rail and tabs wrap/stack without document overflow greater than 4px.
  </behavior>
  <action>Add `test.describe('Phase 10.5 integrated modal shell', ...)` to `tests/step1.spec.ts`. Per D-04, D-05, D-06, D-07, D-18, D-21, D-22, D-24, and D-26, assert the modal tab order, summary rail labels, default absence of over-budget adjustment action for a normal balanced seed, no nested modal opening when navigating tabs, neutral copy checks for advice/blame wording, and no overflow at 1280, 768, and 390 viewports.</action>
  <verify>
    <automated>npx playwright test tests/step1.spec.ts -g "Phase 10.5 integrated modal shell"</automated>
  </verify>
  <done>Tests fail until the integrated modal shell, rail, order, and responsive behavior exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the tabbed modal shell and summary rail</name>
  <files>apps/main/modules/financial-modal-controller.js, apps/main/styles.css, tests/step1.spec.ts</files>
  <behavior>
    - `open()` initializes one integrated state with active tab and draft inputs.
    - Tabs render in order: `월 수입`, `월 생활비`, `투자`, `저축`, `결과/자동 저축`.
    - The rail derives totals from draft state and is visible across tabs.
    - The rail stays simple unless the savings/investment commitment exceeds available income.
  </behavior>
  <action>Refactor `financial-modal-controller.js` around an integrated modal state object: `baselineInputs`, draft category arrays, `activeDetailTab`, selected item, and create-flow state. Render a tablist before the tab body and a summary rail outside the tab body so it remains visible per D-04. Use the Phase 10.5 order from D-05, not the older SPEC order. Derive rail values from draft inputs: total income, living expense total, investment total, automatic savings, and status badge per D-06. Per D-07, render over-budget warning and adjustment action only when `savings + invest` exceeds available flow; detailed adjustment behavior is completed in Plan 10.5-04. In `styles.css`, add flat, token-based modal/rail/tab styles with mobile stacking/wrapping and stable button/input dimensions. Avoid decorative gradients, nested cards, crowded tables, and always-open full item editors per D-21 and D-22.</action>
  <verify>
    <automated>npx playwright test tests/step1.spec.ts -g "Phase 10.5 integrated modal shell|Phase 10.5 financial settings entry contract"</automated>
  </verify>
  <done>The modal presents one ordered `재무설정 상세` flow with an always-visible five-value summary rail and responsive shell behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| draft financial state -> modal DOM | Local/imported financial values are rendered into the integrated editor. |
| tab controls -> modal state | User navigation changes visible editor state without durable writes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.5-04 | Tampering | summary rail rendering | mitigate | Build all rail nodes with DOM APIs and `textContent`; derive values from draft inputs only. |
| T-10.5-05 | Denial of Service | modal responsive layout | mitigate | CSS stacks rail/tabs at mobile widths and Playwright asserts overflow <= 4px. |
| T-10.5-06 | Information Disclosure | modal copy | accept | App is local-first; no network or auth boundary is introduced. |
| T-10.5-SC | Tampering | npm installs | mitigate | No package installs are planned; if an install appears, stop and re-run package legitimacy review. |
</threat_model>

<verification>
<automated>npm run check</automated>
<automated>npx playwright test tests/step1.spec.ts -g "Phase 10.5 integrated modal shell|Phase 10.5 financial settings entry contract"</automated>
</verification>

<success_criteria>
- The `재무설정 상세` modal tab order is exactly `월 수입`, `월 생활비`, `투자`, `저축`, `결과/자동 저축`.
- The summary rail is visible on every tab and shows the five locked values.
- Over-budget warning/action is absent for balanced data and reserved for the excess state.
- Modal layout has no horizontal document overflow at 390px, 768px, or desktop widths.
</success_criteria>

<output>
Create `.planning/phases/10.5-financial-settings-ux-integration-repair/10.5-02-SUMMARY.md` when done.
</output>
