---
phase: 10.5-financial-settings-ux-integration-repair
plan: 05
type: execute
wave: 5
depends_on:
  - "10.5-04"
files_modified:
  - apps/main/styles.css
  - tests/step1.spec.ts
  - .planning/phases/10.5-financial-settings-ux-integration-repair/10.5-UI-REVIEW.md
autonomous: true
requirements:
  - UXR-01
  - UXR-02
  - UXR-03
  - UXR-04
must_haves:
  truths:
    - "D-21 D-22 D-23 D-24: Final UI polish prioritizes convenience, avoids crowding/mobile horizontal scanning, uses neutral copy, and is ready for overall >=18/24 with Copywriting, Visuals, Spacing, and Experience Design each >=3/4."
    - "D-25 D-26: Verification covers the core open/tab/edit/variable-row/adjustment/save/cancel/mobile flow."
    - "D-27: Regression tests preserve no `#householdBudgetPanel`, `#summaryCards` before Sankey, sanitizer/persistence/Sankey behavior, no repeated-save duplicates, and no Phase 10.5 couple UI."
  artifacts:
    - path: "apps/main/styles.css"
      provides: "Final responsive, spacing, typography, and overflow hardening for the integrated modal"
      contains: "financial-detail"
    - path: "tests/step1.spec.ts"
      provides: "Full Phase 10.5 regression matrix and UI review guard tests"
      contains: "Phase 10.5 regression hardening"
    - path: ".planning/phases/10.5-financial-settings-ux-integration-repair/10.5-UI-REVIEW.md"
      provides: "Scored 6-pillar UI review evidence for the strict D-23 gate"
      contains: "overall"
  key_links:
    - from: "tests/step1.spec.ts"
      to: "apps/main/modules/persistence-controller.js"
      via: "save/cancel and repeated-save localStorage checks"
      pattern: "commitImmediateInputs"
    - from: "tests/step1.spec.ts"
      to: "apps/main/modules/render-orchestrator.js"
      via: "summary-before-Sankey and Sankey label/branch regression checks"
      pattern: "summaryBeforeSankey"
---

<objective>
Harden the repaired UX against regressions and prepare for UI review.

Purpose: Close the phase with a high-signal regression matrix for the integrated flow, Step 1 persistence/Sankey contracts, mobile overflow, and copy/spacing issues found by the Phase 10 UI review.
Output: Final responsive CSS adjustments and Playwright tests for no couple UI, no separate panel, tab order, summary rail, variable rows, automatic adjustment, save/cancel, repeated save, Sankey, and mobile overflow.
</objective>

<execution_context>
@C:/Users/LENOVO/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/LENOVO/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10.5-financial-settings-ux-integration-repair/10.5-CONTEXT.md
@.planning/phases/10.5-financial-settings-ux-integration-repair/10.5-RESEARCH.md
@.planning/phases/10-step-1-2-household-budget-foundation/10-UI-REVIEW.md
@.planning/phases/10-step-1-2-household-budget-foundation/10-UI-SPEC.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STACK.md
@apps/main/styles.css
@apps/main/modules/persistence-controller.js
@apps/main/modules/render-orchestrator.js
@apps/main/modules/input-sanitizer.js
@tests/step1.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add final Phase 10.5 regression hardening tests</name>
  <files>tests/step1.spec.ts</files>
  <behavior>
    - No separate household panel or Phase 10.5 couple UI appears after repeated open/save/cancel cycles.
    - Visible text assertions confirm `부부합산`, `본인 설정`, and `배우자 설정` are absent after repeated open/save/cancel cycles.
    - `#summaryCards` remains before Sankey and existing Sankey labels/branches remain stable after modal saves.
    - Save/cancel, repeated save, sanitizer preservation, variable `actualSpent`, and fixed-row exclusion are covered together.
    - Mobile and tablet widths have no document overflow greater than 4px while the modal is open on each tab.
  </behavior>
  <action>Add `test.describe('Phase 10.5 regression hardening', ...)` to `tests/step1.spec.ts`. Per D-23, D-25, D-26, and D-27, create an end-to-end seed that opens `재무설정 상세`, navigates every tab, edits a large income/living expense/investment value, expands a variable row, triggers and resolves excess adjustment, saves, reloads, repeats save, and verifies localStorage, summary cards, and Sankey labels. Include assertions that `#householdBudgetPanel`, `data-household-overview`, spouse/self controls, exact visible labels `부부합산`, `본인 설정`, and `배우자 설정`, nested modals, duplicate financial items, duplicate household sections, and duplicate Sankey branches do not appear. Add responsive loops for 1280x900, 768x1024, and 390x844 while modal content is open.</action>
  <verify>
    <automated>npx playwright test tests/step1.spec.ts -g "Phase 10.5 regression hardening"</automated>
  </verify>
  <done>The final regression tests fail until the full integrated workflow and all forbidden regressions are protected.</done>
</task>

<task type="auto">
  <name>Task 2: Polish CSS for UI review readiness</name>
  <files>apps/main/styles.css, tests/step1.spec.ts</files>
  <action>Review the new Phase 10.5 selectors in `apps/main/styles.css` against D-21, D-22, D-23, and D-24 plus the Phase 10 UI review findings. Keep new financial-detail styles flat, token-based, and compact: no decorative gradients, no negative letter spacing, no text clipping in buttons/badges, no mobile side-by-side amount compression, and no nested-card visual structure. Ensure editable money inputs have readable dimensions, tab labels wrap safely, the summary rail stacks on mobile, the pending/action area does not cover focused inputs, and copy remains neutral. Adjust tests only to assert durable UX contracts, not pixel-perfect implementation details.</action>
  <verify>
    <automated>npm run check</automated>
    <automated>npx playwright test tests/step1.spec.ts -g "Phase 10.5 regression hardening|Phase 10.5 automatic savings adjustment|Phase 10.5 living expense variable rows|Phase 10.5 integrated modal shell|Phase 10.5 financial settings entry contract|Phase 09 final responsive user flow coverage"</automated>
  </verify>
  <done>New Phase 10.5 styles satisfy the pre-review polish checklist, all targeted Playwright contracts pass, and the implementation is ready to be scored by `$gsd-ui-review` or an equivalent 6-pillar review artifact.</done>
</task>

<task type="auto">
  <name>Task 3: Run complete Step 1 verification</name>
  <files>tests/step1.spec.ts, .planning/phases/10.5-financial-settings-ux-integration-repair/10.5-UI-REVIEW.md</files>
  <action>Run the complete Step 1 suite after targeted tests pass. Confirm D-25 and D-27 by checking that existing sanitizer, persistence, summary-card, Sankey, repeated-save, and mobile overflow contracts still pass alongside the new Phase 10.5 coverage. If failures identify stale Phase 10 couple-board expectations, update those tests to the Phase 10.5 context authority instead of restoring the forbidden UI. After tests pass, run `$gsd-ui-review 10.5` or produce an equivalent scored 6-pillar UI review artifact for D-23, and fail the phase unless the score is overall >= 18/24 and Copywriting, Visuals, Spacing, and Experience Design are each >= 3/4.</action>
  <verify>
    <automated>npm run check</automated>
    <automated>npx playwright test tests/step1.spec.ts</automated>
    <automated>$gsd-ui-review 10.5 or an equivalent scored 6-pillar UI review that writes `.planning/phases/10.5-financial-settings-ux-integration-repair/10.5-UI-REVIEW.md`</automated>
  </verify>
  <done>`npm run check`, the full Step 1 Playwright suite, and the scored UI review gate pass with overall >= 18/24 and Copywriting, Visuals, Spacing, and Experience Design each >= 3/4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| modal save -> localStorage/render pipeline | Final draft state becomes durable local-first state and rerenders summary/Sankey. |
| UI review copy/layout -> user interpretation | Financial planning labels can be misread as advice or blame if copy regresses. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.5-13 | Tampering | repeated save flow | mitigate | Playwright asserts repeated save does not duplicate items, sections, or Sankey branches. |
| T-10.5-14 | Denial of Service | mobile modal layout | mitigate | Responsive tests assert no document overflow and no clipped controls at 390px and 768px. |
| T-10.5-15 | Information Disclosure | financial copy | mitigate | Tests/review scan visible copy for advice/blame framing and keep neutral labels. |
| T-10.5-SC | Tampering | npm installs | mitigate | No package installs are planned; if an install appears, stop and re-run package legitimacy review. |
</threat_model>

<verification>
<automated>npm run check</automated>
<automated>npx playwright test tests/step1.spec.ts -g "Phase 10.5 regression hardening|Phase 10.5 automatic savings adjustment|Phase 10.5 living expense variable rows|Phase 10.5 integrated modal shell|Phase 10.5 financial settings entry contract|Phase 09 final responsive user flow coverage"</automated>
<automated>npx playwright test tests/step1.spec.ts</automated>
<automated>$gsd-ui-review 10.5 or an equivalent scored 6-pillar UI review artifact; pass only if overall score is >= 18/24 and Copywriting, Visuals, Spacing, and Experience Design are each >= 3/4.</automated>
</verification>

<success_criteria>
- Targeted and full Step 1 Playwright tests pass.
- `npm run check` passes.
- `$gsd-ui-review 10.5` or an equivalent scored 6-pillar UI review artifact passes the strict review gate: overall >= 18/24 and Copywriting, Visuals, Spacing, and Experience Design each >= 3/4.
- Forbidden Phase 10.5 UI remains absent while sanitizer, persistence, summary-before-Sankey, repeated save, and Sankey behavior remain intact.
</success_criteria>

<output>
Create `.planning/phases/10.5-financial-settings-ux-integration-repair/10.5-05-SUMMARY.md` when done.
</output>
