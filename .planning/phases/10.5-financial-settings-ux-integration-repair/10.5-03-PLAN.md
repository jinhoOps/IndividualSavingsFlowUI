---
phase: 10.5-financial-settings-ux-integration-repair
plan: 03
type: execute
wave: 3
depends_on:
  - "10.5-02"
files_modified:
  - apps/main/modules/financial-modal-controller.js
  - apps/main/modules/household-budget.js
  - apps/main/styles.css
  - tests/step1.spec.ts
autonomous: true
requirements:
  - UXR-03
  - UXR-04
must_haves:
  truths:
    - "D-08: `월 생활비` renders compact child rows where selecting one row reveals target, actual, remaining amount, status, `월말 예상`, and helper copy."
    - "D-09: Variable expense actual/target controls live under `월 생활비`; fixed expenses do not gain actual-spending controls."
    - "D-10 D-18 D-19: Projection helper copy stays visible, each tab starts simple, and only the selected item exposes detailed inputs."
  artifacts:
    - path: "apps/main/modules/financial-modal-controller.js"
      provides: "Living-expense child row rendering and selected-row editing"
      exports: ["createFinancialModalController"]
    - path: "apps/main/modules/household-budget.js"
      provides: "Variable expense budget row data and projection helper copy"
      exports: ["buildVariableExpenseBudgetRows", "HOUSEHOLD_PROJECTION_NOTE"]
    - path: "apps/main/styles.css"
      provides: "Compact variable row and expanded row styles"
      contains: "financial-variable"
    - path: "tests/step1.spec.ts"
      provides: "Variable row target/actual/projection/fixed-row exclusion tests"
      contains: "Phase 10.5 living expense variable rows"
  key_links:
    - from: "apps/main/modules/financial-modal-controller.js"
      to: "apps/main/modules/household-budget.js"
      via: "`buildVariableExpenseBudgetRows(draftInputs)` for variable child details"
      pattern: "buildVariableExpenseBudgetRows"
    - from: "apps/main/modules/financial-modal-controller.js"
      to: "apps/main/modules/input-sanitizer.js"
      via: "save path preserves `actualSpent` only for variable expenses"
      pattern: "actualSpent"
---

<objective>
Expose variable living-expense child details inside `월 생활비`.

Purpose: Make Phase 10's data helpers usable without reviving a separate default-screen budget panel or adding fixed-expense actual controls.
Output: Expandable variable living-expense rows, target/actual draft editing, remaining/status/projection display, helper copy, fixed-row exclusion, and responsive row tests.
</objective>

<execution_context>
@C:/Users/LENOVO/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/LENOVO/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10.5-financial-settings-ux-integration-repair/10.5-CONTEXT.md
@.planning/phases/10.5-financial-settings-ux-integration-repair/10.5-RESEARCH.md
@.planning/phases/10-step-1-2-household-budget-foundation/10-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@apps/main/modules/financial-modal-controller.js
@apps/main/modules/household-budget.js
@apps/main/modules/input-sanitizer.js
@apps/main/styles.css
@tests/step1.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add variable living-expense child row tests</name>
  <files>tests/step1.spec.ts</files>
  <behavior>
    - The `월 생활비` tab shows compact variable row summaries by default.
    - Selecting a variable row expands editable `목표` and `실제` controls plus remaining/status/projection details.
    - Projection area includes `월말 예상` and `현재 사용 속도를 단순 환산한 참고값입니다.`
    - Fixed expense rows either remain compact line items or detail rows without `실제` actual-spending controls.
  </behavior>
  <action>Add `test.describe('Phase 10.5 living expense variable rows', ...)` to `tests/step1.spec.ts`. Seed Step 1 with one variable `변동비` item and one fixed `고정비` item. Per D-08, D-09, D-10, D-18, D-19, D-22, and D-26, test compact default state, selected-row expansion, target/actual draft updates, visible remaining/status/month-end projection, helper copy, fixed-row actual-control exclusion, empty variable-row state, and 390px no-overflow behavior.</action>
  <verify>
    <automated>npx playwright test tests/step1.spec.ts -g "Phase 10.5 living expense variable rows"</automated>
  </verify>
  <done>Tests fail until variable rows are exposed under the integrated `월 생활비` tab with the required child details.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render and edit selected variable rows under monthly living expense</name>
  <files>apps/main/modules/financial-modal-controller.js, apps/main/modules/household-budget.js, apps/main/styles.css, tests/step1.spec.ts</files>
  <behavior>
    - `월 생활비` tab uses `buildVariableExpenseBudgetRows()` for variable child detail data.
    - Only the selected row renders target/actual inputs and derived detail values.
    - Edits update draft `expenseItems[index].amount` and `expenseItems[index].actualSpent` without committing durable state.
    - Empty variable rows render a clear non-breaking empty state.
  </behavior>
  <action>In `financial-modal-controller.js`, render the `월 생활비` tab with one large living-expense summary value followed by compact child rows for variable expense items per D-08 and D-18. Use `buildVariableExpenseBudgetRows()` and `HOUSEHOLD_PROJECTION_NOTE` from `household-budget.js`; only adjust `household-budget.js` if the helper output lacks a stable original item id/index needed to map edits back to draft expense items. When a variable row is selected, reveal target (`amount`) and actual (`actualSpent`) controls plus remaining amount, status label, `월말 예상`, and helper copy per D-10. Keep fixed expenses out of actual-spending controls per D-09. Preserve the selected-item editing pattern from D-19 by keeping non-selected rows compact. Add CSS for compact row summaries, expanded detail fields, readable money inputs, and mobile stacking.</action>
  <verify>
    <automated>npx playwright test tests/step1.spec.ts -g "Phase 10.5 living expense variable rows|Phase 10.5 integrated modal shell"</automated>
  </verify>
  <done>The `월 생활비` tab exposes variable target/actual/remaining/status/projection details without a separate panel, fixed actual controls, or durable mutation before save.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| expense row controls -> draft expense items | User-entered target/actual values become candidate financial state. |
| household-budget helper output -> modal DOM | Derived projection/status values are displayed to the user. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.5-07 | Tampering | variable row amount controls | mitigate | Parse values with existing Won helpers and let final save pass through `sanitizeInputs()`. |
| T-10.5-08 | Tampering | fixed expense handling | mitigate | Tests assert fixed rows do not render or persist actual-spending controls. |
| T-10.5-09 | Information Disclosure | row names in modal | mitigate | Render item names with `textContent` and existing DOM creation patterns. |
| T-10.5-SC | Tampering | npm installs | mitigate | No package installs are planned; if an install appears, stop and re-run package legitimacy review. |
</threat_model>

<verification>
<automated>npm run check</automated>
<automated>npx playwright test tests/step1.spec.ts -g "Phase 10.5 living expense variable rows|Phase 10.5 integrated modal shell"</automated>
</verification>

<success_criteria>
- Variable expense child rows are under `월 생활비`, not on the default screen.
- Selecting one variable row reveals target, actual, remaining, status, `월말 예상`, and the projection helper copy.
- Fixed expenses do not gain actual-spending controls.
- Draft edits remain unsaved until the existing financial modal save action.
</success_criteria>

<output>
Create `.planning/phases/10.5-financial-settings-ux-integration-repair/10.5-03-SUMMARY.md` when done.
</output>
