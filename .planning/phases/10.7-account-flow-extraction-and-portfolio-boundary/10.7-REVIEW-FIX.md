---
phase: 10.7
source_review: 10.7-REVIEW.md
status: fixed
fixed_at: 2026-06-29T06:15:00Z
findings_in_scope:
  critical: 4
  warning: 2
fixed:
  critical: 4
  warning: 2
skipped: 0
info_not_in_fix_scope: 1
---

# Phase 10.7 Review Fix Report

## Fixed Findings

- CR-01: `sankey-builder.js` now maps `snapshot.transfers` into Network/Sankey transfer data with `isManual: true`, so saved manual transfer rules remain visible in account flow output.
- CR-02: `calculator.js` now uses local target indexes when applying surplus transfer amounts to selected savings/invest buckets.
- CR-03: `input-sanitizer.js` now normalizes invalid income allocations back to a single default allocation that matches the income amount.
- CR-04: `apps/portfolio/modules/dom.js` now escapes stored ids/periods/asset values before HTML attribute insertion and avoids interpolated selector strings for stored ids.
- WR-01: `tests/step1.spec.ts` now expects the restored manual transfer UI and verifies preserved manual transfers in generated flow data.
- WR-02: `docs/adr/0002-account-flow-belongs-to-portfolio-boundary.md` is marked superseded so it no longer contradicts restored Step 1 account-flow ownership.

## Additional Hardening

- `apps/main/index.html` restores the manual transfer editor panel that existing DOM/render/controller modules expected.
- `apps/main/modules/list-renderer.js` escapes manual transfer ids before rendering delete-button data attributes.

## Verification

- `npm run check`
- `npx playwright test tests/step1.spec.ts -g "modal capability absorption|source-account network flow|routes surplus" --reporter=list`

