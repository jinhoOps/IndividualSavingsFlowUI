---
phase: 10.7-account-flow-extraction-and-portfolio-boundary
verified: 2026-06-26T09:55:10Z
status: passed
score: 8/8 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 7/8
  gaps_closed:
    - "Portfolio consumes the accountFlowHandoff sidecar shape created by Step 1 sanitize/import paths for richer account-flow handoff review."
  gaps_remaining: []
  regressions: []
---

# Phase 10.7: Account Flow Extraction and Portfolio Boundary Verification Report

**Phase Goal:** Remove account-flow modeling from Step 1 so its Sankey returns to the simple `수입 -> 지출(소비/저축/투자)` model, while defining whether richer 계좌흐름도 behavior belongs in Portfolio or a separate app/page.
**Verified:** 2026-06-26T09:55:10Z
**Status:** passed
**Re-verification:** Yes - after gap fix commit `7964c4ad91d8dea5ecea09bd03de9ec06443fc84`

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | Step 1 ordinary financial setup no longer exposes account-flow management concepts. | VERIFIED | Regression still passes. `tests/step1.spec.ts` covers absence of normal account-flow paths, account selects, correction controls, transfer UI, and account-flow modal controls. |
| 2 | Primary Step 1 Sankey is simple income -> consumption/savings/investment. | VERIFIED | `apps/main/modules/sankey-builder.js` remains the primary graph builder; Step 1 regression `Phase 10.7 simple Sankey ignores legacy account flow fields` passes in the combined run. |
| 3 | Automatic account correction no longer affects Step 1 primary sanitize/Sankey paths. | VERIFIED | Step 1 source-boundary regressions pass; `sanitizeInputs()` builds simplified primary fields and `buildAccountFlowHandoff()` separately. |
| 4 | Legacy account/allocation data is preserved as sidecar, not live primary Step 1 state. | VERIFIED | `apps/main/modules/input-sanitizer.js:63-90` attaches optional `accountFlowHandoff`; `buildAccountFlowHandoff()` emits `accounts`, `incomes`, `expenseItems`, `savingsItems`, `investItems`, `transfers`, and correction metadata while primary fields omit live account data. |
| 5 | Financial Detail Modal does not reintroduce account-flow editing while preserving non-account row edits. | VERIFIED | Phase 10.7 modal regressions in `tests/step1.spec.ts` pass, including removal of account-flow controls and preservation of simple row edits. |
| 6 | Step 1 render/controller path is cleaned of network-map, transfer-rule, and correction-refresh wiring. | VERIFIED | Step 1 source/UI boundary tests pass; `render-orchestrator.js:20-39` only shows Portfolio guidance when sidecar data exists and does not rehydrate account-flow UI. |
| 7 | Portfolio is documented and surfaced as the destination boundary with main -> Portfolio guidance. | VERIFIED | `docs/adr/0002-account-flow-belongs-to-portfolio-boundary.md` records Portfolio as the destination; Step 1 Portfolio guidance regression passes. |
| 8 | Portfolio consumes the sanitizer-created sidecar contract for account-flow handoff review. | VERIFIED | Gap closed. `apps/portfolio/modules/step1-connector.js:30-53` now counts sanitizer-produced `accountFlowHandoff.incomes[].allocations`, fallback `income.accountId`, and `expenseItems/savingsItems/investItems[].accountId` before falling back to the older `incomeAllocations/itemAccounts` shape. `tests/step3.spec.ts:64-123` now seeds Portfolio with a snapshot produced by importing `sanitizeInputs()` from Step 1 and asserts the saved sidecar has `incomes`, `expenseItems`, and `investItems` while `incomeAllocations` and `itemAccounts` are absent. |

**Score:** 8/8 truths verified (0 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `apps/main/modules/sankey-builder.js` | Simple Step 1 primary Sankey builder | VERIFIED | Regression coverage remains passing in the combined Step 1 + Step 3 run. |
| `apps/main/modules/sankey-renderer.js` | Simple-flow Sankey metadata/help text | VERIFIED | Compatible with current `buildSankeyData()` output; Step 1 visual/topology tests pass. |
| `apps/main/modules/input-sanitizer.js` | Simplified primary sanitizer and sidecar extraction | VERIFIED | `sanitizeInputs()` strips primary account fields and emits `accountFlowHandoff` via `buildAccountFlowHandoff()`. |
| `apps/main/modules/financial-modal-controller.js` | Modal-only simple financial editing | VERIFIED | Modal boundary regressions pass. |
| `apps/main/index.html` / `apps/main/modules/render-orchestrator.js` | Step 1 Portfolio guidance | VERIFIED | Guidance is navigation-only and triggered by sidecar presence. |
| `apps/portfolio/modules/step1-connector.js` | Portfolio handoff detection | VERIFIED | Now consumes actual sanitizer sidecar fields and remains wired through `apps/portfolio/app.js:34-36`. |
| `apps/portfolio/modules/state.js` / `apps/portfolio/modules/dom.js` | Portfolio-owned handoff state/UI | VERIFIED | `state.setAccountFlowHandoff()` normalizes connector counts; `dom.renderAccountFlowHandoffStatus()` renders available/empty status. |
| `docs/adr/0002-account-flow-belongs-to-portfolio-boundary.md` | Destination boundary ADR | VERIFIED | Documents Portfolio boundary and handoff-only sidecar contract. |
| `tests/step1.spec.ts` / `tests/step3.spec.ts` | Regression coverage | VERIFIED | Step 3 now tests sanitizer-produced sidecar shape; combined regression run passes 74 tests. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `sankey-builder.js` | `sankey-renderer.js` | Compatible simple Sankey shape | VERIFIED | Step 1 Sankey/topology regressions pass. |
| `persistence-controller.js` | `input-sanitizer.js` | `commitImmediateInputs()` and import/share restore call `sanitizeInputs()` | VERIFIED | Previously verified; Step 1 sanitizer/import regressions still pass. |
| `financial-modal-controller.js` | `input-sanitizer.js` | Modal save commits through persistence | VERIFIED | Modal save regressions pass. |
| `render-orchestrator.js` | `sankey-builder.js` | `renderAll()` renders simple Sankey | VERIFIED | `render-orchestrator.js:53-54` still calls simple Sankey rendering path. |
| `step1-connector.js` | `input-sanitizer.js` | Reads sidecar shape created by sanitize/import path | VERIFIED | `tests/step3.spec.ts:68-91` imports `sanitizeInputs()` to create the stored Step 1 snapshot, then imports `Step1Connector.fetchAccountFlowHandoff()` and asserts counts from that sanitizer-created sidecar. |
| `apps/main/index.html` | `apps/portfolio/index.html` | Main-to-Portfolio guidance link | VERIFIED | Step 1 Portfolio guidance regression passes. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|---|---|---|---|---|
| Step 1 Sankey | `snapshot` | `buildMonthlySnapshot(inputs)` from sanitized primary fields | Yes | FLOWING |
| Step 1 guidance | `inputs.accountFlowHandoff` | `sanitizeInputs()` / persisted Step 1 state | Yes | FLOWING |
| Portfolio handoff status | `state.data.accountFlowHandoff` | `Step1Connector.fetchAccountFlowHandoff()` -> `state.setAccountFlowHandoff()` -> `renderAccountFlowHandoffStatus()` | Yes | FLOWING - connector counts actual sanitizer sidecar arrays, state normalizes counts, DOM renders status metrics. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Portfolio consumes sanitizer-created Step 1 sidecar shape | `npx playwright test tests/step3.spec.ts --reporter=list` | `2 passed (5.8s)` | PASS |
| Step 1 simplification plus Portfolio boundary regression | `npx playwright test tests/step1.spec.ts tests/step3.spec.ts --reporter=list` | `74 passed (2.2m)` | PASS |
| TypeScript/project check | `npm run check` | `tsc --noEmit` completed | PASS |

### Probe Execution

Step 7c: SKIPPED - no phase-declared or conventional `scripts/**/tests/probe-*.sh` probes found.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| UXR-13 | Plans 01, 03, 04 | User sees simple Step 1 financial flow without account-flow management concepts. | SATISFIED | Step 1 UI/source/modal regressions pass; account-flow management controls remain absent from ordinary Step 1. |
| UXR-14 | Plans 01, 02, 04 | Step 1 Sankey no longer depends on account/allocation modeling or automatic correction. | SATISFIED | Simple Sankey and sanitizer/source-boundary regressions pass. |
| UXR-15 | Plans 02, 03, 05 | Existing account/allocation data is preserved or converted for Portfolio/account-flow handoff without corrupting simplified Step 1 data. | SATISFIED | `sanitizeInputs()` preserves legacy account/allocation fields in `accountFlowHandoff`; Step 3 now consumes that actual sanitizer sidecar while primary Step 1 account fields remain undefined after Portfolio visit. |
| UXR-16 | Plans 05, 06 | Rich account-flow management has a documented destination boundary with main-to-destination guidance. | SATISFIED | ADR 0002, Step 1 Portfolio guidance, and Portfolio handoff status path are all present and covered by passing tests. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---:|---|---|---|
| `apps/portfolio/modules/step1-connector.js` | 110, 129 | `return null` | INFO | Existing error/no-data fallback for `fetchLatestSnapshot()`, not a Phase 10.7 handoff stub. |
| `tests/step3.spec.ts` | 60 | Empty catch body | INFO | Test cleanup best-effort IndexedDB deletion; not product behavior. |

### Human Verification Required

None.

### Gaps Summary

No blocking gaps remain. The previous gap was closed by changing Portfolio connector normalization to consume the sanitizer-produced `accountFlowHandoff` sidecar arrays and by replacing the Step 3 fixture with a snapshot created through the real Step 1 `sanitizeInputs()` path.

---

_Verified: 2026-06-26T09:55:10Z_
_Verifier: the agent (gsd-verifier)_
