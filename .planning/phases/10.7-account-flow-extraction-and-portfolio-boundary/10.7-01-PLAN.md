---
phase: 10.7-account-flow-extraction-and-portfolio-boundary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/main/modules/sankey-builder.js
  - apps/main/modules/sankey-renderer.js
  - tests/step1.spec.ts
autonomous: true
requirements:
  - UXR-13
  - UXR-14
must_haves:
  truths:
    - "D-01/D-02: Step 1 Sankey primary topology is simple income to consumption, savings, and investment categories."
    - "D-03/D-09: account correction and account allocation do not influence primary Step 1 Sankey data."
  artifacts:
    - path: "apps/main/modules/sankey-builder.js"
      provides: "Simple Step 1 primary Sankey builder"
      exports: ["buildSankeyData"]
    - path: "apps/main/modules/sankey-renderer.js"
      provides: "Simple-flow Sankey metadata and help text"
    - path: "tests/step1.spec.ts"
      provides: "Phase 10.7 Sankey topology regression tests"
  key_links:
    - from: "apps/main/modules/sankey-builder.js"
      to: "apps/main/modules/sankey-renderer.js"
      via: "buildSankeyData returns compatible nodes, links, transfers, totalValue, splitGroups shape"
      pattern: "buildSankeyData"
---

<objective>
Create the simple Step 1 Sankey core for Phase 10.7.

Purpose: UXR-13 and UXR-14 require Step 1 to stop using account-flow management as the primary financial-flow model. Per D-01, D-02, D-03, and D-09, the primary graph must render `수입 -> 지출(소비/저축/투자)` without account nodes, income allocation links, correction fallback, or automatic balancing side effects.

Output: `buildSankeyData()` returns a renderer-compatible simple graph, and Step 1 tests prove legacy account-rich payloads no longer change the primary Sankey topology.
</objective>

<execution_context>
@C:/Users/LENOVO/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/LENOVO/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10.7-account-flow-extraction-and-portfolio-boundary/10.7-CONTEXT.md
@.planning/phases/10.7-account-flow-extraction-and-portfolio-boundary/10.7-RESEARCH.md
@.planning/phases/10.7-account-flow-extraction-and-portfolio-boundary/10.7-PATTERNS.md
@.planning/phases/10.6.1-legacy-editor-removal-and-detail-modal-capability-absorption/10.6.1-VERIFICATION.md
@docs/adr/0001-financial-detail-modal-is-the-only-primary-editor.md
@apps/main/modules/sankey-builder.js
@apps/main/modules/sankey-renderer.js
@tests/step1.spec.ts
</context>

<truths>
- UXR-13 / D-01: User sees Step 1 as simple income to consumption, savings, and investment, not as account-flow management.
- UXR-14 / D-02: `buildSankeyData()` does not require account or allocation fields for the primary graph.
- D-03 / D-09: `recommendAccountId()`, account correction metadata, and automatic account balancing are not part of the primary Sankey path.
- Preserve Phase 10.6.1 and ADR 0001: this plan changes the visual flow model, not the modal-only editor boundary.
</truths>

<tasks>
<task id="10.7-01-T1" type="auto" tdd="true">
  <name>Task 1: Add failing simple Sankey topology test</name>
  <files>tests/step1.spec.ts</files>
  <behavior>
    - Browser-import `sanitizeInputs`, `buildMonthlySnapshot`, and `buildSankeyData`, then feed a legacy payload containing accounts, income allocations, item account ids, transfers, and account correction fields.
    - Assert primary Sankey labels include income and top-level category destinations such as `총수입`, `생활비` or `고정비(고정지출)`, `저축`, and `투자`.
    - Assert primary Sankey labels and links exclude account node labels, allocation-only account edges, and automatic balance transfer labels.
  </behavior>
  <action>Add or rewrite a Phase 10.7 Step 1 Sankey topology test before changing implementation. Keep it near the existing Phase 09 Sankey topology tests so the executor intentionally replaces now-invalid account-topology assertions. The test must encode D-01/D-02/D-03/D-09 and must fail against the current account-based builder.</action>
  <verify>
    <automated>npx playwright test tests/step1.spec.ts -g "Phase 10.7.*Sankey" --reporter=list</automated>
  </verify>
  <acceptance_criteria>The new Phase 10.7 Sankey test fails before implementation and names the simple topology and account-flow absence expectations.</acceptance_criteria>
  <done>The new Phase 10.7 Sankey test fails before implementation and names the simple topology and account-flow absence expectations.</done>
</task>

<task id="10.7-01-T2" type="auto" tdd="true">
  <name>Task 2: Implement simple primary Sankey builder</name>
  <files>apps/main/modules/sankey-builder.js, apps/main/modules/sankey-renderer.js, tests/step1.spec.ts</files>
  <behavior>
    - `buildSankeyData(snapshot, sortMode, sankeyGrouping)` keeps the existing return shape used by `renderSankey`: `nodes`, `links`, `transfers`, `totalValue`, `splitGroups`, and compatible flags.
    - Income sources flow into `total-income`, then into expense, savings, and investment total/detail nodes using snapshot category totals and grouping.
    - The primary builder no longer imports or calls account-correction helpers and does not create account nodes, income allocation links, manual transfer links, or greedy balancing links.
    - Sankey metadata/help copy explains the simple income-to-category flow and does not describe account network exploration.
  </behavior>
  <action>Refactor `buildSankeyData()` to build the simple primary Step 1 topology per D-01/D-02. Remove the `recommendAccountId` import and all primary-graph dependence on `snapshot.accounts`, `incomes[].allocations`, item `accountId`, `transfers`, and account correction metadata per D-03/D-09. Preserve detail grouping and renderer-compatible fields so `renderSankey()` remains stable. Update `sankey-renderer.js` tooltip/meta copy to describe income-to-consumption/savings/investment flow only.</action>
  <verify>
    <automated>npx playwright test tests/step1.spec.ts -g "Phase 10.7.*Sankey" --reporter=list</automated>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>Phase 10.7 Sankey tests pass; TypeScript check passes; primary Sankey builder has no account-correction import and account-rich legacy fields do not alter the simple graph.</acceptance_criteria>
  <done>Phase 10.7 Sankey tests pass; TypeScript check passes; primary Sankey builder has no account-correction import and account-rich legacy fields do not alter the simple graph.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted/share input -> Sankey builder | Legacy user-controlled Step 1 payload can contain account/allocation fields and hostile labels. |
| Sankey data -> SVG renderer | Labels become SVG text and tooltip text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.7-01-01 | Tampering | `buildSankeyData()` | mitigate | Ignore account/allocation/correction fields for primary topology; derive from sanitized financial totals only. |
| T-10.7-01-02 | Information Disclosure | `sankey-renderer.js` tooltip | mitigate | Keep tooltip textContent-based and avoid reintroducing account names from legacy payload. |
| T-10.7-SC | Tampering | npm installs | accept | No package-manager installs are planned. |
</threat_model>

<verification>
Run:
- `npx playwright test tests/step1.spec.ts -g "Phase 10.7.*Sankey" --reporter=list`
- `npm run check`
</verification>

<success_criteria>
- UXR-13 and UXR-14 are covered for the core graph path.
- Legacy account/allocation/correction data does not create primary Sankey account nodes or balancing links.
- Existing Sankey renderer consumers still receive a compatible data object.
</success_criteria>

<output>
Create `.planning/phases/10.7-account-flow-extraction-and-portfolio-boundary/10.7-01-SUMMARY.md` when done.
</output>
