---
phase: 10.7-account-flow-extraction-and-portfolio-boundary
plan: 02
type: execute
wave: 2
depends_on:
  - 10.7-01
files_modified:
  - apps/main/modules/input-sanitizer.js
  - apps/main/modules/calculator.js
  - apps/main/modules/persistence-controller.js
  - tests/step1.spec.ts
autonomous: true
requirements:
  - UXR-14
  - UXR-15
must_haves:
  truths:
    - "D-07/D-08: legacy account/allocation data is preserved only as handoff/migration sidecar data."
    - "D-03/D-09: sanitize/load/import/share no longer run automatic account correction for primary Step 1."
  artifacts:
    - path: "apps/main/modules/input-sanitizer.js"
      provides: "Simplified Step 1 canonical sanitizer and account-flow sidecar extraction"
      exports: ["sanitizeInputs"]
    - path: "apps/main/modules/persistence-controller.js"
      provides: "Import/share compatibility path using sanitized simplified model"
    - path: "tests/step1.spec.ts"
      provides: "Sanitizer and import/share sidecar compatibility tests"
  key_links:
    - from: "apps/main/modules/persistence-controller.js"
      to: "apps/main/modules/input-sanitizer.js"
      via: "commitImmediateInputs and external restore paths call sanitizeInputs"
      pattern: "sanitizeInputs"
---

<objective>
Create the migration/storage boundary that lets Step 1 load old account-rich data without keeping account-flow modeling in the primary state.

Purpose: UXR-15 requires existing account/allocation data to survive for Portfolio/account-flow handoff, while UXR-14, D-03, D-07, D-08, and D-09 require primary Step 1 sanitize/render paths to stop repairing or depending on account data.

Output: old `accounts`, `allocations`, item account ids, split-income state, transfer data, and correction metadata are extracted into a sidecar such as `accountFlowHandoff`, while Step 1 primary fields remain simple financial inputs.
</objective>

<execution_context>
@C:/Users/LENOVO/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/LENOVO/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10.7-account-flow-extraction-and-portfolio-boundary/10.7-CONTEXT.md
@.planning/phases/10.7-account-flow-extraction-and-portfolio-boundary/10.7-RESEARCH.md
@.planning/phases/10.7-account-flow-extraction-and-portfolio-boundary/10.7-PATTERNS.md
@apps/main/modules/input-sanitizer.js
@apps/main/modules/calculator.js
@apps/main/modules/persistence-controller.js
@tests/step1.spec.ts
</context>

<truths>
- UXR-15 / D-07: Existing account/allocation data is not destroyed when old saves or share payloads are loaded.
- UXR-15 / D-08: Sidecar data is migration/handoff data, not live Step 1 UI, Sankey, or correction state.
- UXR-14 / D-03 / D-09: `sanitizeInputs()` no longer invokes automatic account correction for primary Step 1.
- Non-account fields from prior phases, including `actualSpent`, `varianceAmount`, `annualRate`, and `maturityMonth`, remain intact.
</truths>

<tasks>
<task id="10.7-02-T1" type="auto" tdd="true">
  <name>Task 1: Add failing sanitizer sidecar compatibility test</name>
  <files>tests/step1.spec.ts</files>
  <behavior>
    - Feed `sanitizeInputs()` a legacy payload with `accounts`, `incomes[].accountId`, `incomes[].allocations`, item `accountId`, `splitIncomeAccounts`, `surplusTransferAccountId`, `transfers`, and `accountCorrections`.
    - Assert primary financial fields preserve amounts, groups, budget actual/range, savings yield, and maturity month.
    - Assert account-flow fields are absent from primary income/outflow item paths or ignored by Step 1 primary calculations.
    - Assert a sidecar property is present with the preserved legacy account-flow data needed by Portfolio handoff.
  </behavior>
  <action>Add a Phase 10.7 sanitizer boundary test that fails on the current automatic repair behavior. Use the sidecar name selected in implementation consistently; prefer `accountFlowHandoff` unless existing code reveals a stronger local naming convention. The test must cite D-07/D-08/D-09 in its title or nearby comments only where useful for traceability.</action>
  <verify>
    <automated>npx playwright test tests/step1.spec.ts -g "Phase 10.7.*sanitizer|Phase 10.7.*handoff" --reporter=list</automated>
  </verify>
  <acceptance_criteria>The new sanitizer/sidecar test fails before implementation and proves old account data is preserved without remaining live primary state.</acceptance_criteria>
  <done>The new sanitizer/sidecar test fails before implementation and proves old account data is preserved without remaining live primary state.</done>
</task>

<task id="10.7-02-T2" type="auto" tdd="true">
  <name>Task 2: Implement simplified sanitizer and handoff sidecar</name>
  <files>apps/main/modules/input-sanitizer.js, apps/main/modules/calculator.js, apps/main/modules/persistence-controller.js, tests/step1.spec.ts</files>
  <behavior>
    - `sanitizeInputs()` creates primary Step 1 data without account repair, default account creation, income allocation repair, item account fallback, transfer normalization as live state, or `accountCorrections` as primary state.
    - A narrow helper in or near `input-sanitizer.js` extracts account-flow legacy fields into a serializable sidecar and preserves source labels/ids defensively.
    - `buildMonthlySnapshot()` and persistence/import/share paths tolerate old payloads and sidecar data without rehydrating account-flow fields into primary item paths.
    - Existing non-account Step 1 fields from Phases 10, 10.6, and 10.6.1 still sanitize and save correctly.
  </behavior>
  <action>Split the sanitizer boundary per D-07/D-08. Remove `repairAccountConnections()` from the primary `sanitizeInputs()` path per D-03/D-09, and move legacy account/allocation preservation into a sidecar helper. Adjust `calculator.js` only where it copies account fields into primary snapshots or uses account-specific surplus/warning behavior for Step 1; convert those uses to aggregate/simple behavior or tolerate absence. Update `persistence-controller.js` import/share restore paths only if they need explicit sidecar retention to avoid dropping old data.</action>
  <verify>
    <automated>npx playwright test tests/step1.spec.ts -g "Phase 10.7.*sanitizer|Phase 10.7.*handoff|Phase 10.7.*Sankey" --reporter=list</automated>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>Old account/allocation payloads load and save as simple Step 1 data plus sidecar; automatic account correction does not run in primary sanitize/render; non-account modal and projection fields still round trip.</acceptance_criteria>
  <done>Old account/allocation payloads load and save as simple Step 1 data plus sidecar; automatic account correction does not run in primary sanitize/render; non-account modal and projection fields still round trip.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| imported/share JSON -> sanitizer | Untrusted legacy payload enters app state. |
| sanitizer -> local persistence | Sanitized and sidecar data are saved locally and may be exported later. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.7-02-01 | Tampering | `input-sanitizer.js` | mitigate | Defensive copy and numeric/string normalization for sidecar; no mutation by correction repair in primary path. |
| T-10.7-02-02 | Denial of Service | import/share restore | mitigate | Ignore malformed sidecar arrays/objects and continue rendering simplified Step 1. |
| T-10.7-SC | Tampering | npm installs | accept | No package-manager installs are planned. |
</threat_model>

<verification>
Run:
- `npx playwright test tests/step1.spec.ts -g "Phase 10.7.*sanitizer|Phase 10.7.*handoff|Phase 10.7.*Sankey" --reporter=list`
- `npm run check`
</verification>

<success_criteria>
- UXR-14 and UXR-15 are covered at the storage and sanitizer boundary.
- Existing account/allocation data is preserved as sidecar only.
- Primary Step 1 state and calculations no longer depend on account correction.
</success_criteria>

<output>
Create `.planning/phases/10.7-account-flow-extraction-and-portfolio-boundary/10.7-02-SUMMARY.md` when done.
</output>
