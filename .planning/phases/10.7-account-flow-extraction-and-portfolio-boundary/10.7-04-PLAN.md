---
phase: 10.7-account-flow-extraction-and-portfolio-boundary
plan: 04
type: execute
wave: 4
depends_on:
  - 10.7-03
files_modified:
  - apps/main/index.html
  - apps/main/modules/dom.js
  - apps/main/modules/render-orchestrator.js
  - apps/main/modules/visualization-controller.js
  - apps/main/modules/event-bindings.js
  - apps/main/modules/ui-controller.js
  - apps/main/styles.css
  - tests/step1.spec.ts
autonomous: true
requirements:
  - UXR-13
  - UXR-14
must_haves:
  truths:
    - "D-01/D-02: Step 1 render/controller source no longer exposes account-flow UI or network-map controls."
    - "D-03/D-09: correction refresh and transfer-rule controller paths are removed from the primary Step 1 path."
  artifacts:
    - path: "apps/main/index.html"
      provides: "Simple Step 1 DOM contract without account-flow panels"
    - path: "apps/main/modules/render-orchestrator.js"
      provides: "Render pipeline without network-map or transfer rendering"
    - path: "tests/step1.spec.ts"
      provides: "Source audit and UI absence regression tests"
  key_links:
    - from: "apps/main/modules/render-orchestrator.js"
      to: "apps/main/modules/sankey-builder.js"
      via: "renderAll builds and renders simple Sankey only"
      pattern: "buildSankeyData"
---

<objective>
Clean Step 1 DOM, render orchestration, controller wiring, styles, and tests so removed account-flow behavior cannot remain active behind hidden UI.

Purpose: UXR-13 and UXR-14 require more than copy changes. Per D-01, D-02, D-03, and D-09, ordinary Step 1 must not render account-flow panels, network maps, transfer controls, correction refresh behavior, or stale account-flow source paths.

Output: Step 1 page source and controller paths reflect the simple primary model, with source audit tests guarding against accidental reintroduction.
</objective>

<execution_context>
@C:/Users/LENOVO/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/LENOVO/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10.7-account-flow-extraction-and-portfolio-boundary/10.7-CONTEXT.md
@.planning/phases/10.7-account-flow-extraction-and-portfolio-boundary/10.7-RESEARCH.md
@.planning/phases/10.7-account-flow-extraction-and-portfolio-boundary/10.7-PATTERNS.md
@apps/main/index.html
@apps/main/modules/dom.js
@apps/main/modules/render-orchestrator.js
@apps/main/modules/visualization-controller.js
@apps/main/modules/event-bindings.js
@apps/main/modules/ui-controller.js
@apps/main/styles.css
@tests/step1.spec.ts
</context>

<truths>
- UXR-13 / D-01: Step 1 source DOM has no ordinary account-flow management panels or controls.
- UXR-14 / D-03 / D-09: correction refresh and transfer-rule paths cannot mutate primary Sankey state.
- D-02: primary render pipeline shows simple Sankey, not account network map.
- Preserve previous modal-only editor boundary and simple Sankey tests from Plans 01-03.
</truths>

<tasks>
<task id="10.7-04-T1" type="auto" tdd="true">
  <name>Task 1: Add failing Step 1 source/UI account-flow audit</name>
  <files>tests/step1.spec.ts</files>
  <behavior>
    - Add a Phase 10.7 source audit that fetches Step 1 HTML and normal-path modules in browser context.
    - Assert removed account-flow UI/controller markers are absent from normal-path files or quarantined only in migration/Portfolio helper files where applicable.
    - Add a UI smoke assertion that Step 1 renders the simple Sankey panel and does not render network-map, correction refresh, transfer-rule, or surplus-transfer account controls.
  </behavior>
  <action>Create source-first regression coverage before cleanup. Scope absence checks to normal Step 1 UI/controller files so migration sidecar and Portfolio connector code can legitimately contain account-flow vocabulary. Include D-01/D-02/D-03/D-09 in the test description for traceability.</action>
  <verify>
    <automated>npx playwright test tests/step1.spec.ts -g "Phase 10.7.*source|Phase 10.7.*UI" --reporter=list</automated>
  </verify>
  <acceptance_criteria>The new source/UI audit fails before implementation while not forbidding sidecar or Portfolio handoff code from preserving legacy fields.</acceptance_criteria>
  <done>The new source/UI audit fails before implementation while not forbidding sidecar or Portfolio handoff code from preserving legacy fields.</done>
</task>

<task id="10.7-04-T2" type="auto" tdd="true">
  <name>Task 2: Remove Step 1 account-flow render and controller wiring</name>
  <files>apps/main/index.html, apps/main/modules/dom.js, apps/main/modules/render-orchestrator.js, apps/main/modules/visualization-controller.js, apps/main/modules/event-bindings.js, apps/main/modules/ui-controller.js, apps/main/styles.css, tests/step1.spec.ts</files>
  <behavior>
    - `renderAll()` no longer calls network-map rendering, transfer-list rendering, transfer-select rendering, or account-value derivation for Step 1 primary output.
    - Visualization/event controllers no longer bind correction refresh or transfer-rule handlers for ordinary Step 1.
    - Step 1 DOM no longer declares account-flow/network-map/correction/transfer controls as hidden inactive panels.
    - Styles remove unreachable account-flow selectors after DOM/controller cleanup, without broad unrelated visual churn.
  </behavior>
  <action>Remove account-flow DOM and controller wiring from the Step 1 primary path. Delete `renderNetworkMap` usage, transfer board/list/select rendering, surplus transfer account select updates, correction refresh handlers, transfer-rule event handlers, and stale DOM handles now made unreachable. Update `ui-controller.js` group iteration so ordinary Step 1 no longer treats `account` as a financial group. Trim only CSS selectors made unreachable by this cleanup. Keep simple Sankey controls such as grouping, sorting, value mode, zoom, and detail/basic display if they still apply to the simple graph.</action>
  <verify>
    <automated>npx playwright test tests/step1.spec.ts -g "Phase 10.7.*source|Phase 10.7.*UI|Phase 10.7.*Sankey|Phase 10.7.*modal" --reporter=list</automated>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>Normal Step 1 source paths contain no active account-flow UI/controller behavior; targeted Phase 10.7 Sankey/modal/source tests pass; TypeScript check passes.</acceptance_criteria>
  <done>Normal Step 1 source paths contain no active account-flow UI/controller behavior; targeted Phase 10.7 Sankey/modal/source tests pass; TypeScript check passes.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM events -> state mutation | User or scripted events can trigger controller handlers. |
| source cleanup -> regression surface | Hidden stale handlers can keep mutating simplified state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.7-04-01 | Elevation of Privilege | `visualization-controller.js` | mitigate | Remove correction/transfer handlers that mutate account fields outside the simple modal save boundary. |
| T-10.7-04-02 | Tampering | `render-orchestrator.js` | mitigate | Remove network-map/transfer rendering from primary render so sidecar data cannot alter UI state. |
| T-10.7-SC | Tampering | npm installs | accept | No package-manager installs are planned. |
</threat_model>

<verification>
Run:
- `npx playwright test tests/step1.spec.ts -g "Phase 10.7.*source|Phase 10.7.*UI|Phase 10.7.*Sankey|Phase 10.7.*modal" --reporter=list`
- `npm run check`
</verification>

<success_criteria>
- UXR-13 and UXR-14 are satisfied across Step 1 source, DOM, render, and controller paths.
- Account correction and transfer-rule behavior cannot affect primary Step 1.
- Source audit tests distinguish forbidden Step 1 primary markers from allowed migration/Portfolio handoff code.
</success_criteria>

<output>
Create `.planning/phases/10.7-account-flow-extraction-and-portfolio-boundary/10.7-04-SUMMARY.md` when done.
</output>
