---
phase: 10.7-account-flow-extraction-and-portfolio-boundary
plan: 01
subsystem: ui
tags: [step1, sankey, account-flow, playwright]

requires:
  - phase: 10.6.1-legacy-editor-removal-and-detail-modal-capability-absorption
    provides: Financial Detail Modal remained the only primary Step 1 editor before account-flow extraction.
provides:
  - Simple Step 1 primary Sankey topology from income to consumption, savings, and investment categories.
  - Phase 10.7 regression coverage proving legacy account-rich payloads do not alter the primary Sankey graph.
affects: [phase-10.7, step1, sankey-renderer, portfolio-boundary]

tech-stack:
  added: []
  patterns:
    - Browser-import Playwright probe for Step 1 module topology contracts.
    - Renderer-compatible Sankey data object without primary account-flow nodes or balancing transfers.

key-files:
  created:
    - .planning/phases/10.7-account-flow-extraction-and-portfolio-boundary/10.7-01-SUMMARY.md
  modified:
    - apps/main/modules/sankey-builder.js
    - apps/main/modules/sankey-renderer.js
    - tests/step1.spec.ts

key-decisions:
  - "Step 1 primary Sankey now ignores legacy account, allocation, transfer, and correction fields and renders only income to category destinations."
  - "Sankey help copy now describes the simple category flow instead of account network exploration."

patterns-established:
  - "Simple Sankey boundary: keep nodes, links, transfers, totalValue, splitGroups, and topLevelTargetIds shape while returning no account nodes or balancing transfers."
  - "Legacy account-rich payload tests should assert absence from primary Step 1 topology rather than correction-driven account labels."

requirements-completed: [UXR-13, UXR-14]

duration: 38 min
completed: 2026-06-26
status: complete
---

# Phase 10.7 Plan 01: Simple Step 1 Sankey Core Summary

**Step 1 Sankey now renders a simple income-to-category graph while ignoring legacy account-flow fields in the primary topology.**

## Performance

- **Duration:** 38 min
- **Started:** 2026-06-26T05:20:00Z
- **Completed:** 2026-06-26T05:58:10Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- Added a TDD RED Playwright topology probe for account-rich legacy Step 1 payloads.
- Reworked `buildSankeyData()` so income sources flow to `total-income`, then directly to expense, savings, investment, debt, and surplus destinations without account nodes.
- Removed primary Sankey dependence on `recommendAccountId`, income allocations, item `accountId`, manual transfer fields, and greedy automatic balancing transfers.
- Updated Sankey metadata help copy to describe the simple income-to-category flow.

## Task Commits

1. **Task 1: Add failing simple Sankey topology test** - `1300be3` (test)
2. **Task 2: Implement simple primary Sankey builder** - `6cbef74` (feat)

**Plan metadata:** pending final docs commit

## Files Created/Modified

- `apps/main/modules/sankey-builder.js` - Builds renderer-compatible simple Sankey data with no primary account-flow nodes, allocation links, or balancing transfers.
- `apps/main/modules/sankey-renderer.js` - Updates Sankey help copy to remove account-network exploration language.
- `tests/step1.spec.ts` - Adds Phase 10.7 browser-import topology regression for legacy account-rich payloads.
- `.planning/phases/10.7-account-flow-extraction-and-portfolio-boundary/10.7-01-SUMMARY.md` - Documents execution outcome.

## Decisions Made

- Kept the existing Sankey return shape (`nodes`, `links`, `transfers`, `totalValue`, `splitGroups`, `topLevelTargetIds`) so renderer consumers remain compatible.
- Left sanitizer/storage account migration for later Phase 10.7 plans; this plan only prevents those fields from influencing the primary Sankey builder.

## Deviations from Plan

None - plan executed exactly as written.

**Total deviations:** 0 auto-fixed.
**Impact on plan:** No scope expansion beyond the declared files.

## Issues Encountered

- The RED test initially expected raw income id `income-main`, but `buildMonthlySnapshot()` intentionally prefixes income nodes as `income-income-main`. The expectation was corrected during GREEN implementation.

## Verification

- `npx playwright test tests/step1.spec.ts -g "Phase 10.7.*Sankey" --reporter=list` - PASS, 1/1 test passed.
- `npm run check` - PASS, `tsc --noEmit` completed.
- Source scan for `recommendAccountId`, `account-correction`, `자동 잔액 맞춤`, and old account-network help copy in Sankey builder/renderer - PASS, no matches.

## Known Stubs

None.

## Threat Flags

None. The plan reduced the existing trust boundary by removing account/allocation/correction fields from primary Sankey topology and kept tooltip text assigned via `textContent`/attribute APIs.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Ready for `10.7-02-PLAN.md` to handle sanitizer/storage migration sidecar and import/share compatibility. Existing account/allocation fields can still exist in sanitized data, but the primary Sankey builder no longer renders or balances from them.

## Self-Check: PASSED

- Summary file exists at `.planning/phases/10.7-account-flow-extraction-and-portfolio-boundary/10.7-01-SUMMARY.md`.
- Task commits exist: `1300be3`, `6cbef74`.
- Required verification commands passed after commits.

---
*Phase: 10.7-account-flow-extraction-and-portfolio-boundary*
*Completed: 2026-06-26*
