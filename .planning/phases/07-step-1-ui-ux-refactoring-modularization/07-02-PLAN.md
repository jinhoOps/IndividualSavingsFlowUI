---
phase: 07-step-1-ui-ux-refactoring-modularization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/step1/index.html
  - apps/step1/styles.css
  - apps/step1/styles/layout.css
  - apps/step1/styles/components.css
  - apps/step1/styles/responsive.css
  - src/entries/step1.ts
autonomous: true
requirements:
  - UI-01
  - UI-02
requirements_addressed:
  - UI-01
  - UI-02
must_haves:
  truths:
    - "Step 1 visual order is Summary, Visualization, Controls, Projection, Comparison on desktop and <=768px."
    - "Step 1 uses DESIGN.md and shared theme tokens for Pearl canvas, flat panels, solid borders, Gowun Batang display text, Gowun Dodum body text, and physical button feedback."
    - "apps/step1/styles.css is reduced by at least 50% from the 2,841-line baseline or remains a small import/override file with a measured split that preserves import order."
    - "At <=768px, management tabs, advanced tabs, action menus, fields, and chart controls do not clip or overlap."
  artifacts:
    - path: "apps/step1/styles.css"
      provides: "reduced Step 1 stylesheet and import/override hub"
      max_lines: 1420
    - path: "apps/step1/styles/layout.css"
      provides: "Step 1 layout and panel flow rules"
    - path: "apps/step1/styles/components.css"
      provides: "Step 1 component overrides"
    - path: "apps/step1/styles/responsive.css"
      provides: "Step 1 responsive rules"
    - path: "src/entries/step1.ts"
      provides: "CSS import order after shared theme"
  key_links:
    - from: "src/entries/step1.ts"
      to: "shared/styles/step-theme.css"
      via: "shared theme import before Step 1 CSS"
      pattern: "shared/styles/step-theme.css"
    - from: "src/entries/step1.ts"
      to: "apps/step1/styles.css"
      via: "Step 1 CSS import after shared theme"
      pattern: "apps/step1/styles.css"
    - from: "apps/step1/index.html"
      to: "apps/step1/styles.css"
      via: "class-based layout instead of durable inline styles"
      pattern: "class="
---

<objective>
Step 1의 거대한 CSS를 DESIGN.md와 `shared/styles/step-theme.css` 중심으로 줄이고, 패널 흐름과 모바일 레이아웃을 Phase 07 결정에 맞게 재정렬합니다.

Purpose: Phase 07 D-01부터 D-08까지 구현하여 Step 1을 더 조밀하고 명확한 작업형 금융 입력 화면으로 만들되, 기존 사용자의 인지 흐름을 보존합니다.
Output: 2,841줄 기준 `apps/step1/styles.css` 50% 이상 감축, Step 1 panel order 정렬, inline durable style 축소, <=768px 모바일 무결성 유지.
</objective>

<execution_context>
@C:/Users/LENOVO/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/LENOVO/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-step-1-ui-ux-refactoring-modularization/07-CONTEXT.md
@.planning/phases/07-step-1-ui-ux-refactoring-modularization/RESEARCH.md
@DESIGN.md
@shared/styles/step-theme.css
@apps/step1/index.html
@apps/step1/styles.css
@src/entries/step1.ts
@.planning/phases/03-multi-account-data-model/03-CONTEXT.md
@.planning/phases/04-account-transfer-ui-ux-polish/04-CONTEXT.md
@.planning/phases/05-portfolio-creation-allocation-ui/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate CSS against shared theme tokens</name>
  <files>apps/step1/styles.css, apps/step1/styles/layout.css, apps/step1/styles/components.css, apps/step1/styles/responsive.css, src/entries/step1.ts</files>
  <action>Measure the current baseline first: `apps/step1/styles.css` is 2,841 physical lines. Remove duplicate/dead Step 1 CSS called out by research, including repeated account row blocks, management tab blocks, status badge blocks, export button blocks, obsolete transfer-board styles with no current DOM owner, undefined token drift such as `--panel-sub`/`--text-muted`, and decorative ambient gradients that conflict with DESIGN.md Pearl canvas. Apply DESIGN.md and `shared/styles/step-theme.css` tokens for canvas, panel, border, typography, spacing, primary/accent colors, and <=8px controls per D-02, D-05, D-08. Perform token consolidation inside `styles.css` first per D-06, then physically split the remaining Step 1 rules into exactly `apps/step1/styles/layout.css`, `components.css`, and `responsive.css`. Keep `styles.css` as the reduced Step 1 import/override hub at or below 1,420 lines, and update `src/entries/step1.ts` so shared theme imports remain before all Step 1 CSS. Do not add a CSS framework or new dependency.</action>
  <verify>
    <automated>npm run check</automated>
    <automated>powershell -NoProfile -Command "$stylesLines=(Get-Content apps\step1\styles.css).Count; if ($stylesLines -gt 1420) { throw \"apps/step1/styles.css line count $stylesLines exceeds 1420\" }"</automated>
    <automated>powershell -NoProfile -Command "rg -n \"--panel-sub|--text-muted|radial-gradient|backdrop-filter|box-shadow: 0 6px|border-radius: (1[0-9]|2[0-9])px\" apps/step1/styles.css apps/step1/styles -g \"*.css\""</automated>
  </verify>
  <done>`styles.css` is at or below 1,420 lines, the three Step 1 split CSS files exist with explicit import order, shared theme tokens are reused, and decorative/duplicated drift has been removed or justified in the summary.</done>
</task>

<task type="auto">
  <name>Task 2: Recompose Step 1 panel hierarchy and durable inline styles</name>
  <files>apps/step1/index.html, apps/step1/styles.css, apps/step1/styles/layout.css, apps/step1/styles/components.css</files>
  <action>Preserve the Phase 07 D-01 cognitive sequence as the visual and keyboard-accessible order: Summary, Visualization, Controls, Projection, Comparison. Prefer DOM order if it can be done without breaking existing `dom.js` selectors; otherwise make the CSS order explicit and verify tab flow. Keep the interface operational and dense per D-03: no landing-page treatment, no marketing hero, no nested decorative card layout, and no new ambient gradients. Move durable inline styles from `index.html` into named classes for the section heads, transfer editor, Sankey title group, network map container, projection options, comparison controls, and preset modal. Leave only genuinely dynamic styles in JavaScript. Preserve Phase 03/04 decisions: Sankey remains a household-flow visualization and account transfer complexity remains in the existing network-map/transfer UI rather than being reinserted into Sankey.</action>
  <verify>
    <automated>npm run check</automated>
    <automated>powershell -NoProfile -Command "$inline=(Select-String -Path apps\step1\index.html -Pattern 'style=').Count; if ($inline -gt 8) { throw \"index.html still has $inline inline style attributes; target is <=8\" }"</automated>
    <automated>powershell -NoProfile -Command "rg -n \"summary-panel|sankey-panel|controls-panel|projection-panel|comparison-panel|style=\" apps/step1/index.html apps/step1/styles.css apps/step1/styles -g \"*.css\""</automated>
  </verify>
  <done>The visible Step 1 panel sequence matches D-01, inline durable styles are reduced from 38 to 8 or fewer, and Sankey/network-map responsibilities from Phase 03/04 remain intact.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve <=768px responsive integrity during CSS reduction</name>
  <files>apps/step1/styles.css, apps/step1/styles/responsive.css</files>
  <action>Rebuild mobile rules around the hard D-04 acceptance target instead of carrying duplicate emergency overrides. Preserve media-query integrity per D-07 with explicit rules for 768px and current 760px behavior: panels stack, management tabs and advanced tabs scroll horizontally without body overflow, action menus fit their parent, item/account editor controls remain tappable, `.sankey-wrap` and network map stay scroll-contained, and projection tables scroll inside `.table-wrap`. Keep the Step 1 visual language from D-08: ISF Sunset primary, ISF Deep Sea accent, Pearl canvas, flat white panels, solid borders, and token-driven radius.</action>
  <verify>
    <automated>npm run check</automated>
    <automated>powershell -NoProfile -Command "$css=Get-Content apps\step1\styles.css; $media=($css | Select-String -Pattern '@media \(max-width: (760|768|520|1080)px\)').Count; if ($media -lt 3) { throw \"responsive media blocks appear truncated; found $media\" }"</automated>
    <automated>powershell -NoProfile -Command "rg -n \"@media \(max-width: (760|768|520|1080)px\)|sankey-wrap|mgmt-tabs|advanced-block|projection-table|action-menu|income-row|account-row\" apps/step1/styles.css apps/step1/styles -g \"*.css\""</automated>
  </verify>
  <done>Mobile CSS is not truncated, <=768px rules cover the named risk areas, and the summary records before/after line counts for `styles.css`, media-query count, and inline style count.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS cascade -> Step 1 interaction controls | Reduced/split styles can hide, clip, or reorder financial inputs and action controls. |
| HTML inline style removal -> runtime layout | Moving durable styles to CSS can break selectors or visual grouping. |
| viewport <=768px -> responsive rules | Mobile users rely on CSS containment and scroll areas for chart/control access. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Denial of Service | `apps/step1/styles.css`, responsive CSS | mitigate | Preserve targeted media rules and verify no body overflow or clipped controls at <=768px in Plan 03. |
| T-07-05 | Tampering | `src/entries/step1.ts` CSS imports | mitigate | Keep shared theme import before Step 1 CSS files and verify import order. |
| T-07-06 | Information Disclosure | CSS/HTML visual hierarchy | accept | No new secret or PII surface is added; risk is visual regression, covered by responsive verification. |
| T-07-SC | Tampering | npm installs | accept | No package-manager install is planned or allowed for this phase. |
</threat_model>

<verification>
Overall checks for this plan:
- `npm run check`
- `powershell -NoProfile -Command "$stylesLines=(Get-Content apps\step1\styles.css).Count; if ($stylesLines -gt 1420) { throw \"apps/step1/styles.css line count $stylesLines exceeds 1420\" }"`
- `powershell -NoProfile -Command "$inline=(Select-String -Path apps\step1\index.html -Pattern 'style=').Count; if ($inline -gt 8) { throw \"index.html inline style count $inline exceeds 8\" }"`
- `powershell -NoProfile -Command "rg -n \"@media \(max-width: (760|768|520|1080)px\)|sankey-wrap|mgmt-tabs|advanced-block|projection-table|action-menu|income-row|account-row\" apps/step1/styles.css apps/step1/styles -g \"*.css\""`
</verification>

<success_criteria>
- `apps/step1/styles.css` is reduced by at least 50% from 2,841 lines to 1,420 lines or fewer.
- CSS split is limited to the named Step 1 boundaries and preserves `shared/styles/step-theme.css` import precedence.
- Step 1 panel order follows Summary, Visualization, Controls, Projection, Comparison on desktop and mobile.
- <=768px mobile layout keeps fields, tabs, action menus, and chart controls accessible without clipping or overlap.
</success_criteria>

<output>
Create `.planning/phases/07-step-1-ui-ux-refactoring-modularization/07-02-SUMMARY.md` when done.
</output>
