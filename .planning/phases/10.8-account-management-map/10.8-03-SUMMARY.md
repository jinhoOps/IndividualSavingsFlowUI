---
phase: 10.8-account-management-map
plan: 03
subsystem: ui
tags: [vite, playwright, account-map, main-entry, shared-navigation, vanilla-js]

requires:
  - phase: 10.8-account-management-map
    plan: 01
    provides: dedicated Account Map route and shared header step
  - phase: 10.8-account-management-map
    plan: 02
    provides: Account Map-owned relationship renderer and draft persistence
provides:
  - Lightweight Main Account Map entry that links to the dedicated Account Map route
  - Shared navigation regression coverage proving Account Map is not routed through Portfolio
  - Restored Main account-flow compatibility regression for accounts, allocations, accountId links, transfers, and surplus routing
affects: [main, account-map, shared-navigation, step1-regressions]

tech-stack:
  added: []
  patterns:
    - Main entry renderer uses DOM APIs and scan-only derived counts
    - Account Map entry remains a route link, not an embedded editor

key-files:
  created:
    - apps/main/modules/account-map-entry-renderer.js
  modified:
    - apps/main/index.html
    - apps/main/styles.css
    - apps/main/modules/dom.js
    - apps/main/modules/render-orchestrator.js
    - tests/step1.spec.ts

key-decisions:
  - "Main renders Account Map as a compact scan-only summary inside the existing summary panel instead of adding a new full editor surface."
  - "The entry derives only rough counts from visible sanitized Main inputs and links to `../account-map/`; Account Map editing remains page-owned."
  - "Shared header Account Map navigation was already present from Plan 10.8-01, so this plan verified it and added route regression coverage rather than rewriting it."

patterns-established:
  - "Use `[data-account-map-entry=\"lightweight\"]` and `[data-account-map-link=\"dedicated\"]` as the Main boundary selectors for future tests."
  - "Keep Account Map Main summaries count-based and DOM-rendered; do not import the full Account Map app renderer into Main."

requirements-completed: [UXR-17, UXR-21]

metrics:
  duration: 11 min
  completed: 2026-06-29T09:20:18Z
  tasks: 3
  files: 6
status: complete
---

# Phase 10.8 Plan 03: Account Map Main Entry Summary

**Compact Main Account Map entry with dedicated-route navigation and restored account-flow compatibility regressions**

## Performance

- **Duration:** 11 min
- **Started:** 2026-06-29T09:09:15Z
- **Completed:** 2026-06-29T09:20:18Z
- **Tasks:** 3
- **Files modified:** 6

## Accomplishments

- Added a small Main Account Map entry inside the existing financial summary panel, with account/relationship/confirmation-needed counts and a direct `../account-map/` link.
- Added `apps/main/modules/account-map-entry-renderer.js` as a scan-only DOM renderer that does not import or embed the full Account Map editor, candidate review, detail drawer, or map canvas.
- Wired the entry into `renderAll()` from visible sanitized Main inputs while preserving existing Sankey, network-map, manual transfer, surplus transfer, and financial modal rendering paths.
- Added Playwright coverage for Main lightweight-only behavior, shared launcher routing, Account Map active state, and restored account-flow compatibility fields.

## Task Commits

1. **Task 1: Add Account Map to shared navigation** - no new commit; already present from Plan 10.8-01 and verified by new launcher regression coverage.
2. **Task 2: Add a lightweight Main entry/mini-summary only** - `1864454` (feat)
3. **Task 3: Add Main navigation and restored-behavior regressions** - `095e432` (test)

## Files Created/Modified

- `apps/main/modules/account-map-entry-renderer.js` - Compact Main summary renderer with count derivation, mini flow visual, and dedicated route link.
- `apps/main/index.html` - Added `#accountMapEntry` host inside the summary panel.
- `apps/main/styles.css` - Added desktop/mobile styles for the compact entry.
- `apps/main/modules/dom.js` - Added `accountMapEntry` DOM reference.
- `apps/main/modules/render-orchestrator.js` - Renders the Account Map entry from current visible Main inputs.
- `tests/step1.spec.ts` - Added Account Map Main entry, shared launcher, and restored account-flow compatibility regressions.

## Verification

- `npm run check` - Passed (`tsc --noEmit`).
- `npx playwright test tests/step1.spec.ts --reporter=list` - Passed, 69 tests.
- `npx playwright test tests/account-map.spec.ts --reporter=list` - Passed, 8 tests.

## Decisions Made

- Kept the Main entry inside the summary panel so it remains lightweight and does not compete with the primary Sankey/network surfaces.
- Used a local Main renderer rather than the Account Map route renderer because Main should expose a route entry and mini-summary only.
- Treated the pre-existing shared header Account Map item as satisfying Task 1 implementation; new tests now guard that it is active on Account Map and separate from Portfolio.

## Deviations from Plan

### Auto-fixed Issues

None - plan executed within the requested boundaries.

### Execution Notes

- `shared/components/app-header.js` already contained the Account Map label, launcher link, and `account-map` active state from Plan 10.8-01, so no new source diff was needed for Task 1.

## Known Stubs

None. Stub scan found only existing input placeholders and internal empty-string/array initialization patterns outside the new Account Map entry path.

## Threat Flags

None. This plan added no network endpoints, authentication paths, file access, or new trust-boundary schema changes. The new Main entry renders user-derived labels/counts through DOM APIs and `textContent`.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Phase 10.8 now has the dedicated Account Map route, map-first experience, and lightweight Main entry with coverage for keeping restored Main account-flow behavior compatible.

## Self-Check: PASSED

- Created/modified files exist: `apps/main/modules/account-map-entry-renderer.js`, `apps/main/index.html`, `apps/main/styles.css`, `apps/main/modules/dom.js`, `apps/main/modules/render-orchestrator.js`, `tests/step1.spec.ts`.
- Task commits exist: `1864454`, `095e432`.
- Verification evidence recorded above matches the final command outputs.

---
*Phase: 10.8-account-management-map*
*Completed: 2026-06-29*
