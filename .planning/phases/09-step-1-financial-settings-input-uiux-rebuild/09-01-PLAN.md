---
phase: 09-step-1-financial-settings-input-uiux-rebuild
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/main/modules/account-correction.js
  - apps/main/modules/constants.js
  - apps/main/modules/input-sanitizer.js
  - apps/main/modules/calculator.js
  - apps/main/modules/sankey-builder.js
  - tests/step1.spec.ts
autonomous: true
requirements:
  - TBD
user_setup: []
must_haves:
  truths:
    - "D-18/D-19: Income defaults to one simple deposit account alias and does not ask for bank name, balance, or detailed account type."
    - "D-21/D-22: Missing or mismatched account connections are deterministically corrected and surfaced as correction metadata."
    - "D-23/D-24: Sankey data always includes a stable total-income node labelled 총수입 between individual incomes and account nodes."
  artifacts:
    - path: "apps/main/modules/account-correction.js"
      provides: "Central account recommendation, repair, and correction-note helpers"
      exports: ["repairAccountConnections", "recommendAccountId", "summarizeAccountCorrections"]
    - path: "apps/main/modules/input-sanitizer.js"
      provides: "Boundary normalization that applies account repair before state/persistence"
      contains: "repairAccountConnections"
    - path: "apps/main/modules/sankey-builder.js"
      provides: "Canonical individual income -> 총수입 -> account -> outflow topology"
      contains: "total-income"
  key_links:
    - from: "apps/main/modules/input-sanitizer.js"
      to: "apps/main/modules/account-correction.js"
      via: "sanitizeInputs account repair"
      pattern: "repairAccountConnections"
    - from: "apps/main/modules/sankey-builder.js"
      to: "apps/main/modules/calculator.js"
      via: "snapshot.incomeBreakdown/accounts"
      pattern: "total-income"
---

<objective>
Create the Phase 09 data foundation for account correction and Sankey topology.

Purpose: Step 1 cannot safely move to summary cards and modal-first editing until existing and imported data are repaired consistently, and the Sankey flow has the mandatory `총수입` aggregate required by D-23/D-24.
Output: A centralized account correction module, sanitizer integration, and tested Sankey data shape that downstream UI plans consume without duplicating fallback logic.
</objective>

<execution_context>
@C:/Users/LENOVO/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/LENOVO/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-step-1-financial-settings-input-uiux-rebuild/09-CONTEXT.md
@.planning/phases/09-step-1-financial-settings-input-uiux-rebuild/RESEARCH.md
@apps/main/modules/constants.js
@apps/main/modules/input-sanitizer.js
@apps/main/modules/calculator.js
@apps/main/modules/sankey-builder.js
@tests/step1.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Centralize account recommendation and correction</name>
  <files>apps/main/modules/account-correction.js, apps/main/modules/constants.js, apps/main/modules/input-sanitizer.js</files>
  <behavior>
    - Existing data with no accounts receives only simple aliases such as `급여계좌`, `생활비계좌`, and `투자계좌` per D-19.
    - Income items default to one deposit account allocation unless a persisted split option is enabled per D-18.
    - Expense, savings, and investment items with missing or invalid `accountId` are corrected to deterministic recommended accounts per D-21.
    - Allocation totals that do not match item totals are corrected into the recommended or largest suitable account and emit a warning note per D-22.
  </behavior>
  <action>Create `account-correction.js` with deterministic helpers `recommendAccountId(inputs, category, item)`, `repairAccountConnections(inputs)`, and `summarizeAccountCorrections(corrections)`. Use existing `MAGIC_MAPPING_DEFAULTS`; add only simple account aliases and a `splitIncomeAccounts` boolean to `constants.js`/sanitized inputs if needed for D-18. Integrate `repairAccountConnections` inside `sanitizeInputs()` after account sanitization and before returning the final object, preserving Won units and returning an `accountCorrections` array with item id, item type, original account, resolved account, amount delta, and human-readable message. Do not introduce bank name, balance, detailed account type, remote banking fields, or a standalone account-management model per D-17/D-19.</action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>Malformed Step 1 inputs are sanitized into usable account-linked data, income defaults to a single deposit account, and corrections are available to UI/Sankey consumers without silent fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rebuild Sankey data around mandatory 총수입</name>
  <files>apps/main/modules/calculator.js, apps/main/modules/sankey-builder.js</files>
  <behavior>
    - `buildSankeyData()` always creates node id `total-income` labelled `총수입` when income exists per D-23.
    - Detail topology is `개별 수입 -> 총수입 -> 계좌 -> 지출/저축/투자` per D-24.
    - Deficit pseudo-income does not inflate `총수입`; it remains a deficit/shortfall indicator.
    - Account resolution in Sankey uses sanitized/corrected account links rather than a private fallback branch per D-21/D-25.
  </behavior>
  <action>Update `buildMonthlySnapshot()` only as needed to carry normalized `accountCorrections`, `splitIncomeAccounts`, and income allocation data to Sankey. Refactor `buildSankeyData()` so individual positive income nodes point to `total-income`, `total-income` points to accounts using sanitized income allocations, and accounts point to expense/savings/invest/debt/surplus nodes. Move account columns to follow the aggregate node and move outflow nodes to the final column. Remove private silent account fallback where centralized correction metadata now provides the resolved id. Preserve existing sort/group options and transfer/network data shape so `renderNetworkMap()` still receives account transfer information.</action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>Sankey data exposes a stable `total-income` aggregate and keeps downstream links account-based without losing existing sort, grouping, transfer, surplus, or deficit behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add focused data-shape coverage</name>
  <files>tests/step1.spec.ts</files>
  <behavior>
    - Browser-imported modules prove invalid account ids are repaired and correction metadata is emitted.
    - Browser-imported `buildSankeyData()` proves `total-income` exists and links are ordered around it.
    - Basic existing Phase 07 Sankey tests continue to pass after the extra column.
  </behavior>
  <action>Add a `Phase 09 account correction and Sankey topology` Playwright describe block to `tests/step1.spec.ts`. Use `page.evaluate()` module imports from `/IndividualSavingsFlowUI/apps/main/modules/...` to call sanitizer/snapshot/builder functions with malformed account ids and mismatched income allocations. Assert corrected account ids, visible correction-note metadata, the `total-income` node label `총수입`, links from income ids to `total-income`, links from `total-income` to account ids, and no `미지정 계좌` fallback in the returned topology.</action>
  <verify>
    <automated>npm run test:e2e -- tests/step1.spec.ts -g "Phase 09 account correction and Sankey topology"</automated>
  </verify>
  <done>Automated E2E/module coverage fails if account correction is silent, if `총수입` is absent, or if Sankey reverts to individual income -> account topology.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage/import/share payload -> sanitizer | Untrusted persisted or imported Step 1 JSON crosses into runtime state. |
| sanitizer -> Sankey builder | Corrected financial data becomes visual financial flow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | `sanitizeInputs()` | mitigate | Normalize account ids and allocation totals through `repairAccountConnections()` before persistence/render use. |
| T-09-02 | Information Disclosure | correction metadata | accept | Correction notes contain local account aliases and amounts already visible in Step 1; no secrets or remote account data are introduced. |
| T-09-03 | Denial of Service | `buildSankeyData()` | mitigate | Treat malformed account/allocations as sanitized input and return stable empty/null states only when income is absent. |
| T-09-SC | Tampering | npm installs | mitigate | No package-manager installs are part of this plan; keep external dependency set unchanged. |
</threat_model>

<verification>
Run `npm run check` and `npm run test:e2e -- tests/step1.spec.ts -g "Phase 09 account correction and Sankey topology"` after implementation. Existing Phase 07 Sankey tests must still pass because this plan changes topology used by the renderer.
</verification>

<success_criteria>
Account correction is centralized, deterministic, visible through metadata, and consumed by sanitizer/Sankey. Sankey topology contains `총수입` as a real node, not a rendered label patch.
</success_criteria>

<source_audit>
GOAL covered by this plan for account/Sankey stability. CONTEXT covered: D-18, D-19, D-21, D-22, D-23, D-24, D-25. RESEARCH covered: account-correction heuristics, normalize-at-boundaries, Sankey redesign, no package installs. REQ: ROADMAP Phase 09 lists Requirements as TBD, so frontmatter uses `TBD`.
</source_audit>

<output>
Create `.planning/phases/09-step-1-financial-settings-input-uiux-rebuild/09-01-SUMMARY.md` when done.
</output>
