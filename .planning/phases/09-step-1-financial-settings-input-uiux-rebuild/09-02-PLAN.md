---
phase: 09-step-1-financial-settings-input-uiux-rebuild
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/main/index.html
  - apps/main/styles.css
  - apps/main/modules/dom.js
  - apps/main/modules/presets.js
  - apps/main/modules/preset-setup-controller.js
  - apps/main/modules/event-bindings.js
autonomous: true
requirements:
  - TBD
user_setup: []
must_haves:
  truths:
    - "D-05/D-06: Preset quick setup is a primary Step 1 input flow that generates expense, savings, and investment allocations from monthly income percentages."
    - "D-09/D-10: 안정, 균형, 성장, 야수, and 사용자 지정 presets are available, and custom starts from the last selected preset values."
    - "D-12/D-13: Final confirmation shows both original percentages and rounded Won amounts after correction."
  artifacts:
    - path: "apps/main/modules/preset-setup-controller.js"
      provides: "Preset modal state, percentage normalization, preview, confirmation, and commit lifecycle"
      exports: ["createPresetSetupController"]
    - path: "apps/main/modules/presets.js"
      provides: "Preset percentage definitions and preview/rounded amount builders"
      exports: ["PRESET_STYLES", "buildPresetPreview", "applyPresetPreview"]
    - path: "apps/main/index.html"
      provides: "Step 1 quick setup modal and confirmation DOM shell"
      contains: "presetModal"
  key_links:
    - from: "apps/main/modules/event-bindings.js"
      to: "apps/main/modules/preset-setup-controller.js"
      via: "controller creation/binding"
      pattern: "createPresetSetupController"
    - from: "apps/main/modules/preset-setup-controller.js"
      to: "persistence.commitImmediateInputs"
      via: "confirmed preset commit"
      pattern: "commitImmediateInputs"
---

<objective>
Turn preset quick setup into a real Step 1 financial input flow.

Purpose: D-05 through D-13 require presets to create a complete editable financial setup from percentages, with Step 3-style confirmation and correction visibility rather than an immediate overwrite shortcut.
Output: Preset percentage definitions, preview/correction logic, modal UI, and event bindings that commit only after final confirmation.
</objective>

<execution_context>
@C:/Users/LENOVO/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/LENOVO/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-step-1-financial-settings-input-uiux-rebuild/09-CONTEXT.md
@.planning/phases/09-step-1-financial-settings-input-uiux-rebuild/RESEARCH.md
@.planning/phases/05-portfolio-creation-allocation-ui/05-CONTEXT.md
@DESIGN.md
@shared/styles/step-theme.css
@apps/main/index.html
@apps/main/styles.css
@apps/main/modules/dom.js
@apps/main/modules/event-bindings.js
@apps/main/modules/presets.js
@apps/portfolio/app.js
@apps/portfolio/modules/dom.js
@apps/portfolio/modules/calculator.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build preset percentage preview contracts</name>
  <files>apps/main/modules/presets.js</files>
  <behavior>
    - Four default presets are named `안정`, `균형`, `성장`, and `야수` per D-09.
    - Presets mainly differ in savings-vs-investment ratio, while expense remains understandable and bounded per D-09.
    - Expense output groups are exactly `고정비`, `변동비`, `행복비`, and `경조사비` per D-07.
    - Preview rows retain original percentage, normalized percentage, rounded Won amount, and correction delta per D-12/D-13.
  </behavior>
  <action>Refactor `PRESET_STYLES` into Korean-labeled percentage presets and add pure helpers such as `buildPresetPreview({ monthlyIncomeWon, presetKey, percentages })`, `normalizePresetPercentages(percentages)`, and `applyPresetPreview(preview)`. Generate `expenseItems`, `savingsItems`, and `investItems` together from monthly income per D-06. Round amount-correction mode cleanly to 10,000 KRW units while preserving integer percentage intent for percentage-correction mode per D-12. Keep internal values in Won and use existing formatter utilities only at render time.</action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>Preset preview data can represent selected preset percentages, generated amounts, original percent intent, normalized/corrected values, and final Step 1 inputs without touching the DOM.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace shortcut modal with guided quick setup</name>
  <files>apps/main/index.html, apps/main/styles.css, apps/main/modules/dom.js, apps/main/modules/preset-setup-controller.js, apps/main/modules/event-bindings.js</files>
  <behavior>
    - Opening `#openPresetBtn` shows a core setup modal, not a decorative shortcut, per D-05.
    - Percentage inputs recalculate and normalize on blur per D-11.
    - `사용자 지정` copies the last selected preset values unless no prior preset exists per D-10.
    - The built screen is for inspection/correction after preset application per D-08.
  </behavior>
  <action>Create `preset-setup-controller.js` with `createPresetSetupController({ persistence, getInputs })` and move preset modal binding out of ad hoc `bindPresetModal()`. Update `index.html` preset modal content to show monthly income, preset segmented controls, editable percentage rows for expense/savings/invest, correction mode choice, preview totals, and a confirmation step. Add DOM refs in `dom.js`, bind from `event-bindings.js`, and remove obsolete salary-only/neutral-only modal assumptions. Style with flat panel/modal tokens from `shared/styles/step-theme.css`; keep controls dense, accessible, and responsive without gradients or nested decorative cards.</action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>Users can open preset setup, choose a Korean investment-attitude preset, edit percentages, blur inputs to normalize, switch to user custom from the last preset, and see generated preview rows before commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add final confirmation and safe commit path</name>
  <files>apps/main/modules/preset-setup-controller.js, apps/main/modules/event-bindings.js</files>
  <behavior>
    - Confirmation summarizes generated expense/savings/invest items, original percentages, rounded amounts, and correction deltas per D-12/D-13.
    - Existing-data overwrite warning remains explicit before replacing current rows.
    - Confirmed data commits through existing persistence and sanitizer boundaries rather than direct localStorage mutation.
  </behavior>
  <action>Implement the confirmation screen inside the preset controller using DOM APIs or escaped template values for user-controlled text. On confirm, call existing `persistence.commitImmediateInputs(applyPresetPreview(preview))`; on cancel/back, preserve the draft preview in the modal. Keep the current overwrite warning behavior but make it part of the confirmation flow, not a browser confirm before the user sees generated details.</action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>Preset application is a preview-confirm-commit flow that preserves percent intent and rounded amount corrections in the visible confirmation before Step 1 state changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| preset modal input -> preset preview | User-edited percentages and income amounts cross into generated financial rows. |
| preset preview -> persistence | Generated rows replace current Step 1 financial settings after confirmation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Tampering | `preset-setup-controller.js` | mitigate | Clamp/normalize percentages on blur and build final inputs through pure preview helpers. |
| T-09-05 | Repudiation | preset confirmation | mitigate | Show original percentages, normalized percentages, rounded amounts, and correction deltas before commit. |
| T-09-06 | Information Disclosure | preset modal | accept | All data is user-entered local financial planning data already visible in Step 1. |
| T-09-SC | Tampering | npm installs | mitigate | No package-manager installs are part of this plan; keep external dependency set unchanged. |
</threat_model>

<verification>
Run `npm run check` after implementation. Full user-flow Playwright coverage is added in Plan 09-04 after summary/modal/Sankey UI surfaces exist.
</verification>

<success_criteria>
Preset quick setup generates complete Step 1 financial rows from percentages, carries original percent intent through rounded amount confirmation, and commits only through the existing persistence/sanitizer path.
</success_criteria>

<source_audit>
GOAL covered by this plan for preset-based quick financial setup. CONTEXT covered: D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-13. RESEARCH covered: preset quick setup, losing-original-percent pitfall, Step 3 confirmation pattern, no package installs. REQ: ROADMAP Phase 09 lists Requirements as TBD, so frontmatter uses `TBD`.
</source_audit>

<output>
Create `.planning/phases/09-step-1-financial-settings-input-uiux-rebuild/09-02-SUMMARY.md` when done.
</output>
