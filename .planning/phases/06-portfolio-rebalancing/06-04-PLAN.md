---
phase: 06-portfolio-rebalancing
plan: 04
type: execute
wave: 1
depends_on: [06-03]
files_modified: [apps/step3/modules/state.js, apps/step3/app.js, apps/step3/modules/dom.js, shared/storage/hub-storage.js]
autonomous: true
requirements: [S3-MVP3]

must_haves:
  truths:
    - "현재의 포트폴리오 구성(계좌/종목)을 이름과 함께 스냅샷으로 저장할 수 있다."
    - "저장된 스냅샷 목록을 확인하고 특정 시점으로 복원할 수 있다."
    - "계좌별 자산 비중을 더 정교하게 관리할 수 있는 UI 요소가 보강된다."
  artifacts:
    - path: "apps/step3/modules/snapshot-manager.js"
      provides: "Portfolio History Management"
---

<objective>
Step 3의 완성도를 높이기 위해 포트폴리오 스냅샷(저장/복원) 기능을 구현하고, 사용자 인터페이스를 최종 보강합니다.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.gemini/knowledge/wiki/Plan_Step3.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: 스냅샷 매니저 구현</name>
  <files>apps/step3/modules/snapshot-manager.js</files>
  <action>
    - `apps/step3/modules/snapshot-manager.js`를 생성합니다.
    - 현재 `IsfState.data`를 스냅샷 객체로 변환하여 저장하는 기능을 작성합니다.
    - `IsfStorageHub`를 통해 `portfolio-snapshots` 키로 데이터를 관리합니다.
  </action>
  <verify>
    - 스냅샷 저장 시 로컬 스토리지에 데이터가 추가되는지 확인
  </verify>
  <done>포트폴리오 스냅샷 로직 완비</done>
</task>

<task type="auto">
  <name>Task 1: 스냅샷 UI (저장/목록) 통합</name>
  <files>apps/step3/modules/dom.js, apps/step3/app.js</files>
  <action>
    - UI에 '현재 구성 저장' 버튼을 추가합니다.
    - 저장된 스냅샷들을 리스트로 보여주고, 클릭 시 복원하는 기능을 구현합니다.
  </action>
  <verify>
    - 저장 버튼 클릭 시 목록이 갱신되는지 확인
    - 목록에서 스냅샷 선택 시 데이터가 이전 상태로 복구되는지 확인
  </verify>
  <done>스냅샷 사용자 인터페이스 완성</done>
</task>

<task type="auto">
  <name>Task 2: 계좌/종목 에디터 편의성 개선</name>
  <files>apps/step3/modules/dom.js, apps/step3/styles.css</files>
  <action>
    - 계좌 유형(ISA, 연금저축 등) 선택을 위한 Select 박스를 추가합니다.
    - 비중(%) 입력 시 합계가 100%를 넘지 않도록 경고 표시 등 UX를 개선합니다.
  </action>
  <verify>
    - 계좌 유형 변경이 정상 저장되는지 확인
    - 비중 합계 경고가 적절히 노출되는지 확인
  </verify>
  <done>에디터 편의성 및 데이터 검증 보강</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| History -> State | 과거 스냅샷 데이터가 현재 상태로 복원됨 (데이터 정합성 주의) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Denial of Service | Local Storage | accept | 스냅샷 개수 제한 (예: 20개) 적용 |
</threat_model>

<success_criteria>
- 사용자가 여러 버전의 포트폴리오를 저장하고 자유롭게 전환할 수 있음.
- 계좌 유형 및 비중 관리가 더 직관적으로 개선됨.
- Step 3의 모든 기능 요구사항이 최종적으로 충족됨.
</success_criteria>

<output>
After completion, create `.planning/phases/06-portfolio-rebalancing/06-04-SUMMARY.md`
</output>
