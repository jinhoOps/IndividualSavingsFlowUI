---
phase: 06-portfolio-rebalancing
plan: 03
type: execute
wave: 1
depends_on: [06-02]
files_modified: [apps/step3/modules/calculator.js, apps/step3/modules/chart-builder.js, apps/step3/app.js, apps/step3/modules/dom.js, apps/step3/modules/state.js]
autonomous: true
requirements: [S3-MVP2, S3-MVP3]

must_haves:
  truths:
    - "목표 비중(Target)과 실제 비중(Actual)의 격차를 정확히 계산할 수 있다."
    - "이번 달 투자 여력(from Step 1)을 활용한 '최적 매수 가이드'를 산출한다."
    - "전체 자산 구성을 보여주는 도넛 차트(SVG)가 정상적으로 렌더링된다."
  artifacts:
    - path: "apps/step3/modules/calculator.js"
      provides: "Rebalancing Engine (Target vs Actual)"
    - path: "apps/step3/modules/chart-builder.js"
      provides: "SVG Donut Chart Renderer"
---

<objective>
포트폴리오 리밸런싱을 위한 핵심 연산 로직을 구현하고, 시각화 차트와 매수 가이드를 UI에 통합합니다.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.gemini/knowledge/wiki/Plan_Step3.md
@.gemini/knowledge/wiki/Plan_Step3_Implementation.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: 리밸런싱 엔진 (Calculator) 구현</name>
  <files>apps/step3/modules/calculator.js</files>
  <action>
    - `apps/step3/modules/calculator.js`를 생성합니다.
    - 현재 총 자산 대비 각 종목의 실제 비중을 계산하는 함수를 작성합니다.
    - 목표 비중과의 격차(Diff)를 구하고, 가용 투자금을 배분하는 '신규 투자금 우선 매수' 로직을 구현합니다.
  </action>
  <verify>
    - 특정 종목의 비중이 부족할 때 가용 투자금이 해당 종목에 우선 배정되는지 연산 결과 확인
  </verify>
  <done>리밸런싱 연산 엔진 완비</done>
</task>

<task type="auto">
  <name>Task 1: 도넛 차트 시각화 (Chart Builder)</name>
  <files>apps/step3/modules/chart-builder.js</files>
  <action>
    - `apps/step3/modules/chart-builder.js`를 생성합니다.
    - Vanilla JS + SVG를 사용하여 자산군별/종목별 비중을 보여주는 도넛 차트를 구현합니다.
    - 데이터 변경 시 차트가 부드럽게 갱신되도록 처리합니다.
  </action>
  <verify>
    - `portfolioChart` 컨테이너에 도넛 모양의 SVG가 정상 노출되는지 확인
  </verify>
  <done>자산 비중 시각화 기능 구현</done>
</task>

<task type="auto">
  <name>Task 2: 리밸런싱 가이드 UI 연동</name>
  <files>apps/step3/modules/dom.js, apps/step3/app.js</files>
  <action>
    - `IsfDom`에 `renderGuide()` 함수를 추가하여 추천 매수 종목과 금액을 리스트 형태로 표시합니다.
    - `App.render()` 주기에서 계산 엔진과 차트 빌더를 호출하여 화면을 전체적으로 업데이트합니다.
  </action>
  <verify>
    - '리밸런싱 액션 가이드' 섹션에 실제 매수 추천 데이터가 노출되는지 확인
  </verify>
  <done>가이드 UI 및 전체 통합 완료</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Calculation -> UI | 엔진의 연산 결과가 사용자에게 시각적으로 전달됨 (정밀도 유지 필수) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | Calculation | accept | 부동 소수점 오차 최소화 (round 처리) |
</threat_model>

<success_criteria>
- 도넛 차트를 통해 현재 포트폴리오 상태를 직관적으로 파악 가능.
- 사용자가 입력한 목표 비중에 따라 구체적인 매수 가이드 금액이 산출됨.
- 모든 시각화 및 가이드가 데이터 변경에 따라 즉각적으로 반응함.
</success_criteria>

<output>
After completion, create `.planning/phases/06-portfolio-rebalancing/06-03-SUMMARY.md`
</output>
