---
phase: 06-portfolio-rebalancing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [apps/step3/index.html, apps/step3/app.js, apps/step3/modules/state.js, apps/step3/modules/step1-connector.js]
autonomous: true
requirements: [S3-MVP1, S3-MVP2]

must_haves:
  truths:
    - "Step 1의 투자 가능 금액(startInvest) 데이터를 수신하여 화면에 표시할 수 있다."
    - "계좌(Account) 및 종목(Asset)의 기본 데이터 모델이 정의되어 있다."
    - "포트폴리오 설정을 로컬 스토리지에 저장하고 불러올 수 있다."
  artifacts:
    - path: "apps/step3/index.html"
      provides: "Portfolio Rebalancing UI Skeleton"
    - path: "apps/step3/modules/step1-connector.js"
      provides: "Data bridge from Step 1"
---

<objective>
Step 3 (포트폴리오 리밸런싱) 개발을 위한 기본 인프라를 구축하고, Step 1과의 데이터 연동 및 기본 레이아웃을 완성합니다.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.gemini/knowledge/wiki/Plan_Step3.md
@.gemini/knowledge/wiki/Plan_Step3_Implementation.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: Step 3 스캐폴딩</name>
  <files>apps/step3/index.html, apps/step3/styles.css, apps/step3/app.js</files>
  <action>
    - `apps/step3/` 디렉토리에 기본 파일을 생성합니다.
    - `index.html`에 ISF 표준 레이아웃(Glassmorphism, Header, Modal)을 구성합니다.
    - `app.js`에서 모듈 로딩 및 초기화 로직을 작성합니다.
  </action>
  <verify>
    - `/apps/step3/index.html` 접속 시 헤더와 기본 레이아웃이 정상 노출되는지 확인
  </verify>
  <done>Step 3 기본 구조 및 UI 스켈레톤 완성</done>
</task>

<task type="auto">
  <name>Task 1: Step 1 데이터 연동 (Connector)</name>
  <files>apps/step3/modules/step1-connector.js, apps/step3/modules/state.js</files>
  <action>
    - `IsfStorageHub.getLatestStep1Snapshot()`을 호출하여 Step 1의 마지막 저장 데이터를 가져옵니다.
    - `totalInvestCapacity` (또는 `startInvest`) 값을 추출하여 Step 3의 `availableCash` 상태로 연동합니다.
  </action>
  <verify>
    - 콘솔 로그 또는 UI 요약 카드에 Step 1의 투자 가능 금액이 올바르게 표시되는지 확인
  </verify>
  <done>Step 1-3 데이터 브릿지 구축 완료</done>
</task>

<task type="auto">
  <name>Task 2: 영속성 레이어 및 데이터 모델링</name>
  <files>apps/step3/modules/state.js, shared/storage/hub-storage.js</files>
  <action>
    - `Account` (ISA, 연금저축 등) 및 `Asset` (종목명, 비중, 현재가) 데이터 모델을 정의합니다.
    - `IsfStorageHub`를 통해 포트폴리오 설정을 `portfolio-settings` 키로 저장/로드하는 로직을 구현합니다.
  </action>
  <verify>
    - 샘플 데이터를 저장한 후 페이지 새로고침 시 데이터가 유지되는지 확인
  </verify>
  <done>포트폴리오 데이터 모델 및 영속성 레이어 완성</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Step 1 -> Step 3 | Step 1의 영속화된 데이터가 Step 3의 초기값으로 입력됨 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | Local Storage | accept | 클라이언트 전용 툴이므로 로컬 보안에 의존 |
</threat_model>

<success_criteria>
- Step 1의 데이터가 Step 3 대시보드에 성공적으로 로드됨.
- 기본 계좌/종목 데이터를 저장하고 복원하는 흐름이 안정적임.
</success_criteria>

<output>
After completion, create `.planning/phases/06-portfolio-rebalancing/06-01-SUMMARY.md`
</output>
