---
phase: 06-portfolio-rebalancing
plan: 02
type: execute
wave: 1
depends_on: [06-01]
files_modified: [apps/step3/modules/dom.js, apps/step3/app.js, apps/step3/modules/state.js, apps/step3/styles.css]
autonomous: true
requirements: [S3-MVP1]

must_haves:
  truths:
    - "사용자는 계좌(Account)를 추가, 수정, 삭제할 수 있다."
    - "각 계좌 내에 종목(Asset)을 추가, 수정, 삭제할 수 있다."
    - "종목별 목표 비중(Target Ratio)과 현재 보유 수량/가격을 입력할 수 있다."
    - "입력된 데이터는 실시간으로 IsfState에 반영되고 로컬 스토리지에 저장된다."
  artifacts:
    - path: "apps/step3/modules/dom.js"
      provides: "Dynamic Editor Rendering & Event Binding"
---

<objective>
포트폴리오 구성을 위한 계좌 및 종목 에디터 UI를 구현하고, 사용자의 입력을 상태(State)와 동기화합니다.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.gemini/knowledge/wiki/Plan_Step3.md
@.gemini/knowledge/wiki/Plan_Step3_Implementation.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: DOM 모듈 생성 및 기본 렌더링</name>
  <files>apps/step3/modules/dom.js</files>
  <action>
    - `apps/step3/modules/dom.js`를 생성합니다.
    - 계좌 리스트(`accountList`)와 종목 리스트를 렌더링하는 함수를 작성합니다.
    - `IsfState`의 데이터를 바탕으로 HTML 문자열을 생성하여 DOM에 주입합니다.
  </action>
  <verify>
    - 초기 로드 시 '등록된 계좌가 없습니다' 메시지가 정상 노출되는지 확인
  </verify>
  <done>에디터 렌더링 엔진 구축</done>
</task>

<task type="auto">
  <name>Task 1: 계좌 추가 및 삭제 기능</name>
  <files>apps/step3/app.js, apps/step3/modules/dom.js, apps/step3/modules/state.js</files>
  <action>
    - '계좌 추가' 버튼 클릭 시 `IsfState.addAccount()`를 호출하고 화면을 갱신합니다.
    - 계좌별 '삭제' 버튼 기능을 구현합니다.
  </action>
  <verify>
    - 버튼 클릭 시 새로운 계좌 카드가 생성되는지 확인
    - 삭제 시 해당 계좌가 리스트에서 사라지는지 확인
  </verify>
  <done>계좌 관리 CRUD 기본 구현</done>
</task>

<task type="auto">
  <name>Task 2: 종목(Asset) 추가 및 입력 바인딩</name>
  <files>apps/step3/modules/dom.js, apps/step3/modules/state.js</files>
  <action>
    - 각 계좌 카드 내에 '종목 추가' 버튼을 구현합니다.
    - 종목명, 티커, 목표 비중, 현재가, 수량 입력 필드를 렌더링합니다.
    - `input` 이벤트 발생 시 `IsfState`의 해당 데이터를 즉시 업데이트(Debounced)하고 저장합니다.
  </action>
  <verify>
    - 종목 추가 시 입력 필드 세트가 나타나는지 확인
    - 입력값 변경 후 새로고침 시 데이터가 유지되는지 확인
  </verify>
  <done>종목 관리 및 실시간 데이터 동기화 완료</done>
</task>

<task type="auto">
  <name>Task 3: 에디터 스타일링 (Glassmorphism)</name>
  <files>apps/step3/styles.css</files>
  <action>
    - 계좌 카드 및 종목 행(Row)에 대한 스타일을 정의합니다.
    - 프로젝트 표준 Glassmorphism 및 Typography를 적용합니다.
  </action>
  <verify>
    - 모바일(760px 이하) 및 데스크톱에서 레이아웃이 깨지지 않는지 확인
  </verify>
  <done>에디터 UI 스타일 최적화</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User Input -> State | 사용자 입력값이 정화(Sanitize)되어 상태에 저장됨 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Information Disclosure | Local Storage | accept | 클라이언트 기기 로컬 데이터 |
</threat_model>

<success_criteria>
- 사용자가 복수의 계좌와 종목을 자유롭게 구성할 수 있음.
- 입력한 모든 수치 데이터가 영속적으로 저장됨.
- UI가 프로젝트 디자인 가이드를 준수하며 반응형으로 동작함.
</success_criteria>

<output>
After completion, create `.planning/phases/06-portfolio-rebalancing/06-02-SUMMARY.md`
</output>
