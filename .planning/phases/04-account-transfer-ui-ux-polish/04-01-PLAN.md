---
phase: 04-account-transfer-ui-ux-polish
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/step1/index.html
  - apps/step1/modules/dom.js
  - apps/step1/modules/state.js
  - apps/step1/modules/calculator.js
  - apps/step1/modules/list-renderer.js
  - apps/step1/app.js
  - apps/step1/styles.css
files_created:
  - apps/step1/modules/network-map-renderer.js
autonomous: true
requirements:
  - CORE-03
  - UX-02
must_haves:
  truths:
    - "Sankey 차트와 이체 네트워크 맵이 단일 시각화 패널 내에 좌우 슬라이드 형태로 부드럽게 토글 전환된다 (D-03)."
    - "계좌 간 자금 흐름 네트워크 맵(#accountFlowNetworkMap)은 빌드 용량이 가벼운 경량 외부 라이브러리 또는 최적화된 SVG 코드로 렌더링된다 (D-01)."
    - "이체선 위를 흐르는 펄스 파티클 애니메이션이 방향성을 가지고 SVG 상에 작동한다 (D-01)."
    - "계좌 노드 호버 시 연관 이체선만 강조되고 무관한 요소는 0.15 투명도로 페이드아웃된다 (D-01)."
    - "State 스키마에 transfers(수동 이체 규칙 리스트)가 영속 보존된다 (D-02)."
    - "자연어 문장형 또는 카드형의 직관적인 수동 이체 추가/삭제 폼 UI가 구현된다 (D-02)."
    - "수동 이체 설정 시 출발 계좌의 출금 가능 예상 잔액 힌트(#sourceBalanceHint)가 실시간으로 제공된다 (D-02)."
    - "등록된 이체 규칙 리스트는 평소 깔끔히 유지되며, 호버 시에만 삭제 X 버튼이 페이드인 노출된다 (D-02)."
    - "금융소득종합과세 실시간 경고 인디케이터가 디자인 스펙(Yellow/Red 보더, 글로우 효과, 뱃지 색상, cubic-bezier 트랜지션) 규격을 준수하여 구현된다 (D-04)."
    - "화면 내 설명 문구가 소거되고 '?' 도움말 아이콘 및 Glassmorphism 기반 전역 툴팁(#globalTooltip)으로 일괄 통합된다 (D-05)."
  artifacts:
    - path: "apps/step1/index.html"
      provides: "시각화 슬라이드 래퍼, 자연어 수동 이체 폼, '?' 아이콘 앵커 및 전역 툴팁 컨테이너 마크업 추가"
    - path: "apps/step1/modules/dom.js"
      provides: "시각화 토글 제어, 수동 이체 폼 잔액 힌트, 전역 툴팁 관련 DOM 참조 추가"
    - path: "apps/step1/modules/state.js"
      provides: "기본 state 템플릿에 transfers: [] 추가 및 스키마 유효성 보완"
    - path: "apps/step1/modules/calculator.js"
      provides: "수동 이체 규칙을 계좌 밸런스에 선반영하고 남은 금액에 대해 밸런싱을 유도하는 로직 고도화"
    - path: "apps/step1/modules/list-renderer.js"
      provides: "이체 편집 목록(삭제 X 호버 페이드인), 잔액 힌트 출력 및 '?' 아이콘 툴팁 트리거 생성 지원"
    - path: "apps/step1/modules/network-map-renderer.js"
      provides: "번들 크기가 최소화된 네트워크 시각화 렌더러(경량 라이브러리 검토/SVG 구현), 펄스 효과, 호버 포커스 기능 탑재"
    - path: "apps/step1/app.js"
      provides: "시각화 탭 토글 슬라이드 트리거, 수동 이체 폼 잔액 힌트 및 전역 툴팁 마우스 이벤트 바인딩, 경고 스펙 상태 연동"
    - path: "apps/step1/styles.css"
      provides: "시각화 가로 슬라이더, Glassmorphism 툴팁, 경고 인디케이터 디자인 스펙 규격, 이체 카드 삭제 X 트랜지션 CSS 구현"
---

<objective>
단일 시각화 패널 내에서 Sankey 차트와 '계좌 간 자금 흐름 지도(Network Map)'를 탭 토글을 통해 부드러운 가로 슬라이드로 전환할 수 있도록 구현합니다. 또한 사용자의 뇌적 연산을 줄이는 수동 이체 규칙 설정 UX(잔액 힌트, 호버 삭제)를 완성하고, 설명 문구를 '?' 아이콘의 Glassmorphism 툴팁으로 리팩토링하며, 금융과세 경고 인디케이터를 정의된 디자인 스펙에 따라 고도화합니다.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-account-transfer-ui-ux-polish/04-CONTEXT.md
@apps/step1/index.html
@apps/step1/modules/dom.js
@apps/step1/modules/state.js
@apps/step1/modules/calculator.js
@apps/step1/modules/list-renderer.js
@apps/step1/app.js
@apps/step1/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: index.html 마크업 개편 및 dom.js DOM 참조 매핑 추가</name>
  <files>
    - apps/step1/index.html
    - apps/step1/modules/dom.js
  </files>
  <read_first>
    - apps/step1/index.html
    - apps/step1/modules/dom.js
  </read_first>
  <action>
    - `index.html`에서 시각화 영역 상단에 탭 방식의 차트 스위치 제어기 `#visualizationToggle` 마크업을 신설합니다.
    - 기존 `#sankeyChartPanel` 내부에 가로 슬라이딩을 지원하는 뷰포트 `#visualizationViewport` 및 내부 슬라이더 `#visualizationSlider` 구조를 배치하고, 기존의 `#sankeyWrap`와 신설할 `#accountFlowNetworkMap`을 가로로 배치합니다.
    - 입력 폼 영역에 수동 이체 설정을 위한 문장형(Sentence UI) 또는 간결한 카드형 `#transferRuleForm` 마크업을 신설합니다. 이때 출발 계좌 선택 드롭다운 옆에 잔액 힌트 영역 `#sourceBalanceHint`를 포함합니다.
    - 설명용 가이드 문구들을 삭제하고 `?` 도움말 아이콘 앵커(`.help-tooltip-trigger`)를 해당 레이블 옆에 주입합니다.
    - HTML 최하단에 Glassmorphism 스타일을 적용할 전역 도움말 툴팁 `#globalTooltip` 마크업을 추가합니다.
    - `dom.js`에서 위 신설된 DOM 요소들(차트 스위치, 슬라이더, 이체 폼, 잔액 힌트, 툴팁 등)의 선택 프로퍼티를 추가 등록합니다.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - index.html의 구조가 무너지지 않고 정상적으로 슬라이더 래퍼 및 툴팁 마크업이 지정되어야 합니다.
    - dom.js에 새로 선언한 DOM 요소들이 누락 없이 반영되어야 합니다.
  </acceptance_criteria>
  <done>HTML 마크업 준비 및 DOM 매핑 추가가 완료됩니다.</done>
</task>

<task type="auto">
  <name>Task 2: state.js에 수동 이체 데이터 스키마 확장</name>
  <files>apps/step1/modules/state.js</files>
  <read_first>
    - apps/step1/modules/state.js
  </read_first>
  <action>
    - `state.js`의 기본 상태(default state) 템플릿 구조에 `transfers: []` 배열 프로퍼티를 추가합니다.
    - 데이터 로드 및 초기화 시 `transfers`가 누락되거나 빈 값일 경우 빈 배열로 방어 처리해 스키마의 무결성을 유지합니다.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - state의 초기 템플릿에 `transfers: []`가 정상적으로 정의되어야 합니다.
  </acceptance_criteria>
  <done>데이터 스키마에 수동 이체 배열 확장이 완료됩니다.</done>
</task>

<task type="auto">
  <name>Task 3: calculator.js 연산 로직 고도화 (수동 이체 선적용 및 밸런싱)</name>
  <files>apps/step1/modules/calculator.js</files>
  <read_first>
    - apps/step1/modules/calculator.js
  </read_first>
  <action>
    - `calculator.js` 및 `sankey-builder.js`에 정의된 연산 흐름을 수정합니다.
    - 수동 이체 규칙 배열(`state.inputs.transfers`)의 출금 및 입금 값을 각 계좌의 최종 밸런스 계산에 선제적으로 적용합니다.
    - 수동 이체 규칙을 차감한 후 남은 잔여 잉여금에 대해서만 자동 이체(Greedy 잔액 맞춤) 밸런싱이 추가적으로 동작하도록 연산 로직을 보완합니다.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - 수동 이체 규칙의 금액이 계좌 잔고 및 밸런스 연산에 정상 합산/차감되어 연산 정합성을 유지해야 합니다.
  </acceptance_criteria>
  <done>수동 이체 연산 코어 고도화가 완료됩니다.</done>
</task>

<task type="auto">
  <name>Task 4: list-renderer.js에 수동 이체 UI 및 툴팁 렌더링 추가</name>
  <files>apps/step1/modules/list-renderer.js</files>
  <read_first>
    - apps/step1/modules/list-renderer.js
  </read_first>
  <action>
    - `renderTransferRulesList(transfers, accounts)` 함수를 구현합니다.
    - 설정된 수동 이체 규칙들을 목록 형태로 렌더링하고, 각 카드 우측에 평소에는 안 보이다가 카드 호버 시 페이드인으로 나타나는 삭제 X 버튼(`.btn-delete-transfer`) 마크업을 생성합니다.
    - 수동 이체 등록 시 선택한 출발 계좌의 남은 "출금 가능 예상 잔액(만원)"을 동적으로 연산하여 `#sourceBalanceHint`에 텍스트 형태로 렌더링하는 헬퍼 함수를 추가합니다.
    - `?` 도움말 아이콘 및 툴팁 데이터셋(`data-tooltip-content`)을 렌더링하는 헬퍼 로직을 탑재합니다.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - 수동 이체 목록이 만원 단위 포맷으로 출력되어야 합니다.
    - 이체 카드 우측에 삭제 버튼 클래스가 알맞게 삽입되어야 합니다.
    - 잔액 힌트와 툴팁 속성이 마크업에 정상적으로 설정되어야 합니다.
  </acceptance_criteria>
  <done>이체 편집 UI 및 툴팁 데이터 렌더러 작성이 완료됩니다.</done>
</task>

<task type="auto">
  <name>Task 5: network-map-renderer.js 신규 시각화 렌더러 모듈 개발</name>
  <files>apps/step1/modules/network-map-renderer.js</files>
  <action>
    - SVG 기반으로 자금 흐름을 도식화하는 `renderNetworkMap(container, accounts, transfers)` 함수를 작성합니다.
    - **라이브러리 검토:** 빌드 크기 오버헤드를 유발하지 않는 경량 라이브러리(예: `d3-force` 모듈 일부 등)를 검토하여 번들 크기를 최소화하거나, 최적화된 순수 SVG 배치 알고리즘을 사용합니다.
    - **노드 및 엣지 배치:** 계좌 노드들을 원형 또는 그리드 형태로 균형 있게 배치하고, 유향 화살표 링크를 연결합니다.
    - **펄스 흐름 애니메이션:** CSS `stroke-dasharray`, `stroke-dashoffset` 및 키프레임을 사용하여 돈이 흘러가는 실시간 펄스 효과를 연출합니다.
    - **호버 인터랙션:** 특정 노드 마우스 진입 시, 해당 노드와 연동되지 않은 모든 요소의 투명도를 `0.15`로 낮추는 마스킹 기능을 구현합니다.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - SVG 네트워크 맵이 데이터와 정상 연동되어 레이아웃이 렌더링되어야 합니다.
    - 펄스 애니메이션 및 마우스 호버 포커스 필터 기능이 에러 없이 동작해야 합니다.
  </acceptance_criteria>
  <done>네트워크 자금 지도 렌더러 모듈 개발이 완료됩니다.</done>
</task>

<task type="auto">
  <name>Task 6: app.js 컨트롤러 통합 및 실시간 라이프사이클 이벤트 바인딩</name>
  <files>apps/step1/app.js</files>
  <read_first>
    - apps/step1/app.js
  </read_first>
  <action>
    - **시각화 슬라이드 전환:** 차트 탭 스위치 클릭 시 슬라이더 엘리먼트 `#visualizationSlider`에 `transform: translateX(-50%)` 등의 스타일을 주어 두 시각화 차트가 부드럽게 스와이프되도록 토글 핸들러를 바인딩합니다.
    - **수동 이체 폼 제어:** 이체 추가 및 삭제 액션을 바인딩하고, 이체 추가 시 중복/순환 예외 검증을 수행한 뒤 `state.inputs.transfers`를 갱신하고 `markDirty` 및 `renderAll()`을 트리거합니다.
    - **잔액 힌트 연동:** 수동 이체 폼의 출발 계좌(`sourceSelect`) 변경 시, 해당 계좌의 잔액 힌트를 동적으로 계산하여 `#sourceBalanceHint`에 갱신하는 실시간 리스너를 바인딩합니다.
    - **전역 툴팁 바인딩:** `.help-tooltip-trigger` 호버 시, 해당 트리거의 위치를 계산해 Glassmorphism `#globalTooltip`을 알맞은 위치에 표시하고 마우스 아웃 시 숨기는 이벤트 리스너를 바인딩합니다.
    - **경고 인디케이터 반응성:** 저축/투자 필드 수정 시 금융소득과세 경고 상태가 렌더링 사이클에 즉각적으로 반영되어 링/뱃지에 점등되도록 라이프사이클 흐름을 확립합니다.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - 차트 스위치 클릭 시 가로 슬라이딩 애니메이션이 실행되어야 합니다.
    - 수동 이체 규칙의 실시간 추가/삭제 및 잔액 힌트 노출이 원활히 동작해야 합니다.
    - 전역 도움말 아이콘 호버 시 툴팁이 올바른 위치에 나타나고 사라져야 합니다.
  </acceptance_criteria>
  <done>애플리케이션 이벤트 제어 및 라이프사이클 통합이 완료됩니다.</done>
</task>

<task type="auto">
  <name>Task 7: styles.css 스타일시트 추가 및 전역 UX 모션 최적화</name>
  <files>apps/step1/styles.css</files>
  <read_first>
    - apps/step1/styles.css
  </read_first>
  <action>
    - **차트 가로 슬라이더 CSS:** `#visualizationViewport` 및 `#visualizationSlider`에 가로 flex 스택 및 `transition: transform 0.4s cubic-bezier(0.25, 1, 0.5, 1);` 스타일을 구성합니다.
    - **Glassmorphism 툴팁 CSS:** `#globalTooltip`에 `backdrop-filter: blur(10px); background: rgba(255,255,255,0.7);` 등의 프리미엄 툴팁 테마를 구현합니다.
    - **경고 인디케이터 디자인 스펙 CSS:**
      - `.account-row--warn`: 보더 Yellow, box-shadow 글로우 효과 적용.
      - `.account-row--crit`: 보더 Red, box-shadow 글로우 효과 적용.
      - 뱃지 텍스트색 및 배경 매핑 및 `transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);` 반영.
    - **이체 카드 삭제 버튼 CSS:** `.btn-delete-transfer`에 평소 opacity 0, 이체 카드 호버 시 opacity 1 및 transition 스타일 추가.
    - **물리적 무결성 수호:** 최하단 미디어 쿼리를 절대로 파손시키지 않으며 수술식으로 스타일을 추가합니다.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - 슬라이더 뷰포트와 툴팁 스타일이 크로스 브라우저/모바일 화면에서 정상 작동해야 합니다.
    - 경고 인디케이터 스펙 및 삭제 버튼 트랜지션이 규격에 맞게 렌더링되어야 합니다.
    - 빌드가 통과해야 합니다.
  </acceptance_criteria>
  <done>디자인 시스템 스펙 및 UX 모션 CSS 일괄 적용이 완료됩니다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client-Side Rendering | 사용자 로컬 브라우저 상태 관리 및 SVG 렌더링 무결성 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Transfer Rules | mitigate | 이체 규칙 추가 시 출발 계좌와 도착 계좌가 동일하지 않은지, 빈 값이나 문자열 주입이 없는지 `IsfUtils` 및 입력 세니타이저 검증 |
| T-04-02 | Denial of Service | Network Rendering | mitigate | 무한 스크롤 및 SVG 노드 무한 루프 겹침을 방지하기 위해 최대 계좌 갯수 및 노드 배치 경계 좌표 고정 |
</threat_model>

<verification>
`npm run build` 명령을 실행하여 번들 및 컴파일 빌드가 통과하는지 확인하고, git diff 도구 및 라인수 검증을 거쳐 스타일과 반응형 코드의 물리적 무결성을 최종 체크합니다.
</verification>

<success_criteria>
1. 시각화 패널 상단 차트 스위치를 통해 Sankey 차트와 '계좌 간 자금 흐름 지도(Network Map)'가 부드러운 가로 슬라이딩 애니메이션으로 전환됨.
2. 경량 외부 라이브러리 검토 또는 SVG 코드를 통해 네트워크 맵이 올바르게 렌더링되며, 이체선 펄스 및 노드 호버 필터링이 정상 작동함.
3. 수동 이체 폼이 문장형/카드형으로 간결하게 구성되고, 출발 계좌 선택 시 출금 가능 예상 잔액 힌트(#sourceBalanceHint)가 실시간 노출되어 UX를 개선함.
4. 등록된 이체 규칙은 깔끔히 유지되며, 호버 시에만 삭제 X 버튼이 페이드인으로 노출됨.
5. 연간 금융소득 종합과세 한도 초과 경고 인디케이터(링, 뱃지, cubic-bezier 트랜지션)가 정의된 디자인 스펙 규격에 일치하게 실시간 점등됨.
6. 화면 내의 긴 가이드 설명들이 제거되고 '?' 도움말 아이콘 및 Glassmorphism 기반 전역 툴팁(#globalTooltip)으로 축소 통합됨.
</success_criteria>

<output>
작업 완료 시 `.planning/phases/04-account-transfer-ui-ux-polish/04-01-SUMMARY.md` 생성
</output>
