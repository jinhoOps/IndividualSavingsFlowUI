---
phase: 04-account-transfer-ui-ux-polish
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/step1/index.html
  - apps/step1/modules/dom.js
  - apps/step1/modules/state.js
  - apps/step1/modules/calculator.js
  - apps/step1/modules/list-renderer.js
  - apps/step1/app.js
  - apps/step1/styles.css
files_created:
  - apps/step1/modules/network-map-renderer.js
autonomous: true
requirements:
  - CORE-03
  - UX-02
must_haves:
  truths:
    - "Sankey Diagram 하단에 SVG로 자체 렌더링되는 전용 네트워크 흐름 맵(#accountFlowNetworkMap)이 신설된다 (D-01)."
    - "네트워크 흐름 맵은 계좌 노드들을 원형 또는 격자 구조로 균형 있게 배치하며, 이체선들을 SVG path 및 화살표 마커로 연결한다 (D-01)."
    - "이체선 위를 흐르는 펄스 파티클 애니메이션이 CSS stroke-dashoffset 또는 keyframes를 통해 방향성을 가지고 작동한다 (D-01)."
    - "계좌 노드 마우스 호버 시 해당 노드와 연결된 입출금 흐름선만 투명도 1.0으로 강조되고 다른 요소들은 투명도 0.15로 페이드아웃된다 (D-01)."
    - "Step1State 데이터 스키마에 transfers(수동 이체 규칙 리스트)가 추가되고 영속화가 유지된다 (D-02)."
    - "출발 계좌, 도착 계좌, 이체 금액을 입력하여 수동 이체 규칙을 추가/삭제할 수 있는 '계좌 간 이체 설정' 폼 UI가 구현된다 (D-02)."
    - "Step 화면 간(1단계 ➔ 2단계 ➔ 3단계) 전환 시 콘텐츠가 좌우로 부드럽게 미끄러지며 페이드되는 CSS 트랜지션 애니메이션이 동작한다 (D-03)."
    - "Step 1의 전체 카드 및 로우 컴포넌트에 마우스 호버 시 transform: translateY(-2px)와 그림자 트랜지션이 균일하게 오버레이된다 (D-03)."
    - "저축/투자 이율 변경 시 종합과세 한도 초과 경고 인디케이터(링/뱃지)가 지연 없이 실시간으로 즉시 점등된다 (D-04)."
  artifacts:
    - path: "apps/step1/index.html"
      provides: "네트워크 흐름 맵 SVG 컨테이너 및 수동 이체 규칙 설정 편집 폼 마크업 추가"
    - path: "apps/step1/modules/dom.js"
      provides: "신규 네트워크 맵 SVG 및 수동 이체 폼 관련 DOM 참조 매핑 추가"
    - path: "apps/step1/modules/state.js"
      provides: "기본 state 템플릿에 transfers: [] 추가 및 스키마 유효성 보완"
    - path: "apps/step1/modules/calculator.js"
      provides: "연산 파이프라인에서 수동 이체 규칙을 선반영하고 남은 금액에 대해 자동 밸런싱을 유도하는 로직 고도화"
    - path: "apps/step1/modules/list-renderer.js"
      provides: "수동 이체 설정 목록 및 규칙 편집 폼 HTML 렌더러 추가"
    - path: "apps/step1/modules/network-map-renderer.js"
      provides: "SVG 노드 배치, 유향 화살표 그리기, 펄스 애니메이션 및 호버 포커스 기능이 내재된 신규 렌더러 모듈 작성"
    - path: "apps/step1/app.js"
      provides: "수동 이체 추가/삭제 이벤트 리스너 바인딩, renderAll에서 네트워크 맵 렌더러 연동, Step 전환 애니메이션 제어"
    - path: "apps/step1/styles.css"
      provides: "네트워크 맵 SVG 스타일, 펄스 흐름 키프레임, Step 슬라이드 트랜지션 및 전역 호버 부유 애니메이션 CSS 일괄 적용"
---

<objective>
계좌 간 N:N 이체 흐름을 명시적으로 보여주기 위한 SVG 기반 '계좌 간 자금 흐름 지도(Network Map)'를 도입하고, 수동 이체 설정 기능 및 편집 UI를 데이터 모델과 연동합니다. 또한 화면 전환 애니메이션 및 마이크로 호버 모션을 가미하여 프리미엄 UX/UI로 폴리싱합니다.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-account-transfer-ui-ux-polish/04-CONTEXT.md
@apps/step1/index.html
@apps/step1/modules/dom.js
@apps/step1/modules/state.js
@apps/step1/modules/calculator.js
@apps/step1/modules/list-renderer.js
@apps/step1/app.js
@apps/step1/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: index.html 및 dom.js 마크업 및 DOM 요소 매핑 갱신</name>
  <files>
    - apps/step1/index.html
    - apps/step1/modules/dom.js
  </files>
  <read_first>
    - apps/step1/index.html
    - apps/step1/modules/dom.js
  </read_first>
  <action>
    - `index.html`에서 기존 생키 레전드 및 이체 보드 영역 하단에, 계좌 간 자금 흐름 지도가 렌더링될 전용 SVG 영역 `#accountFlowNetworkMap` 마크업을 신설합니다.
    - 입력 폼 영역에 수동 이체 규칙을 추가하고 삭제할 수 있는 `계좌 간 이체 설정` 에디터 컨테이너(`#transferEditorSection`) 및 입력 폼(`#transferRuleForm`), 목록 영역(`#transferRuleList`) 마크업을 신설합니다.
    - `dom.js`에서 `#accountFlowNetworkMap`, `#transferEditorSection`, `#transferRuleForm`, `#transferRuleList`, 그리고 필요한 입력 필드들을 DOM 레지스트리 오브젝트에 추가합니다.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - index.html의 전체 레이아웃 구조가 깨지지 않고 신규 SVG와 편집 폼 마크업이 지정되어야 합니다.
    - dom.js에 새로 선언한 DOM 요소들이 정상적으로 매핑되어야 합니다.
  </acceptance_criteria>
  <done>마크업 및 DOM 매핑 추가가 완료됩니다.</done>
</task>

<task type="auto">
  <name>Task 2: state.js에 수동 이체 데이터 스키마 확장</name>
  <files>apps/step1/modules/state.js</files>
  <read_first>
    - apps/step1/modules/state.js
  </read_first>
  <action>
    - `state.js`의 기본 상태(default state) 템플릿 구조에 `transfers: []` 배열 프로퍼티를 추가합니다.
    - 영속화 및 복원 시 `transfers`가 누락되거나 빈 배열일 경우 올바르게 처리할 수 있도록 초기화 로직을 보완합니다.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - state의 초기 템플릿에 `transfers: []`가 정의되어 정상적으로 저장 및 조회되어야 합니다.
  </acceptance_criteria>
  <done>데이터 스키마에 수동 이체 배열 확장이 완료됩니다.</done>
</task>

<task type="auto">
  <name>Task 3: calculator.js 연산 로직 고도화 (수동 이체 반영 및 밸런싱)</name>
  <files>apps/step1/modules/calculator.js</files>
  <read_first>
    - apps/step1/modules/calculator.js
  </read_first>
  <action>
    - `buildSankeyData` 또는 계산 프로세스를 수정하여 사용자가 직접 정의한 수동 이체 목록(`state.inputs.transfers`)을 수집합니다.
    - 수동 이체 규칙(`sourceAccountId` ➔ `targetAccountId`, 금액)을 이체 데이터 집합에 선제적으로 적용합니다.
    - 각 계좌의 최종 밸런스(인플로우 - 아웃플로우)를 계산할 때, 이 수동 이체 금액을 차감 및 가산합니다.
    - 수동 이체 처리를 완료한 후 남은 차액에 대해서만 기존의 자동 이체(Greedy 잔액 맞춤) 밸런싱이 추가적으로 동작하도록 보완하거나, 수동 이체로 완전 제어될 경우의 안전성을 확보합니다.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - 수동 이체 규칙으로 입력된 금액이 계좌 간 흐름 및 계좌 밸런스에 정확히 반영되어 연산되어야 합니다.
    - 수동 이체 및 자동 이체 계산 결과가 꼬이지 않고 정합성을 유지해야 합니다.
  </acceptance_criteria>
  <done>이체 수치 연산 코어 고도화가 완료됩니다.</done>
</task>

<task type="auto">
  <name>Task 4: list-renderer.js에 수동 이체 규칙 편집 UI 렌더링 구현</name>
  <files>apps/step1/modules/list-renderer.js</files>
  <read_first>
    - apps/step1/modules/list-renderer.js
  </read_first>
  <action>
    - `renderTransferRulesList(transfers, accounts)` 함수를 구현합니다.
    - 설정된 수동 이체 규칙들을 목록 형태로 렌더링하며, 각 항목에는 출발/도착 계좌명, 이체 금액(만원 단위), 그리고 삭제 버튼(`.btn-delete-transfer`)을 포함합니다.
    - 규칙 추가 폼의 계좌 선택 드롭다운(`sourceSelect`, `targetSelect`)을 현재 등록된 계좌 목록에 맞게 실시간으로 동기화하는 헬퍼 렌더러를 추가합니다.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - 설정된 수동 이체 목록이 만원 단위 포맷으로 정갈하게 렌더링되어야 합니다.
    - 드롭다운 리스트가 현재 생성된 계좌 정보와 동적으로 일치해야 합니다.
  </acceptance_criteria>
  <done>이체 편집 UI 렌더러 구현이 완료됩니다.</done>
</task>

<task type="auto">
  <name>Task 5: network-map-renderer.js 신규 시각화 렌더러 모듈 개발</name>
  <files>apps/step1/modules/network-map-renderer.js</files>
  <action>
    - SVG 기반으로 자금 흐름을 도식화하는 `renderNetworkMap(container, accounts, transfers)` 함수를 작성합니다.
    - **노드 배치:** 계좌 노드들을 SVG 내에 원형 배치 또는 균등 그리드 배치하여 노드 겹침을 방지하고 레이아웃을 계산합니다.
    - **엣지 드로잉:** 각 계좌 간 이체 관계를 나타내는 화살표 마커가 부착된 라인 패스를 렌더링합니다.
    - **펄스 흐름 애니메이션:** CSS `stroke-dasharray`, `stroke-dashoffset` 및 키프레임을 적용한 중첩 패스를 활용하여 자금이 이동하는 흐름 효과(Flowing Dot)를 렌더링합니다.
    - **호버 인터랙션:** 특정 노드에 마우스 진입 시, 해당 노드와 연결되지 않은 모든 노드/경로의 opacity를 `0.15`로 일괄 감소시키는 포커스 마스크 기능을 구현합니다.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - SVG 맵이 에러 없이 출력되고, 노드와 라인이 데이터와 완벽히 연동되어야 합니다.
    - 마우스 호버 필터링 및 펄스 흐름 모션이 정상 구현되어야 합니다.
  </acceptance_criteria>
  <done>네트워크 자금 지도 렌더러 개발이 완료됩니다.</done>
</task>

<task type="auto">
  <name>Task 6: app.js 컨트롤러 통합 및 실시간 라이프사이클 바인딩</name>
  <files>apps/step1/app.js</files>
  <read_first>
    - apps/step1/app.js
  </read_first>
  <action>
    - `app.js`에서 수동 이체 폼(`submit` 이벤트) 및 삭제 버튼 클릭 이벤트 리스너를 등록합니다.
    - 이체 추가 시 중복이나 순환(A ➔ B, B ➔ A 등)이 입력되지 않도록 유효성을 체크하고, `state.inputs.transfers`에 규칙을 저장한 뒤 `markDirty` 및 `renderAll()`을 트리거합니다.
    - `renderAll()`의 렌더링 사이클에 `networkMapRenderer.renderNetworkMap(...)` 호출을 연동합니다.
    - Step 화면 탭 클릭 시 화면 슬라이드/페이드 트랜지션 클래스를 추가하여 부드럽게 화면을 교체하는 핸들러를 바인딩합니다.
    - 저축/투자 변경으로 인한 금융소득 종합과세 경고가 화면에 지연 없이 즉각적으로 재렌더링에 반영되는지 코어 흐름을 점검합니다.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - 수동 이체의 추가/삭제가 정상적으로 IndexedDB/localStorage에 보존되며 실시간 렌더링되어야 합니다.
    - Step 전환 및 입력 변경 시 실시간 반응성과 트랜지션이 부드럽게 연동되어야 합니다.
  </acceptance_criteria>
  <done>애플리케이션 코어 및 이벤트 바인딩 통합이 완료됩니다.</done>
</task>

<task type="auto">
  <name>Task 7: styles.css 스타일시트 추가 및 전역 UX 모션 최적화</name>
  <files>apps/step1/styles.css</files>
  <read_first>
    - apps/step1/styles.css
  </read_first>
  <action>
    - SVG 네트워크 맵 노드(`.network-node`), 링크(`.network-link`), 화살표(`.network-arrow`) 테마 스타일 정의.
    - 자금 흐름을 시뮬레이션하는 `@keyframes flow-pulse` 정의 및 애니메이션 적용.
    - 수동 이체 설정 폼 UI 스타일링.
    - Step 간 뷰포트 전환 애니메이션을 위한 `.step-container--transition`, `.fade-slide-enter`, `.fade-slide-exit` 스타일 구현.
    - Step 1의 전체 카드에 마우스 호버 시 부유하는 `box-shadow` 및 `transform: translateY(-2px);` 트랜지션 전역 최적화.
    - **물리적 무결성 수호:** 파일 절삭(Truncate) 없이 최하단 미디어 쿼리와 기존 레이아웃을 엄밀히 보존하며 수술하듯 추가합니다.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - PC 및 모바일 반응형 규칙이 정상 작동하며 차트가 화면 내에서 잘리지 않아야 합니다.
    - 펄스 흐름 및 부유 호버 애니메이션이 에디토리얼 브랜드 가이드라인에 부합하게 정갈해야 합니다.
    - 빌드 프로세스가 에러 없이 종료되어야 합니다.
  </acceptance_criteria>
  <done>스타일 고도화 및 반응형 무결성 확보가 완료됩니다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client-Side Rendering | 사용자 로컬 브라우저 상태 관리 및 SVG 렌더링 무결성 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Transfer Rules | mitigate | 이체 규칙 추가 시 출발 계좌와 도착 계좌가 동일하지 않은지, 빈 값이나 문자열 주입이 없는지 `IsfUtils` 및 입력 세니타이저 검증 |
| T-04-02 | Denial of Service | Network Rendering | mitigate | 무한 스크롤 및 SVG 노드 무한 루프 겹침을 방지하기 위해 최대 계좌 갯수 및 노드 배치 경계 좌표 고정 |
</threat_model>

<verification>
`npm run build` 명령을 실행하여 번들 및 컴파일 빌드가 통과하는지 확인하고, git diff 도구 및 라인수 검증을 거쳐 스타일과 반응형 코드의 물리적 무결성을 최종 체크합니다.
</verification>

<success_criteria>
1. 생키 차트 바로 아래에 SVG 기반 '계좌 간 자금 흐름 지도(Network Map)'가 올바르게 렌더링되고, 데이터에 따른 노드 및 화살표 링크가 자동 생성됨.
2. SVG 이체선 상에 방향성이 드러나는 펄스 흐름 애니메이션이 아름답게 연동됨.
3. 특정 계좌 호버 시 연관 이체 경로만 진해지고 그 외 요소는 0.15 투명도로 페이드 처리되는 인터랙티브 포커스 필터 기능이 정상 작동함.
4. 사용자 설정 폼에 '계좌 간 이체 설정' 섹션이 생기고, 출발 계좌/도착 계좌/금액을 지정하여 규칙을 추가 및 삭제 가능하며, 해당 규칙이 스토리지(Active State/History)에 영속화됨.
5. 화면 전환 시 하드 컷 없이 부드러운 페이드-슬라이드 트랜지션 애니메이션이 작동함.
6. Step 1의 전체 카드 및 목록 컴포넌트에 마우스 호버 시 2px 부유감 및 테두리/그림자 변화가 일관되게 나타남.
7. 저축/투자 입력 수정 시 세금 경고(이자/배당 금융소득종합과세 임계 한도 초과)가 실시간으로 안전선 표시 링/뱃지에 지연 없이 반영됨.
</success_criteria>

<output>
작업 완료 시 `.planning/phases/04-account-transfer-ui-ux-polish/04-01-SUMMARY.md` 생성
</output>
