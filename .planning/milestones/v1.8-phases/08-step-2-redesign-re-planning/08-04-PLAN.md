---
phase: 08-step-2-redesign-re-planning
plan: 04
type: execute
wave: 3
depends_on: [08-02]
files_modified:
  - scripts/generate_qqq_data.py
  - scripts/generate_market_data.py
  - public/data/indices/README.md
  - qqq_raw.csv
  - qqq_daily_raw.csv
  - qqq_daily_stooq.csv
autonomous: true
requirements: [UI-03]
must_haves:
  truths:
    - "D-19: public/data/indices/*.json is documented as the evidence base for conservative learning ranges"
    - "D-21: runtime code does not depend on loose root QQQ CSV files"
    - "D-21: qqq_raw.csv, qqq_daily_raw.csv, and qqq_daily_stooq.csv are removed from the repository root or intentionally archived outside runtime paths only if an actual dependency is found"
  artifacts:
    - path: "public/data/indices/README.md"
      provides: "runtime market-data source note and conservative assumption boundary"
    - path: "scripts/generate_qqq_data.py"
      provides: "documented QQQ data generation output target"
    - path: "scripts/generate_market_data.py"
      provides: "documented market data generation output target"
  key_links:
    - from: "scripts/generate_qqq_data.py"
      to: "public/data/indices/qqq.json"
      via: "documented generation output"
      pattern: "public/data/indices"
    - from: "scripts/generate_market_data.py"
      to: "public/data/indices"
      via: "documented generation output"
      pattern: "public/data/indices"
---

<objective>
Step 2 전략 모델이 사용하는 정적 데이터 출처를 문서화하고 root-level QQQ CSV backdata clutter를 정리합니다.

Purpose: D-19와 D-21을 08-02의 계산 모델 범위에서 분리해, 전략 가정/계산 작업과 데이터 위생 작업의 파일 소유권을 명확히 나눕니다.
Output: `public/data/indices/README.md`, 데이터 생성 스크립트 출력 위치 설명, root CSV 정리.
</objective>

<execution_context>
@C:/Users/LENOVO/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/LENOVO/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-step-2-redesign-re-planning/08-CONTEXT.md
@.planning/phases/08-step-2-redesign-re-planning/08-RESEARCH.md
@.planning/phases/08-step-2-redesign-re-planning/08-02-SUMMARY.md
@apps/simulation/modules/assumptions.js
@public/data/indices/qqq.json
@public/data/indices/spy.json
@public/data/indices/schd.json
@scripts/generate_qqq_data.py
@scripts/generate_market_data.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Data source documentation and generator notes</name>
  <files>scripts/generate_qqq_data.py, scripts/generate_market_data.py, public/data/indices/README.md</files>
  <action>Implement D-19 and D-21 documentation. Add `public/data/indices/README.md` explaining that `qqq.json`, `spy.json`, and `schd.json` are the runtime evidence files for conservative learning ranges, while JEPI/QQQI/DIVO use the resolved editable product assumptions and display ranges from `apps/simulation/modules/assumptions.js` per D-18 and D-20. Add concise comments or docstrings to `generate_qqq_data.py` and `generate_market_data.py` stating that generated runtime data belongs under `public/data/indices/`. Do not add live market fetching to Step 2 runtime code.</action>
  <verify>
    <automated>python -m py_compile scripts/generate_qqq_data.py scripts/generate_market_data.py</automated>
    <automated>node -e "const fs=require('fs'); const readme=fs.readFileSync('public/data/indices/README.md','utf8'); for (const token of ['qqq.json','spy.json','schd.json','assumptions.js']) { if (!readme.includes(token)) { console.error(token); process.exit(1); } }"</automated>
  </verify>
  <done>`public/data/indices/README.md` identifies the runtime JSON evidence files and the conservative assumption boundary; both generation scripts name `public/data/indices/` as their output target.</done>
</task>

<task type="auto">
  <name>Task 2: Root QQQ CSV cleanup</name>
  <files>qqq_raw.csv, qqq_daily_raw.csv, qqq_daily_stooq.csv</files>
  <action>Implement D-21 cleanup. First verify no runtime dependency on `qqq_raw.csv`, `qqq_daily_raw.csv`, or `qqq_daily_stooq.csv` exists under `apps`, `src`, `shared`, or `tests`. Delete the three root CSV clutter files from the repository root. If execution discovers an actual dependency outside those runtime paths, relocate only the required file under a clearly documented non-runtime archive path such as `scripts/fixtures/qqq-backdata/` and state the reason in `08-04-SUMMARY.md`; otherwise the root files should be absent.</action>
  <verify>
    <automated>node -e "const fs=require('fs'),path=require('path'); const roots=['apps','src','shared','tests']; const hits=[]; const re=/qqq_raw|qqq_daily_raw|qqq_daily_stooq/; function walk(dir){ if(!fs.existsSync(dir)) return; for(const entry of fs.readdirSync(dir,{withFileTypes:true})){ const p=path.join(dir,entry.name); if(entry.isDirectory()) walk(p); else if(re.test(fs.readFileSync(p,'utf8'))) hits.push(p); } } roots.forEach(walk); if(hits.length){ console.error(hits.join('\n')); process.exit(1); }"</automated>
    <automated>node -e "const fs=require('fs'); const bad=['qqq_raw.csv','qqq_daily_raw.csv','qqq_daily_stooq.csv'].filter((f)=>fs.existsSync(f)); if (bad.length) { console.error(bad.join('\n')); process.exit(1); }"</automated>
  </verify>
  <done>The three root QQQ CSV files are absent from the repository root, and runtime paths do not reference them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static market JSON -> user-visible assumptions | Public data files inform conservative strategy range documentation |
| Repository root -> runtime assets | Loose files can be mistaken for runtime data sources |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10 | Repudiation | `public/data/indices/README.md` | mitigate | Document that JSON files are evidence examples and JEPI/QQQI/DIVO values are editable conservative assumptions, not guarantees |
| T-08-11 | Tampering | root CSV cleanup | mitigate | Remove loose root CSV files after runtime reference scan so execution cannot accidentally rely on stale data |
| T-08-SC | Tampering | npm installs | accept | No package install is planned for this phase |
</threat_model>

<verification>
Run the task-level commands, then confirm `git status --short` shows the three root CSV files as deleted and the README/script documentation changes as modified or added.
</verification>

<success_criteria>
- D-19 and D-21 are covered without increasing the file scope of 08-02.
- Runtime Step 2 code treats `public/data/indices/*.json` as the market-data evidence location.
- Root-level QQQ CSV clutter is removed from the repository root or explicitly archived outside runtime paths only if an actual dependency requires it.
</success_criteria>

<output>
Create `.planning/phases/08-step-2-redesign-re-planning/08-04-SUMMARY.md` when done.
</output>
