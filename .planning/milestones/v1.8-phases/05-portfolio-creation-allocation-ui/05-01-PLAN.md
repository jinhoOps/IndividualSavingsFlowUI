---
phase: 05-portfolio-creation-allocation-ui
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/step3/index.html
  - apps/step3/modules/state.js
  - apps/step3/modules/dom.js
  - apps/step3/modules/calculator.js
  - apps/step3/app.js
  - apps/step3/styles.css
files_created: []
autonomous: true
requirements:
  - PORT-01
  - PORT-02
must_haves:
  truths:
    - "나만의 포트폴리오 만들기 입력 폼이 제공되어 포트폴리오 이름 지정 및 종목 추가/삭제가 가능하다. (PORT-01)"
    - "입력한 종목은 최소 2개 이상 선택되어야 유효하며, 2개 미만일 시 경고 또는 비활성화 처리한다. (PORT-01)"
    - "가로형 프리미엄 세그먼트 컨트롤을 통해 매일/매주/매달 적립 주기를 선택할 수 있다. (PORT-02)"
    - "개별 종목의 매수 금액(원)을 입력할 때 상단에 실시간으로 총 N개의 주식 개수 및 총 매수 금액 합계가 자동 계산되어 표기된다. (PORT-02)"
    - "각 개별 종목 입력창 하단 및 총 매수 금액 영역 옆에 IsfUtils.convertToKoreanWon를 활용한 실시간 한글 금액 힌트가 상시 노출된다. (PORT-02)"
    - "총 매수 금액 대비 각 종목의 비중 %가 실시간 계산되어 반올림된 정수로 화면에 노출된다. (PORT-02)"
    - "개별 종목의 금액은 최소 1,000원 이상이고 1,000원 단위(예: 1,000원, 2,000원 등)만 허용되며, 모든 종목의 금액 입력값이 이 제약조건을 만족하고 종목 개수가 2개 이상일 때만 확인(제출) 버튼이 활성화된다. (PORT-02)"
    - "생성 완료된 포트폴리오는 메인 영역에 에디토리얼 포트폴리오 요약 카드로 렌더링되며, 마우스 호버 시 Sunset Orange 테두리 및 Y축 -2px 부유 효과가 적용된다. (PORT-01)"
    - "포트폴리오 요약 카드 클릭 시 구성 종목의 상세 금액/비중 및 누적 투자액 추이 팝업/모달이 연동되어 상세 내역을 볼 수 있다. (PORT-01)"
  artifacts:
    - path: "apps/step3/index.html"
      provides: "나만의 포트폴리오 만들기 영역 및 에디토리얼 요약 카드 리스트 컨테이너, 주기 선택 가로 세그먼트 마크업 구조 추가"
    - path: "apps/step3/modules/state.js"
      provides: "적립식 포트폴리오 생성/편집/조회 상태 관리 로직 리팩토링"
    - path: "apps/step3/modules/dom.js"
      provides: "포트폴리오 에디터 폼 렌더링, 주기 세그먼트 바인딩, 금액 자동 합산 및 비중 % 실시간 렌더러, 한글 금액 힌트 트리거, 포트폴리오 목록 카드 렌더링 및 디테일 팝업 마크업 탑재"
    - path: "apps/step3/modules/calculator.js"
      provides: "실시간 비중 % 계산 및 개별 금액 1,000원 단위/최소 1,000원 이상 유효성 검증 함수 구현"
    - path: "apps/step3/app.js"
      provides: "UI 이벤트 핸들러(주기 선택, 금액 입력, 확인 버튼 활성화 제어) 및 상태/UI 렌더링 파이프라인 연동"
    - path: "apps/step3/styles.css"
      provides: "가로 세그먼트 컨트롤, 에디토리얼 포트폴리오 요약 카드, 호버 테두리 Sunset Orange 광채 및 부유 효과 CSS 추가"
---

<objective>
나만의 적립식 포트폴리오 만들기 기능의 핵심 UI/UX와 실시간 비중 계산, 개별 종목 금액 1,000원 단위 및 최소 금액 검증, 그리고 한글 금액 힌트가 즉시 반영되는 입력 폼을 개발합니다. 또한 생성된 포트폴리오를 에디토리얼 요약 카드로 렌더링하여 호버 효과 및 클릭 시 세부 정보를 팝업으로 연동합니다.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-portfolio-creation-allocation-ui/05-CONTEXT.md
@apps/step3/index.html
@apps/step3/modules/state.js
@apps/step3/modules/dom.js
@apps/step3/modules/calculator.js
@apps/step3/app.js
@apps/step3/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: index.html 마크업을 적립식 포트폴리오 매니저 컨셉에 맞게 개편</name>
  <files>apps/step3/index.html</files>
  <read_first>
    - apps/step3/index.html
  </read_first>
  <action>
    - `index.html`에서 기존 리밸런싱 관련 섹션을 제거하고, 새로운 적립식 포트폴리오 관리 마크업을 구축합니다.
    - 상단 영역에 생성된 포트폴리오 요약 카드가 출력될 리스트 컨테이너 `#portfolioList`를 배치합니다.
    - "나만의 적립식 포트폴리오 만들기" 에디터 영역 `#portfolioCreator`를 추가합니다.
    - 에디터 내부에 포트폴리오 이름 지정 폼(`포트폴리오명` 입력 필드 `#portfolioName`), 주기 선택 가로형 프리미엄 세그먼트 컨트롤 `#periodSegment`, 구성 종목 추가 및 개별 종목명/금액 입력 테이블 `#creatorAssetTable`을 신설합니다.
    - 실시간 합산 정보 `"총 N개의 주식 | 총 매수 금액: X원"`를 표기할 `#creatorSummary` 영역과 최종 포트폴리오 추가 확인 버튼 `#savePortfolioBtn`을 마크업에 지정합니다.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - index.html 내에 포트폴리오 카드 목록 컨테이너 및 신규 포트폴리오 작성 폼, 가로 세그먼트 마크업이 지정되어야 합니다.
  </acceptance_criteria>
  <done>index.html 마크업 개편이 완료됩니다.</done>
</task>

<task type="auto">
  <name>Task 2: state.js 상태 클래스를 적립식 포트폴리오 데이터 모델 중심으로 리팩토링</name>
  <files>apps/step3/modules/state.js</files>
  <read_first>
    - apps/step3/modules/state.js
  </read_first>
  <action>
    - `IsfState` 클래스의 내부 상태 데이터(`this.data`)에 `portfolios: []` 배열 구조를 신설합니다.
    - 단일 포트폴리오 객체 스펙: `{ id, name, period (매일/매주/매달), totalAmount, assets: [{ id, name, ticker, amount, ratio }] }`
    - 로컬 스토리지 키 `isf-step3-portfolios-v2`를 새로 지정하여 저장하며, 로드 시 기본 빈 배열로 방어 코드 처리합니다.
    - 상태 변경 메서드로 `addPortfolio(portfolio)`, `removePortfolio(id)`, `updateActiveCreator(field, value)`, `saveToStorage()`를 구현합니다.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - state.js가 포트폴리오 배열 데이터 관리 및 영속화 처리를 정상 지원해야 합니다.
  </acceptance_criteria>
  <done>state.js 포트폴리오 데이터 모델 상태 클래스 구현이 완료됩니다.</done>
</task>

<task type="auto">
  <name>Task 3: calculator.js에 실시간 비중 및 금액 합산 / 입력값 유효성 검증 구현</name>
  <files>apps/step3/modules/calculator.js</files>
  <read_first>
    - apps/step3/modules/calculator.js
  </read_first>
  <action>
    - `IsfCalculator`를 적립식 연산 엔진으로 리팩토링합니다.
    - 입력된 개별 종목 금액(원)을 합산하여 `totalAmount`를 반환하는 `sumAmounts(assets)` 메서드를 작성합니다.
    - `totalAmount` 대비 각 종목의 비중 %를 실시간 반올림 정수로 산출하는 `calculateRatios(assets, totalAmount)` 메서드를 구현합니다.
    - 개별 종목 금액이 최소 1,000원 이상이고 1,000원 단위인지 여부를 판별하는 `validateAssetAmount(amount)` 유효성 검증 필터를 구현합니다.
    - 종목 개수가 2개 이상이고, 모든 종목의 금액이 검증 규칙을 완벽하게 만족하는지 검사하는 `validatePortfolio(portfolio)`를 구현합니다.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - 총 매수 금액 자동 합산, 실시간 % 비중 도출 및 1,000원 단위/최소 1,000원 유효성 체크가 정확히 수행되어야 합니다.
  </acceptance_criteria>
  <done>calculator.js 연산 및 검증 로직 구현이 완료됩니다.</done>
</task>

<task type="auto">
  <name>Task 4: dom.js에 포트폴리오 작성 UI 제어기 및 에디토리얼 카드 목록 렌더러 추가</name>
  <files>apps/step3/modules/dom.js</files>
  <read_first>
    - apps/step3/modules/dom.js
  </read_first>
  <action>
    - `IsfDom` 객체의 `nodes` 선택자를 신규 마크업에 맞게 갱신합니다.
    - `renderPortfolioList(portfolios, handlers)`를 작성하여 에디토리얼 요약 카드들을 리스팅합니다. 카드에 이름, 주기, 총액, 종목 수 정보를 바인딩합니다.
    - `renderCreatorForm(activeCreator, handlers)`을 작성하여 포트폴리오 작성기 폼을 동적으로 렌더링하고, 종목 추가/삭제 행을 렌더링합니다.
    - 실시간 비중 % 수치 및 `IsfUtils.convertToKoreanWon` 한글 금액 힌트가 개별 종목 인풋 하단과 요약 텍스트에 나타나도록 UI를 구성합니다.
    - 검증 조건을 만족하지 못하면 추가 확인 버튼 `#savePortfolioBtn`이 `disabled` 처리되고 조건을 충족하면 해제되도록 제어합니다.
    - 포트폴리오 요약 카드를 클릭할 시 나타나는 Glassmorphism 팝업/모달의 세부 내역 렌더링 함수 `showPortfolioDetailModal(portfolio)`를 탑재합니다.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - 포트폴리오 폼 입력 시 비중 및 한글 금액 힌트가 정상적으로 실시간 업데이트되어야 합니다.
    - 카드의 Hover 및 Click 동작에 맞추어 DOM 핸들러가 바인딩되어야 합니다.
  </acceptance_criteria>
  <done>dom.js UI 제어 및 카드 목록 렌더러 구현이 완료됩니다.</done>
</task>

<task type="auto">
  <name>Task 5: app.js와 styles.css 통합 및 시각 효과 고도화</name>
  <files>
    - apps/step3/app.js
    - apps/step3/styles.css
  </files>
  <read_first>
    - apps/step3/app.js
    - apps/step3/styles.css
  </read_first>
  <action>
    - `app.js`에서 앱 실행 시 상태 로드, 초기 포트폴리오 리스트 렌더링, 이벤트 리스너 바인딩을 주입합니다.
    - 주기 세그먼트 가로 컨트롤 클릭 핸들러, 개별 종목 추가/삭제 버튼 클릭 핸들러, 실시간 키 입력 리스너(금액 입력 시 합산/비중 계산/버튼 활성화 트리거)를 구성합니다.
    - 포트폴리오 카드 클릭 시 팝업 렌더링 연동 핸들러를 구성합니다.
    - `styles.css`에 가로 프리미엄 세그먼트 버튼 스타일을 설계합니다.
    - 에디토리얼 요약 카드가 modern dark/glassmorphic 테마를 띄도록 설계하고, 마우스 호버 시 Sunset Orange 테두리 광채 효과 및 Y축 -2px 부유 애니메이션(`.portfolio-card:hover`) 효과를 CSS로 정의합니다.
    - 팝업/모달에 Glassmorphism 블러 효과(`backdrop-filter: blur(10px)`)를 완벽히 구현합니다.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - 포트폴리오 추가 폼의 모든 인터랙션이 에러 없이 동작해야 합니다.
    - Hover Sunset Orange 테두리 광채 효과 및 카드 클릭 팝업 시각 효과가 가이드라인에 맞게 연출되어야 합니다.
  </acceptance_criteria>
  <done>app.js 컨트롤러 통합 및 styles.css 그래픽 고도화가 완료됩니다.</done>
</task>

</tasks>
