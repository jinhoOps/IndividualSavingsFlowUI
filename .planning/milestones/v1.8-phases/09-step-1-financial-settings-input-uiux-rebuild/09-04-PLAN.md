---
phase: 09-step-1-financial-settings-input-uiux-rebuild
plan: 04
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-02
  - 09-03
files_modified:
  - apps/main/index.html
  - apps/main/styles.css
  - apps/main/modules/dom.js
  - apps/main/modules/event-bindings.js
  - apps/main/modules/visualization-controller.js
  - apps/main/modules/sankey-renderer.js
  - tests/step1.spec.ts
autonomous: true
requirements:
  - TBD
user_setup: []
must_haves:
  truths:
    - "D-25: Users can manually refresh missing account connections to auto-correct and re-sort the Sankey flow."
    - "D-26: Basic Sankey mode shows `총수입 -> 계좌 -> 지출/저축/투자 총합`, while detail mode expands groups/items."
    - "D-27: Merged node/link hover information is readable as a line-broken list."
    - "D-28: High-value Korean money labels show only one lower unit below the largest unit."
    - "D-01 through D-28 are covered by Playwright checks for data, UI, Sankey, modal, preset, money display, and responsive behavior."
  artifacts:
    - path: "apps/main/modules/sankey-renderer.js"
      provides: "Readable Sankey tooltip/detail behavior and mobile-safe rendering"
      contains: "formatAllocationBreakdownText"
    - path: "apps/main/modules/visualization-controller.js"
      provides: "Manual account correction refresh and Sankey rerender integration"
      contains: "auto-correct"
    - path: "tests/step1.spec.ts"
      provides: "Phase 09 end-to-end and responsive regression coverage"
      contains: "Phase 09"
  key_links:
    - from: "apps/main/modules/visualization-controller.js"
      to: "apps/main/modules/account-correction.js"
      via: "manual correction refresh"
      pattern: "repairAccountConnections"
    - from: "tests/step1.spec.ts"
      to: "apps/main/index.html"
      via: "Playwright user flow"
      pattern: "Phase 09"
---

<objective>
Polish the Sankey user experience and lock Phase 09 with automated responsive/user-flow coverage.

Purpose: After the data and UI rebuilds, D-25 through D-27 and mobile stability need explicit controls and tests so `총수입`, correction refresh, readable hover metadata, and the new modal/preset flows do not regress.
Output: Manual Sankey correction refresh UI, readable line-broken tooltip/detail rendering, responsive CSS refinements, and Playwright coverage for Phase 09 decisions.
</objective>

<execution_context>
@C:/Users/LENOVO/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/LENOVO/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-step-1-financial-settings-input-uiux-rebuild/09-CONTEXT.md
@.planning/phases/09-step-1-financial-settings-input-uiux-rebuild/RESEARCH.md
@.planning/phases/07-step-1-ui-ux-refactoring-modularization/07-CONTEXT.md
@.planning/phases/08-step-2-redesign-re-planning/08-CONTEXT.md
@DESIGN.md
@shared/styles/step-theme.css
@apps/main/index.html
@apps/main/styles.css
@apps/main/modules/dom.js
@apps/main/modules/visualization-controller.js
@apps/main/modules/sankey-renderer.js
@tests/step1.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Sankey correction refresh and stable basic/detail behavior</name>
  <files>apps/main/index.html, apps/main/styles.css, apps/main/modules/dom.js, apps/main/modules/event-bindings.js, apps/main/modules/visualization-controller.js, apps/main/modules/sankey-renderer.js</files>
  <behavior>
    - Basic mode starts from `총수입` and shows account to category totals per D-26.
    - Detail mode expands groups and individual items through existing grouping controls per D-26.
    - When missing account connections are detected, users can trigger a manual refresh that runs the same correction path and rerenders/re-sorts per D-25.
    - Correction feedback appears as a warning badge or correction note per D-22/D-25.
  </behavior>
  <action>Add a small Sankey correction-refresh button/status near the Sankey controls, wired through `dom.js`, `event-bindings.js`, and `visualization-controller.js`. The handler must call the centralized account repair/sanitizer path from Plan 09-01, persist or mark pending through existing persistence commands, then rerender Sankey and lists. Update renderer behavior so basic mode hides individual income nodes visually when appropriate and emphasizes `총수입 -> 계좌 -> 지출/저축/투자 총합`; detail mode keeps the expanded group/detail controls. Preserve the network-map tab and PNG export behavior.</action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>Users have a visible way to auto-correct missing account connections, and Sankey basic/detail modes match the canonical Phase 09 structure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make merged Sankey hover details readable</name>
  <files>apps/main/styles.css, apps/main/modules/sankey-renderer.js</files>
  <behavior>
    - Merged link tooltip details render as line-broken list text, not comma-crammed metadata, per D-27.
    - Collapsed node detail panels use rows or line breaks for children and remain readable at 390px.
    - Tooltip content remains text-safe and does not inject user-controlled HTML.
  </behavior>
  <action>Refactor `formatAllocationBreakdownText()` and collapsed-node detail rendering so merged items are displayed as newline-separated or row-based lists with one item per line. If `showSankeyTooltip()` remains textContent-based, keep newline formatting and CSS `white-space: pre-line`; if rendering structured rows, build with DOM APIs. Add CSS constraints for tooltip max-width, wrapping, and mobile placement so long Korean labels and Won values stay readable without overlapping adjacent controls.</action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>Sankey merged metadata is readable on hover/tap, preserves escaping, and remains within the Sankey viewport at desktop, 768px, and 390px widths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Phase 09 Playwright coverage and run final gates</name>
  <files>tests/step1.spec.ts</files>
  <behavior>
    - Summary groups/cards, Sankey placement, preset confirmation, modal creation, account correction, basic/detail Sankey, tooltip readability, and mobile layout are covered.
    - Money display precision is covered for `억` and `조` values so labels do not include units more than one step below the largest unit.
    - Tests run against 1280, 768, and 390 viewport widths.
    - Existing Phase 07 render-safety and mobile tests remain intact.
  </behavior>
  <action>Add Phase 09 Playwright tests to `tests/step1.spec.ts` with descriptive `Phase 09 ...` names. Cover: default summary groups `수입+계좌` and `지출+저축+투자`; five category cards with totals/counts/representative rows; Sankey panel below summary; preset flow selecting `안정/균형/성장/야수/사용자 지정`, blur normalization, confirmation with original percentages and rounded amounts; Korean money labels where `억` values show down to `만` only and `조` values show down to `억` only; category card modal open/edit/create with account preselection and inline account alias creation; malformed account payload correction plus manual Sankey refresh; basic mode labels containing `총수입`, category totals, and no individual detail labels; detail mode expanding grouped/detail labels; merged tooltip text containing line breaks; and no horizontal overflow at 1280/768/390. Run screenshots only where existing test style already writes to `test-results`.</action>
  <verify>
    <automated>npm run test:e2e -- tests/step1.spec.ts -g "Phase 09"</automated>
    <automated>npm run check</automated>
    <automated>npm run build</automated>
  </verify>
  <done>Phase 09 behavior is covered by automated Playwright checks and the project passes check/build gates after the UI/Sankey rebuild.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sankey correction refresh -> persisted state | User-triggered repair can change account ids/allocations. |
| Sankey metadata -> tooltip/detail UI | User-controlled item names/groups/account aliases appear in hover/tap surfaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-10 | Tampering | correction refresh | mitigate | Reuse sanitizer/account repair and surface correction notes before/after rerender. |
| T-09-11 | Information Disclosure | tooltip/detail panels | accept | Local financial labels/amounts are displayed in the same local Step 1 page; no remote boundary exists. |
| T-09-12 | XSS/Tampering | `sankey-renderer.js` | mitigate | Keep tooltip/detail content in `textContent` or DOM-created rows; never inject unescaped item metadata. |
| T-09-SC | Tampering | npm installs | mitigate | No package-manager installs are part of this plan; keep external dependency set unchanged. |
</threat_model>

<verification>
Run final gates: `npm run test:e2e -- tests/step1.spec.ts -g "Phase 09"`, `npm run check`, and `npm run build`. If Phase 09 tests pass but older Step 1 tests fail, fix the regression before summary.
</verification>

<success_criteria>
The rebuilt Step 1 experience is covered end to end: summary-first default screen, preset quick setup, compact Korean money display, guided modal creation, deterministic account correction, canonical `총수입` Sankey, readable merged metadata, and mobile-safe layout.
</success_criteria>

<source_audit>
GOAL covered by plans 09-01 through 09-04. CONTEXT coverage: D-01/D-02/D-03/D-04 in 09-03; D-05/D-06/D-07/D-08/D-09/D-10/D-11/D-12/D-13/D-28 in 09-02; D-14/D-15/D-16/D-17/D-18/D-20 in 09-03; D-18/D-19/D-21/D-22/D-23/D-24/D-25 in 09-01; D-25/D-26/D-27/D-28 and full regression coverage in 09-04. RESEARCH coverage: no new packages, account correction boundary, preset confirmation, summary/modal UI, Sankey topology, tooltip readability, mobile verification. Deferred ideas remain excluded: detailed bank-account model, live banking import, broad React rewrite, standalone account-management product surface. REQ: ROADMAP Phase 09 lists Requirements as TBD, so frontmatter uses `TBD`.
</source_audit>

<output>
Create `.planning/phases/09-step-1-financial-settings-input-uiux-rebuild/09-04-SUMMARY.md` when done.
</output>
