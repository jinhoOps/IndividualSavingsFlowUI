# UPDATE

## 운영 원칙
- 개발 시작 전에 항상 `UPDATE.md`를 먼저 확인한다.
- 모든 변경 사항/개발 사항은 작업 종료 시 `UPDATE.md`에 요약 기록한다.
- 상세 로그가 길어지면 기능 단위로 압축하되, 날짜 소제목은 유지해 구분한다.

## 투두
- 상세 TODO는 `TODO.md`를 사용한다.

## 압축 이력 (기존 내역)
### 2025-12-29 ~ 2026-01-26
- Vanilla HTML/CSS/JS 기반 개인 자산흐름 대시보드 MVP를 구축하고, 노드/링크 CRUD, 유효성 검증, 우선순위 정렬, 드로어 UI를 포함한 편집 워크플로를 완성.
- 흐름 시각화는 리스트/바형/Sankey 스타일로 확장했고, 필터/툴팁/레이아웃 보정/겹침 완화 작업을 반복 적용.
- 시뮬레이션 엔진은 월 단위 계산(수입 성장, 저축/투자 반영, 인플레이션/명목·실질 토글, 보유자산 가정)을 단계적으로 고도화.
- 입력/표현 체계를 만원 단위 중심으로 통일하고, 핵심 카드/요약 뷰/초기화 동작 등 UX를 지속 정리.

## 리디자인 이후 압축 (프로토타입 초안)
### 2026-02-25 ~ 2026-02-27
- 상태 정의:
  - 현재 단계를 `개인 자산 흐름 (IndividualSavings Flow UIUX)` 프로젝트의 1단계 `나의 가계 흐름` 프로토타입 초기 완성으로 확정.
  - 핵심 메시지는 `이렇게 벌고, 이렇게 쓴다`로 통일.
- 제품 구조 재편:
  - 화면 구조를 `핵심 카드 + 단일 Sankey + 계산 검증 표 + 하단 입력 섹터` 중심으로 단순화.
  - 기본 실행 경로를 `index.html` 단독 실행 및 GitHub Pages 직접 접속 방식으로 정리.
- 입력 UX 고도화:
  - 수입은 항목 추가/삭제, 생활비·저축·투자는 상세 항목 자동합산 구조로 정리.
  - 상세 항목은 `항목 편집` 모드에서만 이름/추가/삭제 가능하게 분리.
  - 변경 감지 기반 `적용/취소(pendingBar)` 흐름을 안정화하고, 불필요한 상시 노출 이슈를 해결.
- 계산/신뢰도 보강:
  - 부채 계산 순서를 `이자 반영 → 실제 상환(min 규칙) → 부족분 신규차입`으로 정리.
  - 카드/검증표에 `당월 이자`, `실제 상환액`, `부채 증가분`을 노출하고, 실질 순자산(현재가치) 계산을 추가.
  - 부채가 없는 경우 0값 부채 관련 핵심 카드(이자/상환/증가분/소진예상)는 자동 숨김 처리.
- Sankey 완성도 개선:
  - 생활비·저축·투자 모두 `종합 노드 → 하위항목` 2단 분기 구조로 통일.
  - 라벨 배치(마지막 컬럼 우측/중간 좌측), 색상 투명톤, 툴팁/빈 상태 표시 예외를 보정.
- 상태 저장/공유 체계 확장:
  - 저장 우선순위를 `URL 해시(#) -> localStorage -> 기본값`으로 조정해 링크 상태 복원을 메인 경로로 적용.
  - 입력값 액션에 `링크 복사`, `JSON 저장`, `JSON 불러오기`를 추가해 공유와 2차 백업을 지원.
  - 해시 길이 초과 안내, JSON envelope(app/schema/version/timestamp) 검증, hashchange 재동기화 처리를 반영.
- 모바일 조회 우선 보정:
  - 전면 반응형 개편 대신 모바일(`max-width: 760px`)에서 패널 패딩/생키 최소높이/라벨 크기/검증표 셀 폰트를 줄이는 1차 가독성 보정을 적용.
- TODO 체계 정리:
  - `TODO.md`를 P0/P1 우선순위 기반으로 재정리하고, `모바일 조회 우선` 방향을 명시.
  - 용어 단순화, 간편입력→고급설정 이동, 모바일 조회 개선 항목을 최우선 과제로 고정.

### 2026-02-27
- 용어 단순화 1차 적용:
  - `가계 추이 계산 검증`을 `가계 변화 계산표`로 변경.
  - 표 컬럼 `실질 순자산(현재가치)`를 `물가 반영 순자산`으로 변경.
  - 입력 라벨 일부를 쉬운 표현으로 정리(`시뮬레이션 기간`→`계산 기간`, `월 부채상환`→`월 부채 상환`, `생활비 증가율`→`물가상승률` 등).
  - 수식 안내 문구를 `최솟값`/`기준금리` 중심 표현에서 쉬운 설명 중심으로 수정.
- 간편입력 UX 개선:
  - `월 생활비/저축/투자` 읽기전용 필드 클릭 시 `고급 설정`을 자동으로 열고 해당 상세 항목 블록으로 이동하도록 구현.
  - 이동 후 해당 항목의 `항목 편집` 버튼으로 포커스를 이동해 다음 액션 진입을 단축.
  - 이동 가이드를 위해 간편입력 힌트와 읽기전용 필드 시각 스타일(점선 테두리/포인터 커서)을 추가.
- TODO 운영 반영:
  - `TODO.md`의 `현재 진행중`에 Codex 작업 방향(동선 개선 + 용어 단순화 우선)을 명시.

- 용어/표현 롤백:
  - 사용자 피드백에 따라 용어 단순화로 변경했던 문구를 기존 표현으로 원복.
  - 롤백 범위: 섹션 제목, 계산 안내 문구, 검증표 컬럼명, 입력 라벨, pendingBar 문구.
  - `간편입력 읽기전용 항목 클릭 시 고급 설정 이동` 기능은 유지.
- TODO 진행 방향 조정:
  - `TODO.md`의 `현재 진행중` 문구에서 `용어 단순화 우선` 표현을 제거하고 모바일 조회 보정 중심으로 조정.
- 모바일 레이아웃 보정(갤럭시 S20 울트라 기준):
  - `max-width: 760px`에서 입력 영역 그리드를 2열에서 1열로 변경해 가로 밀림을 줄이고 세로 스크롤 중심으로 정리.
  - 상단 액션 버튼을 모바일에서 1열 스택으로 배치해 좌우 잘림 없이 접근 가능하도록 조정.
  - 상세 항목 블록 헤더를 세로 정렬로 변경해 제목/버튼이 겹치지 않도록 보정.
- 생키 차트 좌우 잘림 보정:
  - 라벨 텍스트 폭을 측정해 좌/우 마진을 동적으로 계산하도록 렌더링 로직을 개선.
  - 모바일 뷰포트에서는 노드 폭/간격/높이를 별도로 조정해 차트가 폭에 맞춰 축소되더라도 라벨이 잘리지 않도록 처리.
  - 범례 칩 텍스트 줄바꿈을 허용해 긴 항목명이 모바일에서 가로 넘침을 만들지 않도록 수정.
- 모바일 핵심 카드 레이아웃 조정:
  - 사용자 피드백에 따라 모바일(`max-width: 760px`)에서 `핵심 카드`를 1열에서 2열로 변경.
  - 입력/상단 액션/상세 입력의 세로 스크롤 중심 레이아웃은 그대로 유지.

### 2026-03-01
- TODO 백로그 반영(대안 마련 필요 항목 제외):
  - `고급 설정 > 저축 상세 항목`에 항목별 `연 이자율(%)` 입력을 추가하고, 편집/적용 흐름에서 금액과 함께 저장되도록 확장.
  - 시뮬레이션 엔진의 저축 계산을 `단일 저축 수익률` 방식에서 `항목별 금리` 방식으로 변경(초기 저축/월 저축 분배 후 항목별 월 복리 반영).
  - 생키차트에 `금액 / %` 표기 전환 버튼을 추가하고 노드 값/범례/툴팁 표기를 모드에 맞게 동기화.
  - 모바일 CSS를 재구성해 버튼/입력 터치 영역, 생키 범례, 섹션 간격, 초소형 화면(520px 이하) 레이아웃을 보정.
- 진행 의견 기록:
  - 생키 `확대/축소`는 모바일 스크롤 충돌 가능성이 있어 `버튼형 단계 확대` vs `제스처 확대` 대안 비교 후 적용하기로 `TODO.md` 현재 진행중에 명시.
- 모바일 가독성/생키 확대 기능 후속 반영:
  - 모바일(`max-width: 760px`)에서 글자 가독성이 낮다는 피드백에 따라 `hint/label/button/input/표` 폰트 크기를 전반 상향.
  - 생키 영역에 `확대(+)/축소(-)/기본 크기` 컨트롤과 현재 배율(%) 표시를 추가.
  - 기본 상태는 화면 맞춤(100%)으로 유지하고, 확대 시 생키 래퍼를 스크롤해 상세 라벨을 크게 확인할 수 있도록 `overflow` 동작을 조정.
  - TODO의 생키 모바일 확대 관련 2개 항목을 완료 처리하고, 현재 진행중 메모를 최신 상태로 정리.

### 2026-03-02
- 초기화/샘플 정책 조정:
  - `입력값 초기화` 클릭 시 `수입 항목`과 `각종 퍼센트(증가율/수익률/이자율)`를 제외한 금액 입력값을 0으로 초기화하도록 변경.
  - 초기화 시 `시뮬레이션 기간`은 3년으로 고정.
  - 샘플 데이터의 `시뮬레이션 기간`을 5년으로 변경.
- 생키 수입 유입 표시 개선:
  - 수입 항목이 2개 이상일 때 생키에서 `수입 항목들 -> 월 배분총액 -> 배분 항목` 흐름이 보이도록 노드/링크 생성을 확장.
  - 범례는 컬럼 위치 의존 대신 상위 배분 타겟 ID 기반으로 렌더링하도록 보정.
- 모바일 UX 재보정:
  - 모바일에서 과도했던 글자/버튼/표 크기를 다시 조정해 `작지 않으면서도 답답하지 않게` 균형을 맞춤.
  - 입력값 액션 버튼은 모바일에서 다시 1열 스택으로 정리해 밀집/겹침을 완화.
  - 생키 확대 컨트롤은 3열 그리드(+/-/기본) + 배율 표시 줄로 재배치해 터치 접근성을 유지하면서 헤더 혼잡을 줄임.
  - 다중 수입 유입 생키(4컬럼)에서 첫 컬럼 라벨 배치/간격을 보정해 모바일 화면에서 레이아웃 왜곡을 완화.
- 모바일 좌우 잘림 긴급 복구:
  - `main`이 CSS Grid일 때 내부 패널 최소폭(`min-width:auto`) 때문에 전체 섹션 폭이 뷰포트보다 커지던 문제를 `main/.panel/.section-head`의 `min-width: 0` 보정으로 수정.
  - 헤더 힌트 텍스트가 폭을 밀어내지 않도록 `.hint`에 줄바꿈 허용(`overflow-wrap:anywhere`)을 적용.
- UI 배치/플로팅 액션 변경:
  - `핵심 카드`와 `입력값` 섹션 위치를 교체해 `입력값`이 상단, `핵심 카드`가 하단에 배치되도록 섹션 순서를 조정.
  - 기존 `입력값 섹터로 이동` 텍스트 버튼을 제거하고, 좌하단 반투명 `★` 버튼(핵심 카드 이동) + 우하단 반투명 `▲` 버튼(맨 위 이동) 2개로 변경.
  - 우하단 `▲` 버튼 동작을 위해 `jumpToTop` 스크롤 이벤트를 추가.
- 저축 상세항목 설명/기본수익률 UX 보강:
  - 저축 상세 항목 이자율 입력을 비워둘 수 있도록 수정하고, 빈값일 때는 `저축 기본 수익률`이 fallback으로 적용되도록 로직을 보정.
  - 저축 상세 항목의 연 이자율 입력 placeholder를 `저축기본수익률`로 변경.
  - `저축 기본 수익률`이 자유적립식/목돈 이자 적립 계산 기준이며, 세부 이자율 미입력 항목에 적용된다는 설명 문구를 고급설정에 추가.
- Sankey 범례 묶음 표시:
  - 기존 `상위/하위 항목 개별 칩` 나열 방식에서 `상위 그룹 1개 + 하위 항목 묶음 텍스트` 구조로 변경.
  - 예: `생활비` 그룹 아래에 `관리비 · 식비 · ...`를 한 줄로 묶어 표시해 범례 길이와 중복을 줄임.
- 모바일 입력값 추가 접기 기능:
  - 모바일 UI(`max-width: 760px`)에서만 `입력값 접기/펼치기` 버튼이 노출되도록 추가.
  - 접기 시 입력 폼 영역을 숨겨 차트/카드 확인 후 다시 펼칠 수 있게 보정(데스크톱에서는 항상 펼침 유지).
  - 사용자 피드백 반영으로 토글 UI를 `입력값` 제목 옆의 작은 텍스트(`접기/펼치기`)로 단순화.
- 샘플 데이터 현실화:
  - 샘플 수입을 `급여 310 + 부수입 20`으로 조정.
  - 생활비/저축/투자/부채상환 및 초기자산/증가율/수익률 값을 현실적인 수준으로 재설정해 샘플 버튼 즉시 사용성을 개선.
- 모바일 액션 버튼 그리드 조정:
  - 모바일에서 `class="actions"` 버튼 영역을 1열 스택에서 3열 그리드로 변경해 상단 영역 길이를 축소.
- 저축 기본 수익률 안내 방식 개선:
  - 고급설정 `annualSavingsYield` 입력 아래의 안내 `hint` 문구를 제거해 레이아웃 밀림을 완화.
  - 라벨 옆 `?` 아이콘 툴팁으로 설명을 이동해 필요한 경우에만 안내를 확인할 수 있도록 변경.
- 모바일 Sankey 비율/기본배율 보정:
  - 모바일 생키의 기본 표시 배율을 `약 70%` 기준으로 적용해 초기 과밀도를 낮춤.
  - 동시에 모바일 렌더 높이를 가로폭 연동 비율로 추가 확보해 `가로로 납작해 보이는` 현상을 완화.
  - 모바일에서 축소된 SVG가 좌측에 붙지 않도록 가운데 정렬되게 보정.
- 모바일 Sankey 가로 길이 추가 보정:
  - 사용자 피드백(`배율만 바뀌고 모양 변화가 약함`) 반영으로 모바일 렌더링 시 가로 목표폭을 확대해 열 간격이 더 벌어지도록 조정.
  - 모바일 컬럼 최소 간격을 상향해 Sankey 흐름이 세로로 뭉개지지 않고 `가로로 더 길게` 보이도록 보정.

### 2026-03-03
- 모바일 Sankey 가로 길이 재상향:
  - 사용자 요청에 따라 모바일 가로 확장 계수를 추가 상향(`1.24 -> 1.38`).
  - 모바일 컬럼 최소 간격을 재상향(`112 -> 126`, 다중수입 유입 `98 -> 110`)해 흐름이 더 넓게 펼쳐지도록 조정.
- 모바일 Sankey 기본 배율 상향:
  - 모바일 기본 배율을 `70% -> 85%`로 조정.
- 저축 이자율 입력 placeholder 동적화:
  - 저축 상세항목 `연 이자율` 입력칸의 placeholder를 고정 문자열 대신 `n%(저축기본수익률)` 형식으로 표시하도록 변경.
  - `저축 기본 수익률` 입력값 변경 시, 저축 상세항목 placeholder가 즉시 갱신되도록 동기화.
- 잘림 텍스트 hover 확인 보강:
  - 저축 상세항목 `연 이자율` 입력칸에 placeholder와 동일한 `title`을 부여해, 문구가 잘려 보일 때 마우스 오버 시 전체 텍스트를 확인 가능하게 보강.
- 고급설정 항목 편집 액션 노출 보정:
  - `편집` 진입 직후 `항목 추가/편집 취소` 버튼이 바로 보이도록 변경해 항목 개수가 고정처럼 보이던 UX를 수정.
  - `항목 변경 적용` 버튼은 편집 모드에서 항상 보이되, 실제 변경 전에는 비활성화(disabled) 상태로 표시되도록 조정.
- 입력 안내 예시 금액 조정:
  - 입력 패널 힌트의 예시 금액을 `450(4,500,000원)`에서 `350(3,500,000원)`으로 변경.
- README 간결화/최신화:
  - 문서를 배포 페이지 직접 사용 기준으로 재구성하고, 사용 흐름을 `접속 -> 입력 -> 적용 -> 확인` 중심으로 단순화.
  - 서두의 `개인 자산 흐름 (IndividualSavings Flow UIUX)` 프로젝트 기획 설명은 유지.
  - `문제 정의` 섹션은 유지하면서 현재 기능(생키 전환/확대, 항목별 이자율, 링크/JSON 공유)을 반영해 최신 상태로 정리.
- BUG 수정(입력/확대):
  - 고급설정 항목 편집 중 이름 입력 시 즉시 기본 텍스트로 되채워지던 동작을 제거하고, 빈값 검증은 `항목 변경 적용` 시점에 수행되도록 조정.
  - 웹(데스크톱)에서 생키 확대/축소 버튼이 실효 폭에 반영되지 않던 문제를 수정해 배율 조정이 실제 차트 크기에 적용되도록 보정.
- BUG 수정(항목명 미적용 감지):
  - `미적용 변경사항` 비교 시 생활비/저축/투자 항목의 `항목명` 변경을 시그니처에 포함하도록 보정.
  - 수치가 바뀌지 않아도 항목명만 변경한 경우 pending 상태가 유지되며, 적용 시 변경한 항목명이 정상 반영되도록 수정.
- 공유 링크 보기 모드 추가:
  - 입력값 액션에 `보기 링크 복사` 버튼을 추가해 `?view=1` 파라미터가 포함된 링크를 생성할 수 있도록 확장.
  - 보기 모드 링크로 열면 해시 상태는 반영하되 `localStorage`는 갱신하지 않도록 저장 로직을 분리해, 로컬 작업값이 덮어써지지 않게 보정.
- 공유 링크 UX 통합:
  - `링크 복사` 버튼이 기본적으로 `보기 모드(?view=1)` 링크를 생성하도록 통합.
  - 보기 모드에서 좌측 `★` 옆 `💾` 버튼을 노출하고, 클릭 시 현재 보기 상태를 `localStorage`에 덮어써 저장하도록 추가.
- 보기 링크 저장 버튼 표시/아이콘 정리:
  - 저장 버튼 노출 조건을 `보기 모드 + 공유 해시 상태(#s)`인 경우로 제한해, 일반 접속에서는 표시되지 않도록 보정.
  - 저장 버튼 표시 문자를 이모지(`💾`)에서 일반 심볼(`☆`)로 변경해 플로팅 버튼 톤을 통일.
- 보기 링크 저장 동작 최종 보정:
  - 저장 버튼 아이콘을 단색 흰색 `Save(디스켓)` SVG로 교체해 다른 플로팅 버튼과 톤을 통일.
  - 저장 버튼 클릭 시 `localStorage` 덮어쓰기 후 URL의 `?view=1`을 제거해 즉시 일반 링크 모드로 전환되도록 변경.
- 보기 링크 저장 버튼 표시 안정화:
  - 저장 버튼 표시 여부를 상태값이 아닌 현재 URL 기준(`?view=1` + `#s`)으로 다시 판별하도록 조정.
  - `#saveViewToLocal[hidden] { display:none !important; }`를 추가해 일반 모드에서 버튼이 잔존 표시되지 않도록 보강.
- 헤더 타이틀 홈 링크 추가:
  - 상단 `나의 가계 흐름` 타이틀을 클릭하면 `href="/"`로 이동하도록 링크를 적용.
  - 링크 기본 스타일은 제목 톤 유지(무밑줄)하고, hover/focus 시에만 밑줄을 표시하도록 보정.
- 헤더 타이틀 홈 링크 롤백:
  - 로컬 `index.html` 직접 실행 환경에서 `/` 경로 해석 이슈가 있어, 타이틀 링크를 제거하고 일반 텍스트 제목으로 원복.
- View 모드 첫 방문 안내 추가:
  - View 공유 링크(`?view=1` + `#s`)로 진입한 경우, 상단에 `공유 페이지` 안내 패널을 노출하도록 추가.
  - 안내 문구에 `좌측 하단 저장 아이콘 클릭 시 로컬 브라우저 저장 가능` 동선을 명시.
  - 기본 동작은 View 모드 진입 시 안내를 다시 보여주도록 유지.
  - 안내 패널에 `다시 보지 않기` 체크박스를 추가하고, 체크 후 `확인` 시에만 로컬 키(`isf-view-guide-dismissed-v1`)를 저장해 재노출을 막도록 적용.
- View 안내 패널 숨김 동작 수정:
  - `.view-mode-guide { display:flex; }` 스타일이 `hidden` 속성을 덮어쓰던 문제를 수정.
  - `.view-mode-guide[hidden] { display:none !important; }`를 추가해 `확인` 클릭/일반 모드 전환 시 즉시 숨김이 반영되도록 보정.
- 용어 정리:
  - 사용자 노출 문구에서 `생키차트/생키다이어그램` 표현을 `Sankey Diagram`으로 통일.
  - 적용 범위: 메인 화면 제목/접근성 라벨, README 기능 설명, TODO 진행/버그 항목.

### 2026-03-16
- 로컬 자동백업/복원 기능 추가:
  - 입력값 액션 아래에 `백업 저장 / 백업 선택 / 선택 복원` UI를 추가.
  - 백업은 `localStorage(isf-local-backups-v1)`에 저장되며, 자동백업은 `12시간 간격`으로 실행.
  - 보관 개수는 최대 `60개`로 제한하고, 최신순으로 유지.
  - 복원 시 현재 상태를 먼저 수동 백업한 뒤 선택 백업으로 롤백하도록 구성.
- View 모드 데이터 보호 강화:
  - View 모드(`?view=1`)에서는 자동/수동 백업이 중지되도록 가드 추가.
  - View 모드에서 `저장` 버튼으로 로컬 저장 시, 일반 모드 전환과 함께 수동 백업(출처: view-save)을 같이 남기도록 보강.
- 상태 저장 흐름 확장:
  - `persistPrimaryState`에 자동백업 체크를 통합해 `적용/샘플/초기화/JSON 불러오기/해시 반영` 등 상태 반영 경로에서 동일 정책을 사용.
  - `commitImmediateInputs`에 옵션을 추가해 백업 복원 시 자동백업 중복 생성은 건너뛸 수 있도록 처리.
- 문서 업데이트:
  - `README.md`에 자동백업/복원 기능과 View 링크 데이터 보호 동작을 반영.
  - 브라우저 보안 정책상 임의 파일 자동 저장은 불가하며, 자동백업은 브라우저 로컬 저장소 기반임을 명시.

### 2026-03-20
- TODO 조정:
  - `TODO.md`의 `PWA 적용` 단일 항목을 아래 2개 항목으로 분리/교체.
  - `PWA 오프라인 캐시(Service Worker + manifest) 적용`
  - `백업 저장소 localStorage -> IndexedDB 확장(기존 백업 마이그레이션 포함)`
- PWA 적용:
  - `manifest.webmanifest` 추가(`start_url/scope/display/theme/icon` 설정).
  - `sw.js` 추가: 앱 셸 캐시(install), 구버전 캐시 정리(activate), 네비게이션 network-first + 정적 리소스 cache-first 전략 적용.
  - `index.html` head에 `manifest/theme-color/icon` 링크를 연결.
  - `app.js`에서 HTTP(S) 환경일 때만 서비스워커 등록하도록 추가(`file://` 실행 경로는 자동 제외).
- 백업 저장소 IndexedDB 전환:
  - 백업 저장소를 `IndexedDB(isf-backup-db-v1 / backupEntries)`로 전환.
  - 기존 `localStorage(isf-local-backups-v1)` 백업이 있으면 첫 초기화 시 자동 마이그레이션 후 legacy 키 제거.
  - 백업 로직을 비동기 처리로 변경(수동 백업/복원/자동 백업).
  - 저장소 준비 전/실패 상태를 UI 힌트에 반영하고 버튼 활성 조건에 적용.
- 문서 반영:
  - `README.md`의 백업 저장소 설명을 `IndexedDB` 기준으로 갱신.
  - PWA 오프라인 재진입 지원 내용을 기능 목록에 추가.
- 앱 아이콘 보정:
  - PWA 아이콘(`192/512`) 내부의 원화 표기 배지를 제거해 심볼을 단순화.
- 백업 UI 보정(PC/모바일 공통):
  - 입력 헤더의 액션/백업 영역 구조를 `controls-head-tools` 래퍼로 재정렬해 PC에서 비정상 줄바꿈/정렬 이슈를 완화.
  - 기존 상시 노출 백업 안내 문구를 제거하고, `?` 아이콘(`backupHelp`) 툴팁으로 전환.
  - 툴팁에는 저장 위치(IndexedDB), 자동 백업 주기(12시간), 보관 개수(최대 60), 현재 백업 개수/최신 백업 시점, View 모드 제한 정보를 동적으로 노출.
  - `inline-tip`은 hover뿐 아니라 click/focus에서도 표시되도록 상호작용 상태를 확장.
- 백업 저장 연타 방지 + 확장형 액션 UX 적용:
  - 수동 `백업 저장`은 `1분당 1개` 정책으로 제한하고, 1분 이내 재저장 시 변경분이 있으면 `덮어쓰기 확인` 후 기존 백업을 교체하도록 수정.
  - 1분 이내 동일 내용은 새 백업을 만들지 않고 차단해 백업 폭증을 방지.
  - 입력 헤더 액션을 `링크 복사 + 백업 + 더보기` 최소 노출 구조로 변경.
  - `백업` 메뉴에는 백업 저장/복원/도움말만, `더보기` 메뉴에는 JSON 저장/불러오기/샘플/초기화를 배치해 확장 사용 흐름으로 전환.
  - 백업/더보기 메뉴는 상호 배타적으로 열리며, 액션 실행 후 자동 닫힘 처리.
- 모바일/드롭다운 표시 보정:
  - 모바일에서 상단 기본 액션을 3열에서 2열로 재배치하고 `더보기` 버튼을 전체폭으로 확장해 `링크 복사` 라벨 표시 깨짐을 완화.
  - `summary.btn`를 버튼과 동일한 정렬(`inline-flex`)로 강제해 `백업/더보기` 버튼과 시각 톤을 통일.
  - 백업 드롭다운 패널 폭/그리드를 확장하고 `select option` 글자색/배경색, 선택창 텍스트 렌더링(`-webkit-text-fill-color`)을 명시해 항목명 가시성을 보정.
  - 백업 선택 옵션 라벨을 `#순번 + 모드 + 시각 + 출처`의 짧은 형식으로 변경해 선택 시 텍스트 잘림을 줄임.
- 액션 패널 오버플로우/동선 정리:
  - `링크 복사` 버튼을 기본 노출 영역에서 제거하고 `더보기` 패널 내부로 이동.
  - 데스크톱에서 `백업/더보기` 패널을 `absolute` 오버레이로 표시해 패널 확장 시 인접 버튼(`링크 복사`)이 밀려 화면 밖으로 튀어나가던 레이아웃 이슈를 수정.
  - 모바일(`max-width: 760px`)에서는 기존처럼 패널을 인라인(`position: static`)으로 유지해 터치 레이아웃 안정성을 보존.

## 커밋 메시지 추천
1. `fix: guard sankey tooltip visibility and hide on chart leave`
2. `fix: show advanced editor action buttons only when item changes exist`
3. `feat: use URL hash as primary state storage with JSON backup import/export`
