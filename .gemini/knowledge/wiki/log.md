# Project Evolution Log (연대기적 작업 로그)

## [2026-06-13] feat | 재무설정/가계추이 패널 디폴트 접기, 과거지출 비교 비활성화 및 이미지 저장 엣지 유실 버그 해결 (v0.11.44)
- **목적**: 불필요한 인지 부하 감소를 위해 복잡한 입력/테이블 패널을 기본 상태에서 접어두고, 미구현 기능을 확실하게 차단하며, SVG 다운로드 시 엣지가 누락되던 렌더링 버그 해결.
- **주요 변경사항**:
  - **패널 접기/펼치기 인터랙션 구현**: `controls-panel` (재무 설정) 및 `projection-panel` (가계 추이) 패널 헤더에 아코디언 토글 화살표 버튼(`toggleControlsBtn`, `toggleProjectionBtn`) 추가. 콘텐츠 영역을 감싸는 wrapper를 설계해 기본 로드 시 `hidden` 처리하여 깔끔하게 접어둠.
  - **가계 추이 검증 표현 단순화**: `state.js` 의 `projectionOptions.showFlow` 기본값을 `false` 로 수정하여 기본 상태에서는 수입/생활비 세부 흐름 컬럼들을 제외한 핵심 잔액/순자산 위주로 테이블이 단순 노출되도록 개선. 또한 `시점` ➔ `기간`, `실질 순자산` ➔ `실질 순자산 (현재가치)`으로 텍스트를 직관화.
  - **과거 지출 비교(미구현) 차단**: `.comparison-panel` 의 도구 버튼들을 `disabled` 처리하고, 그 위에 미려한 글래스모피즘 오버레이(`.comparison-panel__overlay`)와 `🚧 준비 중` 배지를 배치해 동작을 완벽히 비활성화.
  - **이미지 다운로드 엣지 유실 버그 해결**: `exportSankeyToPng` 함수 내 `styleEl`에 하드코딩되어 있던 `.sankey-path { fill: none; }` 오타를 `fill: url(#isf-sankey-sunset-deepsea-gradient); fill-opacity: 0.56; stroke: none;` 으로 전면 수정하여 저장된 이미지에서 그라디언트 밴드가 선명하게 출력되도록 정상화.
- **결과**: `npm run build` 빌드 성공, Playwright E2E 테스트 통과 및 v0.11.44 버전 배포 완료.

## [2026-06-13] cleanup | 재무 핵심 지표 영역의 미사용 cardMeta 안내문구 삭제 (v0.11.43)
- **목적**: 불필요한 마크업 및 비어 있는 가이드 태그를 소거하여 구조적 일관성과 미니멀함을 유지.
- **주요 변경사항**:
  - **HTML 엘리먼트 제거**: `apps/step1/index.html` 의 '재무 핵심 지표' 패널 헤더 하단에 위치하던 비어 있는 `<p id="cardMeta" class="hint"></p>` 요소 제거.
  - **DOM 맵핑 청소**: `apps/step1/modules/dom.js` 의 `dom` 인스턴스에서 미사용 중이던 `cardMeta` 엘리먼트 맵핑 코드 제거.
- **결과**: `npm run build` 빌드 성공, Playwright E2E 테스트 통과 및 v0.11.43 버전 배포 완료.

## [2026-06-13] fix | 핵심 요약 지표 카드 내 금액(억/만) 줄바꿈 방지 및 텍스트 중앙정렬 처리 (v0.11.42)
- **목적**: 요약 카드 내에서 억/만 단위의 금액이 들어갈 때 개행되는 현상 방지 및 카드 내 텍스트 정렬의 시각적 일관성 확보.
- **주요 변경사항**:
  - **금액 포맷 띄어쓰기 개선**: `shared/core/utils.js` 의 `formatMoney` 반환 포맷의 공백을 제거하여 `"X 억 Y 만원"` ➔ `"X억 Y만원"` 및 `"X 만원"` ➔ `"X만원"` 형태로 튜닝.
  - **텍스트 중앙정렬 및 줄바꿈 차단**: `shared/styles/step-theme.css` 에서 공통 `.card` 에 `text-align: center`를 적용하여 텍스트를 중앙정렬하고, `.card .value`에 `white-space: nowrap`을 추가하여 억/만 단위 숫자의 개행(줄바꿈)을 차단.
- **결과**: `npm run build` 빌드 성공, Playwright E2E 테스트 통과 및 v0.11.42 버전 배포 완료.

## [2026-06-13] fix | 모바일 Sankey 다이어그램 노드 과밀 겹침 개선 — 자동 그룹 collapse + 상세 팝업

- 문제: 모바일 화면에서 column 2(지출/저축/투자 항목)가 많을수록 배지 레이블이 촘촘하게 겹쳐 판독 불가 상태로 열화됨.
- 해결:
  - `sankey-renderer.js`에 `collapseOverloadedNodes(data)` 함수 신설. column 2 노드 수가 임계값(6개)을 초과하면 두 단계로 fold 처리:
    1. 1단계: `group` 필드의 첫 번째 `-` 이전 prefix를 기준으로 동일 그룹 항목들을 합산 노드로 묶음 (`"관리비 외 N개"` 형식).
    2. 2단계: 1단계 후에도 5개 초과 시, 같은 계좌(`accountId`)로 연결된 항목들을 `"식비 외 N개"` 형식으로 재묶음.
  - collapsed 노드는 황색 계통 배지(`rgba(255,248,220)`) + `▸` 접두사로 시각적 구분.
  - `sankey-builder.js`에서 col2 노드에 `group`·`accountId` 필드를 전달하도록 개선.
  - collapsed 노드 탭/클릭 시 `showCollapsedNodeDetail()` 함수를 통해 상세 항목 테이블 팝업 노출. 터치 좌표 계산으로 팝업 위치 산정, 외부 탭 시 자동 해제.
  - `styles.css`에 `.sankey-node--collapsed`, `.sankey-collapsed-detail`, 팝업 내 테이블 스타일 추가.
- 검증: 모바일 390px 뷰포트에서 샘플 데이터 기준 column 2 노드가 14개 → 3개 그룹으로 collapse됨. 탭 시 팝업 테이블이 정상 노출되고 JS 에러 없음 확인.

## [2026-06-13] feat | 사용성 개선을 위한 샘플데이터 및 투자성향 프리셋 자산규모/이체 규칙 고도화 (v0.11.41)
- **목적**: 다중 계좌, 수동 이체, 금융소득과세 경고 시스템을 사용자가 바로 확인하고 놀 수 있도록 샘플 불러오기 및 프리셋의 파라미터 튜닝.
- **주요 변경사항**:
  - **샘플 데이터(SAMPLE_INPUTS) 튜닝**: `constants.js` 내 샘플 데이터를 수정하여 수동 이체 규칙을 다중(3개)으로 확장. 초기 저축 1.5억 원, 초기 투자 7.5억 원으로 조정하여 연간 이자/배당 소득(합산 2,025만 원)이 금융소득종합과세 경고 기준(1,900만 원)을 초과하도록 세팅.
  - **투자 성향 프리셋 초기 자산 동적 스케일링**: `presets.js` 내 `applyPreset` 및 `applyPresetBySalary`를 리팩터링하여 연봉 규모(`salaryMan`)에 비례해서 초기 현금, 저축, 투자, 부채 자산이 동적으로 생성되도록 보정. 연봉 8,000만~1억 원 이상의 프리셋 선택 시 자산 규모가 커져서 금융소득과세 경고선(1,900만 원)을 유의미하게 넘어서도록 설계.
  - **프리셋 이체 규칙 다중화**: 프리셋 적용 시 수동 이체 규칙(`transfers`)을 3개로 다양화하여 SVG 흐름도와 이체 보드에 복합적인 화살표 네트워크가 한 번에 그려지도록 개선.
- **결과**: `npm run build` 빌드 성공, Playwright E2E 테스트 통과 및 v0.11.41 버전 배포 완료.

## [2026-06-12] feat | Phase 4: 계좌 간 이체 시각화 및 UX 개선 구현 완료 (v0.11.28)
- **목적**: 계좌 간 다대다(N:N) 이체 흐름을 기하학적으로 보여주는 SVG 흐름 지도 도입, 직관적이고 뇌적 연산을 줄이는 수동 이체 설정 및 편집기 탑재, 차트 패널 가로 슬라이딩 스위칭 전환, 도움말의 전역 Glassmorphism 툴팁 통합, 금융소득과세 경고 규격화.
- **주요 변경사항**:
  - **Account Flow Network Map (D-01)**: SVG 기반 네트워크 맵 신설. 계좌 노드 원형 배치, 흐름 펄스 점선 애니메이션(`@keyframes` dashoffset) 및 마우스 호버 시 관련 연결 노드/엣지만 포커싱(이외 요소 `0.15` opacity dimmed) 기능 탑재.
  - **Manual Transfer Rules & UI (D-02)**: `transfers` 데이터 스키마 영속화. 문장형/카드형 폼 UI 및 삭제(X) 버튼 호버 시 페이드 노출 구현. 출발 계좌 변경 시 출금 가능한 실시간 예상 잔액 힌트(`#sourceBalanceHint`) 표출.
  - **시각화 슬라이드 전환 (D-03)**: Sankey ↔ Network Map 탭 전환 시 두 SVG 래퍼를 뷰포트 내 가로 flex 슬라이더(`#visualizationSlider`)로 감싸 `transform` 트랜지션(0.4s cubic-bezier)으로 가로 슬라이딩 스위칭되도록 구현.
  - **과세 경고 디자인 규격 준수 (D-04)**: Warn(Yellow 보더, 0 0 8px 그림자), Crit(Red 보더, 0 0 12px 그림자), 뱃지 글자/배경색 스펙을 `styles.css`에 분리 구현하고 `transition: all 0.3s` 트랜지션 처리.
  - **도움말 ? 아이콘 툴팁화 (D-05)**: 지저분한 가이드 문구들을 소거하고 `?` 아이콘으로 대체, 호버 시 위치를 계산하여 Glassmorphism 전역 툴팁(`#globalTooltip`, blur 10px, 0.78 opacity 백그라운드) 팝업 처리.
- **결과**: `npm run build` 빌드 성공 및 v0.11.28 버전 배포 완료. UAT 성공 기준 100% 충족.

## [2026-06-12] ingest | Phase 4: 계좌 간 이체 시각화 및 UX 개선 기획 및 설계 수립 (v0.11.27)
- **목적**: 사용자 피드백을 반영하여 계좌 간 N:N 이체 흐름 시각화 차트 도입, 수동 이체 설정 및 사용성 극대화 UI 설계, 시각화 패널 내 가로 슬라이딩 전환, 설명문 도움말 ? 아이콘 및 전역 툴팁화, 경고 인디케이터 디자인 규격 수립.
- **주요 합의사항**:
  - **Account Flow Network Map (D-01)**: 생키 차트와 토글되는 SVG 기반 흐름 지도 신설. 빌드가 가벼운 외부 라이브러리(d3-force 등) 도입 또는 자체 SVG 배치 활용.
  - **Manual Transfer Rules & UX (D-02)**: `transfers` 배열을 모델에 영속화. 문장형/카드형 간소 입력 폼 제공, 출발 계좌 선택 시 "출금 가능 예상 잔액" 힌트를 실시간 표출하고, 규칙 카드 호버 시 삭제 X 버튼을 노출해 사용성 강화.
  - **시각화 슬라이드 전환 (D-03)**: Step 간 전환 대신, 단일 시각화 카드 영역 내에서 Sankey ↔ Network Map이 탭/스위치로 가로 슬라이딩되며 부드럽게 교체되는 전환 모션 구현.
  - **인디케이터 디자인 스펙 정의 (D-04)**: Warn(1,900만 원 초과, Yellow 보더/소프트 글로우), Crit(3,400만 원 초과, Red 보더/박스섀도우 글로우)의 규격 정의 및 cubic-bezier 전이 트랜지션 명시.
  - **도움말 ? 아이콘 툴팁화 (D-05)**: UI 텍스트 장벽을 줄이기 위해 모든 가이드 설명을 물음표 아이콘으로 간소화하고 호버 시 Glassmorphism 기반 전역 툴팁 노출.
- **결과**: 피드백이 완벽히 수렴된 `04-CONTEXT.md` 및 `04-01-PLAN.md` 갱신 및 v0.11.27 버전 빌드 무결성 확인 완료.

## [2026-06-12] fix | 실시간 금융소득 경고 핫픽스 및 마이크로 애니메이션 전역 적용 (v0.11.25)
- **목적**: UAT 피드백 수렴을 위한 폼 서브밋 무한 루프 차단, 금융과세 경고의 실시간 연동, 그리고 에디토리얼 호버 부유 트랜지션의 전역 컴포넌트 확장.
- **주요 변경사항**:
  - **폼 엔터 새로고침 차단**: `apps/step1/app.js` 내 `inputsForm` 에 대해 `submit` 이벤트 리스너를 달아 `event.preventDefault()` 처리함으로써 엔터 입력 시 페이지 새로고침 루프를 원천 예방.
  - **실시간 금융과세 경고 연동**: `renderAll`에서 금융소득과세 경고(`calculateAccountFinancialIncomes`) 및 각종 렌더링에 `state.inputs` 대신 `getVisibleInputs()`를 참조하도록 수정하여 사용자가 입력 필드를 변경하는 즉시 화면과 계좌 카드에 링/뱃지가 실시간 점등되도록 결함 핫픽스.
  - **에디토리얼 호버 효과 전역화**: 이체 카드 외에도 `.card`, `.account-row`, `.income-row`, `.expense-row`, `.savings-row`, `.invest-row` 등 Step 1 전체 컴포넌트의 비편집 상태 카드에 마우스 호버 시 Sunset Orange 테두리 및 Y축 -2px 부유 효과를 일괄 적용하여 시각적 디자인 일관성 강화.
- **결과**: `npm run build` 빌드 성공 및 핫픽스 배포 완료.

## [2026-06-12] release | Phase 3: Multi-account Data Model 및 이체 보드 구현 완료 (v0.11.22)
- **목적**: 다중 계좌 데이터 모델 확장 및 내부 이체 링크의 생키 다이어그램 제외 로직 구현 및 연간 금융소득 과세 주의/경고 알림 연동.
- **주요 변경사항**:
  - **Sankey 단순화 및 이체 흐름 보드 신설**: Sankey 다이어그램에서 내부 이체를 links 배열에서 배제하고, 하단에 `#accountTransferBoard`를 신설하여 PC 3열 그리드 및 모바일 수직 피드로 렌더링.
  - **금융소득 과세 경고 인디케이터**: 연간 금융소득 1,900만 원 초과 시 `warn`("금융소득 종합과세 주의"), 3,400만 원 초과 시 `crit`("금융소득 종합과세 대상 (한도 초과)") 경고 링 및 뱃지 적용.
  - **인터랙션 모션 및 반응형**: 이체 카드 호버 시 2px 부유 효과, 화살표 가로 바운스 키프레임 효과를 적용하여 프리미엄 UX 완성.
  - **자동 버전 범프**: `npm run build` 시 패키지 버전 및 sw.js 버전을 v0.11.22로 자동 bump하는 스크립트 실행 및 빌드 무결성 검증 완료.
- **결과**: Phase 3 성공 기준 100% 충족 및 빌드 통과.

## [2026-06-12] ingest | Phase 3: Multi-account Data Model 기획 및 UI-SPEC 명세 수립
- **목적**: 다중 계좌 데이터 모델 도입에 따른 UI 설계 및 Sankey Diagram의 정보 과부하 문제 해결을 위한 기획 수립 (요구사항: CORE-01, CORE-02).
- **주요 합의사항**:
  - **Sankey Simplification (D-01)**: 생키 차트는 수입원 ➔ 계좌 ➔ 대분류(지출/저축/투자)의 3단 흐름만 렌더링하도록 단순화하고, 복잡한 N:N 내부 이체(transfer) 링크는 렌더링에서 배제하여 스크롤 유발 및 레이아웃 꼬임을 방지함.
  - **Account Transfer Board (D-02)**: 계좌 간의 내부 N:N 이체 및 분배 흐름은 생키가 아닌 별도의 '계좌 간 이체 현황 보드(Account Transfer Board)'를 도입하여 리스트 카드 및 화살표 흐름으로 직관화함. 모바일에서는 수직 피드로 정렬되고 PC에서는 3열 그리드로 와이드 스케일링됨.
  - **안전 한도 경고 (D-03)**: 저축/투자 계좌별로 금융종합소득세 한도 초과(1,900만 원 초과 시 `warn`, 3,400만 원 초과 시 `crit`) 발생 시 계좌 카드에 붉은 링 및 뱃지 경고 노출.
- **결과**: `03-CONTEXT.md` 및 `03-UI-SPEC.md` 작성 완료.

## [2026-06-12] feat | Phase 2: Core Components & Layout 구현 완료 (v0.11.21)
- **목적**: 주요 컴포넌트(버튼, 폼, 카드 등)의 에디토리얼 스타일 개편 및 모바일 우선 반응형 레이아웃 적용 완료 (요구사항: UI-03, UX-01).
- **주요 변경 사항**:
  - **모바일 1열 스택 및 12px 패딩 (D-01)**: 모바일 화면(<= 760px)에서 `.control` 요소를 가로 100% 1열 스택 정렬 및 12px 패딩 적용.
  - **PC 3열 그리드 오버라이드 (D-01)**: PC 화면(>= 768px/761px)에서 입력 필드 영역이 3열 그리드로 넓게 확장되도록 개선.
  - **가로 스와이프 탭 도입 (D-03)**: 모바일 뷰(<= 768px)에서 메인 탭(`.mgmt-tabs`) 및 상세 탭(`.advanced-block > .tab-list`)이 개행되지 않고 가로 스와이프 및 에디토리얼 언더라인 스타일로 전환.
  - **인라인 확장 카드 편집 (D-02)**: 모바일 편집 활성화 시(`.is-editing`) 카드가 세로 3단 스택 구조(1층: 이름, 2층: 계좌 select + 금액 input, 3층: 삭제 버튼)로 수직 확장 배치.
  - **Flat Hairline & Focus Design (D-04)**: 카드/버튼 그림자를 제거하고 1px 실선 테두리 적용. 입력 필드 포커스 시 브랜딩 Sunset 오렌지 아웃라인 포커스 링 점등 및 부드러운 transition 추가.
- **결과**: `npm run build` 프로덕션 빌드가 에러 없이 완료되어 v0.11.21로 업데이트되었으며, 모바일 화면에서의 입력 편리성을 향상시키고 에디토리얼 톤앤매너를 시각적으로 정교하게 구현함.

## [2026-06-12] ingest | Phase 2: Core Components & Layout 기획 및 계획 수립 완료
- **목적**: 주요 컴포넌트(버튼, 폼, 카드 등)의 에디토리얼 스타일 개편 및 모바일 우선 반응형 레이아웃 설계를 구체화하고 관련 계획(PLAN.md, UI-SPEC.md, UAT.md)을 수립함.
- **주요 내용**:
  - **모바일 1열 스택 및 콤팩트 여백(D-01)**: 모바일 화면(<= 760px)에서 `.control` 요소를 가로 100% 1열 스택 정렬 및 12px 패딩 적용. PC 화면(>= 768px)에서 3열 그리드로 오버라이드.
  - **인라인 확장 카드 편집(D-02)**: 모바일 편집 모드(`.is-editing`) 시 카드가 세로 3단 스택 구조로 정렬되도록 레이아웃 정의.
  - **스와이프 탭(D-03)**: 모바일 탭 줄바꿈 문제를 해결하기 위한 가로 스와이프 및 에디토리얼 언더라인 스타일 탭 구조 명세.
  - **Flat Hairline & Focus Design(D-04)**: 카드/버튼 그림자 제거, 1px 보더 처리. 인풋 포커스 시 브랜딩 Sunset 오렌지 및 Accent 그린 아웃라인 피드백 링 적용.
- **결과**: `02-RESEARCH.md`, `02-UI-SPEC.md`, `02-01-PLAN.md`, `02-UAT.md` 생성 완료.

## [2026-06-11] feat | 대쉬보드 모드(기존 유저) 진입 시 패널 정렬 순서 정상화 및 후순위 패널 배치 교정 (v0.11.20)
- **목적**: 기존 가계 데이터가 로컬 스토리지에 존재하는 유저가 접속했을 때 활성화되는 대쉬보드 모드(`body.is-dashboard-mode`)에서 패널들의 `order` 속성이 꼬여 "과거 지출 비교"와 "가계 추이 검증" 패널이 중간에 떠오르던 오류를 수정함.
- **주요 변경 사항**:
  - **대쉬보드 모드 패널 순서 재조정**: `apps/step1/styles.css`에서 `body.is-dashboard-mode` 하위 패널들의 `order` 값을 일관성 있게 수정함.
    - `.summary-panel` (지표 요약) ➔ `order: 1`
    - `.sankey-panel` (Sankey 차트) ➔ `order: 2`
    - `.controls-panel` (재무 설정) ➔ `order: 3`
    - `.projection-panel` (가계 추이 검증) ➔ `order: 4`
    - `.comparison-panel` (과거 지출 비교) ➔ `order: 5`
- **결과**: `npm run build` 프로덕션 빌드가 성공하여 v0.11.20으로 업데이트되었으며, 일반/대쉬보드 모드 전체에 걸쳐 사용자의 설정 변경 및 실시간 생키 시각화가 최상단에 먼저 배치되고 "가계 추이 검증"과 "과거 지출 비교"는 항상 화면의 최하단에 정렬되는 일관성 높은 레이아웃(UX) 무결성을 구축함.

## [2026-06-11] feat | 재무 핵심 요약 지표 카드 수 제한 및 접기/펼치기(상세 정보) 구분 기능 구현 (v0.11.19)
- **목적**: 요약 카드 패널(핵심 지표)에 표시되는 정보의 개수가 과도하게 많아져(최대 11개) 인지 과부하가 생기는 문제를 방지하고자, 가장 주요한 6개 지표만 최초 노출하고 나머지 상세 지표들은 접고 펼칠 수 있는 토글 영역으로 물리적으로 격리하여 탐색 효율성을 향상함.
- **주요 변경 사항**:
  - **상세 지표 접기/펼치기 상태 관리**: `apps/step1/modules/state.js` 내에 상세 카드 확장 상태를 관리하는 `state.showDetailedCards` 불리언 변수를 신설함.
  - **요약 카드 동적 분할 렌더링**: `apps/step1/modules/list-renderer.js`의 `renderCards` 함수를 고도화하여, 지표 카드가 6개를 초과하는 경우 7번째 카드부터 `.is-detailed` 클래스를 부여하고 `state.showDetailedCards`가 `false`일 때 숨김(`display: none`) 처리하도록 로직을 개편함. 또한 하단에 동적 토글 버튼(`toggleDetailedCardsBtn`)을 추가하고 클릭 시 런타임 DOM 조작으로 카드를 부드럽게 토글하며 텍스트를 "상세 지표 더보기 ▾" / "상세 지표 접기 ▴"로 갱신하도록 구성함.
  - **버튼 스타일 정의**: `apps/step1/styles.css` 최하단에 토글 버튼 및 래퍼 클래스(`.summary-toggle-wrap`, `#toggleDetailedCardsBtn`)에 대한 크림색 테마 기반의 반응형 플랫 스타일(호버 피드백 및 그림자 효과)을 적용함.
- **결과**: `npm run build` 프로덕션 빌드가 성공하여 v0.11.19로 업데이트되었으며, 부채가 있는 복잡한 가계 데이터 시뮬레이션 시에도 가장 중요한 6대 핵심 지표만 우선 인지할 수 있는 개선된 정보 계층화(UX)를 검증 완료함.

## [2026-06-11] feat | 모바일 수입 계좌 배정 라벨 텍스트 축소 및 줄바꿈/이탈 방지 스타일 보완 (v0.11.18)
- **목적**: 모바일 화면에서 수입 항목의 다중 배정 계좌 정보가 가로로 길어져서 생기는 레이아웃 이탈 문제를 확실하게 해결하기 위해 계좌명의 접미사를 정리하고, CSS 줄바꿈 속성을 강화함.
- **주요 변경 사항**:
  - **계좌 배지 텍스트 생략 로직 추가**: `apps/step1/modules/list-renderer.js`에 계좌명에서 "계좌" 혹은 "통장" 접미사를 자동으로 지워주는 `getShortAccountName` 헬퍼 함수를 추가하고, 비편집 상태의 수입 배지(`.badge-account--income`) 및 배분 배지(`.badge-account--outflow`) 렌더링 시 계좌명을 짧게 노출하도록 로직을 개편함. (예: "급여계좌" -> "급여", "생활비계좌" -> "생활비")
  - **CSS 줄바꿈 및 자동 개행 스타일링**: `apps/step1/styles.css` 내의 모바일 미디어 쿼리 블록에서 `.income-meta` 컨테이너에 `flex-wrap: wrap`, `gap: 4px`를 주어 좁은 화면에서 배지들이 자동 개행되도록 하였으며, 개별 `.badge-account` 배지에 `white-space: normal` 및 `word-break: break-all`을 주어 텍스트가 극도로 길어지더라도 화면 밖으로 탈출하는 물리적 현상을 원천 방지함.
- **결과**: `npm run build` 프로덕션 빌드가 성공하여 v0.11.18로 업데이트 완료되었으며, 수입 명칭 및 계좌 배지 정보가 화면이 좁은 모바일 환경에서도 아름답게 정리되어 표현되도록 반응형 정합성을 보장함.

## [2026-06-11] feat | 모바일 분배 수입 입력 레이아웃 이탈 수정 및 투자성향 프리셋 초기 자산 정합성 강화 (v0.11.17)
- **목적**: 모바일 환경에서 분배 수입 편집 카드 내부 필드가 화면 밖으로 벗어나는 레이아웃 파손 현상을 수정하고, 기본 설정값 및 성향별 프리셋 적용 시 초기 자산(현금, 저축, 투자, 부채)과 부채 이자율의 세부 수치를 요구사항에 맞게 변경하여 완성도를 높임.
- **주요 변경 사항**:
  - **모바일 수입 분배 UI 반응형 최적화**: `apps/step1/styles.css` 내의 모바일 미디어 쿼리 블록에 다중 분배 수입 편집 카드(`.income-row.is-editing.is-multi-allocation`), 필드 래퍼(`.income-row-fields`), 그리고 개별 분배 행(`.income-allocation-row`)의 CSS 레이아웃을 flex 수직 쌓기 및 grid 컬럼 재조정으로 보완함. 이를 통해 모바일 좁은 화면에서도 "전체 수입(만원)" 라벨과 입력 필드가 화면 밖으로 탈출하지 않도록 조치함.
  - **기본 입력값(DEFAULT_INPUTS) 갱신**: `apps/step1/modules/constants.js` 내의 기본 설정 객체에서 초기 현금(`startCash: 500만원`), 초기 저축잔액(`startSavings: 1000만원`), 초기 투자잔액(`startInvest: 500만원`), 현재 부채(`startDebt: 500만원`)로 수정하였으며, 기본 부채 이자율을 기존 4.2%에서 **5.2%**로 상향 이식함.
  - **투자 성향 프리셋 초기 자산 동적 매핑**: `apps/step1/modules/presets.js` 내의 `applyPreset` 및 `applyPresetBySalary` 함수에서 자산 초기값 반환 방식을 리팩터링함. 공통적으로 초기 현금과 현재 부채는 각각 500만 원으로 고정 적용하고, 적극형/야수형의 경우 초기 저축잔액 500만 원, 초기 투자잔액 1000만 원으로 적용하며, 안정형/중립형의 경우 초기 저축잔액 1000만 원, 초기 투자잔액 500만 원으로 스위칭 매핑되도록 처리함.
- **결과**: `npm run build` 프로덕션 빌드가 성공하여 v0.11.17로 갱신되었으며, 모바일 환경의 UI 레이아웃 무결성을 준수하고 투자 성향별로 균형 잡힌 가계부 초기 자산 프리셋이 동적으로 정교하게 대입되도록 검증을 완료함.

## [2026-06-11] feat | 부채 입력 폼의 '현재 자산 및 부채' 통합 배치로 탐색 UX 개선 (v0.11.16)
- **목적**: 사용자가 대출 등의 부채 현황을 입력하고 싶을 때 직관적으로 바로 찾을 수 있도록 ⚙️ 설정 탭 내 자산 입력부와 부채 입력부를 하나의 통합된 영역으로 결합해 사용성을 개선함.
- **주요 변경 사항**:
  - **인풋 필드 이사 및 명칭 개선**: 기존 ⚙️ 설정 탭 하단 '증가율 / 수익률 / 초기부채' 영역에 흩어져 있던 `startDebt` (초기 부채) 필드를 상단의 현금/저축/투자 자산 입력 필드가 모인 곳으로 이동시키고, 레이블 명칭을 **'현재 부채'**로 개선함.
  - **설정 블록 리브랜딩**: 자산 및 부채의 성격과 일치하도록 상단 블록 제목을 '초기 자산' ➔ **'현재 자산 및 부채'**로, 하단 블록 제목을 '증가율 / 수익률 / 초기부채' ➔ **'증가율 및 수익률'**로 가독성 높게 명칭을 변경함.
- **결과**: `npm run build` 프로덕션 빌드가 에러 없이 완료되었으며, ⚙️ 설정 탭 진입 시 사용자가 대출 등의 현재 보유 부채를 자산 현황과 함께 즉시 직관적으로 발견하여 설정할 수 있도록 UX 장벽을 제거함.

## [2026-06-11] feat | 데스크톱 레이아웃 패널 정렬 순서 동기화 및 부차 지표 하단 배치 (v0.11.15)
- **목적**: 과거 지출 비교 및 가계 추이 검증 패널의 중요도가 비교적 낮으므로 데스크톱 환경에서도 해당 패널들을 가장 하단으로 밀어내고, 재무 설정과 생키 다이어그램이 최상단 지표 바로 밑에 연달아 노출되도록 레이아웃 질서를 재정립함.
- **주요 변경 사항**:
  - **데스크톱 CSS Order 속성 동기화**: `apps/step1/styles.css`에서 데스크톱 레이아웃에 적용되는 패널들의 `order` 속성 값을 모바일 뷰와 완벽하게 일치하도록 조정함.
    - `summary-panel` (지표 요약) ➔ `order: 1`
    - `controls-panel` (재무 설정) ➔ `order: 2`
    - `sankey-panel` (Sankey 차트) ➔ `order: 3`
    - `projection-panel` (시뮬레이션 검증) ➔ `order: 4`
    - `comparison-panel` (과거 지출 비교) ➔ `order: 5`
- **결과**: `npm run build` 프로덕션 빌드가 에러 없이 완료되었으며, 데스크톱과 모바일 환경 전체에 걸쳐 중요도가 높은 재무 설정과 생키 흐름선이 먼저 노출되고, 참조용 추이 검증과 과거 비교 패널이 가장 아래로 정렬되는 최적화된 사용자 시선의 흐름(UX)을 구축함.

## [2026-06-11] fix | 생키 차트 계좌 노드의 누적 금액 표기 누락 버그 해결 (v0.11.14)
- **목적**: Sankey Diagram 상에서 계좌(accounts) 노드의 표시 금액이 `0원` 또는 `0만원`으로 잘못 고정 노출되는 버그를 해결하여, 계좌를 경유하는 실제 자금 흐름 규모를 투명하게 시각화함.
- **주요 변경 사항**:
  - **계좌 노드 금액 동적 업데이트**: `apps/step1/modules/sankey-builder.js` 내의 `buildSankeyData` 함수의 3번 과정(내부 이체 계산) 직후, 각 계좌 노드로 유입되는 모든 이체 및 수입 링크 금액을 합산하여 해당 계좌 노드의 `value` 값을 동적으로 채워 넣는 로직을 추가함.
- **결과**: `npm run build` 프로덕션 빌드가 에러 없이 완료되었으며, 생키 차트에서 각 계좌명 옆에 해당 계좌로 유출입되는 실제 금액 총합이 만원/원 정합성에 맞춰 완벽하게 렌더링됨.

## [2026-06-11] feat | 가계 요약 패널 리브랜딩 및 재무 설정/자동이체 레이아웃 상하 배치 최적화 (v0.11.13)
- **목적**: '핵심 카드' 패널의 타이틀 명칭이 모호하다는 사용자의 지적을 반영하여 금융 맥락에 부합하는 명칭으로 개선하고, 재무 설정 패널과 잉여현금 자동이체 배너의 배치 순서를 사용자 행동 흐름에 최적화하여 조작 편의성을 높임.
- **주요 변경 사항**:
  - **패널 타이틀 리브랜딩**: `apps/step1/index.html`에서 기존 '핵심 카드' 지표 영역의 `h2` 제목을 '재무 핵심 지표'로 개명하여 직관성을 높임.
  - **자동이체 배너 위치 이동**: 기존에 핵심지표 패널 하단에 묶여 있던 잉여현금 자동이체 설정 배너(`#surplusTransferBanner`)를 '재무 설정'(`.controls-panel`) 내부 상단의 가이드라인 힌트 바로 밑으로 이동시킴으로써 수입/지출 설정과 이체 설정이 한 흐름으로 이어지게 배치함.
  - **레이아웃 순서 및 모바일 뷰 최적화**: '재무 설정'(`.controls-panel`)을 핵심 지표 패널 바로 아래이자 Sankey Diagram 위로 올림으로써 선제적 입력 조작이 가능하도록 변경함. 또한 `apps/step1/styles.css` 하단의 모바일 미디어 쿼리 내 `order` 속성을 조정(1: 요약, 2: 설정, 3: Sankey, 4: 시뮬레이션, 5: 과거 비교)하여 반응형 레이아웃의 시각적 연속성을 보장함.
  - **여백 스타일 보완**: `styles.css` 내 `.surplus-transfer-banner`에 `margin-bottom: var(--sp-md)` 규칙을 추가해 탭 메뉴와의 시각적 경계 여백을 확보함.
- **결과**: `npm run build` 프로덕션 빌드가 에러 없이 완료되었으며, 주요 가계 설정과 자동이체 지정이 지표 아래에 나란히 배치되어 조작과 실시간 시각화 반영이 가장 자연스러운 논리적 선후관계를 가짐.

## [2026-06-11] feat | 샘플 데이터 수입 금액 및 계좌별 배분 현실화 (v0.11.12)
- **목적**: 사용자의 요청에 따라 샘플 데이터의 수입 규모를 더 대중적이고 현실적인 수준(합산 320만원)으로 재조정하여 자산 흐름 설정 시 공감을 유도함.
- **주요 변경 사항**:
  - **샘플 데이터 수치 조정**: `apps/step1/modules/constants.js` 내의 `SAMPLE_INPUTS` 수입을 주급여 310만원, 부수입 10만원(합계 320만원)으로 하향 수정함.
  - **계좌별 분배 조율**: 총 수입 하향에 따라 계좌별 분배(allocations)도 주급여에 대해 급여계좌 100만원, 생활비계좌 150만원, 주식계좌 60만원으로, 부수입에 대해 급여계좌 100%인 10만원으로 현실감 있게 쪼개지도록 분배액을 매칭함.
- **결과**: `npm run build` 프로덕션 빌드가 에러 없이 완료되었으며, 현실적인 금액을 토대로 다중 분배 및 Sankey 흐름선이 렌더링되도록 구현을 보완함.

## [2026-06-11] feat | 프리셋 빠른 생성 모달 입력 단위를 연봉 설정으로 환원 및 자동 세후 연산 적용 (v0.11.11)
- **목적**: 프리셋 설정 시 사용자가 연봉을 입력하면 세후 실수령액으로 변환하여 적용하던 기존 설계 사양을 복원하여 사용자가 직관적인 실수령 기반 자산 흐름을 구성하도록 개선함.
- **주요 변경 사항**:
  - **연봉-월수입 선언적 보간 및 역산 알고리즘**: `apps/step1/modules/presets.js` 에 연봉 금액(만원)에 대응하는 세후 월 실수령액(원)을 구하는 선언적 선형 보간 함수 `calculateMonthlyIncomeFromAnnualSalary` 및 현재 수입으로부터 연봉을 역추적하는 `calculateAnnualSalaryFromMonthlyIncome` 역산 함수를 추가함.
  - **연봉 기반 프리셋 연산**: `applyPresetByIncome`을 `applyPresetBySalary`로 전면 대체하여, 입력받은 연봉(최대 9900만원)을 세후 월수입으로 우선 계산한 후 성향별 다중 계좌 분배(지출/저축/투자)를 생성하도록 로직을 개편함.
  - **입력 UI 변경**: `apps/step1/index.html`에서 프리셋 입력 레이블을 '연봉 설정 (최대 9,900만원)'으로 수정하고 기본값을 4000(만원)으로 설정하였으며, `apps/step1/app.js`에서 모달 오픈 시 현재 수입 총합을 연봉으로 역산하여 기본값으로 필드에 채워주도록 흐름을 재설계함.
- **결과**: `npm run build` 프로덕션 빌드가 에러 없이 완료되었으며, 사용자가 자신의 세전 연봉을 그대로 입력하기만 하면 세후 실수령액이 계산되어 가계 흐름 설계에 자동으로 정밀 적용되는 완성도 높은 UX를 보장함.

## [2026-06-11] feat | 기본 설정/샘플 및 투자성향 프리셋 수입의 다중 계좌 분배 고도화 (v0.11.10)
- **목적**: 수입의 다중 계좌 분배 기능이 동작하는 모습을 직관적으로 인지할 수 있도록 기본 및 샘플 데이터를 조정하고, 위험도가 높은 프리셋 유형일수록 주식 계좌로의 분배 비중이 커지도록 동적 연산 알고리즘을 구현함.
- **주요 변경 사항**:
  - **기본 및 샘플 데이터 설정 보완**: `apps/step1/modules/constants.js` 내의 `DEFAULT_INPUTS`와 `SAMPLE_INPUTS`의 수입 정보에 급여계좌(`acc-salary`), 생활비계좌(`acc-living`), 주식계좌(`acc-stock`)로 쪼개지는 다중 분배(`allocations` 배열)를 기정 설정하여 앱 실행 즉시 이체 정합성과 다중 계좌 분배가 렌더링되도록 함.
  - **투자 성향별 동적 분배 연산**: `apps/step1/modules/presets.js` 내의 `applyPreset` 및 `applyPresetByIncome` 함수에서 선택된 투자 성향의 지출/저축/투자 비중에 대응하여 생활비계좌(`acc-living`), 급여계좌(`acc-salary`), 주식계좌(`acc-stock`)의 분배액을 계산하고, 단수 오차를 최종 보정하는 알고리즘을 이식함. 이로써 안정형(주식 비중 10%)부터 야수형(주식 비중 70%)까지 위험도가 높아질수록 주식계좌 비중이 자동 극대화됨.
- **결과**: `npm run build` 프로덕션 빌드가 에러 없이 완료되었으며, 다중 분배 로직의 시각적 확인이 용이해지고 투자 성향에 일치하는 위험도 기반 계좌 쪼개기가 완벽하게 작동함.

## [2026-06-11] feat | Step 1 프리셋 빠른 생성 모달 UI 및 투자성향별 분배 적용 기능 구현 (v0.11.9)
- **목적**: 기존 수입 탭 내의 고정식 프리셋 블록을 제거하고, 재무 설정 패널 헤더에 모달 기동 버튼을 추가하여 모달 내에서 투자 성향 및 월수입(최대 9900만원)을 설정하고 즉시 전체 재무 입력 폼에 덮어쓰기 적용할 수 있는 직관적인 UX를 구축함.
- **주요 변경 사항**:
  - **프리셋 모달 폼 설계**: `apps/step1/index.html` 하단에 안정형, 중립형, 적극형, 야수형 성향 버튼과 월수입 입력 필드, 취소 및 적용 버튼으로 구성된 `presetModal` 마크업 구조를 생성하고, 기존 구형 `#presetBlock`을 제거함.
  - **이벤트 핸들러 구현 및 바인딩**: `apps/step1/app.js`에서 프리셋 모달 열기/닫기(애니메이션 클래스 `.is-active` 토글 포함), 투자 성향 선택 상태 전환, 그리고 최종 적용 버튼 클릭 시 최대 9900만원 유효성 검증 및 데이터 덮어쓰기 경고 동의를 거쳐 `applyPresetByIncome` 연산을 통해 전체 데이터를 덮어쓰도록 구현함.
  - **DOM 매핑 확장**: `apps/step1/modules/dom.js` 에 모달 제어용 엘리먼트(`openPresetBtn`, `presetModal`, `closePresetBtn`, `closePresetModalCancel`, `applyModalPresetBtn`, `presetIncomeAmount`, `presetModalStyleBtns`)를 신규 등록함.
  - **CSS 스타일 보완**: `apps/step1/styles.css`에서 모달 내 성향 버튼 활성화 상태 스타일을 추가하고, 월 급여 입력 인풋 필드(`presetIncomeAmount`)에 "만원" 단위가 예쁘게 붙도록 suffix 스타일 및 모바일 화면 가로 맞춤 반응형 규칙을 보완함.
- **결과**: `npm run build` 프로덕션 빌드가 성공적으로 완료되었으며, 사용자가 모달을 통해 월 급여 규모와 성향에 알맞은 재무 구성을 클릭 몇 번으로 손쉽게 프리셋을 적용하고 차트에 연동하는 개선된 UX를 완성함.

## [2026-06-11] fix | 수입 다중 분배 유효성 검증 적용 및 Sankey 차트 흐름선 꼬임 에러 수정 (v0.11.7)
- **목적**: 수입 항목별 계좌별 분배 시 전체 수입을 초과하여 배분되는 것을 방지하고, 미배분된 잔여 금액으로 인해 Sankey 차트 내 입출금 수지가 맞지 않아 발생하던 렌더링 에러를 해결함.
- **주요 변경 사항**:
  - **초과 배분 유효성 검증**: `apps/step1/app.js` 의 `applyItemEditor` 실행 시점에 수입 항목별 분배액 합산이 전체 수입 금액을 초과할 경우, 에러 경고 모달을 띄우고 데이터 적용을 중단하는 유효성 체크 로직 추가.
  - **Sankey 잔여분 자동 밸런싱**: `apps/step1/modules/sankey-builder.js` 에서 수입 다중 배분 금액의 합산이 전체 수입보다 작을 경우, 그 차액(잔여 금액)을 디폴트 수입 계좌(`src.accountId`)로 자동 흐르게 하여 Sankey 다이어그램의 수지(Inflow = Outflow) 정합성을 100% 보장함.
- **결과**: `npm run build` 프로덕션 빌드가 에러 없이 최종 완료되었으며, 수입 초과 배분 데이터 오염을 예방하고 차트에 즉각 정밀한 이체선들이 반영되도록 무결성을 확보함.

## [2026-06-11] feat | Step 1 수입 다중 계좌 분배(Multi-Allocation) 기능 구현 완료 (v0.11.6)
- **목적**: 하나의 수입원(예: 주급여)에서 여러 개의 계좌로 나누어 분배하여 입금될 수 있도록 데이터 구조를 확장하고 UI 및 Sankey 차트 연동을 고도화함.
- **주요 변경 사항**:
  - **데이터 모델 확장**: `apps/step1/modules/input-sanitizer.js`에서 수입 아이템에 `allocations` 배열 스키마를 추가하고 정제(Sanitization) 규칙을 구현함. (기존 단일 `accountId` 데이터와도 100% 호환되도록 fallback 구현)
  - **다중 분배 UI 폼 개발**: `apps/step1/modules/list-renderer.js`에서 수입 편집 모드 진입 시 각 수입 하단에 '계좌별 분배 설정' 영역을 렌더링하도록 템플릿을 개편하고, 분배 추가/삭제 및 개별 금액 조절 인풋 서브폼 구현.
  - **이벤트 델리게이션 바인딩**: `apps/step1/app.js`에서 분배 항목 관련 select/input 변경 이벤트를 처리하고, 분배 항목 추가/삭제 버튼 클릭 이벤트를 위임 수용하도록 제어 로직 보강.
  - **서명(Signature) 로직 갱신**: `apps/step1/modules/state-helpers.js` 내의 `getItemEditorSignature` 함수에 `allocations` 배열 직렬화를 포함하여, 분배 내역 수정 시에도 변경 적용 버튼이 올바르게 활성화되도록 개선.
  - **Sankey 차트 동적 맵핑**: `apps/step1/modules/sankey-builder.js` 및 `calculator.js`에서 다중 분배 목록이 존재할 경우, 각 분배 계좌별로 독립적인 금액 흐름 링크를 동적으로 생성하여 뿜어내도록 알고리즘 개편.
  - **디자인 스타일 보강**: `apps/step1/styles.css` 하단에 다중 분배 폼 및 서브 행들을 위한 Editorial 스타일의 연한 배경 및 그리드 간격 스타일 추가.
- **결과**: `npm run build` 프로덕션 빌드가 성공적으로 완료되었으며, 수입 ➔ 다중 계좌 분배를 통한 정교한 가계 흐름 설계가 가능해지고 UAT 테스트 5개 항목을 전원 Pass로 통과함.

## [2026-06-11] fix | Step 1 관리 탭 런타임 바인딩 에러 해결 및 UX/스타일링 무결성 강화 (v0.11.5)
- **목적**: Step 1 재무 설정 관리 탭 전환 기능 누락 버그와 최초 리스트 렌더링 부재 오류를 해결하여 사용성을 정상화하고, 단위 라벨(::after)과 인풋 placeholder의 정렬 틀어짐 문제를 해소함.
- **주요 변경 사항**:
  - **탭 전환 활성화**: `apps/step1/app.js` 내에 누락되어 있던 `activateMgmtTab` 및 `initMgmtTabs` 함수를 정의하고, 앱 기동 시점(`init`)에 관리 탭 클릭 리스너를 올바르게 바인딩함.
  - **최초 리스트 렌더링 구현**: 앱 최초 기동 및 펜딩 데이터 변경 사항이 반영/취소되는 시점에 `refreshInputsPanel` 호출을 통해 수입, 계좌, 지출, 저축, 투자 리스트들이 `listRenderer.renderItemList`에 의해 정상적으로 렌더링되도록 개선함.
  - **탭 간 데이터 동기화 최적화**: `apps/step1/modules/list-renderer.js`에서 계좌 참조 대상을 기존 `state.inputs.accounts`에서 `(state.draftInputs || state.inputs).accounts`로 전면 대체하여, 계좌 탭에서 펜딩 상태로 수정한 내용이 수입/지출/저축/투자 드롭다운과 배지에 즉각 동기화되도록 수정함.
  - **디자인 무결성 개선**: `apps/step1/styles.css`에서 단위 라벨(`::after`)의 `bottom` 위치를 `24px`로 미세 정렬하여 수직 중앙 정렬을 맞추고, 숫자 인풋의 placeholder 정렬을 `text-align: right`로 통일하여 시각적 어긋남을 완치함.
- **결과**: `npm run build` 프로덕션 빌드가 성공적으로 완료되었으며, 탭 조작 시의 데이터 연동과 입력 인터페이스의 시각적 무결성을 동시에 확보함.

## [2026-06-11] setup | OpenGSD Gemini 환경 글로벌 설정 및 미변환 경로 패치 완료
- **목적**: OpenGSD의 최신 `gsd-core` 패키지를 사용하여 Gemini 환경에 맞춤화된 글로벌 컨텍스트 모니터 훅 및 설정을 갱신하고, 일부 템플릿 파일 내 미변환 `.claude` 경로를 Gemini 환경으로 수동 패치함.
- **변경 사항**:
  - `npx @opengsd/gsd-core@latest --gemini --global` 명령을 실행하여 `commands/gsd` 설치 및 Gemini 특화 BeforeAgent, AfterAgent, BeforeModel 등의 컨텍스트 모니터 훅을 구성함.
  - 설치 완료 시 보고된 4개 파일(`autonomous.md`, `plan-phase.md`, `graphify.toml`, `surface.toml`) 내 미변환 `.claude` 경로 및 옵션명들을 `.gemini`/`--gemini` 등으로 수동 치환하고, 이를 글로벌 설치 경로(`C:\Users\LENOVO\.gemini`)에 반영하여 보존함.
- **결과**: Gemini 환경 하에서 경로 매핑 오류 없이 `/gsd:new-project` 등 OpenGSD 자동화 워크플로우를 원활히 기동할 수 있도록 무결성을 확보함.

## [2026-06-10] feat | Step 1 Spotlight 온보딩 가이드 스킵 및 튜토리얼 다시보기 구현
- **목적**: 튜토리얼(온보딩) 진행 시 귀찮음을 방지하고 즉시 스킵할 수 있는 X(닫기) 버튼을 구현하고, 필요 시 언제든 튜토리얼을 다시 재생할 수 있도록 헤더에 복구 메뉴를 추가함.
- **변경 사항**:
  - **온보딩 스킵**: `apps/step1/modules/onboarding-manager.js`에서 툴팁 HTML 템플릿에 우측 상단 X 닫기 버튼을 주입하고 클릭 시 `cleanup()`이 실행되어 닫히며 완료로 저장되도록 수정.
  - **인스턴스 반환**: `initOnboarding` 래퍼 함수가 `IsfOnboardingManager` 인스턴스를 반환하여 외부에서 제어할 수 있도록 수정.
  - **헤더 복구 메뉴**: `shared/components/app-header.js`에 `current-step="1"` (Step 1)일 때만 드롭다운 메뉴 하단에 '💡 튜토리얼 다시보기' 버튼이 렌더링되도록 개선. 클릭 시 `request-onboarding` 커스텀 이벤트를 dispatch.
  - **이벤트 연동 및 재시작**: `apps/step1/app.js`에서 `request-onboarding` 이벤트를 수신하면 온보딩 매니저를 `reset()` 후 즉시 `start()` 하도록 바인딩.
  - **디자인 스타일**: `apps/step1/styles.css`에 온보딩 툴팁 닫기 버튼 및 콘텐츠 래퍼에 대한 Glassmorphism/호버 피드백 스타일을 보존 무결성에 맞게 이식.
- **결과**: Vite 프로덕션 빌드가 성공적으로 완료되었으며 온보딩 스킵 사용성 및 접근성이 획기적으로 향상됨.

## [2026-06-10] execute | Phase 17: Step 1 수입/지출 통장 쪼개기 및 자금 흐름 개선 구현 완료
- **목적**: Step 1 수입/지출 입력 및 자산 시각화 흐름에 '통장 쪼개기(계좌 관리)' 모델을 반영하여 실가계 금융 매핑 무결성을 구축함.
- **주요 변경 사항**:
  - **계좌 모델 및 입력 UI**: accounts 모델, default/sample preset 계좌 설정, magic mapping 규칙을 constants.js 및 input-sanitizer.js에 추가. 통장 CRUD를 처리하는 편집 UI 마크업(index.html) 및 state 관리(state.js, state-helpers.js) 완료.
  - **잉여현금 이체 배너**: 요약 카드 하단에 잉여현금(surplus > 0) 발생 시 나타나는 Glassmorphism 문장형 UI 배너 마크업 및 드롭다운 이벤트 핸들러(app.js) 개발.
  - **계좌 기반 4단계 Sankey**: 기존 대분류 노드를 계좌 노드로 대체. 수입->계좌, 계좌->세부항목 링크를 매핑하고 계좌 간의 Inflow/Outflow 과부족을 배분(Math.min)하여 내부 이체 링크(tone: "transfer")를 수학적으로 자동 연산해내는 알고리즘(sankey-builder.js, calculator.js) 구현.
- **결과**: Vite 프로덕션 빌드(npm run build) 정상 완료 및 760px 모바일 화면 반응형 검사 회귀 방지 보증.

## [2026-06-10] plan | Phase 17: Step 1 수입/지출 통장 쪼개기 및 자금 흐름 개선 계획 수립
- **목적**: Phase 17 구현을 위한 구체적인 기술 설계 및 검증 계획(PLAN.md, VALIDATION.md)을 완성함.
- **주요 내용**:
    - **통장(계좌) 관리 UI/모델**: `constants.js`, `input-sanitizer.js`, `state.js` 등에 계좌 모델 및 기본 프리셋을 이식하고, 각 항목 편집 시 매핑 드롭다운 구현 계획 수립.
    - **잉여현금 자동 이체**: 요약 카드 하단에 문장형 배너 및 드롭다운 선택 UI 배치 설계.
    - **중간 계좌 레이어 Sankey**: 대분류 대신 계좌 노드를 렌더링하고, 계좌 간 Inflow/Outflow 수지 연산을 통해 자금 부족처로의 내부 이체 흐름선을 자동 도출하는 알고리즘 구체화.
- **결과**: `17-PLAN.md` 및 `17-VALIDATION.md` 생성 완료.

## [2026-06-10] ingest | Phase 17: Step 1 수입/지출 통장 쪼개기 및 자금 흐름 개선 기획 수립
- **목적**: 사용자가 수입을 단순 금액으로 입력하던 방식에서 벗어나, 급여계좌/생활비계좌/주식계좌 등의 '통장 쪼개기' 흐름을 설계하고 잉여현금의 주식계좌 이체 경로를 시각화할 수 있도록 기획함.
- **주요 합의사항**:
    - **계좌 모델(1-A)**: 수입/지출/저축/투자 항목을 커스텀 계좌(기본 프리셋 제공 및 자동 매직 매핑 적용)에 연결하여 입력 복잡도 최소화.
    - **잉여 이체(2-A)**: 수입/지출 요약 카드 하단에 자연어 기반 한 줄 UI (`💡 남는 잉여현금 [X만 원]은 [주식계좌 ▾]로 자동 이체됩니다.`)를 통한 이체 목적지 지정.
    - **Sankey 융합(대안 A)**: 기존의 대분류 노드 자체를 커스텀 계좌 노드로 대체하여, 5단계로 확장하지 않고 4단계 레이어 구조(`수입원 -> 중간 계좌 -> 세부 항목`)를 유지함으로써 UX 단순성 보존.
- **결과**: `17-CONTEXT.md` 및 `17-DISCUSSION-LOG.md`를 기획 산출물로 확보하고 마일스톤에 Phase 17 사양 추가.

## [2026-06-09] feat | Step 1 생활비 고정비 기본 그룹 세분화 및 묶음 규칙 적용
- **목적**: 사용자의 가계부 가시성 개선을 위해 기본 생활비 항목 중 고정비 성격을 가진 항목들을 `생활비-고정비-@@@` 형태의 기본 그룹으로 묶어 분류 효율성을 극대화함.
- **변경 사항**:
    - **고정비 그룹핑**: `apps/step1/modules/constants.js` 및 `presets.js` 내 기본 생활비 설정 항목들을 수정하여 주거비(대출상환)는 `생활비-고정비-주거비`로, 공과금(관리비/수도세/가스비/전기세)은 `생활비-고정비-공과금`으로, 통신비는 `생활비-고정비-통신비`로, 보험료는 `생활비-고정비-보험료`로 자동 그룹핑되도록 수정.
    - **초기값 합산 보정**: 기본 지출 구성이 세분화됨에 따라 `DEFAULT_INPUTS.monthlyExpense` 디폴트 금액을 145만 원에서 **172만 원**으로 합산 상향 보정.
- **결과**: 가계 흐름 다이어그램(Sankey) 및 목록 렌더링 시 고정비가 일목요연하게 묶여 노출되도록 사용성 개선.

## [2026-06-09] feat | Step 2 적극형 레버리지 프리셋 추가 및 자산규모 경고 가이드 구현
- **목적**: 적극형 레버리지/알파 안건(1~4안)을 Step 2 프리셋에 이식하여 실전 포트폴리오(QLD, TQQQ, VOO, QQQM, SGOV 등) 시뮬레이션 기반을 마련하고, 자산 규모가 작을 때의 배당 투자 경고 장치를 구축함.
- **변경 사항**:
    - **적극형 프리셋 추가**: `apps/step2/modules/ui-controller.js` 및 `index.html`에 QLD, TQQQ, VOO, QQQM, SGOV 등 기초자산 기대수익률을 적용한 '적극적 레버리지/알파형 1~4안' 프리셋 신설.
    - **1억 미량 자산 경고 배너**: 총 운용 자금 규모(초기 투자금 + 시뮬레이션 기간 동안의 투입액)가 1억 원(10,000 만원) 미만일 때, 지수 추종/성장주 위주의 투자를 추천하는 조건부 경고 안내 배너(`dividendWarningBanner`) 구현 및 UI 연동.
- **결과**: 투자자의 다양한 위험 성향 반영 최적화 및 건전한 자산 형성 가이드라인 제공.

## [2026-06-09] fix | Step 1 모듈화 리팩토링 유실 함수 복구 및 런타임 안정화 (v0.11.2)
- **목적**: Step 1 모듈화 과정에서 유실되어 런타임 흰 화면 및 ReferenceError를 일으키던 4종의 필수 헬퍼 함수를 복원하고 앱 동작을 정상화함.
- **변경 사항**:
    - **헬퍼 함수 복원**: `apps/step1/app.js`에서 유실되었던 `hasShareState`, `bindReadonlyAdvancedNavigation`, `dismissViewModeGuide`, `switchToNormalMode` 함수 정의 복구.
    - **정상 작동 검증**: Playwright를 이용해 `npm run dev` 실행 환경에서 런타임 오류가 없음을 재검증 완료.
- **결과**: Step 1 메인 화면 렌더링 및 공유 데이터 연동 기능 완벽 복구.

## [2026-05-12] fix | 코드 리뷰 피드백 반영 및 시스템 안정화 (v0.9.51)
- **목적**: v0.9.50에서 지적된 시뮬레이션 논리 오류, Step 3 의존성 누락, 데이터 유실 위험 등을 해결하여 시스템 신뢰도를 확보함.
- **변경 사항**:
    - **시뮬레이션 엔진**: PR 모드 시 하락장에서의 자산 잔고 감소 오류를 수정하고, 발생한 배당금을 현금 잔고에 합산하여 순자산 정합성 확보.
    - **Step 3 (Rebalancing)**: `IsfUtils` 임포트 누락 해결, UI 단위 중복 제거, 스냅샷 내 투자 여력(Capacity) 추출 로직 보완.
    - **데이터 무결성**: 부부 데이터 병합 시 [나]/[너] 접두사 중복 방지 로직 도입 및 파트너 데이터 Sanitize 강제.
    - **보안/안정성**: 스마트 등록 시 편집기 활성화 여부 체크 로직 추가 및 `package.json` 패치 버전 업데이트.
- **결과**: 코드 리뷰 지적 사항 전수 해결 및 Phase 11 안정화 단계로의 연착륙 성공.

## [2026-05-06] bump | Step 2 프리셋 명칭 표시 UI 개선 및 패치 (v0.9)
- **목적**: 시뮬레이션 가성 설정 시 적용된 프리셋을 명확히 인지할 수 있도록 UI를 개선하고 버전을 패치함.
- **변경 사항**:
    - **Step 2 UI**: '프리셋 조합' 레이블 우측에 선택된 프리셋 명칭(Badge) 표시 로직 추가.
    - **커스텀 감지**: 입력값 직접 수정 시 프리셋 명칭 자동 제거 로직 구현.
    - **버전 패치**: 전역 버전을 v0.9에서 v0.9로 업데이트 (package.json, sw.js, utils.js 등 6종).
- **영향**: 사용자 경험(UX) 개선 및 전략 선택 상태의 가독성 향상.

## [2026-05-06] ingest | Modern Hybrid 아키텍처 전환 및 금융 시뮬레이션 고도화 완결 (v0.9)
- **목적**: `feat/modern-poc` 브랜치의 모든 성과를 `main`으로 머지하고, 프로젝트를 현대적 인프라와 정밀한 금융 로직이 결합된 v0.9 체제로 전환함.
- **주요 변경사항**:
    - **통합 머지**: Vite, TS, Tailwind v4 기반의 현대화된 인프라를 메인 라인에 통합.
    - **금융 정책 확립**: 4% 안전 마진 정책을 수립하고 `TAX_CONFIG`를 통해 소득세 계산 로직을 투명화함.
    - **버전 관리**: 프로젝트 전반의 버전을 v0.9으로 일원화하고 피처 브랜치 정리 수행.
    - **지식 자산화**: `Financial_Taxation_Reference.md`를 포함한 위키 노드들을 최신화하고 상호 참조 구조 강화.
- **결과**: v1.0 정식 릴리스를 향한 기술적/기획적 토대 완성.

## [2026-05-06] ingest | No-build 원칙 완화 및 Modern Hybrid 전략 공식화
- **목적**: v0.8.x 현대화 피벗(Vite/TS/Tailwind 도입)에 맞춰 프로젝트 핵심 원칙인 'No-build 보존'을 'No-build 지향 (Modern Hybrid)'으로 완화하고 관련 지식 베이스를 현행화함.
- **주요 변경사항**:
    - **원칙 수정**: `GEMINI.md` 및 `Operating_Principles.md`의 No-build 강제 조항을 '지향'으로 수정하고, 타입 안정성과 DX를 위한 현대적 인프라 수용을 명시함.
    - **에이전트 지침 업데이트**: `isf-developer`, `isf-planner` 에이전트 정의 파일에 Modern Hybrid 전략(Vite/TS 활용 및 레거시 호환성 유지)을 반영함.
    - **표준 문서 현행화**: `DESIGN.md`, `README.md`, `PROJECT.md` 및 `orchestration` 스킬 내의 No-build 관련 제약 사항을 현실화된 전략에 맞게 교정함.
- **결과**: 인프라 변화와 프로젝트 헌법 간의 정합성이 확보되었으며, 에이전트가 현대화된 도구를 제약 없이 활용할 수 있는 근거를 마련함.

## [2026-05-04] fix | 버전 표기 변수화 및 관리 체계 일원화 (v0.8.3)
- **목적**: UI 우측 상단에 하드코딩되어 있던 버전 표기를 변수화하고, 런타임 버전 관리 지점을 `IsfUtils`로 일원화하여 유지보수성을 향상함.
- **주요 변경사항**:
    - **중앙 집중화**: `shared/core/utils.js`에 `IsfUtils.APP_VERSION` 상수를 도입하여 앱 전체의 런타임 기준점으로 설정.
    - **UI 동적화**: `AppHeader` 컴포넌트가 `IsfUtils.APP_VERSION`을 참조하도록 수정하여 버전 변경 시 자동으로 UI에 반영되도록 개선.
    - **진입점 최적화**: `apps/step1/app.js` 및 `apps/step2/app.js`에서 PWA 매니저 초기화 시 하드코딩된 문자열 대신 중앙 변수를 사용하도록 교체.
- **결과**: 버전 업데이트 시 관리 포인트가 줄어들었으며, UI와 실제 앱 버전 간의 불일치 가능성을 원천 차단함.

## [2026-05-02] fix | Step 1 모바일 레이아웃 복구 및 입력 가시성 확보 (v0.7.6)
- **목적**: 모바일 환경(760px 이하)에서 가계 흐름 입력 패널의 FAB 및 레이아웃이 파손되어 편집이 불가능하던 치명적 결함을 해결하고 패치 버전을 릴리스함.
- **주요 변경사항**:
    - **CSS 구조 재편**: 미디어 쿼리 우선순위를 조정하고 `.controls-block`의 그리드 레이아웃이 모바일의 수직 스택을 침범하지 않도록 `min-width: 0` 및 명시적 너비 제어 적용.
    - **UI 가시성 복구**: 모바일에서 가려졌던 '접기/펼치기' 버튼과 '항목 편집 FAB'이 정상 노출되도록 레이어 순서 및 z-index 보정.
    - **Triple Sync 완결**: `sw.js`, `manifest.webmanifest`, `app.js`(Step 1/2) 버전을 v0.7.6으로 일괄 동기화하여 PWA 캐시 갱신 무결성 보장.
- **결과**: 모바일 기기에서의 사용성이 완전히 회복되었으며, 사용자 입력 데이터의 물리적 무결성이 확보됨.

## [2026-05-02] fix | 지식 위생 관리(Wiki Hygiene) 및 위키 정합성 강화 (Lint)
- **목적**: `Plan_Step3_Implementation.md` 누락 해결 및 전역 위키 노드의 링크 정합성을 확보하여 지식 엔트로피를 낮춤.
- **주요 변경사항**:
    - **인덱스 현행화**: `INDEX.md`에 `Plan_Step3_Implementation` 노드 추가 및 로드맵/구현 계획 구분 명확화.
    - **링크 표준화**: `[[NodeName.md]]` 형식의 링크에서 확장자를 제거하여 `[[NodeName]]` 컨벤션으로 일원화 (`Knowledge_Harness.md`, `Version_Management_Principles.md`).
    - **결손 링크 복구**: `archive/Plan_Step1.md` 내의 부서진 `[[TODO]]` 링크를 `TODO.md` 참조 텍스트로 수정.
    - **상호 참조 강화**: `Plan_Step3`와 `Plan_Step3_Implementation` 간의 상호 연결 노드(Related Nodes) 추가.
    - **스킬 참조 교정**: `Knowledge_Harness.md`에서 위키 노드가 아닌 `wiki-librarian` 스킬을 가리키는 대괄호 제거.
- **결과**: 지식 베이스의 구조적 무결성이 회복되었으며, 에이전트의 컨텍스트 탐색 효율이 향상됨.

## [2026-04-30] fix | 전역 버전 정리 및 안정화 (v0.7.4)
- **목적**: 0.7.3과 0.7.4 간의 버전 혼선을 정리하고, 모든 구성 요소의 버전을 최신 패치 버전인 v0.7.4로 일원화함.
- **주요 변경사항**:
    - **버전 동기화**: `sw.js`, `manifest.webmanifest`, `apps/step1/app.js`, `apps/step2/app.js` 및 모든 하위 모듈의 버전을 v0.7.4로 확정.
    - **지식 베이스 현행화**: `Project_History.md`, `TODO.md`, `Architecture_Reference.md` 등 위키 문서 내의 버전 정보를 v0.7.4로 업데이트.
    - **시스템 무결성 확인**: Triple Sync가 정상적으로 작동하며, PWA 업데이트가 최신 버전을 지시하고 있음을 확인함.
- **결과**: 프로젝트 전반의 버전 정합성이 확보되어 관리 엔트로피가 해소됨.

## [2026-04-30] fix | 디자인 시스템 토큰 적용 및 UI 무결성 강화 (v0.7.3)
- **목적**: `DESIGN.md`에 정의된 디자인 토큰(색상, 타이포그래피, 간격)을 `shared/styles/step-theme.css`에 전면 적용하여 시각적 일관성을 확보하고, 물리적 무결성을 검증함.
- **주요 변경사항**:
    - **토큰화**: 하드코딩된 폰트 크기 및 간격 값을 `--text-caption`, `--sp-md` 등의 CSS 변수로 교체.
    - **UX 강화**: 버튼 클릭 시 `transform: scale(0.96)` 액티브 상태 추가하여 물리적 피드백 구현.
    - **Triple Sync**: `sw.js`, `manifest.webmanifest`, `app.js`(Step 1/2)의 버전을 0.7.3으로 일괄 동기화하여 PWA 업데이트 무결성 보장.
    - **무결성 검증**: 브라우저 서브에이전트를 통해 데스크톱 및 모바일(760px 이하) 레이아웃의 정상 동작과 미디어 쿼리 보존 여부를 최종 확인.
- **결과**: 디자인 시스템 사양과 실제 구현체 간의 정합성이 완벽히 일치하며, PWA 환경에서의 안정적인 배포 준비 완료.

## [2026-04-24] update | Step 2 자동 연동 로직 교정 (Sync by Default)
- 기존의 수동 선택 방식에서 '자동 반영 후 수정 시 확인'으로 로직 역전 반영
- 사용자가 진입 시 즉시 Step 1 결과가 반영된 그래프를 보도록 가이드 업데이트

## [2026-04-24] ingest | 데이터 통합 및 입력 재사용 철학 동기화 (Single Hub & Input Reuse)
- 모든 Step이 IsfStorageHub를 SSOT로 공유하고 입력값을 재사용하도록 isf-planner 지침 및 아키텍처 참조 업데이트



## [2026-04-24] fix | v0.7.2 타임스탬프 파싱 버그 수정 및 Step 2 자동 동기화 도입
- **목적**: Step 1 저장 시 날짜가 1970년으로 표시되는 오류를 해결하고, Step 2 진입 시 사용자의 편의성을 위해 Step 1 데이터를 자동으로 불러오도록 개선.
- **주요 변경사항**:
    - **타임스탬프 무결성**: `shared/core/utils.js`의 `formatTimestamp` 함수가 ISO 8601 문자열을 인식하지 못하던 문제(parseInt 사용)를 `Date.parse` 병행 사용으로 해결하여 1970년 표기 버그 완치.
    - **Step 2 UX 개선**: `apps/step2/modules/step1-connector.js`의 `checkStep1SyncData` 로직을 고도화하여, Step 2의 월 투자액이 0일 경우 Step 1의 최신 투자 데이터를 자동으로 연동하도록 구현.
- **결과**: 데이터 관리의 신뢰도를 높이고, 사용자 입력을 최소화하는 지능형 연동(Intelligent Sync) 기반 마련.

## [2026-04-24] fix | v0.7.2 전역 버전 동기화 및 Triple Sync 완결
- **목적**: v0.7.1 패치 이후 누락된 버전 정보들을 전수 조사하여 `Triple Sync Rule`에 따른 정합성 확보.
- **주요 변경사항**:
    - **Triple Sync 완결**: `sw.js`(APP_VERSION), `manifest.webmanifest`(version), `apps/step1/app.js`(appVersion)를 0.7.2로 일치시켜 PWA 캐시 갱신 무결성 보장.
    - **Step 2 동기화**: `apps/step2/app.js` 및 관련 모든 모듈(7종)의 파일 헤더와 내부 `appVersion` 상수를 0.7.2로 업데이트.
    - **지식 위생 관리**: `Architecture_Reference.md` 및 `Version_Management_Principles.md` 내의 구버전(v0.7.1) 흔적을 현행화.
- **결과**: 인프라, 앱 로직, 지식 베이스가 모두 v0.7.2로 일원화되어 버전 관리의 엔트로피가 해소됨.

## [2026-04-24] fix | v0.7.1 Step 1/2 안정화 및 리브랜딩 보강 (v0.7.1)
- **목적**: v0.7.0 이후 발견된 파일 누락(404), CSS 깨짐, 리브랜딩 미비 사항을 해결하여 앱의 완성도와 안정성 확보.
- **주요 변경사항**:
    - **Step 1 브리지 복구**: `storage-manager.js`를 복원하여 로컬 저장(`IsfStorageHub` 연동) 및 공유 링크(`sid`) 로딩 기능 정상화.
    - **리브랜딩 완결**: 공통 헤더의 "포트폴리오"를 "배당 시뮬"로 변경하고, Step 2의 섹션 제목을 "배당 성장 시뮬레이션"으로 수정.
    - **CSS 무결성 확보**: `apps/step2/styles.css`에 누락된 시뮬레이션 대시보드(탭, 툴팁, 결과 표 등) 스타일을 추가하고 레거시 클래스 정리.
    - **스타일 가이드라인 수립**: `GEMINI.md`에 BEM/Snake 표기법 강제 규칙을 추가하여 향후 디자인 시스템의 일관성 보장.
- **결과**: v0.7.0에서 발생한 치명적 버그 해결 및 "배당 시뮬레이션" 중심의 UX 정체성 확립.

## [2026-04-24] merge | v0.7.0 최종 머지 및 브랜치 정리 완료
- **내용**: `feature/step2-dividend-sim-only` 브랜치를 `main`으로 성공적으로 머지하고 피처 브랜치 삭제 완료.
- **상태**: v0.7.0 정식 릴리스 상태로 전환. 향후 실사용 피드백 기반의 패치 버전(v0.7.x) 운영 단계 진입.
- **다음 단계**: Step 3(포트폴리오 시각화) 기획 및 v0.7.x 안정화 패치.

## [2026-04-23] fix | v0.7.0 데이터 마이그레이션 체계 구축 및 저장소 안정화
- **원인**: 
    - **하위 호환성 단절**: Step 2 리브랜딩으로 스토리지 키가 변경되어 기존 데이터 및 백업 이력 접근 불가.
    - **초기화 경합**: `IsfStorageHub`가 전역 `IsfUtils`에 직접 의존하여 스크립트 로드 순서에 따라 `ReferenceError` 발생 위험.
- **조치**: 
    - **자동 마이그레이션**: `IsfStorageHub.ensureMigration` 및 `IsfBackupManager.migrateAppKey`를 구현하여 구버전 키(`my-portfolio-flow`)의 데이터를 신버전 키(`my-dividend-simulation`)로 안전하게 자동 이전.
    - **독립성 확보**: `hub-storage.js` 내부에 전용 ID 생성기(`_createId`)를 내장하여 전역 유틸리티 의존성 제거.
    - **폴백 보강**: Step 2 유틸리티 브리지(`utils.js`)에 누락된 메서드(`sanitizeRate`, `roundTo`)를 추가하고 전역 사양과 동기화.
- **결과**: 기존 사용자 데이터 보호와 앱 초기화 안정성을 동시에 확보하며 v0.7.0 통합 저장소 체제 안착.

## [2026-04-23] release | 통합 스토리지 허브(IsfStorageHub) 도입 및 데이터 연동 최적화 (v0.7.0)
- **목적**: 파편화된 스토리지 로직을 `IsfStorageHub`로 단일화하여 데이터 정합성을 높이고, Step 1/2 간의 데이터 공유를 IndexedDB 직접 참조 방식으로 단순화함.
- **주요 변경사항**:
    - **중앙 집중형 허브**: `shared/storage/hub-storage.js`를 리팩터링하여 LocalStorage(영속성), IndexedDB(스냅샷/포트폴리오), BackupManager(자동/수동 백업)를 통합 관리.
    - **Async/Await 전환**: 기존 콜백/프로미스 혼용 방식을 `async/await` 및 `perform` 헬퍼 패턴으로 통일하여 코드 가독성 및 에러 핸들링 강화.
    - **직접 데이터 브릿지**: Step 2에서 별도의 `bridge` 테이블 없이 `step1Snapshots`의 최신 데이터를 직접 조회하여 '월 투자 금액'을 연동.
    - **안전한 뷰 모드 저장**: 공유 링크(View Mode)에서 '내 기기에 저장' 시, 기존 로컬 데이터를 즉시 백업(`auto/view-save`)한 후 덮어쓰는 안전장치 구현.
    - **레거시 지원**: 구버전 공유 링크(`sid`) 호환성을 위해 `apps/step1/modules/storage-manager.js`에 최소한의 레거시 로딩 로직 보존.
- **결과**: 데이터 유실 위험 최소화, 앱 간 연동 속도 향상, 스토리지 관련 유지보수 효율 극대화.

## [2026-04-23] release | Step 2 배당 시뮬레이션 특화 개편 및 Step 3 분리 계획 (v0.7.0)
- **목적**: Step 2의 복잡도를 낮추고 '배당 성장 시뮬레이션'이라는 핵심 가치에 집중하며, 포트폴리오 구성 및 시각화 기능을 별도의 Step 3로 격상하여 전문화함.
- **주요 변경사항**:
    - **Step 2 기능 슬림화**: 기존의 계좌/종목 비중 관리 및 도넛/Sankey 차트 기능을 완전히 제거하고, Step 1에서 넘어온 '월 투자 여력' 기반의 순수 시뮬레이션 대시보드로 전환.
    - **모듈 최적화**: `renderers.js`, `state.js`, `calculator.js` 등에서 포트폴리오 비중 계산 및 렌더링 로직 대거 삭제하여 코드 응집도 향상.
    - **Step 3 신설 계획**: Step 1의 실시간 데이터를 바탕으로 동적 포트폴리오를 구성하고 Target vs Actual 격차를 분석하는 기능을 Step 3로 이관하기로 결정.
    - **브랜치 전략**: `feature/step2-dividend-sim-only` 브랜치를 통해 안전하게 리팩터링 수행 및 검증.
- **결과**: Step 2의 사용 목적 명확화 및 대규모 기능 확장을 위한 단계별 아키텍처 로드맵(Step 1 -> 2 -> 3) 확립.

## [2026-04-22] fix | 단위 정합성(만원/원) UI 및 데이터 저장 표준 강화 (v0.6.1)
- **목적**: 사용자의 인지 편의성(만원 단위 입력/표시)과 시스템의 정밀도(원 단위 저장/계산)를 완벽히 분리하고, 전역 포맷터를 통해 일관된 UI 경험 제공.
- **주요 변경사항**:
    - **전역 포맷터 개선**: `IsfUtils.formatMoney`가 '원' 데이터를 받아 '만원' 문자열(예: "350 만원")로 변환하도록 수정.
    - **Step 1/2 UI 동기화**: 모든 요약 카드, 테이블 헤더, 툴팁에 `(만원)` 단위를 명시하고 포맷터를 적용하여 단위 혼선 제거.
    - **데이터 정책 SSOT**: `Data_Model_Reference.md` 및 `GEMINI.md`에 "UI: 만원, 저장: 원" 원칙을 명문화하여 지식 자산화.
- **결과**: 금액 입력 및 조회 시의 단위 오류 원천 차단 및 앱 전반의 시각적 정합성 확보.

## [2026-04-22] ingest | Step 1 지출 내역 비교 막대 그래프 기능 정의 및 이슈 생성 (#4)
- **내역**: 시계열 지출 내역 비교를 위한 요구사항 정의 및 GitHub Issue(#4) 생성 완료.
- **주요 내용**: X축(범주), Y축(금액), 상단 범례를 포함한 그룹형 막대 그래프 설계안 확정.
- **결과**: `TODO.md` 및 작업 로그 동기화 완료.

## [2026-04-22] release | Minor Up: Step 2 리팩터링 및 Sankey/배당 시뮬레이션 고도화 (v0.6.0)
- **목적**: Step 2의 대규모 기능 고도화 및 아키텍처 개선 완료에 따른 Minor 버전 업그레이드 및 전역 동기화.
- **조치**: 
    - **Quad Sync 실행**: `manifest.webmanifest`, `sw.js`, `apps/step1/app.js`, `apps/step2/app.js`의 버전을 **0.6.0**으로 일관되게 업데이트.
    - **안정성 보강**: `renderSankey` 런타임 에러 방지(Optional Chaining) 및 데이터 정규화(Deep Merge) 로직 추가 적용.
    - **문서 동기화**: `TODO.md` 및 주요 지식 노드의 버전 마커를 0.6.0으로 갱신하여 관리 체계 일원화.
- **결과**: PWA 환경에서의 강제 업데이트 유도 및 신규 기능(Sankey, 고성능 배당 엔진)의 공식 릴리스 완료.

## [2026-04-22] fix | Step 2 렌더링 안정성 및 데이터 정규화 보완 (v0.6.0)
- **원인**: 
    - **Sankey 런타임 에러**: `state.draft.accounts` 또는 `allocations`가 미정의된 상태에서 `length` 참조 시 크래시 발생 가능성 확인.
    - **필드 누락**: 과거 버전 데이터 로드 시 `dividendSim` 등 신규 필수 필드가 누락되어 시뮬레이션 계산 중 오류 발생.
- **조치**: 
    - **안전 장치**: `renderSankey` 내 배열 접근 시 옵셔널 체이닝(`?.`) 및 기본값(`|| []`)을 적용하여 데이터 불완전 상태 대응.
    - **정규화 강화**: `normalizeLoadedPortfolio`에서 `createEmptyDraft`와 딥 머지(Deep Merge)를 수행하여 모든 필수 필드 구조 보장.
- **결과**: Step 2 모듈화 체제의 최종 안정성 확보 및 v0.6.0 정식 릴리스 준비 완료.

## [2026-04-21] ingest | 지식 위생 관리(Wiki Hygiene) 및 위키 정기 정화
- **목적**: v0.5 리팩토링 이후 혼재된 과거 계획(Plan)과 현재 진실(SSOT) 간의 충돌 제거 및 지식 엔트로피 감소.
- **조치**: 
    - **스킬 강화**: `wiki-librarian` 스킬에 '지식 위생 관리' 원칙 명문화.
    - **아카이브 실행**: `Plan_Step1/2`, `UI_UX_Overhaul_v0.3/4` 노드에 `[ARCHIVED]` 배너 추가 및 최신 문서 링크 연결.
    - **지식 보강**: `Step2_Modularization_Refactoring` 노드 신규 생성 및 `Architecture_Reference` 최신화.
    - **인덱스 재편**: `INDEX.md`를 '현행 설계'와 '과거 기록' 섹션으로 이원화하여 에이전트 오작동 방지.
    - **이력 슬림화**: `Project_History` 상세 내역을 `log.md`로 일원화.
- **결과**: 에이전트의 컨텍스트 로딩 정확도 향상 및 지식 베이스의 SSOT(Single Source of Truth) 신뢰도 회복.

## [2026-04-21] feature | Step 2 모듈화 및 고성능 배당 시뮬레이션 엔진 도입 (v0.5.12)
- **목적**: Step 2 포트폴리오 관리 시스템의 모듈화 리팩터링 완료 및 투자 고도화를 위한 시뮬레이션 엔진(DRIP/성장률) 및 Sankey 흐름 시각화 통합.
- **주요 변경사항**:
    - **7개 전문 모듈 분리**: `apps/step2/modules/` (constants, dom, state, calculator, renderers, bridge, storage-handler) 체계 구축.
    - **배당 시뮬레이션 고도화**: 배당 성장률(DGR), 자본 성장률(CGR), 배당 재투자(DRIP), 인플레이션 반영 실질 가치 연산 엔진 탑재.
    - **Sankey 흐름도 추가**: `월 투자 여력 -> 계좌 -> 종목`으로 이어지는 자금 흐름 시뮬레이션 탭 구현.
    - **PWA 동기화**: `sw.js` 및 `manifest.webmanifest` 버전을 0.5.12로 업데이트하고 모듈 파일들에 대한 오프라인 캐싱 지원.
- **결과**: Step 2의 아키텍처 결함 해결 및 포트폴리오 관리 도구로서의 기능적 완성도 대폭 향상.

## [2026-04-21] fix | Step 1 단위 중복 계산 및 Step 2 브리지 배너 표시 오류 해결 (v0.5.11)
- **원인**: 
    - **단위 중복 계산**: `sanitizeInputs` 함수에서 반환되는 객체에 `modelVersion: 10` 속성이 누락되어, 후속 입력이나 저장 시 `migrateInputsToWon` 로직이 데이터를 레거시(만원 단위)로 오인하여 매번 10,000배씩 중복 계산하는 버그 발생.
    - **브리지 배너 미제거**: Step 2의 `.bridge-banner` 클래스가 `display: flex`를 명시적으로 사용하고 있어, `hidden` 속성만으로는 사라지지 않는 CSS 명시성(Specificity) 문제 발생. 또한 데이터 로드 중 오류 발생 시 배너를 닫는 로직이 실행되지 않음.
- **조치**: 
    - **정규화 보완**: `input-sanitizer.js`의 `sanitizeInputs` 반환 객체에 `modelVersion: 10`을 명시적으로 포함하여 "원 단위" 정규화 상태를 보장함.
    - **CSS 강화**: `styles.css`에 `.bridge-banner[hidden] { display: none !important; }`를 추가하여 강제적으로 배너를 숨김 처리함.
    - **로직 안정화**: Step 2의 `loadStep1Data` 클릭 핸들러에 `try...catch...finally` 구문을 도입하여 데이터 로드 성공 여부와 관계없이 배너가 항상 닫히도록 개선함.
- **결과**: Step 1 금액 입력 시의 기하급수적 수치 폭증 해결 및 Step 2 브리지 연동 UX의 시각적 피드백 무결성 확보.

## [2026-04-19] fix | Step 1 데이터 연동 무결성 및 패널 기능 복구 (v0.5.8)
- **원인**: 
    - **항목 삭제 불가**: `handleItemClick`에서 `event.target instanceof HTMLElement` 체크로 인해 SVG 아이콘이나 내부 요소를 클릭할 경우 삭제 로직이 무시됨.
    - **시각적 비동기화**: 항목 편집기에서 개별 항목 수정 후 '적용'을 눌러도, 화면 상단의 읽기 전용 '간편 입력' 필드(월 생활비 등)가 즉시 업데이트되지 않아 데이터 연동이 끊긴 것으로 오인됨.
    - **모바일 토글 노출 오류**: 초기 로딩 시 `syncMobileInputsPanelVisibility`가 호출되지 않아 데스크톱 환경에서도 모바일 전용 '접기' 버튼이 비정상적으로 노출됨.
- **조치**: 
    - **클릭 로직 개선**: `target.closest`를 사용하여 SVG 아이콘 클릭 시에도 상위 삭제 버튼의 데이터를 정확히 식별하도록 수정.
    - **실시간 UI 동기화**: `syncDerivedMonthlyInputsToUi` 함수를 추가하고 `markPendingChanges` 호출 시마다 읽기 전용 합산 필드들을 강제 업데이트하여 시각적 무결성 확보.
    - **초기화 보정**: `init` 과정에 모바일 레이아웃 동기화 로직을 포함하여 환경별 최적화된 초기 UI 보장.
    - **버전 업데이트**: `sw.js`, `manifest`, `app.js`(Step 1/2) 등 모든 구성 요소의 버전을 **0.5.8**로 동기화.
- **결과**: 항목 삭제 및 데이터 연동 피드백 정상화, 앱 초기 진입 시 레이아웃 무결성 확보.

## [2026-04-19] fix | Step 1 초기화 오류 및 모바일 UX 개선 패치 (v0.5.5)
- **원인**: 
    - **Sankey null 참조**: 앱 초기화(`init`) 과정에서 데이터(`snapshot`)가 생성되기 전에 `syncSankeyZoomUi`가 호출되어 `renderSankey`에서 `null` 속성 접근 오류 발생.
    - **미정의 함수 호출**: 레이아웃 변경 시 호출되는 `syncAllItemEditorUi` 함수가 정의되지 않아 화면 회전/리사이즈 시 JS 실행 중단.
    - **점프 네비게이션 피드백 부족**: 간편 입력의 (자동합산) 필드 클릭 시 고급 설정 탭이 열리지만, 화면 스크롤 이동이 없어 사용자가 동작 여부를 인지하기 어려움.
- **조치**: 
    - **안전 로딩**: `syncSankeyZoomUi` 내부에 `snapshot` 존재 여부 체크 로직 추가.
    - **함수 교체**: `syncAllItemEditorUi` 호출을 실제 정의된 `syncMobileItemEditorFab`으로 교체하여 런타임 오류 해결.
    - **UX 강화**: `navigateToAdvancedGroup`에 `scrollIntoView`를 적용하여 클릭 시 해당 영역으로 자동 스크롤되도록 개선.
    - **버전 업데이트**: `sw.js`, `manifest`, `app.js` 등 모든 구성 요소의 버전을 **0.5.5**로 동기화.
- **결과**: 앱 초기 로딩 및 화면 전환 안정성 확보, 모바일 환경의 탐색 편의성 증대.

## [2026-04-17] fix | Step 1 헤더 버튼 복구 및 UI 정리 (v0.5.3)
- **원인**: 
    - **바인딩 타이밍 문제**: `app.js` 초기화 시점에 커스텀 엘리먼트(`app-header`, `data-hub-modal`)가 아직 DOM에 준비되지 않아 `null` 참조로 인한 이벤트 바인딩 실패 발생.
    - **중복 UI**: 기능을 대체한 기존 플로팅 버튼(`#jumpToInputs`)이 남아있어 사용자 혼선 야기.
    - **리소스 로드 에러**: `index.html` 내 아이콘 경로가 잘못 지정되어 PWA 환경에서 리소스 로드 실패.
- **조치**: 
    - **안정성 강화**: `bindModalEvents` 시작 시점에 요소를 다시 찾는 로직을 추가하여 요소 획득 보장 및 바인딩 성공 확인.
    - **UI 정리**: 불필요해진 `#jumpToInputs` 버튼과 관련 로직(`dom.js`, `app.js`)을 전면 제거.
    - **리소스 교정**: 아이콘 경로를 `../../icons/`로 정규화하여 렌더링 및 캐싱 이슈 해결.
- **결과**: 상단 헤더 기능을 통한 데이터 관리 접근성 정상화 및 UI 간소화 달성.


이 파일은 프로젝트의 변화, 지식의 습득, 주요 결정을 시간 순으로 기록하는 append-only 로그입니다.

## [2026-04-17] fix | Step 1 통합 편집 UX 및 모바일 FAB 지원 패치 (v0.5.2)
- **원인**: 
    - **편집 프로토콜 불일치**: 수입 항목만 별도의 편집 모드 없이 즉시 수정 가능하여 다른 항목(지출/저축/투자)과의 UX 일관성 결여 및 의도치 않은 데이터 변경 위험 존재.
    - **초기화 크래시**: `state.js` 내 `itemEditors` 객체에 `income` 키가 누락되어 수입 항목 상호작용 시 `ReferenceError` 발생.
    - **UI 요소 누락**: 리팩토링 과정에서 각 항목별 정렬 셀렉터(`select`)가 HTML에서 누락되어 정렬 기능 상실.
    - **모바일 접근성**: 항목 편집 모드 진입 시 모바일 화면 하단에서 편집 액션을 수행할 수 있는 FAB(Floating Action Button) 연동 부족.
- **조치**: 
    - **UX 통일**: 모든 수입 항목에 "항목 편집" 버튼 및 편집 모드 프로토콜을 적용하여 `readonly` 해제 후 수정 가능하도록 통일.
    - **안정성 확보**: `state.itemEditors.income` 스키마를 추가하여 런타임 크래시 해결 및 `renderSankey` 호출 시 전역 상태의 정렬 모드를 명시적으로 전달하도록 수정.
    - **UI 복구**: 생활비/저축/투자 헤더에 누락되었던 정렬 셀렉터들을 모두 복구하고 `dom.js` 및 이벤트 리스너에 재연동.
    - **모바일 강화**: `syncMobileItemEditorFab` 및 `getActiveItemEditorGroupKey`를 구현하여 모바일 뷰(760px 이하)에서 활성화된 편집 그룹에 맞는 FAB 노출 및 액션 바인딩.
- **결과**: 앱 전반의 편집 인터페이스 일관성 확보 및 모바일 조작 편의성 증대, 초기화 단계의 치명적 오류 해결.


## [2026-04-17] ingest | Minor Up 브랜치 전략 강화 및 위키 동기화 정책 추가
- **원인**: 대규모 변경 시 메인 브랜치 무결성 보호 및 지식의 영속적 최신화 필요성 증대.
- **조치**: 
    - `GEMINI.md` 및 `orchestration` 스킬에 "Minor Up 발생 시 피처 브랜치 필수 사용" 규칙 명문화.
    - `wiki-librarian` 스킬에 "정책 변경 시 위키 노드 동기화 필수" 절차 추가.
    - `Version_Management_Principles.md` 위키 노드에 강화된 브랜치 전략 반영.

## [2026-04-17] ingest | GEMINI.md 지식 슬림화 및 위키 이관 완료
- **내역**: `GEMINI.md`의 `Gemini Added Memories` 섹션을 삭제하고 해당 지식을 `[[Version_Management_Principles]]` 노드로 일원화함.
- **효과**: "Constitutional Rules"와 "Operating Policies"의 분리를 명확히 하여 지식 중복 관리 제거.

## [2026-04-17] ingest | core-developer 스킬 폐지 및 지식 통폐합
- **원인**: `core-developer` 스킬이 `isf-developer` 에이전트 정의 및 `orchestration` 스킬과 과도하게 중복되어 컨텍스트 효율을 저해함.
- **조치**: 
    - **스킬 폐지**: `.gemini/skills/core-developer/` 디렉토리 전체 삭제.
    - **지식 이관**: '스프린트 계약(Sprint Contract)' 절차를 `orchestration/SKILL.md`로 이관하여 협업 프로세스에 통합.
    - **에이전트 강화**: '물리적 파일 무결성(절삭 방지)' 및 '단위 정합성 강제' 수칙을 `isf-developer.md` 에이전트 정의 파일에 직접 명문화.
    - **참조 정리**: `GEMINI.md`, `INDEX.md` 등 프로젝트 최상위 문서의 참조 링크 갱신.
- **결과**: 지식 구조 슬림화로 에이전트의 컨텍스트 사용 효율 증대 및 개발 사명 인식 명확화.

## [2026-04-17] fix | Step 1 모듈화 안정화 및 런타임 오류 패치 (v0.5.1)
- **원인**: 
    - **임포트 누락**: `modules/state.js`에서 `constants.js`의 `HASH_STATE_PARAM` 참조가 누락되어 URL 해시 기반 상태 복원 실패(ReferenceError).
    - **UI 렌더러 소실**: `app.js` 리팩터링 과정에서 수입/지출 리스트 렌더링 함수 4종이 유실되어 앱 초기화 중단.
    - **계산 로직 왜곡**: 수입 항목 합산 시 전역 합산 함수(`getMonthlyIncomeTotalMan`)가 아닌 일반 할당 함수를 호출하여 데이터 왜곡 발생.
- **조치**: 
    - **참조 정규화**: `state.js` 내 상수 임포트 구조를 바로잡고, 유실된 UI 렌더러 및 이벤트 바인딩 로직(`bindReadonlyAdvancedNavigation` 등)을 전면 복구.
    - **정합성 교정**: `handleItemInput/Click` 내 계산 함수를 `getMonthlyIncomeTotalMan`으로 교체하여 단위 및 카테고리별 정합성 확보.
    - **UI 복구**: `renderAllocationItemHtml`의 메타 정보(그룹/만기) 표시 로직 및 백업 저장소 준비 상태(`backupStoreReady`) 전환 로직 복구.
- **결과**: Step 1 모듈화 체제 안정화 및 기능 무결성 회복.

## [2026-04-17] feature | Step 1 Monolith 리팩터링 및 No-build 모듈화 (v0.5.0)
- **목적**: 4,600행에 달하는 `apps/step1/app.js`의 비대화를 해소하고 유지보수성 및 LLM 컨텍스트 효율을 극대화.
- **주요 변경사항**:
    - **11개 전문 모듈 분리**: `apps/step1/modules/` 디렉토리를 생성하고 책임에 따라 로직(상수, DOM, 상태, 정합성, 포맷터, 계산기, 브리지 등)을 11개의 파일로 분해.
    - **경량 컨트롤러 전환**: 기존 `app.js`를 500행 규모의 오케스트레이션 컨트롤러로 변모시키고 `type="module"` 선언.
    - **Shared 유틸리티 통합**: `debounce`, `roundTo`, IndexedDB 공통 로직을 `shared/core/utils.js`로 이관하여 중복 코드 제거.
- **결과**: `Step1_Modularization_Refactoring.md` 지식 노드 생성 및 앱 전반의 파일 가독성/응집도 획기적 향상.

## [2026-04-16] feature | Step 2 데이터 브리지 버그 해결 및 로직 복구 (v0.4.2)
- **원인**: 
    - **단위 불일치**: Step 1(브리지)의 '원' 단위와 Step 2 UI의 '만원' 단위 격차로 인한 알림/계산 오류 발생.
    - **로직 소실**: 리팩토링 과정에서 `apps/step2/app.js` 내의 필수 헬퍼 함수(14종 이상)가 누락되어 앱 기능 마비.
- **조치**: 
    - **단위 정렬**: `IsfUtils.toWon` 및 `Math.round(... / 10000)`를 적용하여 브리지 연동 및 비교 로직의 단위 정합성 확보.
    - **로직 전면 복구**: `createEmptyDraft`, `getAccountById`, `markDirty/Clean` 등 핵심 헬퍼 함수군을 재구현하여 삽입.
    - **지식 영속화**: `Data_Model_Reference`, `Data_Bridge_Import_Pattern`, `Architecture_Reference` 위키에 해당 팩트와 패턴을 기록.
- **결과**: Step 2 포트폴리오 구성 및 브리지 알림 기능 정상화, 향후 리팩토링 시 로직 유실 방지를 위한 가이드라인 수립.

## [2026-04-16] feature | UI/UX 안정화 및 v0.4.1 릴리스
- **원인**: v0.4.0 대개편 후 발생한 레이아웃 `order` 중복 및 구식 백업 UI 요소(Ghost Listeners) 잔존.
- **조치**: 
    - **레이아웃 안정화**: `apps/step1/styles.css`에서 `order`속성 중복을 제거하고 모바일/데스크톱 간 일관된 우선순위(`Summary -> Sankey`) 확립.
    - **코드 위생 관리**: `DataHubModal` 도입으로 불필요해진 `backupSelect`, `exportJson`, `restoreBackup` 등 구식 리스너 및 헬퍼 함수 10여 개 제거.
    - **버전 동기화**: `app.js`(Step 1/2), `sw.js`, `manifest.webmanifest` 버전을 모두 **0.4.1**로 통일하여 PWA 캐시 무결성 확보.
- **결과**: UI 파손 위험 감소 및 앱 초기 로딩/상태 관리 로직의 경량화 달성.

## [2026-04-16] ingest | 하네스 보수 및 LLM Wiki 패턴 이식
- **원인**: v0.2.4 리팩토링 중 대규모 CSS 스타일 유실 사고 발생.
- **조치**: 
    - `GEMINI.md`에 '스타일 및 반응형 무결성' Mandate 추가.
    - `core-developer` 스킬에 '물리적 파일 무결성' 수칙 도입.
    - `orchestration` 및 `isf-evaluator`에 '시각적 회귀 테스트' 검증 항목 강제.
    - `wiki-librarian` 스킬을 'LLM Wiki' 패턴(Ingest/Synthesize/Lint)으로 전면 개편.
- **결과**: 지식의 기록뿐만 아니라 코드의 물리적 보존과 지식의 복리 적립 시스템 구축.

## [2026-04-16] ingest | PWA 안정성 및 스토리지 정책 강화
- **내역**: `pwa-manager.js` 알림 통합, `hub-storage.js` 상한(20개) 정책 적용.

## [2026-04-16] feature | Sprint 2.2: UI/UX Overhaul v0.4 (사용자 중심 적응형 개편)
- **목적**: 기존 사용자의 인지 부하 감소 및 파편화된 기능 통합.
- **주요 변경사항**:
    - **적응형 대시보드**: 데이터 보유량에 따라 입력 위주에서 결과 위주(차트 상단)로 레이아웃 자동 전환.
    - **통합 데이터 허브**: 백업/공유/히스토리를 모달 하나로 통합 (`data-hub-modal.js`).
    - **저장 인디케이터**: 헤더 내 상태 표시기로 저장 성공 여부 시각화 (v0.4 신규 UI).
    - **스마트 브리지**: Step 2 진입 시 Step 1 데이터를 자동으로 인식하여 병합 제안.
- **버전**: v0.2.5 -> v0.4.0 (UI/UX 도약 및 PWA 동기화).

## [2026-04-16] feature | Sprint 2.1: HCD 개선 및 모바일 최적화 (v0.2.5)
- **목표**: 도날드 노먼의 HCD 원칙 적용 및 모바일 가독성/피드백 강화.
- **주요 변경사항**:
    - **결손 노드 시각화**: `netCashflow < 0` 시 Sankey Diagram에 '결손(부채/자산인출)' 노드 자동 생성하여 데이터 정합성(`Total In = Total Out`) 확보.
    - **모바일 Sankey 최적화**: `SANKEY_MOBILE_BASE_ZOOM`을 `0.65`로 하향 조정하고, `orientationchange` 발생 시 `viewBox` 리셋 로직 추가하여 가독성 개선.
    - **사용자 피드백 강화**: `IsfFeedback.notifyAutoSave(status)` 인터페이스 도입 및 데이터 저장 시 실시간 상태 알림 연동.
    - **디자인 시스템 정규화**: `shared/styles/step-theme.css`에 시맨틱 노드 컬러 변수(`--node-*`) 도입 및 앱 스타일 연동.
- **버전**: v0.2.4 -> v0.2.5 (PWA 동기화 완료).
- **결과**: 모바일에서의 전체 가계 흐름 파악 용이성 증대 및 데이터 불균형 시의 시각적 오류 해결.

## [2026-04-24] ingest | 지식 위생 관리(Wiki Hygiene) 및 SSOT 정립 완료
- **목적**: v0.7.0 통합 스토리지 허브 체제 안착에 따른 과거 계획 문서 대대적 정리 및 현행 설계(SSOT) 중심의 지식 구조 재편.
- **조치**: 
    - **SSOT 최신화**: `Architecture_Reference`, `Data_Model_Reference` 등에 분산된 최신 정보를 합성하고, 아키텍처 결함 해결 사례 기록 및 최신화 완료.
    - **과거 계획 아카이브**: v0.5 리팩터링 및 v0.7 스토리지 허브 통합 등 완료된 모든 계획서를 `archive/` 디렉토리로 이동 및 [ARCHIVED] 마킹.
    - **인덱스 재편**: `INDEX.md`를 '현행 설계'와 '과거 기록' 섹션으로 명확히 이원화하여 에이전트 오작동 방지.
- **결과**: 지식 베이스의 엔트로피를 낮추고 에이전트의 컨텍스트 사용 효율 및 의사결정 정확도 대폭 향상.

## [2026-04-24] ingest | 지식 위생 관리 및 물리적 아카이빙(Physical Archiving) 단행
- **내역**: 물리적 디렉토리 구조 변경을 통한 지식 위생 관리 및 아카이빙 수행.
- **조치**: `.gemini/knowledge/wiki/archive/` 디렉토리를 신규 생성하여 구버전 계획서들을 물리적으로 격리함.
- **대상**: Storage_Hub_Integration_v0.7, Modularization_Refactoring(Step1/2), Plan_Step(1/2), UI_UX_Overhaul(v0.3/v0.4).
- **결과**: wiki/ 루트 디렉토리의 가독성을 확보하고 검색 엔진(grep/glob)의 검색 결과 노이즈 제거.

## [2026-04-24] ingest | 에이전트 협업 프로세스 동기화 (orchestration 스킬)
- 에이전트 간의 기획, 구현, 평가 핸드오프 절차 및 입출력 규격 정의 완료.

## [2026-04-30] ingest | 디자인 시스템 고도화 및 토큰 마이그레이션 완료
- DESIGN.md의 최신 타이포그래피 토큰(Black Han Sans & Gowun Dodum) 반영.
- shared/styles/step-theme.css에 --text-* 변수 도입 및 버튼 scale(0.96) 피드백 추가.
- apps/step1 및 apps/step2의 하드코딩된 스타일을 공통 토큰으로 마이그레이션 완료.
- isf-evaluator를 통한 시각적/반응형 정합성 최종 검증 완료.

## [2026-05-04] ingest | Modernization Pivot & Storage v2 Implementation
- **프로젝트 헌법 개정**: No-build 원칙 폐기 및 Vite/TS/Tailwind v4 도입 준비.
- **통합 스토리지 v2 설계**: IsfStore.ts 및 BackupService.ts를 통한 TypeScript 기반 데이터 관리 체계 수립.
- **단위 정합성 수호**: Branded Types (Won, ManWon) 도입으로 연산 실수 방지 로직 설계.
- **CI/CD 구축**: GitHub Actions를 통한 PWA 자동 배포 워크플로우 설정.
- **위키 인덱스 재구조화**: 현대화된 아키텍처 중심으로 지식 맵 갱신 및 레거시 아카이브.

## [2026-05-04] release | Phase 5: Step 1 Spotlight 온보딩 가이드 및 시스템 무결성 강화
- **목적**: 신규 사용자를 위한 프리셋 선택 가이드(Spotlight)를 제공하고, 기능 추가 시 발생할 수 있는 시스템 무결성 붕괴를 방지하기 위한 안전한 통합 로직 구현.
- **주요 변경사항**:
    - **Onboarding Manager 고도화**: `IsfOnboardingManager`를 리팩터링하여 툴팁의 반응형 위치 계산(모바일 플로팅 대응) 및 이벤트 리스너의 철저한 정리를 통한 메모리 누수 방지.
    - **Spotlight UI 개선**: `DESIGN.md` 브랜드 컬러(`--tone-primary`)를 적용하고, 기존 `box-shadow` 방식의 한계를 극복하기 위해 `z-index` 레이어와 오버레이를 분리한 현대적인 하이라이트 효과 적용.
    - **시스템 무결성 수호**: 온보딩 로직이 기존의 14종 이상 필수 헬퍼 함수 및 데이터 바인딩 로직을 훼손하지 않도록 `app.js` 초기화 시퀀스 최적화 및 방어적 코드 삽입.
- **결과**: 초기 사용자 이탈율을 줄이는 온보딩 UX가 완성되었으며, 대규모 기능 추가에도 Step 1의 핵심 기능들이 안정적으로 작동함을 검증함.

## [2026-05-06] fix | Step 2 초기 자산 연동 및 시뮬레이션 논리 결함 수정 (v0.8.9)
- **목적**: 배당 시뮬레이션이 항상 0원에서 시작하여 실제 자산 성장 추이를 반영하지 못하던 버그를 해결하고, 데이터 연동 정합성을 확보함.
- **주요 변경사항**:
    - **데이터 모델 확장**: Step 2 `state.js`에 `totalInitialAsset` 필드를 추가하고, Step 1의 `startInvest`(투자 자산) 데이터를 자동으로 수신하도록 `step1-connector.js` 고도화.
    - **계산 로직 교정**: `calculator.js` 내 시뮬레이션 엔진이 `totalInitialAsset`을 시작 원금 및 자산의 초기값으로 사용하도록 수정.
    - **UI/UX 레이블 통일**: Step 1과 Step 2에서 혼용되던 금융종합소득과세 경고 문구를 '과세주의/과세경고'로 전면 통일하여 일관된 시각적 언어 구축.
- **결과**: 사용자의 현재 자산 상태가 반영된 정확한 배당 시뮬레이션이 가능해졌으며, 앱 간 데이터 브릿지의 신뢰도가 향상됨.

## [2026-05-06] feat | Step 1 초기 자산 구조 개선 및 디자인 시스템 정제 (v0.8.8)
- **목적**: 초기 자산 입력 편의성을 높이고 데스크톱/모바일 환경에서의 시각적 가독성을 강화함.
- **주요 변경사항**:
    - **자산 필드 파편화**: '초기 자산' 단일 필드를 현금/저축/투자 3종으로 분리하여 '간편 입력' 섹션으로 전면 배치.
    - **타이포그래피 개선**: Step 1/2 H1 타이틀 스타일을 통일하고 모바일/데스크톱 반응형 폰트 크기 최적화.
    - **SimChart 버그 수정**: SVG 패딩 최적화를 통해 축 라벨 잘림 현상을 해결하고, 데이터 포인트의 가시성(White Stroke)을 강화함.
- **결과**: 초기 자산 구성의 직관성이 확보되었으며, 차트 기반의 시뮬레이션 피드백이 더욱 명확해짐.

## [2026-04-22] fix | v0.7.13 ~ v0.7.11 시뮬레이션 UI 고도화 패치
- **v0.7.13**: `simTable` 헤더 툴팁을 JS 기반 전역 핸들러로 전환하여 `overflow` 문제를 해결하고, 차트 툴팁의 DRIP 설정 연동 로직 최적화.
- **v0.7.11**: 브라우저 기본 툴팁 대신 즉시 나타나는 커스텀 CSS 툴팁(`data-tooltip`) 시스템 도입 및 차트 툴팁 내 PR/TR 수치 동시 표시 기능 강화.
## [2026-05-07] bump | 전역 버전 동기화 및 정책 보완 (v0.9.5)
- **목적**: PWA 캐시 갱신 및 배포 무결성을 위해 기술적 명세에 패치 버전을 포함한 v0.9.5를 적용하고 관련 정책을 보완함.
- **변경 사항**:
    - **버전 동기화**: `package.json`, `manifest.webmanifest`, `IsfUtils.APP_VERSION`, `sw.js`를 모두 **v0.9.5**로 업데이트.
    - **정책 명문화**: 로드맵은 마이너 버전(X.Y) 중심, 기술 파일은 패치 버전(X.Y.Z) 유지 원칙을 `Version_Management_Principles.md`에 반영.
- **결과**: 버전 관리의 직관성과 기술적 정밀도를 동시에 확보.

## [2026-05-07] feat | 'ISF CODE' 기반 공유 시스템 도입 및 리다이렉트 버그 해결 (v0.9)
- **목적**: 기존 URL 해시 기반 자동 리다이렉트 방식에서 발생하는 PWA/환경별 오동작을 해결하고, 사용자 친화적인 수동 코드 공유 시스템으로 전환.
- **주요 변경사항**:
    - **DataHubModal 혁신**: '공유 및 연동' 탭을 신설하여 'ISF CODE' 발급 및 수동 입력 UI를 중앙 집중화함.
    - **수동 공유 워크플로우**: 긴 URL 대신 압축된 데이터 페이로드를 직접 코드로 발급하고, 클립보드 복사 및 수동 입력을 통한 데이터 로드 기능을 Step 1/2 공통 적용.
    - **시스템 정제**: 더 이상 사용되지 않는 '공유 링크 복사' 버튼을 제거하고, 코드 기반의 명확한 데이터 전송 체계로 통합하여 리다이렉트 관련 버그를 근본적으로 차단.
- **결과**: 서비스 워커나 PWA 환경에서의 URL 파싱 오류를 방지하고, 사용자 간 설정 공유의 안정성과 직관성을 대폭 향상함.

## [2026-05-06] ingest | 금융소득종합과세 정책 및 안전 마진 지침 수립
- **목적**: 금융소득종합과세에 대한 법적 기준과 프로젝트 고유의 안전 마진(1,900만 원/3,400만 원) 정책을 명문화하여 지식 자산화함.
- **주요 내용**: 
    - `Financial_Taxation_Reference.md` 생성: 2,000만 원 기준 및 누진세율 정보 포함.
    - 안전 마진 설정: 실제 기준보다 보수적인 1,900만 원(5% 마진) `warn` 및 3,400만 원 `crit` 경고 정책 수립.
    - 위키 연결: `INDEX.md` 및 `UI_Standards_Reference.md`에 정책 노드 연결 완료.
- **결과**: 금융 정책 관련 비즈니스 로직의 근거를 확보하고, 개발 및 기획 시 일관된 기준 제공.

## [2026-05-20] ingest | v1.4 마일스톤 감사 통과 및 런타임 버그 수정 (v0.10.1)
- **목적**: Milestone v1.4 (코어 안정화 및 UX 고도화) 감사를 수행하여 DoD 달성 여부를 최종 검증하고, 감사 과정에서 발견된 치명적인 런타임 버그를 해결함.
- **주요 변경사항**:
    - **마일스톤 감사**: `PROJECT.md`, `ROADMAP.md`, `REQUIREMENTS.md`와 `DESIGN.md`가 모두 v1.4 상태에 부합함을 검증하고, `v1.4-MILESTONE-AUDIT.md`를 'Passed' 상태로 최종 갱신 완료.
    - **Step 2 런타임 오류 수정**: `apps/step2/app.js` 내의 `featureController` 임포트 경로가 잘못 지정(`"./feature-controllers.js"`)되어 모듈 404 에러를 일으키던 문제를 `"./modules/feature-controllers.js"`로 외과적 수정 완료.
    - **아키텍처 일관성**: Step 2 배당 시뮬레이션의 현대적 컨트롤러 패턴(`ui-controller.js`, `feature-controllers.js`) 리팩토링 무결성 확인.
- **결과**: v1.4 마일스톤 공식 Passed 달성 및 시스템 런타임 안정성 전면 확보.

## [2026-05-20] chore | 마일스톤 페이즈 디렉토리 아카이브 및 정리 완료 (gsd-cleanup)
- **목적**: 기존 마일스톤(v1.0, v1.1~v1.3, v1.4) 개발 과정에서 `.planning/phases/` 내에 누적된 과거 페이즈 디렉토리들을 각 마일스톤 하위의 전용 아카이브 디렉토리로 안전하게 이관하여 현재 활성화된 페이즈 관리 환경을 깨끗하게 정리(Hygiene)함.
- **변경 사항**:
    - **아카이브 디렉토리 생성**: `.planning/milestones/` 하위에 `v1.0-phases`, `v1.3-phases`, `v1.4-phases` 디렉토리를 생성하여 격리 공간 확보.
    - **v1.0 MVP 페이즈 이관**: `1-preset`, `2-adjustment`, `2.1-template-refinement` 페이즈 폴더를 `v1.0-phases/`로 안전하게 물리적 이동.
    - **v1.1~v1.3 시뮬레이션 및 온보딩 페이즈 이관**: `03-chart-visualization`, `04-kpi-dashboard`, `05-onboarding`, `06-portfolio-rebalancing`, `10-backtest-migration` 페이즈 폴더를 `v1.3-phases/`로 물리적 이동.
    - **v1.4 코어 안정화 및 UX 고도화 페이즈 이관**: `11-stability`, `12-advanced` 페이즈 폴더를 `v1.4-phases/`로 물리적 이동.
    - **시스템 위생 관리**: `.planning/phases/` 내부를 깨끗이 비워 차기 스프린트(v1.5 포트폴리오 고도화) 준비 상태 완비.
- **결과**: 과거 완료된 개발 이력의 완벽한 아카이빙을 통해 지식 검색 노이즈를 제어하고, 활성 작업 영역의 청결성을 극대화함.

## [2026-05-20] fix | 금융종합소득과세 경고 임계값 정합성 보정 (v0.11.1)
- **목적**: `GEMINI.md` 기획 지침(1,900만 원/3,400만 원)과 코드 상수 간의 불일치를 해소하여 비즈니스 규칙의 정밀함과 데이터 정합성을 완벽히 확보함.
- **변경 사항**:
    - **코드 상수 수정**: `shared/core/utils.js` 내 `FINANCIAL_INCOME_WARN_THRESHOLD_WON`을 1,900만 원(`19000000`), `FINANCIAL_INCOME_CRIT_THRESHOLD_WON`을 3,400만 원(`34000000`)으로 교정 완료.
    - **지식 문서 동기화**: `Financial_Taxation_Reference.md` 문서 내 주의/치명적 경고 기준(임계값) 정보를 1,900만 원 및 3,400만 원으로 갱신하여 지식 베이스의 일관성 확보.
- **결과**: 기획서에 명시된 비즈니스 룰과 런타임 계산 로직 및 지식 문서의 무결성을 100% 일치시킴.

## [2026-06-10] audit | v1.6 마일스톤 감사 통과 및 빌드 무결성 확인 (v0.11.2)
- **목적**: Milestone v1.6 (코드 리팩터링, UX 개선 및 안정성 강화) 감사를 완료하고, 빌드 시스템 및 런타임 안정성을 최종 검증함.
- **변경 사항**:
    - **마일스톤 감사 및 갱신**: `v1.6-MILESTONE-AUDIT.md` 감사 보고서를 최신 검증 내용으로 업데이트 완료 (audited: "2026-06-10T17:58:00.000Z").
    - **레거시 함수 검증**: 이전에 런타임 ReferenceError 우려가 보고되었던 4종의 레거시 유령 함수(`hasShareState`, `bindReadonlyAdvancedNavigation`, `dismissViewModeGuide`, `switchToNormalMode`)들이 현재 `apps/step1/app.js`에 정상 정의 및 바인딩되어 안정적으로 실행 중임을 교차 확인 완료.
    - **빌드 및 PWA 무결성**: 로컬 빌드 및 버전 동기화 커맨드(`npm run build`)가 913ms 만에 무결하게 성공함을 확인. PWA 프리캐시 엔트리가 올바르게 갱신 및 생성됨.
- **결과**: v1.6 마일스톤 공식 Passed 달성 및 상용 배포 가능 수준의 시스템 안정성 보증.

## [2026-06-11] bump | 계좌 연결 결함 수정 및 자동 버전 범프 빌드 시스템 이식 (v0.11.3)
- **목적**: Phase 17 도입 후 발생한 계좌(accountId) 변경 정보 유실 결함을 핫픽스하고, 빌드(npm run build) 시 자동으로 패치 버전을 범프하는 파이프라인을 구축함.
- **변경 사항**:
    - **계좌 연결 결함 해결**: `apps/step1/app.js`에서 항목 변경 감지 시 `accountId`가 데이터 구조에 저장되지 않던 누락 버그를 수정하고, select 요소의 기민한 반응을 위해 `change` 이벤트를 추가로 연동함.
    - **자동 버전 범프 빌드 이식**: `npm run build` 시 package.json의 패치 버전을 1씩 자동으로 올리는 `scripts/bump-version.js` 스크립트를 신규 개발하여 빌드 체인에 추가함.
    - **Phase 15/999.15 정리**: 누락되었던 `15-SUMMARY.md` 보고서를 작성하여 Phase 15를 정식 마감하고, Phase 999.15 백로그의 요약 보고서(`999.15-SUMMARY.md`) 및 UAT 계획서(`999.15-UAT.md`)를 도출함.
- **결과**: 가계 흐름 계좌 쪼개기 모델의 데이터 저장 무결성을 회복하고, 번거로운 수동 버전업 단계를 배포 파이프라인으로 자동화함.

## [2026-06-11] fix | 수입 0원 집계 결함 핫픽스 및 프리셋 UX 간소화 (v0.11.4)
- **목적**: UAT 중 발견된 수입 0원 표시 버그를 해결하고 프리셋 적용 시 펜딩 바 노출 없이 즉시 반영되도록 개선함.
- **변경 사항**:
    - **수입 0원 집계 버그 수정**: `apps/step1/modules/ui-controller.js` 내에서 `getMonthlyIncomeTotalWon` 함수를 호출할 때 배열이 아닌 `inputs` 객체 전체를 인자로 잘못 전달하던 오타 버그를 `inputs.incomes`로 정정함.
    - **프리셋 적용 UX 간소화**: `apps/step1/app.js`에서 프리셋을 선택하고 적용할 때, 펜딩 변경사항 바가 나타나 수동 적용을 한 번 더 유도하던 방식을 변경하여 `commitImmediateInputs(nextInputs)`로 즉시 영속화 및 자동 빌드 저장되도록 개선함.
- **결과**: 프리셋 적용 후 즉시 정상적인 Sankey 차트 흐름(수입원 ➔ 계좌 ➔ 세부항목)이 렌더링되며, 펜딩 확인 절차가 줄어들어 입력 사용성이 크게 개선됨.



## [2026-06-12] release | Phase 4 UAT 검증 통과 및 프리미엄 UI/UX 버그 핫픽스 (v0.11.31)
- **목적**: Milestone v1.7 (다중 계좌 매핑 및 에디토리얼 UI 개편) Phase 4의 최종 UAT 검증을 수행하고, 발견된 레이아웃 파손 및 이체/경고/툴팁 연동 버그들을 일괄 수선하여 마일스톤을 공식 마감함.
- **주요 변경사항**:
    - **모바일 2x3 카드 정비**: 모바일 해상도(760px 이하)에서 요약 정보 카드(`summary-cards`)의 보조 텍스트(`.sub`)를 숨김 처리(생략)하고, 2열 격자 구조(`repeat(2, 1fr)`)를 강제하여 2x3 형태로 완벽하게 맞춤.
    - **수동 이체 에디터 복구**: HTML 마크업에서 완전히 누락되었던 수동 이체 에디터(`transferEditorSection`)를 `mgmtPanelAccount` 탭 내부 하단에 이식하여 실시간 출금 가능 예상 잔액 힌트 및 수동 이체 규칙의 실시간 추가/삭제가 가능하도록 정상화.
    - **이체망 LR 배치 및 노드 금액 표기**: 계좌이체망의 레이아웃을 기존 Circular(원형)에서 좌우로 전개되고 상하 지그재그로 어긋나는 LR(Left-to-Right) 구조로 전면 리팩토링하고, 노드 내부에 금액(만원)도 함께 표기되도록 2줄로 렌더링. 하단 legend-group 에는 연 환산 금액(연 X만)을 같이 포맷팅하여 노출함.
    - **도움말 툴팁 통합**: 화면에 방만하게 늘어져 인지 부하를 일으키던 긴 설명문들(순자산 공식, 세부 계산법)을 완전히 제거하고 `?` 아이콘의 Glassmorphism 전역 툴팁(#globalTooltip) 내부로 깔끔하게 축소 통합.
    - **글로벌 과세경고 LED 도입**: 개별 계좌 카드에서 번잡하게 텍스트로 뜨던 과세 뱃지를 제거하여 색상 링으로만 표현을 단순화하고, 화면 상단 핵심 지표 타이틀 옆에 작지만 선명하게 반짝이는 LED 형태의 글로벌 경고 표시등(`globalFinancialIncomeIndicator`)을 장착하여 상태(warn/crit)를 일목요연하게 파악 가능하도록 개선. 특히, LED 위치를 `app-header` 컴포넌트의 `logo-group` 내부(current-step-label 바로 뒤)로 이동하여 자연스럽고 완성도 높은 레이아웃을 구현함.
    - **자동 저장 상태 인디케이터 복원**: `app-header.js`에 자동 저장 상태를 점으로 표현하는 `#statusIndicator` LED 요소를 복원하고, `step-theme.css` 스타일 및 깜빡임 애니메이션을 탑재함. 모바일(760px 이하)에서는 텍스트를 자동 생략하도록 보완함.
    - **도움말 툴팁 파서 강화**: 전역 도움말 툴팁(`#globalTooltip`)이 텍스트 데이터셋을 HTML 렌더링 시 안전하게 변환(볼드, 인라인 코드, 구분선, 숫자 기호 리스트 등)할 수 있도록 파서 기능을 구현하여 툴팁 내부 설명의 가독성을 극대화함.
    - **재무설정 접기 잔여 코드 정리**: 마크업에서 제거된 `toggleInputsMobile` 버튼의 `app.js` 클릭 이벤트 바인딩 및 `dom.js` 프로퍼티 선언을 외과적으로 완전 소거하여 코드 무결성을 달성함.
    - **수익률/기타 설정 꼬임 해결**: 탭 분리 중 부모 관계가 꼬여 화면에 미표기되던 증가율/수익률 설정 블록(`ratesAdvancedBlock`)의 DOM 위치를 올바른 탭 본문 하단으로 이사하여 기능 복구.
- **결과**: v1.7 마일스톤의 최종 Passed 달성 및 빌드 안정성 확보.

## [2026-06-12] release | UX 개선 및 계좌이체망 애니메이션/과세경고 LED 고도화 (v0.11.35)
- **목적**: 모바일 화면 복잡도 완화, 계좌이체망 흐름 애니메이션 품질 개선, 금융소득과세 경고의 미니멀 인디케이터화 및 툴팁 문장 가독성 혁신.
- **주요 변경사항**:
    - **과세주의/경고 뱃지 제거 및 LED 일원화**: 가계 추이 계산 검증 테이블 및 배당 시뮬레이션 테이블에서 개별 과세 뱃지를 완전히 지우고, 화면 상단 헤더 로고 그룹 내 글로벌 LED 인디케이터(`globalFinancialIncomeIndicator`)에 상태(warn/crit)와 상세 안내 툴팁을 일원화하여 표시하도록 개선.
    - **AppHeader 컴포넌트 고도화**: 헤더 컴포넌트에 `setFinancialWarning(maxStatus, message)` 메서드를 추가하고, `dom.js`에서 동적 getter로 인디케이터 요소를 가져오게 변경하여 컴포넌트 렌더링 시점에 따른 null 바인딩 버그를 영구 해결.
    - **재무 핵심 지표 카드 4개 압축**: 부채 유무와 상관없이 필수 4개 카드("월 순현금흐름", "현재 순자산", "n년 후 순자산", "미래자산 투입률")만 노출하도록 카드를 간소화하고, 상세 지표 더보기 및 불필요한 토글 제거. 현재 순자산 카드의 sub 텍스트에 자산 세부내역(현금, 저축, 투자, 부채)을 모두 통합 표기하여 핵심 가치를 보존함.
    - **수동이체 에디터 아코디언화**: 모바일에서 화면 복잡도를 대폭 줄이기 위해 수동이체 설정 에디터를 아코디언 형태로 개편, 기본적으로 접힌 상태를 유지하고 필요 시 토글하여 사용할 수 있도록 압축감 부여.
    - **계좌이체망 애니메이션 자연화 및 억 표시**: 기존 linearGradient 슬라이딩 방식을 대체하여 각 연결선 경로에 정밀하게 맞춰진 동적 그라데이션 및 CSS `stroke-dasharray` / `stroke-dashoffset` 흐름 애니메이션 도입. 이체망의 텍스트와 노드 금액 포맷팅 시 `* 10000` 을 곱하여 만원 단위를 원 단위로 정확하게 변환해 `formatMoney` 헬퍼가 억/만원 포맷팅을 완벽히 수행하도록 수정.
    - **수익률/기타 설정 탭 이동**: 탭 분리 배치 오류로 화면에 렌더링되지 못하고 유실되어 있던 "증가율 및 수익률 설정(ratesAdvancedBlock)"의 마크업 위치를 탭 4인 설정(Settings) 패널 하단으로 이동하여 정상화.
    - **모바일 생키차트 복잡도 해소**: 모바일 환경에서 생키차트 및 계좌이체망 SVG에 `min-width: 680px`를 강제하고 가로 스크롤을 허용하며 스크롤 힌트 배너를 추가하여, 화면이 찌그러져 붕괴되는 현상 해소.
    - **툴팁 수식 박스 도입**: 툴팁 내부 설명에서 수학 수식을 깔끔하게 격리해 보여주기 위해 `$$` 감쌈 문법과 `.formula-box` CSS 스타일을 신규 수립하여 가독성을 극적으로 제고함.
    - **모바일 토글 요소 크기 수정**: 모바일 미디어 쿼리에서 sm 사이즈 버튼 및 모드 전환용 `.tab-btn--sm`이 `min-height: 44px`로 늘어지던 크기 버그를 수정하여 30px로 컴팩트하게 강제 고정.
- **결과**: 모바일 반응형 무결성을 복구하고, 불필요한 레이아웃 요소를 전면 압축/제거하여 프리미엄 UX 기준을 달성함.

## [2026-06-13] release | step1 UI/UX 곡률 통일, 비신축 줌(Sankey) 및 이미지 다운로드 안전성 강화 (v0.11.38)
- **목적**: `DESIGN.md` 에 따른 전체 컴포넌트의 테두리 곡률 통일화 및 생키 다이어그램의 가로 줌 개선, 그리고 안전한 PNG 내보내기를 통한 visual UI/UX 무결성 확보.
- **변경 사항**:
    - **곡률 통일화**: `step-theme.css` 내 `.btn` 의 기본 `border-radius`를 알약 모양(999px)에서 `var(--rd-sm)` (8px)로 하향 적용하고, `var(--rd-lg)` 는 주요 CTA 버튼(`.btn-primary`)에만 제한적으로 사용하도록 수정. 이로써 입력 폼, 일반 버튼, 배지, 콤보박스 간의 통일감이 확보되고 미니멀하고 단정한 flat panel 디자인을 완성함.
    - **비신축형 생키 줌(Sankey Zoom)**: 줌 배율 변경 시 SVG의 종횡비 연산 때문에 화면 전체가 세로로 비대칭 팽창하던 버그를 해결. 줌을 viewBox 가로 너비와 CSS width(px)에만 곱해주고, CSS height는 최대 440px(모바일 360px)로 정확히 고정하여 화면 스크롤 부하 없이 가로 간격만 널찍하게 줌인되는 UX를 구현함.
    - **안전한 PNG 내보내기 및 2중 폴백**: 이미지 파일 직렬화 시 한글 인코딩 및 로컬 CORS 제한으로 렌더링이 실패하는 버그를 base64 인코딩을 적용해 영구 해결. 캔버스 렌더링에 최종 실패할 경우 사용자의 다운로드가 막히지 않도록 원본 SVG 파일을 바로 다운로드시키는 triggerDownload 폴백 메커니즘을 적용.
    - **Playwright 테스트 검증 자동화**: `tests/step1.spec.ts` 테스트 스크립트를 신규 구축하고 `package.json`에 `test:e2e` 커맨드를 장착하여, 헤더 타이틀 로드, 생키 크기 제한, 곡률(border-radius: 8px) 규격 검증 등의 Visual Audit을 자동화.
- **결과**: `02-UI-REVIEW.md` 평가 점수 24/24 만점(APPROVED)을 획득하고 디자인 일관성과 다이어그램 조작성의 완벽한 품질을 보증함.

## [2026-06-13] release | 모바일 생키/계좌망 찌그러짐 개선 및 소멸식 스크롤 힌트 연동 (v0.11.39)
- **목적**: 모바일 화면에서 생키 차트와 계좌망이 찌그러지는 비율 파괴 버그를 해결하고, 화면을 고정적으로 가리던 스크롤 힌트 배너의 인터랙션을 개선하여 사용성 확보.
- **변경 사항**:
    - **모바일 최소폭 및 비율 고정**: 모바일 뷰포트 시 `sankey-renderer.js` 와 `network-map-renderer.js` 의 최소 가로폭을 `680px`로 제어하고 px 단위로 강제 고정하여 SVG viewBox 비율에 맞춰 찌그러지거나 왜곡되는 버그를 제거. 이로써 텍스트 및 라벨 겹침 현상이 완전 소거되고 부드러운 가로 스크롤 가독성을 보장함.
    - **CSS min-width 충돌 소거**: `styles.css` 내에서 SVG에 `min-width: 680px`를 강제하여 렌더러의 px 조작과 충돌하며 비율을 파괴하던 스타일을 제거하고, 힌트 배너를 `inline-flex` 정렬 구조로 변경함.
    - **소멸식 스크롤 힌트 배너**: 스크롤 컨테이너에 `scroll` 리스너를 결합하여 사용자가 가로 스크롤을 시도할 경우 배너가 0.5초 동안 서서히 fade-out되어 소멸하도록 구현. 또한 배너 우측에 터치식 닫기(×) 버튼을 동적으로 달아주어 즉각 소거할 수 있게 함으로써 화면 가림 부하 해소.
- **결과**: 모바일 반응형 차트 찌그러짐 버그를 원천 봉쇄하고, 사용성 피드백에 신속히 대응하여 프리미엄 수준의 조작 가독성을 회복함.

## [2026-06-13] release | 다중 계좌 매핑 기반 프리셋 및 샘플 데이터 보강 (v0.11.40)
- **목적**: 사용자가 다중 계좌 매핑 및 수동 이체 규칙의 다이내믹한 작동 방식을 바로 체험할 수 있도록 '샘플 불러오기' 데이터와 '투자 성향 프리셋'을 실제 신규 기능들에 대응되도록 개편함.
- **변경 사항**:
    - **CMA비상금계좌 도입 및 매핑 세분화**: `constants.js` 의 `SAMPLE_INPUTS` 에 `acc-cma` (CMA비상금계좌) 계좌를 추가하고, 예시 적금 항목 중 '주택청약'의 `accountId`를 `acc-cma` 로 할당하여 수입 배분 흐름이 여러 계좌를 거쳐 다채롭게 세부 항목으로 분산되는 모습을 보여줌.
    - **예시 수동 이체(transfers) 탑재**: 샘플 데이터에 급여계좌에서 CMA비상금계좌로 매달 30만원씩 이체하는 수동 이체 규칙을 기본 장착하여, 계좌 간의 N:N 자금 이동 선로가 생키차트와 계좌망에 자동으로 그어지는 시각 효과를 극대화함.
    - **프리셋 적용 데이터 모델 확장**: `presets.js` 내 `applyPreset` 및 `applyPresetBySalary` 호출 시 단순히 수입/지출/저축 금액만 덮어쓰던 방식을 탈피하여, 4대 대표 계좌(`acc-salary`, `acc-living`, `acc-stock`, `acc-cma`) 정보와 예시 이체 내역(수입의 8%를 CMA로 이체), 그리고 이에 연동되는 세부 항목들의 `accountId` 매핑 데이터가 동적으로 구성되어 덮어쓰여지도록 고도화함.
- **결과**: 사용자가 프리셋 적용이나 샘플 불러오기를 클릭했을 때, 다중 계좌 및 수동 이체 등 v1.7의 핵심 UX 밸류 체인을 사전 학습 없이도 직관적으로 이해할 수 있는 완벽한 가이드 데이터를 제공함.

## [2026-06-15] release | 1원 단위 입력 전면 통일 및 실시간 금액 변환 힌트 탑재 (v0.11.46)
- **목적**: 1만원 단위의 입력 편의성을 위해 구별해둔 구조가 잦은 산술 오차와 버그(10,000배 복리 중복 계산 등)를 유발하므로, 전체 시스템을 1원 단위로 전면 통일하고, 입력 편의성을 위해 실시간 한글 금액 변환 힌트를 표시해주는 방식으로 긴급 수정함.
- **주요 변경사항**:
    - **단위 변환 함수 1:1 리턴화**: `shared/core/utils.js` 및 `src/core/types/money.ts` 내의 `toWon`과 `toMan` 헬퍼 함수가 단위를 변환하지 않고 1:1로 리턴하도록 수정하여 데이터의 1원 단위 일관성을 영속적으로 보장.
    - **실시간 한글 금액 변환 헬퍼 개발**: `IsfUtils.convertToKoreanWon(value)` 함수를 추가하여 억, 만, 원 단위를 한글 금액 형태(예: `500만 원`, `1억 2,300만 원`)로 변환해주는 헬퍼 탑재.
    - **전역 실시간 힌트 인풋 리스너 등록**: DOM 로딩 완료 시점에 전역 `input` 이벤트를 리스닝하여, 사용자가 금액 관련 입력창(`type="number"`)에 숫자를 칠 때 입력 필드 하단에 실시간으로 한글 금액을 표시해주는 힌트 엘리먼트(`.realtime-won-hint`)를 동적으로 생성 및 표시.
    - **프리셋 생성 연봉 입력 단위 교정**: `presets.js` 내의 `calculateMonthlyIncomeFromAnnualSalary`, `calculateAnnualSalaryFromMonthlyIncome`, `applyPresetBySalary`가 원 단위의 연봉 입출력을 지원하도록 수학 팩터와 상한 범위를 1원 단위 기준으로 패치.
    - **UI 라벨 및 텍스트 원 단위 정렬**: `apps/step1/index.html`, `apps/step1/modules/list-renderer.js`, `apps/step2/index.html` 내의 `(만원)` 단위 텍스트 라벨과 초기값 가이드를 모두 `(원)`으로 교체.
- **결과**: 만원 단위 변환으로 인한 데이터 왜곡 가능성을 원천적으로 제거하고, 실시간 한글 변환 가이드를 제공하여 인지 편의성과 시스템 안전성을 모두 만족시킴.

## [2026-06-15] release | 모바일 패널 헤더 터치 토글 UX 개선 및 토글 버튼 줄바꿈 방지 (v0.11.48)
- **목적**: 모바일 화면에서 재무설정 및 가계추이 검증 패널의 접기/펼치기 버튼(▾)이 줄바꿈되어 화면이 지저분해지는 현상을 해결하고, 모바일에서의 조작 편의성을 위해 헤더 영역 전체를 클릭/터치했을 때 접고 펴기가 작동하도록 UX를 개선함.
- **주요 변경사항**:
    - **모바일 헤더 클릭 이벤트 연동**: `apps/step1/app.js`에서 모바일 뷰(`window.innerWidth <= 760`)일 때, 패널 헤더(`.section-head`) 전체를 클릭해도 토글이 작동하도록 이벤트 리스너 추가. 프리셋 버튼, 물음표 툴팁 등 개별 조작 요소를 클릭할 때는 오작동을 차단하기 위해 이벤트 버블링 방지(`e.stopPropagation` 및 target 체크) 적용.
    - **패널 토글 함수 모듈화**: 중복된 토글 상태 전환 코드를 `toggleControlsPanel()` 및 `toggleProjectionPanel()` 공통 헬퍼 함수로 분리하여 코드의 가독성과 유지보수성 향상.
    - **모바일 CSS 최적화**: `apps/step1/styles.css` 최하단에 미디어 쿼리를 추가하여 모바일 환경에서만 헤더에 `cursor: pointer` 스타일을 부여하고, `section-head-tools`에 flex 줄바꿈 방지(`flex-wrap: nowrap`) 및 토글 버튼의 신축 방지(`flex-shrink: 0`)를 적용하여 줄바꿈 현상을 근본적으로 해결.
- **결과**: 모바일 기기에서의 터치 토글이 매우 직관적으로 단순화되었으며, 토글 버튼의 비정상적인 줄바꿈 현상이 완벽하게 해결되어 프리미엄 UX 감성을 보강함.



