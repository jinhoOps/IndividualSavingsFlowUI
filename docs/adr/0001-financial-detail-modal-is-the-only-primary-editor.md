# Financial Detail Modal Is the Only Primary Editor

Step 1 uses the summary surface for reading and scanning financial state, while ordinary financial edits must complete inside the Financial Detail Modal. Hidden legacy editor DOM, the item-editor controller, the global item-editor pending bar, and tests that require that secondary editor are not part of the target UX; any remaining useful capability should be absorbed into the Financial Detail Modal instead of preserving a second editing surface.
